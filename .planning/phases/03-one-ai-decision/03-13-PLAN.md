---
phase: 03-one-ai-decision
plan: 13
type: execute
wave: 7
depends_on: [03-01, 03-02, 03-03, 03-04, 03-05, 03-06, 03-07, 03-08, 03-09, 03-10, 03-11, 03-12]
files_modified:
  - .planning/phases/03-one-ai-decision/evidence/01-test-report.txt
  - .planning/phases/03-one-ai-decision/evidence/02-harness-selftest.txt
  - .planning/phases/03-one-ai-decision/evidence/03-canned-report.txt
  - .planning/phases/03-one-ai-decision/evidence/04-staging-ai-player.log
  - .planning/phases/03-one-ai-decision/evidence/05-decision-in-panel.png
  - .planning/phases/03-one-ai-decision/evidence/06-ai-assist-terminal-line.png
  - .planning/phases/03-one-ai-decision/evidence/07-panel-after-refresh.png
  - .planning/phases/03-one-ai-decision/evidence/08-refused-not-configured.png
  - .planning/phases/03-one-ai-decision/evidence/09-auto-unknown-option.png
  - .planning/phases/03-one-ai-decision/evidence/10-decision-failure-disengage.png
  - .planning/phases/03-one-ai-decision/evidence/11-logs-section.png
  - .planning/phases/03-one-ai-decision/evidence/12-log-page-two-pane.png
  - .planning/phases/03-one-ai-decision/evidence/13-hand-play-unchanged.png
  - .planning/phases/03-one-ai-decision/evidence/14-connections-cleaned.png
  - .planning/phases/03-one-ai-decision/03-SECURITY-AGENDA.md
  - .planning/phases/03-one-ai-decision/03-13-SUMMARY.md
autonomous: false
requirements: [REQ-single-decision, REQ-reasoning-visibility, REQ-env-config]

must_haves:
  truths:
    - "The full Go suite and the frontend build are green on the phase branch and no package was added, proven by evidence/01-test-report.txt containing zero unexpected FAIL lines and an empty dependency-drift section (T-3-SC)"
    - "The canned-report harness is falsifiable before it is trusted, proven by evidence/02-harness-selftest.txt showing a clean self-test and a negative self-test producing FAIL C3 with a non-zero exit (T-3-10)"
    - "With the Gemini variables absent, staging starts normally and #AUTO ON is refused with the D-20 sentence while the switch stays off, proven by RUN A of evidence/03-canned-report.txt and by evidence/08-refused-not-configured.png (D-20)"
    - "With the Gemini variables set, one #AUTO ON produces one decision whose reasoning and command the owner reads in the panel, whose command the game answers, and which is still there after a refresh, proven by evidence/05-decision-in-panel.png, evidence/06-ai-assist-terminal-line.png, evidence/07-panel-after-refresh.png and the decisions step of RUN B (D-01, D-08, D-09, D-12)"
    - "Every step of the decision is readable in the staging log as [AI-PLAYER] lines carrying ids, a stage and lengths, with no game text, reasoning, key or endpoint on them, proven by evidence/04-staging-ai-player.log (T-3-07, T-3-02)"
    - "A first failure sends nothing, says why in both the panel and the terminal, and lands the switch off, proven by evidence/10-decision-failure-disengage.png (D-13)"
    - "Every session on a saved profile is in the logbook, reachable in a new tab from Settings, with the owner's lines and the AI's lines marked apart, proven by evidence/11-logs-section.png and evidence/12-log-page-two-pane.png (D-14, D-16, D-17)"
    - "DR-2-01 is closed in code and no one-time sign-in code appears for a fresh sign-in in the staging log, and DR-2-02 is closed by deleting the six throwaway profiles through the app, proven by evidence/04-staging-ai-player.log and evidence/14-connections-cleaned.png"
    - "Every one of the four ROADMAP Phase 3 success criteria has a row in 03-13-SUMMARY.md citing a named evidence file, with line numbers for text evidence and a PASS or FAIL verdict, and no row cites a database query"
    - "The Phase 3 security review has a written agenda carrying the transcript retention question, the resume-now-acts question, the log page's authorization scope, the staging key, the driver's wheel-grab bypass and the two carried-forward risks, with nothing decided in advance, proven by 03-SECURITY-AGENDA.md"
  artifacts:
    - path: ".planning/phases/03-one-ai-decision/evidence/01-test-report.txt"
      provides: "Canned report: go build, go vet, the full go test -v suite, the targeted Phase 3 runs, npm run build, the dependency-drift check and the model-literal grep, captured verbatim"
      contains: "GO TEST EXIT"
    - path: ".planning/phases/03-one-ai-decision/evidence/02-harness-selftest.txt"
      provides: "The harness proving itself: a clean self-test and a negative self-test that fails on purpose"
      contains: "FAIL C3"
    - path: ".planning/phases/03-one-ai-decision/evidence/03-canned-report.txt"
      provides: "scripts/verify-phase3.sh against staging in two runs: Gemini variables unset, then set"
      contains: "PASS C3"
    - path: ".planning/phases/03-one-ai-decision/evidence/04-staging-ai-player.log"
      provides: "Staging log excerpt: migration 011 applied, the decision stages, the transcript open and close, and a fresh sign-in with no code"
      contains: "[AI-PLAYER] decision"
    - path: ".planning/phases/03-one-ai-decision/evidence/05-decision-in-panel.png"
      provides: "Screenshot: the AI Assist panel showing one decision's reasoning and the command it issued"
    - path: ".planning/phases/03-one-ai-decision/evidence/12-log-page-two-pane.png"
      provides: "Screenshot: the per-profile log page with a session selected, human and AI lines marked apart"
    - path: ".planning/phases/03-one-ai-decision/03-SECURITY-AGENDA.md"
      provides: "The Phase 3 security review agenda: six open items plus two closures, each with three dispositions and none pre-chosen"
      contains: "Remediate Now"
    - path: ".planning/phases/03-one-ai-decision/03-13-SUMMARY.md"
      provides: "One row per ROADMAP Phase 3 success criterion citing the evidence that proves it, with a verdict"
      contains: "Phase Validation"
  key_links:
    - from: "scripts/verify-phase3.sh run against staging"
      to: "evidence/03-canned-report.txt"
      via: "two runs concatenated under RUN A and RUN B headings"
      pattern: "PASS C3"
    - from: "the driver's [AI-PLAYER] lines on staging"
      to: "evidence/04-staging-ai-player.log"
      via: "railway logs --environment staging filtered on the [AI-PLAYER] prefix during the walkthrough"
      pattern: "stage=(request|dispatch|sent|failed)"
    - from: "the browser play screen and log page on staging"
      to: "evidence/05 through evidence/14"
      via: "one screenshot per observable, end-user perspective only"
      pattern: "AI-ASSIST"
---

<objective>
Demonstrate the phase and file the proof. CLAUDE.md rule 2: Phase 3 is complete when the ROADMAP's Phase Validation line has actually been performed, not when its tasks are done. The owner's evidence rule narrows how: every criterion is proven by a canned report — a test run, a script run or a log excerpt captured verbatim to a file — or by an end-user screenshot of the browser as the owner sees it. A database query proves nothing here and is not to be run.

Purpose: turn four ROADMAP success criteria into four cited rows, each pointing at a file a reader can open; close the two security risks carried forward from Phase 2; and hand the Phase 3 security review a written agenda so the questions this phase raised get decided rather than forgotten.
Output: an `evidence/` directory holding two local reports, one two-part staging report, one staging log excerpt and ten screenshots; a security-review agenda; and `03-13-SUMMARY.md` mapping each criterion to the evidence that proves it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/ROADMAP.md
@.planning/phases/03-one-ai-decision/03-VALIDATION.md
@.planning/phases/03-one-ai-decision/03-UI-SPEC.md
@.planning/phases/03-one-ai-decision/03-CONTEXT.md
@.planning/phases/03-one-ai-decision/03-RESEARCH.md
@.planning/todos/pending/2026-09-15-phase3-security-carry-forward.md
@.planning/phases/02-autopilot-switch/02-SECURITY-AGENDA.md

ROADMAP Phase Validation line for Phase 3 (what this plan must demonstrate):
Player-observable on staging — with autopilot engaged, one decision and its reasoning appear in the play screen and the game responds to the issued command. Diagnostic — a decisions row exists for that decision and is returned after a page refresh; a `go test` proves the command passed through the ICM dispatcher in the automation context; with the Gemini environment variables unset, engagement fails with a clear error and no command is sent.

Evidence directory (created in task 03-13-01): `.planning/phases/03-one-ai-decision/evidence/`. Every filename below is fixed — every earlier plan's `must_haves` cites these exact names.

| File | Type | Produced by |
|------|------|-------------|
| `01-test-report.txt` | canned report | task 03-13-01 |
| `02-harness-selftest.txt` | canned report | task 03-13-01 |
| `03-canned-report.txt` | canned report, two runs | tasks 03-13-02 (RUN A) and 03-13-03 (RUN B) |
| `04-staging-ai-player.log` | log excerpt | task 03-13-03 |
| `05` through `10` | screenshots | tasks 03-13-02 (`08`, `09`) and 03-13-03 (the rest) |
| `11` through `14` | screenshots | task 03-13-03 |

Environment: Railway project `mudpuppy`, environment `staging`, service `MudPuppy`. Production is untouched. Log capture used the Railway CLI (`railway logs --environment staging`) in Phases 1 and 2 and that is the known-good route; the Railway MCP tool is an acceptable alternative, and the tool actually used must be named in the SUMMARY.

Two standing instructions that constrain the whole walkthrough:
- **Do not sign the owner out at any point.** Sign-in is one-time-code based and signing back in is the owner's step, not the executor's. Nothing in Phase 3's validation requires a new session — and after plan 03-07, the code is no longer in the log to read.
- **Never invent an API key.** The Gemini key is supplied by the owner at the checkpoint in task 03-13-02. If it is not supplied, capture RUN A and the D-20 evidence, stop, and report which files could not be produced.
</context>

<tasks>

<task type="auto">
  <name>Task 03-13-01: The test report and the harness self-test are captured verbatim as files</name>
  <files>.planning/phases/03-one-ai-decision/evidence/01-test-report.txt, .planning/phases/03-one-ai-decision/evidence/02-harness-selftest.txt</files>
  <read_first>
    - .planning/phases/03-one-ai-decision/03-VALIDATION.md (Sampling Rate — the phase-gate commands — and the Per-Task Verification Map, whose automated commands are the ones that must appear in this report)
    - .planning/phases/03-one-ai-decision/03-01-SUMMARY.md through 03-12-SUMMARY.md (what each plan reports as done, so gaps are caught before the checkpoints)
    - scripts/verify-phase3.sh (its usage block: the flags, the environment variables and the report path argument)
    - .planning/phases/02-autopilot-switch/evidence/01-test-report.txt (the Phase 2 report's exact layout, which this one matches so every canned report in this project reads the same way)
  </read_first>
  <action>
    Create the directory `.planning/phases/03-one-ai-decision/evidence/`.

    From the repository root, run each command below and append its command line, its full output and its exit status to `evidence/01-test-report.txt`. Capture verbatim — no summarising, no truncation, no reformatting. Prefix each with a line reading `### <command>` and follow each with `EXIT: <n>` so the file is greppable.

    1. `go build ./...`
    2. `go vet ./internal/... ./cmd/...`
    3. `go test ./... -v`
    4. `go test ./internal/session/... -race -v`
    5. `go test ./internal/session/... -run TestWindow -race -v`
    6. `go test ./internal/session/... -run TestTranscript -race -v`
    7. `go test ./internal/session/... -run TestAutopilotHandler_AIConfigRefusal -race -v`
    8. `go test ./internal/session/... -run TestPushAI -race -v`
    9. `go test ./internal/gemini/... -race -v`
    10. `go test ./internal/config/... -v`
    11. `go test ./internal/driver/... -run TestHandleEngage -race -v`
    12. `go test ./internal/icm/... -run TestDispatch_AutomationPassThrough -race -v`
    13. `go test ./internal/auth/... -run TestOTPNotLogged -v`
    14. `go test ./internal/profiles/... -run "TestListSessions|TestGetSessionTranscript|TestDecisionsReload" -v`
    15. `go test ./internal/store/... -run TestEngageGate -v` — the Phase 1 regression the gate still depends on
    16. `cd frontend && npm run build`
    17. `git diff --stat <phase-base-commit> -- go.mod frontend/package.json` under a `### DEPENDENCY DRIFT` heading. This phase installs no package, so a non-empty section is a supply-chain finding (T-3-SC), not a nuisance.
    18. `grep -rniE "gemini-[0-9]|flash-latest|generativelanguage" internal/ cmd/ --include=*.go | grep -v "_test.go"` under a `### MODEL LITERALS` heading. Zero output is the pass condition (REQ-env-config, CON-model-config).

    End the file with a line `GO TEST EXIT: <n>` carrying the exit status of command 3, matching the convention both earlier harnesses use.

    `internal/icm` failures that predate this phase are annotated as the pre-existing baseline with a one-line note naming them; any failure in a package this phase touched blocks the phase and must be fixed before the checkpoints below are attempted.

    Then produce `evidence/02-harness-selftest.txt` the same way, with two `### ` sections: `bash scripts/verify-phase3.sh --self-test /tmp/phase3-selftest.txt` followed by the contents of that file and its exit status, and `bash scripts/verify-phase3.sh --self-test-negative /tmp/phase3-negative.txt` followed by the contents of that file and its exit status. The first must be clean and exit 0; the second must contain `FAIL C3` and exit non-zero. This is what makes the staging report worth reading: the harness is demonstrably able to fail (T-3-10).

    Run no SQL. Nothing in this phase is proven by a query.
  </action>
  <verify>
    <automated>bash scripts/verify-phase3.sh --self-test /tmp/phase3-selftest.txt && grep -c "GO TEST EXIT" .planning/phases/03-one-ai-decision/evidence/01-test-report.txt && grep -c "FAIL C3" .planning/phases/03-one-ai-decision/evidence/02-harness-selftest.txt</automated>
  </verify>
  <acceptance_criteria>
    - `evidence/01-test-report.txt` exists and contains a `### ` heading for each of the eighteen commands
    - The report contains `EXIT: 0` for `go build ./...`, for `go vet`, for each of the eleven targeted `go test` runs and for `npm run build`
    - The report contains zero lines matching `^--- FAIL` outside the annotated pre-existing `internal/icm` baseline, and the annotation names each baseline entry
    - The `-race` sections contain no `DATA RACE` line
    - The `### DEPENDENCY DRIFT` section is empty (T-3-SC)
    - The `### MODEL LITERALS` section is empty (REQ-env-config)
    - The report ends with a `GO TEST EXIT:` line
    - `evidence/02-harness-selftest.txt` contains both self-test sections; the first shows zero lines beginning with `FAIL` and `EXIT: 0`, the second contains at least one `FAIL C3` line and a non-zero exit status
    - No SQL statement appears anywhere in either file
  </acceptance_criteria>
  <done>The diagnostic half of the Phase Validation line exists as two files a reader can open, with no new failures, no data race, no dependency drift, no model literal in source, and a harness that has proven it can fail.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 03-13-02: Staging refuses to drive before it is configured, and is then configured by the owner</name>
  <files>.planning/phases/03-one-ai-decision/evidence/03-canned-report.txt, .planning/phases/03-one-ai-decision/evidence/08-refused-not-configured.png, .planning/phases/03-one-ai-decision/evidence/09-auto-unknown-option.png</files>
  <what-built>
    The model registry read from environment configuration and the `AIConfigured` predicate (plan 03-02), the second engage gate that refuses `#AUTO ON` with the D-20 sentence when that predicate is false (plan 03-06), the narrowed `#AUTO` grammar (plan 03-04), and the canned-report harness that drives all of it over HTTP (plan 03-12). Together they make the "not configured" half of Phase 3 provable on staging before any API key exists — which is the only order in which it can honestly be demonstrated.
  </what-built>
  <read_first>
    - scripts/verify-phase3.sh (its usage block: `BASE_URL`, `SESSION_COOKIE`, `CONNECTION_ID`, `NOT_OWNED_CONNECTION_ID`, the report path and the four invocations)
    - .planning/phases/03-one-ai-decision/evidence/02-harness-selftest.txt (from task 03-13-01 — the harness has already proven it can fail)
    - .planning/phases/03-one-ai-decision/03-02-SUMMARY.md (the five environment variable names the owner must set, recorded there by the executor of that plan)
    - .planning/phases/03-one-ai-decision/03-UI-SPEC.md Copywriting Contract (the exact D-20 refusal line and the exact D-11 unknown-option line, so the screenshots can be checked character for character)
    - .planning/phases/01-profile-foundation-and-policy-gate/01-05-PLAN.md (the Phase 1 staging capture procedure that worked, including the Railway CLI route)
  </read_first>
  <action>
    Deploy the branch to Railway staging with the Gemini environment variables still absent, prove the D-20 refusal and the D-11 grammar line from that state, and only then have the owner supply the key so the variables can be set. The owner approves this checkpoint before the walkthrough in task 03-13-03 begins.
  </action>
  <how-to-verify>
    1. Deploy the branch to Railway project `mudpuppy`, environment `staging`, service `MudPuppy`. Do not deploy to production.
    2. Confirm in the deploy log that migration `011` was applied at startup and that the process started normally with no Gemini variables set (D-20's "the server starts normally"). Keep this excerpt; it goes into `evidence/04-staging-ai-player.log` in the next task.
    3. Using the browser session that is already signed in — do not sign out — pick or create a connection profile that **has** accepted the Safety and Abuse policy, and note its `connection_id`.
    4. Run the harness against staging into a temporary file:
       `BASE_URL=https://<staging-host> SESSION_COOKIE='<cookie>' CONNECTION_ID=<connection_id> scripts/verify-phase3.sh /tmp/phase3-run-a.txt`
       Then write `evidence/03-canned-report.txt` beginning with a line `### RUN A (Gemini variables unset)` followed by that file's contents verbatim.
    5. Read RUN A and confirm two things with your own eyes, because they are the claims no unit test can make about the deployed system: the `on` step answered `refused-not-configured` with the sentence `Autopilot refused: AI is not configured on this server` character for character and left `autopilot_state` at `off`; and the `### MODEL LITERALS`-equivalent source-grep step produced no output.
    6. `08-refused-not-configured.png` — in the browser, connect that profile to the MUD and type `#AUTO ON`. The shot must show the bracketed refusal line in the terminal and the header badge still reading `Autopilot: Off` (D-20).
    7. `09-auto-unknown-option.png` — type `#AUTO STATUS`. The shot must show the single red line `[Autopilot: don't know what you're talking about. Your options are ON or OFF]`, with no other output, no change to the badge and no command reaching the game (D-11).
    8. Ask the owner for the Gemini API key and set the five environment variables named in `03-02-SUMMARY.md` on the `staging` environment of service `MudPuppy`. Never invent, guess or reuse a key from anywhere; if the owner does not supply one, stop here and report it. Redeploy or restart so the new configuration is live, and confirm from the log that the process started normally again.
    Do not run a SQL query at any point in this task. Do not sign the owner out. Do not touch production.
  </how-to-verify>
  <verify>
    <human-check>RUN A is in evidence/03-canned-report.txt with the refusal outcome and the exact D-20 sentence, both screenshots exist and show what is described, the Gemini variables are set on staging from the owner's own key, and the service restarted cleanly.</human-check>
  </verify>
  <acceptance_criteria>
    - `evidence/03-canned-report.txt` begins with `### RUN A (Gemini variables unset)` and its header records the staging `BASE_URL` and a git SHA
    - RUN A contains `PASS C3`, zero lines starting with `FAIL`, and prints the response body under every step
    - RUN A's autopilot step shows `refused-not-configured` and the sentence `Autopilot refused: AI is not configured on this server` character for character
    - `evidence/08-refused-not-configured.png` shows the bracketed refusal in the terminal and a badge reading `Autopilot: Off`, from the end-user perspective: the MUDPuppy browser page with the header badges visible, no devtools pane, no terminal window, no raw JSON in frame
    - `evidence/09-auto-unknown-option.png` shows exactly one red line with the D-11 wording character for character and an unchanged badge
    - The five Gemini environment variables are set on the `staging` environment only, and the SUMMARY records their names but never their values
    - The SUMMARY names the deploy method and states that production was not deployed to
    - No SQL query appears in this task's evidence or in the SUMMARY, and the owner was not signed out
  </acceptance_criteria>
  <resume-signal>Type "approved" once RUN A and both screenshots are on disk and the Gemini variables are set on staging, or name the file that could not be produced and what the report or screen said instead.</resume-signal>
  <done>Staging has proven it refuses to drive when it is not configured, and is now configured with the owner's own key.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 03-13-03: The owner watches the AI make one decision, and every observable becomes a file</name>
  <files>.planning/phases/03-one-ai-decision/evidence/03-canned-report.txt, .planning/phases/03-one-ai-decision/evidence/04-staging-ai-player.log, .planning/phases/03-one-ai-decision/evidence/05-decision-in-panel.png, .planning/phases/03-one-ai-decision/evidence/06-ai-assist-terminal-line.png, .planning/phases/03-one-ai-decision/evidence/07-panel-after-refresh.png, .planning/phases/03-one-ai-decision/evidence/10-decision-failure-disengage.png, .planning/phases/03-one-ai-decision/evidence/11-logs-section.png, .planning/phases/03-one-ai-decision/evidence/12-log-page-two-pane.png, .planning/phases/03-one-ai-decision/evidence/13-hand-play-unchanged.png, .planning/phases/03-one-ai-decision/evidence/14-connections-cleaned.png, .planning/phases/03-one-ai-decision/03-13-SUMMARY.md</files>
  <what-built>
    The rolling text window (plan 03-01) under the Gemini client and its env registry (plan 03-02), through the driver that validates, dispatches through the now-live ICM engine (plan 03-03) and sends (plan 03-08), onto the websocket push and the decisions endpoint (plan 03-09), into the AI Assist panel and the terminal line (plan 03-10), with every connection transcribed (plan 03-05) and readable from the Logs page (plan 03-11). This is the Phase Validation line's player-observable half, and it is the larger half: the criterion that the owner sees one decision and its reasoning has no other proof.
  </what-built>
  <read_first>
    - .planning/ROADMAP.md §Phase 3 (Success Criteria 1-4 and the Phase Validation line)
    - .planning/phases/03-one-ai-decision/03-UI-SPEC.md (what each surface must show and say, verbatim)
    - .planning/phases/03-one-ai-decision/03-CONTEXT.md (D-01, D-06 to D-10, D-12 to D-17, and the DR-2-02 profile list)
    - .planning/phases/03-one-ai-decision/03-VALIDATION.md Manual-Only Verifications table (the rows this task satisfies)
    - .planning/phases/03-one-ai-decision/evidence/03-canned-report.txt (RUN A from task 03-13-02 — the HTTP-level result the browser must agree with)
    - .planning/todos/pending/2026-09-15-phase3-security-carry-forward.md (DR-2-02's six profile names, to be deleted at the end of this task)
  </read_first>
  <action>
    Walk the play screen and the Logs page on staging against a live MUD in the order below, capture one PNG per observable into `evidence/` using the exact filenames listed, append RUN B of the canned report and the `[AI-PLAYER]` log excerpt, close DR-2-02, and then write `03-13-SUMMARY.md` with the four-row criterion table specified at the end. Capture with a browser automation tool such as Chrome MCP, or let the owner take the shots by hand — either is acceptable, as it was in Phases 1 and 2, and the owner confirms the set at this checkpoint.
  </action>
  <how-to-verify>
    On staging, signed in as the owner, with a live MUD connection and the Gemini variables now set. Do not sign out at any point. Every shot must be the browser as the owner sees it — the play screen or the Logs page with the header visible, no devtools pane, no terminal window, no raw JSON in frame.

    1. Connect an AI-activated profile (policy accepted) to the MUD and hand-play a couple of commands so the window has text in it. Confirm the AI Assist panel is present and open with its empty state (D-07).
    2. Type `#AUTO ON`. `05-decision-in-panel.png` must show the panel carrying one decision: the short reasoning in plain language, then the command it issued on its own line (D-08, D-10). The badge must read `Autopilot: On`.
    3. `06-ai-assist-terminal-line.png` — the terminal in the same session, showing `[AI-ASSIST > {command}]` and, beneath it, the game's own reply to that command (D-09, ROADMAP criterion 1). One frame, both facts.
    4. Hard-refresh the page and take no other action. `07-panel-after-refresh.png` must show the same decision still in the panel, rebuilt from the server (D-12, ROADMAP criterion 2).
    5. Confirm nothing further was issued: after the single decision the badge still reads `Autopilot: On` and no second command appeared (D-03). Record this observation in the SUMMARY's step notes; it needs no separate shot.
    6. `10-decision-failure-disengage.png` — force one failure without touching the server's configuration: set the profile's AI model name in Settings to a model that does not exist, return to the play screen, type `#AUTO OFF` then `#AUTO ON`. The shot must show a failure notice with the same wording in both the panel and the terminal and the badge reading `Autopilot: Off`, with no command sent to the game (D-13). Set the model name back to blank afterwards.
    7. `11-logs-section.png` — in Settings for that profile, select Logs. The shot must show the section with its description and the `Open Logging` button (D-16). Press it, and confirm the Settings tab did not navigate — record that observation in the SUMMARY.
    8. `12-log-page-two-pane.png` — in the newly opened tab, select the session from this walkthrough. The shot must show the session list on the left and the transcript on the right, with a human-typed line, the AI-issued `[AI-ASSIST > ...]` line and game output all visible and visibly distinct (D-14, D-17).
    9. `13-hand-play-unchanged.png` — create a fresh profile, do **not** accept the policy on it, connect and hand-play several commands. The shot must show ordinary play with no AI Assist panel, no minimized tab, no new colour and no new control, with the badge reading `Autopilot: Off` (03-UI-SPEC.md's Regression Invariant, Phase 2 D-12).
    10. Run the harness again against staging into a temporary file with the same arguments as RUN A, and append to `evidence/03-canned-report.txt` a line `### RUN B (Gemini variables set, after the walkthrough)` followed by that file's contents verbatim. Confirm its decisions step now returns the walkthrough's decision with a reasoning, a command and an outcome, and its sessions step returns the walkthrough's session with `human`, `ai` and `game` lines — this is how "a decisions row exists and is returned after a refresh" is proven without a query.
    11. Capture the `[AI-PLAYER]` lines this walkthrough produced with `railway logs --environment staging | grep '\[AI-PLAYER\]'`, or the Railway MCP log tool, or the dashboard log pane with copy-all. Name the tool used in the SUMMARY. Save to `evidence/04-staging-ai-player.log`, together with the startup excerpt from task 03-13-02 showing migration `011` applied. Confirm it contains, for the test connection id: `[AI-PLAYER] autopilot` lines with `cause=engage` and `cause=refused-not-configured`; `[AI-PLAYER] decision` lines with `stage=request`, `stage=dispatch` and `stage=sent`, and a `stage=failed` line from step 6; and `[AI-PLAYER] transcript` lines with `event=open` and `event=close`.
    12. Read the log excerpt back and confirm it contains no MUD output, no reasoning text, no command text, no conduct-rules or approach-guidance text, no API key, no endpoint URL (T-3-07, T-3-02) — and, for DR-2-01, no one-time sign-in code for any sign-in that occurred during the capture window.
    13. Close DR-2-02: delete the six throwaway staging profiles named in the carry-forward todo — Phase 2 Harness, Harness B, Walkthrough, Refusal, Hand Play, and Phase 1 Evidence Walkthrough — through the app, using the owner's signed-in session. Never sign the owner out to do it. `14-connections-cleaned.png` must show the Connections list with none of the six present. Any profile still needed as a Phase 3 fixture is deleted too, after its evidence is captured.
    14. Write `03-13-SUMMARY.md`. Its centrepiece is a table with one row per ROADMAP Phase 3 success criterion 1 to 4, each row naming the evidence that proves it and a PASS or FAIL verdict:

    | Criterion | What it claims | Evidence | Verdict |
    |-----------|----------------|----------|---------|
    | 1 | With autopilot engaged, the driver reads the game text, sends one request with conduct rules and approach guidance included, and issues the returned command through the ICM automation context | `evidence/01-test-report.txt` lines n-m (TestHandleEngage), `evidence/05-decision-in-panel.png`, `evidence/06-ai-assist-terminal-line.png`, `evidence/04-staging-ai-player.log` lines n-m (stage=request, stage=dispatch, stage=sent) | PASS |
    | 2 | The decision and the reasoning appear in the play screen as they happen and are stored, surviving a page refresh | `evidence/05-decision-in-panel.png`, `evidence/07-panel-after-refresh.png`, `evidence/03-canned-report.txt` line n (RUN B decisions step), `evidence/12-log-page-two-pane.png` | PASS |
    | 3 | Model names and API key come from environment configuration; nothing is hard-coded; the server fails clearly if they are absent when engagement is attempted | `evidence/03-canned-report.txt` line n (RUN A, PASS C3), `evidence/08-refused-not-configured.png`, `evidence/01-test-report.txt` line n (MODEL LITERALS section empty) | PASS |
    | 4 | An automation-context command is dispatched through the ICM dispatcher and safety checker, and the frontend adapter no longer falls back silently | `evidence/01-test-report.txt` lines n-m (TestDispatch_AutomationPassThrough), `evidence/03-canned-report.txt` line n (ICM route non-404), `evidence/13-hand-play-unchanged.png` | PASS |

    Line numbers are required for report and log citations — "see the report" is not a citation. Screenshots are cited by filename. No row may cite a database query. Add a short closing section recording the DR-2-01 and DR-2-02 closures and pointing at `03-SECURITY-AGENDA.md`.
  </how-to-verify>
  <verify>
    <human-check>All ten screenshots exist under evidence/ with the exact filenames, each showing the stated content; RUN B is appended to the canned report and its decisions and sessions steps return the walkthrough's own data; the log excerpt carries every named line and no text it should not; the six DR-2-02 profiles are gone; and the SUMMARY's four-row table cites a real file and line number for every criterion.</human-check>
  </verify>
  <acceptance_criteria>
    - All ten PNGs exist with exactly these names: `05-decision-in-panel.png`, `06-ai-assist-terminal-line.png`, `07-panel-after-refresh.png`, `08-refused-not-configured.png`, `09-auto-unknown-option.png`, `10-decision-failure-disengage.png`, `11-logs-section.png`, `12-log-page-two-pane.png`, `13-hand-play-unchanged.png`, `14-connections-cleaned.png`
    - Every PNG is from the end-user perspective: the MUDPuppy browser page with the header visible, no devtools pane, no terminal, no raw JSON in frame
    - `05-decision-in-panel.png` shows a reasoning sentence and a separate command line inside the panel, with the badge reading `Autopilot: On`
    - `06-ai-assist-terminal-line.png` shows `[AI-ASSIST > ` and the game's reply in the same frame
    - `07-panel-after-refresh.png` shows the same decision after a reload with no re-engage action taken
    - `10-decision-failure-disengage.png` shows the same failure sentence in the panel and the terminal and a badge reading `Autopilot: Off`
    - `12-log-page-two-pane.png` shows a session selected with a `>` human line, an `[AI-ASSIST > ...]` line and unprefixed game output all visible
    - `13-hand-play-unchanged.png` shows no AI Assist panel and no minimized tab anywhere on the page
    - `14-connections-cleaned.png` shows a Connections list containing none of the six DR-2-02 profile names
    - `evidence/03-canned-report.txt` contains both `### RUN A` and `### RUN B` sections; RUN B contains `PASS C2`, `PASS C3`, `PASS C4` and zero lines starting with `FAIL`
    - `evidence/04-staging-ai-player.log` contains at least one line for each of `cause=engage`, `cause=refused-not-configured`, `stage=request`, `stage=dispatch`, `stage=sent`, `stage=failed`, `event=open` and `event=close`, plus the migration `011` startup line
    - `grep -ciE "conduct_rules|approach_guidance|reasoning=|AIza|x-goog-api-key|generativelanguage" .planning/phases/03-one-ai-decision/evidence/04-staging-ai-player.log` returns 0 (T-3-07, T-3-02)
    - `grep -ci "code:" .planning/phases/03-one-ai-decision/evidence/04-staging-ai-player.log` returns 0 (DR-2-01)
    - `03-13-SUMMARY.md` contains a four-row criterion table; every row cites at least one evidence file, report and log citations carry line numbers, and every row carries a PASS or FAIL
    - No row in the table cites a SQL query or a database inspection as its evidence, and the SUMMARY states the owner was never signed out
  </acceptance_criteria>
  <resume-signal>Type "approved" to record the phase as demonstrated, or list the screenshots that could not be produced and what was on screen instead.</resume-signal>
  <done>The ROADMAP Phase Validation has been performed on staging, every criterion points at a file — a report line, a log line, or an image — and the six leftover staging profiles are gone.</done>
</task>

<task type="auto">
  <name>Task 03-13-04: The Phase 3 security review gets a written agenda with nothing decided in advance</name>
  <files>.planning/phases/03-one-ai-decision/03-SECURITY-AGENDA.md</files>
  <read_first>
    - .planning/phases/03-one-ai-decision/03-CONTEXT.md Deferred Ideas, the paragraph beginning "Security review items for this phase (raise, do not decide here)" — the owner's own list, in his framing
    - .planning/phases/03-one-ai-decision/03-RESEARCH.md Security Domain (the threat table, the policy tensions, and Open Question 1 on ICM's RecordExecution gap) and the Assumptions Log entry A1 on free-tier rate limits
    - .specify/specs/safety-and-abuse-policy-v1.md sections 1, 2, 5 and 6 (the text the transcript capture and the resume behaviour are weighed against — quote it, do not paraphrase from memory)
    - .planning/todos/pending/2026-09-15-phase3-security-carry-forward.md (DR-2-01 and DR-2-02 with the owner's wording and the proposed remediations, verbatim)
    - .planning/phases/02-autopilot-switch/02-SECURITY-AGENDA.md (the format this document follows) and .planning/RISK-REGISTER.md (where the owner's decisions are appended)
    - the `threat_model` blocks of plans 03-01 through 03-12 (the T-3 register this phase produced, including every row dispositioned `accept` or `defer to security review`)
  </read_first>
  <action>
    Write `.planning/phases/03-one-ai-decision/03-SECURITY-AGENDA.md`: the input to the Phase 3 security review, not its output. It decides nothing. Every open item carries the same three dispositions — Accept, Defer, Remediate Now — with none marked as chosen and none recommended, because that choice is the owner's at the review.

    Open with one paragraph stating what the agenda is for and that the project cannot close while any item on it remains deferred.

    Six open items, in this order:

    1. **Transcript and window retention against the policy's data handling.** State plainly what is now stored: every saved-profile connection's full game traffic from connect to disconnect, including other players' speech and including hand play with no AI involved (D-14), plus the game-text window the model saw on each decision (`ai_decisions.window_text`, D-12). Quote policy section 6's sentence on captured game text verbatim and state the tension in one sentence. Record that no retention limit, no deletion path and no export exists today, and that the decisions endpoint deliberately does not serve `window_text` to the browser. Name the candidate remediation, should Remediate Now be chosen, as a retention window after which lines are deleted, with the window itself left to the owner. Cross-reference T-3-09 and T-3-15.
    2. **A resume now acts.** Phase 2's security review accepted AR-2-01, an unbounded WAITING state that returns to ON by itself, when nothing acted on it. From this phase a resume fires a real decision and a real command (D-02). State that plainly, quote policy section 2, and note that `AutopilotRecord.WaitingSince` is still stored so a bounded waiting lifetime remains available without redesign. Cross-reference T-3-14 and Phase 2's T-2-08.
    3. **The log page's authorization scope.** State what is enforced: every transcript and decision read resolves through `ProfileStore.GetProfileByConnection(userID, connectionID)` and the transcript read also filters on the connection id, so a guessed session id returns nothing. State the open question the owner asked to have raised: whether per-profile, signed-in scoping is the right boundary for stored game text at all, given the page is a plain SPA route anyone with the owner's session can open. Cross-reference T-3-04.
    4. **The Gemini key on staging and the free-tier limits.** State that the key lives only in Railway staging environment variables, travels only in an `x-goog-api-key` header, is never logged and never appears in the repository or in any evidence file. Record that Google does not publish fixed free-tier numbers (RESEARCH Assumption A1), that a rate-limit answer is treated as an ordinary D-13 failure with no retry (D-19), and that the per-session call cap is Phase 4's mechanism, not this phase's. Cross-reference T-3-02 and T-3-19.
    5. **The driver sends without passing the human wheel-grab.** State the mechanism exactly: the driver reaches the MUD through `SendCommandAs(..., "ai")` rather than the browser's websocket ingress, so the wheel-grab check that disengages on human input never sees it — by construction, since the driver is not human input — while ICM's dispatcher and safety checker do gate it and the transcript marks the line `ai`. State the residual for the owner: a server-side send path exists that no browser-side control mediates. Cross-reference T-3-06 and Phase 2's T-2-01, which Phase 2's agenda said Phase 3 must re-confirm.
    6. **Prompt injection through game text.** State the mitigation that exists — Go-side validation rejecting a multi-line command or one prefixed `#`, `@`, `$` or `%`, applied before the dispatcher, with a test row per attack shape — and the residual: a hostile room description can still steer *which* legitimate game command the model chooses. Cross-reference T-3-01.

    Then a **closures** section recording, for the owner to confirm rather than decide: **DR-2-01** closed by plan 03-07 — reproduce the owner's "Mark this as MUST FIX in next Phase" and the proposed remediation verbatim, state what was built, cite the `TestOTPNotLogged` lines in `evidence/01-test-report.txt` and the absence of a `code:` line in `evidence/04-staging-ai-player.log`, and carry forward the one part of that remediation that is not a code change and remains open: "review Railway log access on the staging project", with its own three dispositions. And **DR-2-02** closed by plan 03-13 — the six profiles deleted through the app, cited to `evidence/14-connections-cleaned.png`.

    Then a short carry-forward table of the Phase 3 threats dispositioned `accept` inside the plans rather than mitigated, so the review sees them in one place: T-3-SC (no packages installed; confirm `go.mod` and `frontend/package.json` are unchanged, citing the report's empty dependency-drift section), T-3-19 (cost and free-tier consumption) and T-3-25 (the retired `#AUTO STATUS` line). State for each that it was accepted at plan level and is listed for confirmation, not re-litigation.

    Close with two forward notes: Phase 4 must decide whether the driver calls ICM's `RecordExecution` after a successful send, because ICM's own circuit-breaker and rate-limit counters do not accumulate for pass-through commands (RESEARCH Open Question 1, harmless at one command per engage and not harmless in a loop); and the seventh failure notice added for an ICM refusal, which is copy the owner has not yet seen.

    Build no remediation in this task and change no code. If the owner chooses Remediate Now on any item, that becomes its own plan or its own phase. Append nothing to `.planning/RISK-REGISTER.md` here — the owner's decisions are recorded there at the review, not before it.
  </action>
  <verify>
    <automated>test -f .planning/phases/03-one-ai-decision/03-SECURITY-AGENDA.md && grep -c "Remediate Now" .planning/phases/03-one-ai-decision/03-SECURITY-AGENDA.md && grep -c "DR-2-01" .planning/phases/03-one-ai-decision/03-SECURITY-AGENDA.md && grep -c "DR-2-02" .planning/phases/03-one-ai-decision/03-SECURITY-AGENDA.md && grep -c "T-3-14" .planning/phases/03-one-ai-decision/03-SECURITY-AGENDA.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/03-one-ai-decision/03-SECURITY-AGENDA.md` exists and contains six numbered open items in the order listed above
    - Each of the six items, and the carried-forward Railway log-access question, lists all three dispositions `Accept`, `Defer` and `Remediate Now`, and none is marked as selected or recommended
    - The agenda quotes policy sections 2 and 6 rather than paraphrasing them, and cites `T-3-01`, `T-3-02`, `T-3-04`, `T-3-06`, `T-3-09`, `T-3-14`, `T-3-15` and `T-3-19`
    - The closures section reproduces the owner's DR-2-01 and DR-2-02 wording verbatim from the carry-forward todo and cites a named evidence file for each closure
    - The agenda contains a carry-forward table naming `T-3-SC`, `T-3-19` and `T-3-25`
    - The agenda states that the project cannot close while any item remains deferred
    - The agenda contains the two forward notes: ICM's `RecordExecution` gap for Phase 4, and the seventh failure notice
    - Source assertion: `git status --porcelain` shows changes only under `.planning/` for this task, and `.planning/RISK-REGISTER.md` is unchanged
  </acceptance_criteria>
  <done>The security review has a written agenda: six open questions and two closures, each with three choices and none of them made in advance.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repository → Railway staging | The deployed binary may diverge from the repo; the canned report's git SHA header and the log are what reconcile them |
| Owner browser session → the live MUD and the live model | The walkthrough exercises the real session, websocket, driver and vendor call end to end |
| Evidence files → readers | Logs, reports and screenshots are committed to the repo; whatever they contain is disclosed |
| Owner's API key → Railway environment variables | A real credential is set on staging during this plan |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigating Task |
|-----------|----------|----------|-----------|-------------|-----------------|
| T-3-10 | Repudiation (unfalsifiable evidence) | High | a canned report that always passes | mitigate | 03-13-01 — the negative self-test is captured to `evidence/02-harness-selftest.txt` before the staging report is trusted |
| T-3-02 | Information Disclosure | High | the Gemini key reaching the repository, a screenshot, a report or a log excerpt | mitigate | 03-13-02 sets the variables on staging and records names only; 03-13-03 greps the log excerpt for key and endpoint patterns before filing it |
| T-3-07 | Information Disclosure | Medium | the committed log excerpt and screenshots carrying game text, reasoning or profile prose | mitigate | 03-13-03 — the excerpt is read back and grepped for conduct-rules, guidance and reasoning text; screenshots are end-user frames with no raw JSON |
| T-3-05 (DR-2-01) | Information Disclosure / Spoofing | High | the one-time sign-in code in the staging log | mitigate | 03-13-03 — the capture window covers a fresh sign-in and the acceptance criteria assert no `code:` line; 03-13-04 records the closure and carries the open log-access question to the review |
| T-3-44 (DR-2-02) | Information Disclosure | Low | six throwaway profiles left on the owner's staging account | mitigate | 03-13-03 — deleted through the app with the owner's own session, proven by `evidence/14-connections-cleaned.png`; the owner is never signed out |
| T-3-17 | Tampering (wrong target) | Medium | a deploy, an environment variable or a harness run aimed at production | mitigate | 03-13-02 — staging only, and the report header records `BASE_URL` and the git SHA so every piece of evidence is attributable to an environment |
| T-3-06 | Elevation of Privilege | High | the driver's send path bypassing the browser-side wheel-grab | defer to security review | 03-13-04 — written onto the agenda as item 5 with three dispositions and no decision, re-confirming Phase 2's T-2-01 as that phase's agenda required |
| T-3-09 / T-3-15 | Information Disclosure (policy) | Medium | full transcript and decision-window retention | defer to security review | 03-13-04 — agenda item 1, quoting policy section 6, with no remediation built here |
| T-3-14 | Policy risk | Medium | a WAITING-to-ON resume now firing a real command | defer to security review | 03-13-04 — agenda item 2, quoting policy section 2 |
| T-3-45 | Repudiation (carried-forward risks forgotten) | Medium | DR-2-01 and DR-2-02 | mitigate | 03-13-04 — both closures are recorded with their original wording and cited evidence, and the agenda states the project cannot close while any item remains deferred |
| T-3-SC | Tampering (supply chain) | Low | `go.mod`, `frontend/package.json` | accept | 03-13-01 — the `### DEPENDENCY DRIFT` section of the test report must be empty for the whole phase |
</threat_model>

<verification>
1. `evidence/01-test-report.txt` exists with every command captured verbatim, zero new FAIL lines, no data race, an empty dependency-drift section and an empty model-literals section.
2. `evidence/02-harness-selftest.txt` shows a clean self-test and a negative self-test containing `FAIL C3` with a non-zero exit.
3. `evidence/03-canned-report.txt` contains RUN A and RUN B; RUN A proves the D-20 refusal, RUN B proves the stored decision and the transcript, and neither contains a `FAIL` line.
4. `evidence/04-staging-ai-player.log` contains the migration `011` line, every named autopilot cause, every named decision stage, the transcript open and close, and no game text, reasoning, key, endpoint or sign-in code.
5. Ten screenshots exist under `evidence/` with the exact filenames listed, all from the end-user perspective.
6. `03-13-SUMMARY.md`'s four-row table cites a real file, with a line number for text evidence, per ROADMAP criterion, with a verdict.
7. `03-SECURITY-AGENDA.md` lists six open items plus the carried-forward log-access question, each with three dispositions and none chosen, and records both DR closures.
8. Both checkpoints are explicitly approved by the owner.
9. No evidence in this phase is a database query, and the owner was never signed out.
</verification>

<success_criteria>
- ROADMAP Phase 3 criterion 1 demonstrated by the `TestHandleEngage` lines in `evidence/01-test-report.txt`, screenshots `05-decision-in-panel.png` and `06-ai-assist-terminal-line.png`, and the `stage=request`, `stage=dispatch` and `stage=sent` lines in `evidence/04-staging-ai-player.log` (D-01, D-05, D-10).
- Criterion 2 demonstrated by `05-decision-in-panel.png`, `07-panel-after-refresh.png`, the RUN B decisions step of `evidence/03-canned-report.txt` and `12-log-page-two-pane.png` (D-08, D-12, D-14, D-16, D-17).
- Criterion 3 demonstrated by the `PASS C3` line of RUN A in `evidence/03-canned-report.txt`, screenshot `08-refused-not-configured.png`, and the empty `### MODEL LITERALS` section of `evidence/01-test-report.txt` (D-18, D-19, D-20).
- Criterion 4 demonstrated by the `TestDispatch_AutomationPassThrough` lines in `evidence/01-test-report.txt`, the non-404 ICM route step of the canned report, and `13-hand-play-unchanged.png` showing ordinary play untouched.
- The `#AUTO` grammar and the failure behaviour hold in front of the owner, demonstrated by `09-auto-unknown-option.png` (D-11) and `10-decision-failure-disengage.png` (D-13).
- DR-2-01 and DR-2-02 are closed rather than re-presented, demonstrated by the absence of a sign-in code in `evidence/04-staging-ai-player.log` and by `14-connections-cleaned.png`, both recorded in `03-SECURITY-AGENDA.md`.
- The Phase 3 security review can begin, because its agenda exists in writing with the transcript retention question, the resume-now-acts question, the log page's scope, the staging key, the driver's wheel-grab bypass and prompt injection all on it.
</success_criteria>

<output>
Create `.planning/phases/03-one-ai-decision/03-13-SUMMARY.md` when done, containing the evidence file inventory, the four-row criterion table with citations and verdicts, the DR-2-01 and DR-2-02 closure notes, and a pointer to `03-SECURITY-AGENDA.md`.
</output>
