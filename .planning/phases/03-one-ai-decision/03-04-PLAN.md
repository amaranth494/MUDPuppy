---
phase: 03-one-ai-decision
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/services/automation/evaluator.ts
autonomous: true
requirements: [REQ-single-decision]

must_haves:
  truths:
    - "Typing #AUTO ON or #AUTO OFF still does exactly what Phase 2 made it do, proven by evidence/05-decision-in-panel.png and evidence/10-decision-failure-disengage.png both showing the switch responding to the directive"
    - "Typing #AUTO with no argument, #AUTO STATUS, or any other argument prints one red local line reading [Autopilot: don't know what you're talking about. Your options are ON or OFF] and sends nothing to the game and nothing to the server, proven by evidence/09-auto-unknown-option.png (D-11)"
    - "The Phase 2 #AUTO STATUS diagnostic line no longer exists in the browser grammar, proven by the source assertion that no 'STATUS' branch remains in the AUTO case (D-11 amends Phase 2 D-05)"
    - "The server's status action on POST /api/v1/session/autopilot is untouched and still available to the badge and the panel, proven by the source assertion that no Go file changed in this plan"
  artifacts:
    - path: "frontend/src/services/automation/evaluator.ts"
      provides: "The narrowed #AUTO grammar: ON and OFF, and one line for everything else"
      contains: "Your options are ON or OFF"
  key_links:
    - from: "frontend/src/services/automation/evaluator.ts case 'AUTO'"
      to: "context.outputMessage"
      via: "the existing getAnsiColorCode('red') local-echo path, never a call to the server"
      pattern: "getAnsiColorCode\\('red'\\)"
---

<objective>
Narrow the directive to the two positions the switch actually has. `#AUTO` accepts `ON` and `OFF`; a bare `#AUTO`, `#AUTO STATUS`, or anything else prints one line saying so. The Phase 2 diagnostic status line is retired — the badge and the AI Assist panel are the status surfaces now.

Purpose: D-11 amends Phase 2's D-05 in the owner's own words: "I don't think auto status should do anything. It should just be auto on, auto off. And if you put something else in there, it just says, don't know what you're talking about. Your options are on or off." This is a browser-grammar change only, with no server change: the `status` action on `POST /api/v1/session/autopilot` stays as a wire action the badge and panel may use.
Output: one narrowed `case 'AUTO':` block in the automation evaluator.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/03-one-ai-decision/03-CONTEXT.md
@.planning/phases/03-one-ai-decision/03-UI-SPEC.md
@.planning/phases/03-one-ai-decision/03-PATTERNS.md
@.planning/phases/02-autopilot-switch/02-CONTEXT.md

Evidence rule for this phase (owner-directed, binding): every criterion is proven by a canned report or an end-user screenshot; a database query is never evidence. This plan has no automated test available — the repository has no frontend test runner (`frontend/package.json`'s `"test"` script is a no-op) — so its automated gate is `npm run build` plus source assertions, and its owner-facing proof is screenshot `evidence/09-auto-unknown-option.png`.

The copy is locked by 03-UI-SPEC.md's Copywriting Contract and must be reproduced character for character, including the lower-case "don't":

`[Autopilot: don't know what you're talking about. Your options are ON or OFF]`

03-UI-SPEC.md section 3 also fixes that this is a terminal-only line — a directive's own reply — and never a panel system line.
</context>

<tasks>

<task type="auto">
  <name>Task 03-04-01: #AUTO answers only to ON and OFF, and says so once for anything else</name>
  <files>frontend/src/services/automation/evaluator.ts</files>
  <read_first>
    - frontend/src/services/automation/evaluator.ts lines 1150-1245 (the whole `case 'AUTO':` block — the `rawArg === '' || rawArg === 'STATUS'` branch mapping to `action = 'status'` at ~1175, the existing red invalid-argument line at ~1188, the `getAnsiColorCode` helper at ~49, and the outcome-handling switch including the `'status'` arm at ~1218-1225)
    - .planning/phases/03-one-ai-decision/03-UI-SPEC.md sections "Copywriting Contract" (the exact D-11 line) and "3. `#AUTO` grammar line"
    - .planning/phases/03-one-ai-decision/03-PATTERNS.md section "`#AUTO` grammar: single `case 'AUTO':` block, narrowed in place"
    - .planning/phases/02-autopilot-switch/02-CONTEXT.md decisions D-05, D-10, D-11 (what Phase 2 built here, so nothing else in the block is disturbed)
  </read_first>
  <action>
    Edit the single `case 'AUTO':` block in `frontend/src/services/automation/evaluator.ts`. Change only what D-11 requires; the `ON` and `OFF` paths, their server call, their notices and their outcome handling stay exactly as Phase 2 left them.

    Remove the branch that maps a bare `#AUTO` and `#AUTO STATUS` to `action = 'status'`, and remove the `'status'` arm of the outcome-handling switch that printed the Phase 2 diagnostic line. After this change the only two argument values the block accepts are `ON` and `OFF` (keep the existing case-insensitive uppercase normalisation of `rawArg`).

    Make every other argument — including the empty string, `STATUS`, and any unrecognised word — fall into the single existing invalid-argument branch, and replace that branch's message text with the locked D-11 wording from 03-UI-SPEC.md, character for character:
    `[Autopilot: don't know what you're talking about. Your options are ON or OFF]`
    Keep the existing `getAnsiColorCode('red')` colouring and the existing `context.outputMessage?.(...)` local-echo call — this line is written locally, is never sent to the MUD, and never calls the server. Exactly one line is printed; do not also print a usage hint or an empty line.

    Do not touch the `AutopilotAnswer`/`setState` signature in `frontend/src/services/automation.ts`, the `setAutopilot` client in `frontend/src/services/api.ts`, or anything in `internal/session/handler.go`. D-11 retires `#AUTO STATUS` as a typed directive only; the server's `status` wire action remains available to the badge and to the AI Assist panel, and removing it is explicitly out of scope for this plan.

    If TypeScript reports the `'status'` member of the action union as now-unused, leave the union as it is — it is still the server's wire vocabulary.
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion (the locked copy, verbatim): `grep -c "Your options are ON or OFF" frontend/src/services/automation/evaluator.ts` returns 1, and the same line contains `don't know what you're talking about`
    - Source assertion (D-11 retires the branch): inside the `case 'AUTO':` block there is no branch testing for `'STATUS'` — `sed -n '/case .AUTO.:/,/^    }/p' frontend/src/services/automation/evaluator.ts | grep -c "STATUS"` returns 0
    - Source assertion (one line, not two): the invalid-argument branch contains exactly one `context.outputMessage` call
    - Source assertion (local only): the invalid-argument branch contains no `setState`, no `fetch`, and no call into `api.ts`
    - Source assertion (server untouched): `git diff --name-only` for this plan lists only `frontend/src/services/automation/evaluator.ts`
    - Source assertion (colour reuse, per 03-UI-SPEC.md): the invalid-argument branch still calls `getAnsiColorCode('red')`
    - `cd frontend && npm run build` exits 0 with no TypeScript error
    - `git diff --stat frontend/package.json` is empty (T-3-SC)
    - Deferred proof: `evidence/09-auto-unknown-option.png` shows the line in the terminal after typing `#AUTO STATUS`, with no other output and the badge unchanged
  </acceptance_criteria>
  <done>`#AUTO` takes ON and OFF; anything else gets one red line and nothing reaches the game or the server.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Typed input → the directive grammar | Anything the owner types that starts with `#` is interpreted locally before it can reach the game |
| Directive grammar → the MUD | A directive must never leak through as a game command |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigating Task |
|-----------|----------|----------|-----------|-------------|-----------------|
| T-3-24 | Tampering | Low | an unrecognised `#AUTO` argument leaking to the MUD as a game command | mitigate | 03-04-01 — the unknown-argument path writes a local line through `context.outputMessage` and returns; the acceptance criteria assert there is no `setState` and no `fetch` on that path |
| T-3-25 | Repudiation (a status surface that lies) | Low | retiring the `#AUTO STATUS` diagnostic line | accept | D-11 makes the badge and the AI Assist panel the status surfaces; both read server-held state (Phase 2's badge, plan 03-10's panel), so no truthfulness is lost with the line |
| T-3-SC | Tampering (supply chain) | Low | `frontend/package.json` | accept | This plan installs no package; `frontend/package.json` must be unchanged at plan end |
</threat_model>

<verification>
1. `cd frontend && npm run build` exits 0.
2. The three source assertions in the acceptance criteria hold (locked copy present, no `STATUS` branch, no server call on the invalid path).
3. `git diff --name-only` shows exactly one changed file and no Go file.
4. `git diff --stat frontend/package.json` is empty (T-3-SC).
5. Screenshot `evidence/09-auto-unknown-option.png` is captured by plan 03-13.
</verification>

<success_criteria>
- `#AUTO` accepts only `ON` and `OFF`, and any other argument prints the one locked line, demonstrated by `evidence/09-auto-unknown-option.png` and by the verbatim-copy source assertion (D-11).
- The Phase 2 `#AUTO STATUS` diagnostic line is retired from the browser grammar, demonstrated by the no-`STATUS`-branch source assertion (D-11 amending Phase 2 D-05).
- Nothing about `#AUTO ON` and `#AUTO OFF` changes, demonstrated by those directives still working in `evidence/05-decision-in-panel.png` and `evidence/10-decision-failure-disengage.png`.
</success_criteria>

<output>
Create `.planning/phases/03-one-ai-decision/03-04-SUMMARY.md` when done.
</output>
