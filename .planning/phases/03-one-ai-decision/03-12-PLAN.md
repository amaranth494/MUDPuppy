---
phase: 03-one-ai-decision
plan: 12
type: execute
wave: 5
depends_on: [03-05, 03-06, 03-09]
files_modified:
  - scripts/verify-phase3.sh
  - scripts/fixtures/phase3/
  - scripts/fixtures/phase3-negative/
autonomous: true
requirements: [REQ-env-config, REQ-reasoning-visibility]

must_haves:
  truths:
    - "One command turns Phase 3's HTTP-reachable behaviour into a PASS/FAIL line per ROADMAP success criterion, with the request path and the response body printed under every step, proven by evidence/03-canned-report.txt (evidence rule)"
    - "The harness can fail: run against a deliberately wrong fixture set it prints FAIL C3 and exits non-zero, proven by evidence/02-harness-selftest.txt (T-3-10)"
    - "The harness needs no database and no live model: it asserts only on HTTP responses, go test output and source greps, proven by the source assertion that no SQL client is invoked anywhere in the script"
    - "With the Gemini variables unset, the harness shows #AUTO ON refused with the D-20 sentence and the switch still off, which is the one Phase 3 behaviour provable on staging before the owner supplies a key, proven by the C3 steps of evidence/03-canned-report.txt (D-20)"
    - "No model identifier is a literal in Go source, proven by the grep step recorded in evidence/03-canned-report.txt returning zero matches outside test files (REQ-env-config)"
    - "The ICM routes that have never existed now answer, proven by the C4 route step of evidence/03-canned-report.txt returning a status other than 404"
  artifacts:
    - path: "scripts/verify-phase3.sh"
      provides: "The Phase 3 canned-report harness: live mode, self-test, negative self-test, and a no-tests flag"
      contains: "PASS C3"
    - path: "scripts/fixtures/phase3/"
      provides: "A correct-server fixture set the self-test asserts against"
    - path: "scripts/fixtures/phase3-negative/"
      provides: "A deliberately wrong fixture set that must produce FAIL C3"
  key_links:
    - from: "scripts/verify-phase3.sh"
      to: "POST /api/v1/session/autopilot and GET /api/v1/profiles/{id}/decisions and .../sessions"
      via: "curl with the supplied session cookie, response bodies printed under each step"
      pattern: "/api/v1/"
    - from: "scripts/verify-phase3.sh"
      to: "evidence/03-canned-report.txt"
      via: "the script's own tee of the report path argument"
      pattern: "PASS C"
---

<objective>
Make Phase 3 checkable by one command. `scripts/verify-phase3.sh` drives every Phase 3 behaviour that is reachable over HTTP or provable by a source grep, prints a PASS, FAIL or SKIP line per ROADMAP success criterion with the request path and response body under each step, and proves it can fail before anyone trusts a clean run.

Purpose: the owner's evidence rule allows exactly two kinds of proof, a canned report or a screenshot, and this is the canned report. Phase 2's `scripts/verify-phase2.sh` established the shape, including the part that matters most: a negative self-test, so a report full of PASS lines carries information rather than reassurance.
Output: the harness, a correct fixture set and a deliberately wrong one.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/ROADMAP.md
@.planning/phases/03-one-ai-decision/03-VALIDATION.md
@.planning/phases/03-one-ai-decision/03-PATTERNS.md
@.planning/phases/03-one-ai-decision/03-CONTEXT.md
@.planning/phases/03-one-ai-decision/03-05-SUMMARY.md
@.planning/phases/03-one-ai-decision/03-06-SUMMARY.md
@.planning/phases/03-one-ai-decision/03-09-SUMMARY.md

Evidence rule for this phase (owner-directed, binding): every criterion is proven by a canned report — a test or script run captured verbatim to a file — or an end-user screenshot. A database query is never evidence, and this script must not contain one: no `psql`, no `DATABASE_URL` use, no SQL of any kind. Everything it asserts, it asserts through the application's own HTTP surface, `go test`, or a source grep.

ROADMAP Phase 3's four success criteria, which are the C1 to C4 of this report:
1. With autopilot engaged, the driver reads the current game text, sends one request to Gemini with the profile's conduct rules and approach guidance included, and issues the returned command through the ICM automation context.
2. The decision and the model's reasoning appear in the play screen as they happen and are stored in the session log, where they survive a page refresh.
3. Model names and API key come from environment configuration on staging; nothing is hard-coded, and the server fails clearly if they are absent when engagement is attempted.
4. A command submitted on the server in the automation execution context is dispatched through the ICM engine's dispatcher and safety checker, demonstrable by a diagnostic test, and the frontend adapter's ICM calls no longer fall back silently to browser-side logic.

C1 and the live half of C2 are not HTTP-reachable: they need a connected game and a live model, and engaging autopilot at all requires a connected session. They are printed as SKIP lines naming the screenshots and log excerpt that prove them instead — never as PASS.
</context>

<tasks>

<task type="auto">
  <name>Task 03-12-01: One command produces a PASS/FAIL line per Phase 3 criterion against a running server</name>
  <files>scripts/verify-phase3.sh</files>
  <read_first>
    - scripts/verify-phase2.sh (whole file — the usage block, `set -uo pipefail` with the comment explaining why `errexit` is omitted, the `_get_field` JSON helper, the step/PASS/FAIL/SKIP printing, the fixture-mode plumbing, the report header with `BASE_URL` and the git SHA, and the appended `go test` run)
    - .planning/phases/03-one-ai-decision/03-VALIDATION.md (the Phase Requirements to Test Map rows that name this script, and the Test Infrastructure row describing it)
    - internal/session/handler.go (plan 03-06 — the `refused-not-configured` outcome and the exact refusal sentence) and `internal/profiles/decisions.go` / `internal/profiles/logs.go` (plans 03-09 and 03-05 — the response shapes to assert on)
    - .planning/phases/02-autopilot-switch/02-07-PLAN.md (how the Phase 2 report was captured and what the owner read in it)
  </read_first>
  <action>
    Create `scripts/verify-phase3.sh` following `scripts/verify-phase2.sh`'s shape exactly: a usage block explaining the four invocations, `set -uo pipefail` with the same comment about why `errexit` is deliberately omitted (a failed check must not abort the report), the same `_get_field` JSON helper, the same per-step printing of the request path and full response body, the same `tee` to the report path argument, and the same header recording `BASE_URL` and the current git SHA so every report is attributable to an environment and a commit.

    Environment inputs: `BASE_URL`, `SESSION_COOKIE`, `CONNECTION_ID` (a profile that **has** accepted the policy, so the Phase 1 gate passes and the Phase 3 configuration gate is the one under test), and optional `NOT_OWNED_CONNECTION_ID` defaulting to a fixed literal UUID no profile will ever have. Flags: `--self-test`, `--self-test-negative`, `--no-tests`.

    Steps and their criterion mapping:

    - `C3` — the configuration gate and the source rule, the heart of this report:
      1. `POST /api/v1/session/autopilot` with `{"action":"on","connection_id":"$CONNECTION_ID"}`. When the Gemini variables are unset on the target server, assert `outcome` is `refused-not-configured` and the message is `Autopilot refused: AI is not configured on this server` character for character, and that `state` is `off`. When they are set, assert the outcome is one of `engaged` or `refused-no-session` and print a note that the configuration gate passed — the report must read correctly in both worlds, and which one it ran in is recorded in the header.
      2. `GET /api/v1/session/status` and assert `autopilot_state` agrees with the previous step.
      3. A source grep step: `grep -rniE "gemini-[0-9]|flash-latest|generativelanguage" internal/ cmd/ --include=*.go | grep -v "_test.go"` must produce no output. Print the command and its output under the step; a non-empty result is FAIL C3.
    - `C2` — the stored half of reasoning visibility:
      4. `GET /api/v1/profiles/$CONNECTION_ID/decisions` and assert HTTP 200 with a `decisions` array present (possibly empty before the walkthrough, non-empty after — assert presence and well-formedness, and when it is non-empty assert the newest entry carries `reasoning`, `command` and `outcome`). This is the step that proves a decision survives a refresh without anyone running a query.
      5. `GET /api/v1/profiles/$NOT_OWNED_CONNECTION_ID/decisions` and assert it is refused, not answered with someone else's rows.
      6. `GET /api/v1/profiles/$CONNECTION_ID/sessions` and assert HTTP 200 with a `sessions` array; when non-empty, read the newest id and `GET .../sessions/{id}` asserting a `lines` array whose entries carry a `source` in `human|ai|game|marker`. This is the transcript half of the session log (D-14, D-17).
      7. Print `SKIP C2 (live half)` naming `evidence/05-decision-in-panel.png` and `evidence/07-panel-after-refresh.png`.
    - `C4` — the ICM wiring:
      8. `POST /api/v1/icm/validate` with a minimal body and assert the status is anything other than 404 — before this phase those routes did not exist at all.
      9. Append `go test ./internal/icm/... -run TestDispatch_AutomationPassThrough -v` and `go test ./internal/driver/... -run TestHandleEngage -v` unless `--no-tests` is given, and assert both exit 0.
    - `C1`:
      10. Print `SKIP C1` naming `evidence/06-ai-assist-terminal-line.png` and the `stage=request`, `stage=dispatch` and `stage=sent` lines of `evidence/04-staging-ai-player.log`, with a one-line explanation that engaging autopilot requires a connected game session this script must not fabricate.

    End the report with a `GO TEST EXIT: <n>` line, matching the convention both earlier harnesses use so every canned report in this project reads the same way. Exit non-zero when any FAIL line was printed.

    No `psql`, no `DATABASE_URL`, no SQL anywhere. No API key is read, printed or required by this script: it never calls the model.
  </action>
  <verify>
    <automated>bash -n scripts/verify-phase3.sh && grep -c "PASS C3\|FAIL C3" scripts/verify-phase3.sh</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/verify-phase3.sh` exists, is executable, and `bash -n scripts/verify-phase3.sh` exits 0
    - Running it with no arguments prints the full usage block and exits non-zero
    - The script contains `set -uo pipefail` and the comment explaining why `errexit` is omitted
    - Source assertion (evidence rule): `grep -ciE "psql|DATABASE_URL|SELECT |INSERT |sqlcmd" scripts/verify-phase3.sh` returns 0
    - Source assertion (no secret handling): `grep -ciE "AI_MODEL_.*_KEY|GEMINI_API_KEY|x-goog-api-key" scripts/verify-phase3.sh` returns 0
    - The script contains the locked D-20 sentence `Autopilot refused: AI is not configured on this server` for its character-for-character assertion
    - The script contains steps producing lines beginning `PASS C2`, `PASS C3`, `PASS C4`, `SKIP C1` and `SKIP C2`
    - The script ends its report with a `GO TEST EXIT:` line and exits non-zero when any `FAIL` line was printed
    - `git diff --stat go.mod frontend/package.json` is empty (T-3-SC)
  </acceptance_criteria>
  <done>One command produces a report that states, criterion by criterion, whether Phase 3's reachable half holds, with every request and response printed underneath.</done>
</task>

<task type="auto">
  <name>Task 03-12-02: The harness proves it can fail before anyone trusts a clean run</name>
  <files>scripts/fixtures/phase3/, scripts/fixtures/phase3-negative/, scripts/verify-phase3.sh</files>
  <read_first>
    - scripts/verify-phase2.sh (the `--self-test` / `--self-test-negative` plumbing: how a fixture file stands in for a live response, the ordered `FIXTURE_FILES` array, and how the negative set differs by exactly one wrong response)
    - scripts/fixtures/phase2/ and scripts/fixtures/phase2-negative/ (the fixture file naming and JSON shapes)
    - .planning/phases/02-autopilot-switch/02-07-PLAN.md task 02-07-01 (what `evidence/02-harness-selftest.txt` has to contain and why: a harness that cannot fail proves nothing)
    - scripts/verify-phase3.sh (task 03-12-01)
  </read_first>
  <action>
    Create `scripts/fixtures/phase3/` holding one JSON file per HTTP step in task 03-12-01, in the order the script consumes them, named the way the Phase 2 fixtures are named. They represent a correct server: the autopilot refusal with `outcome: "refused-not-configured"` and the exact D-20 message, a status response agreeing with it, a decisions response with one well-formed decision carrying a reasoning, a command and `outcome: "sent"`, a refusal for the not-owned connection, a sessions list with one session, and a transcript with one `human` line, one `ai` line and one `game` line.

    Create `scripts/fixtures/phase3-negative/` as a copy of that set with exactly one deliberate error: the autopilot response carries `outcome: "engaged"` and `state: "on"` where the correct server refuses. One wrong response, so the negative run's output points at a single criterion rather than being noisy.

    Wire `--self-test` and `--self-test-negative` in `scripts/verify-phase3.sh` to read those directories instead of calling `curl`, exactly as the Phase 2 harness does, and to skip the appended `go test` runs and the source-grep step so both modes run with no server and no repository state. The self-test must exit 0 with zero `FAIL` lines; the negative self-test must print `FAIL C3` and exit non-zero.

    No fixture contains a real API key, a real session cookie, a real email address or real game text. Use obviously synthetic values — a transcript line of `You are standing in a small room.` and a command of `north` are enough.
  </action>
  <verify>
    <automated>bash scripts/verify-phase3.sh --self-test /tmp/phase3-selftest.txt && ! bash scripts/verify-phase3.sh --self-test-negative /tmp/phase3-negative.txt && grep -c "FAIL C3" /tmp/phase3-negative.txt</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/fixtures/phase3/` and `scripts/fixtures/phase3-negative/` both exist with one file per HTTP step, and the two sets differ in exactly one file (`diff -rq` names one file)
    - `bash scripts/verify-phase3.sh --self-test /tmp/phase3-selftest.txt` exits 0 and the produced report contains zero lines beginning with `FAIL`
    - `bash scripts/verify-phase3.sh --self-test-negative /tmp/phase3-negative.txt` exits non-zero and the produced report contains at least one `FAIL C3` line
    - Both self-test modes run with no server reachable and no `curl` call: `grep -n "curl" scripts/verify-phase3.sh` shows every invocation guarded by the live-mode branch
    - Source assertion (no secrets in fixtures): `grep -rciE "@|AIza|session=[a-f0-9]{16}" scripts/fixtures/phase3 scripts/fixtures/phase3-negative` returns 0
    - `bash -n scripts/verify-phase3.sh` exits 0
    - `git diff --stat go.mod frontend/package.json` is empty (T-3-SC)
  </acceptance_criteria>
  <done>The harness has demonstrated it can fail, so a clean staging report is worth reading.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator shell → staging over HTTP | The harness carries a real session cookie for the owner's account |
| Harness output → the repository | The report is committed as evidence, so whatever it prints is disclosed |
| Fixture files → the repository | Fixtures are committed and read by anyone browsing the repo |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigating Task |
|-----------|----------|----------|-----------|-------------|-----------------|
| T-3-10 | Repudiation (unfalsifiable evidence) | High | a canned report that always passes | mitigate | 03-12-02 — the negative self-test is captured to `evidence/02-harness-selftest.txt` before the staging report is trusted, and the two fixture sets differ by exactly one response |
| T-3-42 | Information Disclosure | Medium | a session cookie, an API key or real game text in the committed report or fixtures | mitigate | 03-12-01 asserts the script never reads or prints a key; 03-12-02 asserts the fixtures contain no address, key or realistic cookie; plan 03-13 reads the report back before filing it |
| T-3-17 | Tampering (wrong target) | Medium | a harness run aimed at production | mitigate | 03-12-01 — the report header records `BASE_URL` and the git SHA, so every piece of evidence is attributable to an environment |
| T-3-43 | Repudiation | Medium | proving a stored decision by querying the database | mitigate | 03-12-01 — the decisions and transcript steps go through the application's own endpoints, and a grep asserts no SQL client appears in the script |
| T-3-SC | Tampering (supply chain) | Low | `go.mod`, `frontend/package.json` | accept | This plan installs no package; both must be unchanged at plan end |
</threat_model>

<verification>
1. `bash -n scripts/verify-phase3.sh` exits 0 and running with no arguments prints usage and exits non-zero.
2. `bash scripts/verify-phase3.sh --self-test /tmp/phase3-selftest.txt` exits 0 with zero FAIL lines.
3. `bash scripts/verify-phase3.sh --self-test-negative /tmp/phase3-negative.txt` exits non-zero with at least one `FAIL C3` line.
4. `grep -ciE "psql|DATABASE_URL|SELECT " scripts/verify-phase3.sh` returns 0.
5. `git diff --stat go.mod frontend/package.json` is empty (T-3-SC).
</verification>

<success_criteria>
- Phase 3's HTTP-reachable half is checkable by one command with a PASS/FAIL line per ROADMAP criterion, demonstrated by `evidence/03-canned-report.txt` (evidence rule, ROADMAP criteria 2, 3 and 4).
- The report is falsifiable, demonstrated by the `FAIL C3` line and non-zero exit in `evidence/02-harness-selftest.txt` (T-3-10).
- The D-20 refusal holds on staging with the Gemini variables unset, demonstrated by the C3 steps of `evidence/03-canned-report.txt` (D-20, REQ-env-config).
- Nothing about the model is hard-coded in Go source, demonstrated by the zero-output grep step of the same report (REQ-env-config, CON-model-config).
</success_criteria>

<output>
Create `.planning/phases/03-one-ai-decision/03-12-SUMMARY.md` when done, listing the fixture files and the one file that differs in the negative set.
</output>
