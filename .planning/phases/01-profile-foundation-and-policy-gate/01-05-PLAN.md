---
phase: 01-profile-foundation-and-policy-gate
plan: 05
type: execute
wave: 4
depends_on: [01-01, 01-02, 01-03, 01-04, 01-06]
files_modified:
  - .planning/phases/01-profile-foundation-and-policy-gate/evidence/01-test-report.txt
  - .planning/phases/01-profile-foundation-and-policy-gate/evidence/02-staging-startup.log
  - .planning/phases/01-profile-foundation-and-policy-gate/evidence/03-canned-report.txt
  - .planning/phases/01-profile-foundation-and-policy-gate/evidence/04-staging-ai-player.log
  - .planning/phases/01-profile-foundation-and-policy-gate/evidence/05-policy-first.png
  - .planning/phases/01-profile-foundation-and-policy-gate/evidence/06-accepted-line.png
  - .planning/phases/01-profile-foundation-and-policy-gate/evidence/07-values-after-reload.png
  - .planning/phases/01-profile-foundation-and-policy-gate/evidence/08-values-new-session.png
  - .planning/phases/01-profile-foundation-and-policy-gate/evidence/09-blank-fields.png
  - .planning/phases/01-profile-foundation-and-policy-gate/evidence/10-after-timers-save.png
  - .planning/phases/01-profile-foundation-and-policy-gate/evidence/11-recreated-profile-policy-again.png
  - .planning/phases/01-profile-foundation-and-policy-gate/01-05-SUMMARY.md
autonomous: false
requirements: [REQ-profile-ai-fields, REQ-policy-gate]

must_haves:
  truths:
    - "The full Go suite, the policy diff gate and the frontend build are green on the phase branch and no package was added, proven by evidence/01-test-report.txt containing zero FAIL lines and an empty dependency-drift section (T-1-SC)"
    - "Migration 010 is live on Railway staging, proven by the log lines `Migrations completed successfully (version=10` and `AI Player columns ensured` in evidence/02-staging-startup.log — settling RESEARCH Open Question 1 from the log, not from a query"
    - "Phase 1 holds end to end on staging, proven by evidence/03-canned-report.txt showing PASS for C1, C2, C3 and C4 with the response body printed under every step"
    - "The engage gate refuses then allows the same profile purely on the strength of the stored acceptance, proven by the `engage gate ... allowed=false` and `engage gate ... allowed=true` lines in evidence/04-staging-ai-player.log"
    - "Acceptance is recorded once, proven by exactly one `[AI-PLAYER] policy accepted` line and a following `[AI-PLAYER] policy already accepted` line carrying the same timestamp in evidence/04-staging-ai-player.log"
    - "A fresh profile presents the policy before anything is editable, proven by screenshot evidence/05-policy-first.png, and one Accept press reveals the acceptance line and the editor without a reload, proven by evidence/06-accepted-line.png"
    - "Edited values survive a hard reload and a new login session, proven by screenshots evidence/07-values-after-reload.png and evidence/08-values-new-session.png; cleared fields come back blank, proven by evidence/09-blank-fields.png"
    - "An unrelated timers save leaves the AI fields intact, proven by screenshot evidence/10-after-timers-save.png and the final PASS line of evidence/03-canned-report.txt (T-1-06)"
    - "Deleting the profile and recreating it for the same game asks for the policy again, proven by screenshot evidence/11-recreated-profile-policy-again.png (D-06)"
  artifacts:
    - path: ".planning/phases/01-profile-foundation-and-policy-gate/evidence/01-test-report.txt"
      provides: "Canned report: go build, full go test -v, the policy diff gate, npm run build, and the dependency-drift check, captured verbatim"
      contains: "GO TEST EXIT"
    - path: ".planning/phases/01-profile-foundation-and-policy-gate/evidence/02-staging-startup.log"
      provides: "Staging startup excerpt proving migration 010 applied"
      contains: "Migrations completed successfully (version=10"
    - path: ".planning/phases/01-profile-foundation-and-policy-gate/evidence/03-canned-report.txt"
      provides: "scripts/verify-phase1.sh output against staging: PASS/FAIL per ROADMAP criterion with response bodies"
      contains: "PASS C3"
    - path: ".planning/phases/01-profile-foundation-and-policy-gate/evidence/04-staging-ai-player.log"
      provides: "Staging log excerpt carrying the acceptance, repeat-acceptance, both gate decisions and the settings save"
      contains: "[AI-PLAYER] policy accepted"
    - path: ".planning/phases/01-profile-foundation-and-policy-gate/evidence/05-policy-first.png"
      provides: "Screenshot: fresh profile shows policy text, Version 1.0 and a single Accept Policy button, no editor"
    - path: ".planning/phases/01-profile-foundation-and-policy-gate/evidence/06-accepted-line.png"
      provides: "Screenshot: ✓ Accepted v1.0 on <date> plus the five-field editor, no page reload"
    - path: ".planning/phases/01-profile-foundation-and-policy-gate/evidence/07-values-after-reload.png"
      provides: "Screenshot: all five edited values present after a hard reload"
    - path: ".planning/phases/01-profile-foundation-and-policy-gate/evidence/11-recreated-profile-policy-again.png"
      provides: "Screenshot: a recreated profile for the same game is asked for the policy again"
    - path: ".planning/phases/01-profile-foundation-and-policy-gate/01-05-SUMMARY.md"
      provides: "One row per ROADMAP success criterion 1-4, each citing the evidence file and line numbers or screenshot that prove it, with a PASS/FAIL verdict"
      contains: "Phase Validation"
  key_links:
    - from: "cmd/server/main.go startup logging"
      to: "evidence/02-staging-startup.log"
      via: "railway logs capture after deploy"
      pattern: "Migrations completed successfully \\(version=10"
    - from: "scripts/verify-phase1.sh run against staging"
      to: "evidence/03-canned-report.txt"
      via: "the script's own tee of the report path"
      pattern: "PASS C4"
    - from: "internal/profiles/handler.go [AI-PLAYER] log lines"
      to: "evidence/04-staging-ai-player.log"
      via: "railway logs filtered on the [AI-PLAYER] prefix during the canned run"
      pattern: "engage gate connection_id=.* allowed=true"
    - from: "browser AI Player section on staging"
      to: "evidence/05-policy-first.png … evidence/11-recreated-profile-policy-again.png"
      via: "one screenshot per observable state"
      pattern: "policy-first"
---

<objective>
Demonstrate the phase and file the proof. CLAUDE.md rule 2: Phase 1 is complete when the ROADMAP's Phase Validation line has actually been performed, not when its tasks are done. The owner's evidence rule narrows how: every criterion is proven by a canned report, a log excerpt, or a screenshot. A database query proves nothing here and is not to be run as evidence.

Purpose: turn four ROADMAP success criteria into four cited rows, each pointing at a file on disk that a reader can open.
Output: an `evidence/` directory holding one test report, two staging log excerpts, one canned report and eight screenshots, plus `01-05-SUMMARY.md` mapping each criterion to the evidence that proves it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/ROADMAP.md
@.planning/phases/01-profile-foundation-and-policy-gate/01-VALIDATION.md
@.planning/phases/01-profile-foundation-and-policy-gate/01-UI-SPEC.md
@.planning/phases/01-profile-foundation-and-policy-gate/01-01-SUMMARY.md
@.planning/phases/01-profile-foundation-and-policy-gate/01-02-SUMMARY.md
@.planning/phases/01-profile-foundation-and-policy-gate/01-03-SUMMARY.md
@.planning/phases/01-profile-foundation-and-policy-gate/01-04-SUMMARY.md
@.planning/phases/01-profile-foundation-and-policy-gate/01-06-SUMMARY.md

ROADMAP Phase Validation line for Phase 1 (what this plan must demonstrate):
Diagnostic — `go test` covers default resolution for blank AI settings and the engage-gate decision (not accepted, accepted); server logs on staging show migration 010 applied at startup, one structured line per policy acceptance carrying the connection id, policy version 1.0 and the timestamp, and one line per engage-gate decision carrying the connection id and the outcome. Proof for every criterion is a UAT finding from the browser walkthrough or a server log excerpt; database queries are not accepted as evidence.
Player-observable — open the AI Player section on a fresh profile and the policy appears first; accept once, then the settings editor is usable; edit the three fields, reload, values persist; call the engage gate on an un-accepted profile and receive the refusal message; on the accepted profile it passes; delete the profile, recreate it, and the policy is asked again.

Evidence directory (create it in task 01-05-01): `.planning/phases/01-profile-foundation-and-policy-gate/evidence/`. Every filename below is fixed — earlier plans' `must_haves` cite these exact names.

| File | Type | Produced by |
|------|------|-------------|
| `01-test-report.txt` | canned report | task 01-05-01 |
| `02-staging-startup.log` | log excerpt | task 01-05-02 |
| `03-canned-report.txt` | canned report | task 01-05-02 (`scripts/verify-phase1.sh`) |
| `04-staging-ai-player.log` | log excerpt | task 01-05-02 |
| `05-policy-first.png` … `11-recreated-profile-policy-again.png` | screenshots, end-user perspective only | task 01-05-03 |

Environment: Railway project `mudpuppy`, environment `staging`. Production is untouched.
</context>

<tasks>

<task type="auto">
  <name>Task 01-05-01: The test report is captured verbatim as a file</name>
  <files>.planning/phases/01-profile-foundation-and-policy-gate/evidence/01-test-report.txt</files>
  <read_first>
    - .planning/phases/01-profile-foundation-and-policy-gate/01-VALIDATION.md (Sampling Rate section — the phase-gate commands)
    - .planning/phases/01-profile-foundation-and-policy-gate/01-01-SUMMARY.md, 01-02-SUMMARY.md, 01-03-SUMMARY.md, 01-04-SUMMARY.md, 01-06-SUMMARY.md (what each earlier plan reports as done, so gaps are caught before the checkpoints)
  </read_first>
  <action>
    Create the directory `.planning/phases/01-profile-foundation-and-policy-gate/evidence/`.

    From the repository root, run each command below and append its command line, its full output and its exit status to `evidence/01-test-report.txt`. Capture verbatim — no summarising, no truncation, no reformatting. Prefix each with a line reading `### <command>` and follow each with `EXIT: <n>` so the file is greppable.

    1. `go build ./...`
    2. `go test ./... -v`
    3. `diff -u .specify/specs/safety-and-abuse-policy-v1.md internal/policy/safety-and-abuse-policy-v1.md` (empty output is the pass — record the empty section explicitly rather than omitting it)
    4. `go test ./internal/store/... -run TestResolveAISettings -v`
    5. `go test ./internal/store/... -run TestEngageGate -v`
    6. `go test ./internal/policy/... -run TestParseVersion -v`
    7. `go test ./internal/profiles/... -run TestAISettings -v`
    8. `go test ./internal/profiles/... -run TestEngageGateHandler -v`
    9. `go test ./internal/profiles/... -run TestAIPlayerLogLines -v`
    10. `bash scripts/verify-phase1.sh --self-test /tmp/phase1-selftest.txt` followed by the contents of that file (proves the canned-report harness itself is sound before it is trusted against staging)
    11. `cd frontend && npm run build`
    12. `git diff --stat <phase-base-commit> -- go.mod frontend/package.json` under a `### DEPENDENCY DRIFT` heading — this phase installs no packages, so a non-empty section is a supply-chain finding (T-1-SC), not a nuisance.

    End the file with a line `GO TEST EXIT: <n>` carrying the exit status of command 2, matching the convention `scripts/verify-phase1.sh` uses, so both canned reports read the same way.

    `internal/icm` failures that predate this phase are annotated in the report as the pre-existing baseline with a one-line note naming them; any failure in `internal/store`, `internal/policy` or `internal/profiles` blocks the phase and must be fixed before the checkpoints below are attempted.

    Run no SQL. The schema is proven by the staging startup log in the next task.
  </action>
  <verify>
    <automated>go test ./... -v > /dev/null && bash scripts/verify-phase1.sh --self-test /tmp/phase1-selftest.txt && cd frontend && npm run build && grep -c "GO TEST EXIT" ../.planning/phases/01-profile-foundation-and-policy-gate/evidence/01-test-report.txt</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/01-profile-foundation-and-policy-gate/evidence/01-test-report.txt` exists and contains a `### ` heading for each of the twelve commands
    - The report contains zero lines matching `^FAIL` outside the annotated pre-existing `internal/icm` baseline, and `grep -c "^--- FAIL" evidence/01-test-report.txt` matches only baseline entries named in the annotation
    - The report contains `EXIT: 0` for `go build ./...`, for each of the six targeted `go test` runs, for the `--self-test` run and for `npm run build`
    - The `### DEPENDENCY DRIFT` section is empty (T-1-SC)
    - The report's `diff -u` section is present and empty
    - The report ends with a `GO TEST EXIT:` line
    - No SQL statement appears anywhere in the report
  </acceptance_criteria>
  <done>The diagnostic half of the Phase Validation line exists as a file a reader can open, with zero new failures and no dependency drift.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 01-05-02: Staging proves itself in its own logs and in the canned report</name>
  <files>.planning/phases/01-profile-foundation-and-policy-gate/evidence/02-staging-startup.log, .planning/phases/01-profile-foundation-and-policy-gate/evidence/03-canned-report.txt, .planning/phases/01-profile-foundation-and-policy-gate/evidence/04-staging-ai-player.log</files>
  <what-built>
    Migration `010_add_ai_fields` with the version-reporting startup log and the AI column fallback (plan 01-01), the five endpoints with their four `[AI-PLAYER]` log lines (plan 01-03), and the verification harness `scripts/verify-phase1.sh` (plan 01-06). Together they make staging provable from its own log plus one report file, with no database access at all. RESEARCH Open Question 1 asked whether the deploy path actually applies repo migrations; the startup log now answers it directly.
  </what-built>
  <read_first>
    - migrations/010_add_ai_fields.up.sql
    - cmd/server/main.go (the migrate bootstrap with `m.Version()` and both fallback blocks)
    - scripts/verify-phase1.sh (usage block: `BASE_URL`, `SESSION_COOKIE`, `CONNECTION_ID`, report path)
    - .planning/phases/01-profile-foundation-and-policy-gate/01-RESEARCH.md Pitfall 2 and Open Question 1
  </read_first>
  <action>
    Deploy the branch to Railway staging, then produce three evidence files with no database access: `evidence/02-staging-startup.log` (the startup excerpt proving migration 010 applied), `evidence/03-canned-report.txt` (the output of `scripts/verify-phase1.sh` run against staging with a fresh profile), and `evidence/04-staging-ai-player.log` (the `[AI-PLAYER]` lines that run produced). Capture logs with the Railway MCP log tool, the Railway CLI `railway logs --environment staging`, or the dashboard log pane — name the one used in the SUMMARY. Follow the numbered steps below exactly and in order; the owner approves the three files at this checkpoint.
  </action>
  <how-to-verify>
    1. Deploy the branch to Railway project `mudpuppy`, environment `staging`.
    2. Capture the startup log. Use the Railway MCP server's log tool if it is connected, otherwise the Railway CLI (`railway logs --environment staging` — or `railway logs -d <deployment-id>`), otherwise the deployment's log pane in the Railway dashboard with copy-all. Save the excerpt from `Running database migrations` through `AI Player columns ensured` into `.planning/phases/01-profile-foundation-and-policy-gate/evidence/02-staging-startup.log`. It must contain the literal `Migrations completed successfully (version=10` and the literal `AI Player columns ensured`, and no `Warning: Failed to ensure AI Player columns` line. Name in the SUMMARY which tool produced the capture and which mechanism applied the columns (the migrate step, or the fallback) — that closes Open Question 1 for every later phase.
    3. Create a connection profile on staging that has never accepted the policy, and note its `connection_id`. Log in through the browser and copy the session cookie (the login is OTP-based, so the cookie always comes from a real login).
    4. Run the harness against staging and save its report:
       `BASE_URL=https://<staging-host> SESSION_COOKIE='<cookie>' CONNECTION_ID=<connection_id> scripts/verify-phase1.sh .planning/phases/01-profile-foundation-and-policy-gate/evidence/03-canned-report.txt`
       Expect exit status 0 and a summary block reading PASS for C1, C2, C3 and C4 with zero `FAIL` lines.
    5. Capture the `[AI-PLAYER]` log lines that run just produced, with the same Railway tool as step 2, filtering on the prefix (`railway logs --environment staging | grep '\[AI-PLAYER\]'`). Save them to `.planning/phases/01-profile-foundation-and-policy-gate/evidence/04-staging-ai-player.log`. The excerpt must contain, for that connection id: one `[AI-PLAYER] policy accepted` line carrying `version=1.0` and a timestamp, one `[AI-PLAYER] policy already accepted` line carrying the same timestamp, at least one `[AI-PLAYER] engage gate ... allowed=false` line, at least one `[AI-PLAYER] engage gate ... allowed=true` line, and at least one `[AI-PLAYER] ai settings saved` line.
    6. Read the three files back and confirm none of them contains conduct-rules text, approach-guidance text or policy prose (threat T-1-11).
    Do not run a SQL query at any point in this task. If the columns were missing, the startup log and the canned report would both say so.
  </how-to-verify>
  <verify>
    <human-check>The three evidence files exist on disk with the strings named above, the canned report shows PASS for all four criteria with zero FAIL lines, and no SQL was run.</human-check>
  </verify>
  <acceptance_criteria>
    - `evidence/02-staging-startup.log` contains `Migrations completed successfully (version=10` and `AI Player columns ensured`, and contains no `Warning: Failed to ensure AI Player columns`
    - `evidence/03-canned-report.txt` exists, its header records the staging `BASE_URL` and a git SHA, and it contains `PASS C1`, `PASS C2`, `PASS C3`, `PASS C4` and zero lines starting with `FAIL`
    - `evidence/03-canned-report.txt` prints the engage-gate refusal body containing the refusal sentence character for character, and the over-length rejection body `{"error":"Conduct rules must be 20000 characters or less"}`
    - `evidence/04-staging-ai-player.log` contains exactly one `[AI-PLAYER] policy accepted` line for the test connection id, a following `[AI-PLAYER] policy already accepted` line with the same `accepted_at`, both `allowed=false` and `allowed=true` gate lines, and an `ai settings saved` line
    - `grep -ci 'conduct_rules\|approach_guidance\|Respect the game' evidence/02-staging-startup.log evidence/04-staging-ai-player.log` returns 0 (T-1-11)
    - The SUMMARY names the tool used for log capture and states which mechanism applied the columns
    - No SQL query appears in this task's evidence or in the SUMMARY
  </acceptance_criteria>
  <resume-signal>Type "approved" once the three evidence files are on disk, or name the file that could not be produced and what the log or report said instead.</resume-signal>
  <done>Staging's own log proves the schema and the acceptance/gate behaviour, and one report file proves all four criteria over HTTP — with no database inspection anywhere.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 01-05-03: The owner sees it work, and each observable is a screenshot on disk</name>
  <files>.planning/phases/01-profile-foundation-and-policy-gate/evidence/05-policy-first.png, .planning/phases/01-profile-foundation-and-policy-gate/evidence/06-accepted-line.png, .planning/phases/01-profile-foundation-and-policy-gate/evidence/07-values-after-reload.png, .planning/phases/01-profile-foundation-and-policy-gate/evidence/08-values-new-session.png, .planning/phases/01-profile-foundation-and-policy-gate/evidence/09-blank-fields.png, .planning/phases/01-profile-foundation-and-policy-gate/evidence/10-after-timers-save.png, .planning/phases/01-profile-foundation-and-policy-gate/evidence/11-recreated-profile-policy-again.png, .planning/phases/01-profile-foundation-and-policy-gate/01-05-SUMMARY.md</files>
  <what-built>
    The AI Player section (plan 01-04) over the five endpoints (plan 01-03) over the acceptance record and gate function (plan 01-01) with the embedded policy (plan 01-02). This is the ROADMAP Phase Validation's player-observable half. The owner's rule: a visual claim is proven by an image, not by prose and not by a query.
  </what-built>
  <read_first>
    - .planning/ROADMAP.md Phase 1 (Success Criteria and the Phase Validation line)
    - .planning/phases/01-profile-foundation-and-policy-gate/01-UI-SPEC.md (what each state must show and say)
    - .planning/phases/01-profile-foundation-and-policy-gate/01-CONTEXT.md (D-03, D-04, D-06, D-12)
    - .planning/phases/01-profile-foundation-and-policy-gate/evidence/03-canned-report.txt (from task 01-05-02 — the API-level result the browser must agree with)
  </read_first>
  <action>
    Walk the AI Player section on staging in the order below and capture one PNG per observable into `evidence/`, using the eight exact filenames listed. Capture with a browser automation tool (Chrome MCP or equivalent) or let the owner take them by hand — either is acceptable, and the owner confirms the set at this checkpoint. Then write `01-05-SUMMARY.md` with the four-row criterion table specified below, citing evidence files by name and text evidence by line number.
  </action>
  <how-to-verify>
    On staging, logged in as the owner, starting from a connection profile that has never accepted the policy. Capture one PNG per step into `.planning/phases/01-profile-foundation-and-policy-gate/evidence/` using the exact filenames below. Screenshots may be captured by the executor with a browser automation tool (Chrome MCP or equivalent) or taken by the owner by hand — either is acceptable, and the owner confirms the set at this checkpoint. Every shot must show the browser window including the AI Player nav entry, so the context of each image is unambiguous.

    1. `05-policy-first.png` — Settings → the connection → AI Player on the fresh profile. Must show the policy text, the `Version 1.0` line and a single `Accept Policy` button, with no conduct-rules, approach-guidance, model, cap or threshold field and no reconnect control anywhere on screen (criterion 3; D-03, D-04, D-13).
    2. (No screenshot for the gate refusal in this phase.) The refusal has no end-user surface until Phase 2 wires `#AUTO ON`; its proof is the canned report's `C3` lines in `evidence/03-canned-report.txt` and the `[AI-PLAYER] engage gate ... allowed=false` / `allowed=true` lines in `evidence/04-staging-ai-player.log`. Screenshots in this phase show only what the end user sees in the browser: no devtools, no terminal, no JSON.
    3. `06-accepted-line.png` — press `Accept Policy` once. Must show `✓ Accepted v1.0 on <today's date>` followed by the five-field editor, with no page reload between the press and the shot (criterion 3).
    4. `07-values-after-reload.png` — enter conduct rules, approach guidance, a model name, a call cap and a disengage threshold; press `Save AI Settings`; hard-reload; reopen AI Player. Must show all five values still populated (criterion 2).
    5. `08-values-new-session.png` — log out, log back in, reopen AI Player. Must show the same five values (criterion 2).
    6. `09-blank-fields.png` — clear model name, call cap and disengage threshold; save; reload. Must show all three blank with their hint text legible (server default, no cap, built-in error handling) and no substituted numbers (criterion 1; D-09, D-10, D-11).
    7. `10-after-timers-save.png` — open Timers for the same connection, save timers, reopen AI Player. Must show the conduct rules and approach guidance still present (T-1-06).
    8. `11-recreated-profile-policy-again.png` — delete that connection profile, create a new one for the same game, open AI Player. Must show the policy presented again (criterion 4, D-06).

    Then write `01-05-SUMMARY.md`. Its centrepiece is a table with one row per ROADMAP Phase 1 success criterion 1..4, each row naming the evidence that proves it and a PASS or FAIL verdict:

    | Criterion | What it claims | Evidence | Verdict |
    |-----------|----------------|----------|---------|
    | 1 | Blank model name / call cap / threshold behave as specified; over-length rejected | `evidence/01-test-report.txt` lines <n>-<m> (TestResolveAISettings), `evidence/03-canned-report.txt` line <n> (PASS C1), `evidence/09-blank-fields.png` | PASS |
    | 2 | Values readable, editable, and survive reload and a new session | `evidence/03-canned-report.txt` line <n> (PASS C2), `evidence/07-values-after-reload.png`, `evidence/08-values-new-session.png`, `evidence/10-after-timers-save.png` | PASS |
    | 3 | Policy first, editor only after acceptance, gate refuses then allows | `evidence/05-policy-first.png`, `evidence/06-accepted-line.png`, `evidence/03-canned-report.txt` C3 lines <n>-<m>, `evidence/04-staging-ai-player.log` lines <n>-<m> | PASS |
    | 4 | Acceptance recorded once with version 1.0 and a timestamp; dies with the profile | `evidence/03-canned-report.txt` line <n> (PASS C4), `evidence/04-staging-ai-player.log` line <n>, `evidence/11-recreated-profile-policy-again.png` | PASS |

    Line numbers are required for report and log citations — "see the report" is not a citation. Screenshots are cited by filename.
  </how-to-verify>
  <verify>
    <human-check>All eight screenshots exist under evidence/ with the exact filenames, each showing the stated content, and the SUMMARY's four-row table cites a real file and line number for every criterion.</human-check>
  </verify>
  <acceptance_criteria>
    - All seven PNGs exist with exactly these names: `05-policy-first.png`, `06-accepted-line.png`, `07-values-after-reload.png`, `08-values-new-session.png`, `09-blank-fields.png`, `10-after-timers-save.png`, `11-recreated-profile-policy-again.png`
    - Every PNG is taken from the end-user perspective: the MUDPuppy browser page as the owner sees it, with no devtools pane, terminal, or raw JSON in frame
    - `05-policy-first.png` shows zero settings inputs — the editor is not merely disabled, it is absent
    - `06-accepted-line.png` shows `✓ Accepted v1.0 on` with today's date and the editor beneath it
    - `09-blank-fields.png` shows three empty fields with hint text, not zeros or a substituted model name
    - `11-recreated-profile-policy-again.png` shows the policy state on a newly created profile for the same game
    - `01-05-SUMMARY.md` contains a four-row criterion table; every row cites at least one evidence file, log and report citations carry line numbers, and every row carries a PASS or FAIL
    - No row in the table cites a SQL query or a database inspection as its evidence
  </acceptance_criteria>
  <resume-signal>Type "approved" to record the phase as demonstrated, or list the screenshots that could not be produced and what was on screen instead.</resume-signal>
  <done>The ROADMAP Phase Validation has been performed on staging and every criterion points at a file — a report line, a log line, or an image.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repository → Railway staging | The deployed binary and the deployed schema may diverge from the repo; the startup log is what reconciles them |
| Owner browser session → profile data | The walkthrough exercises the real session-scoped path end to end |
| Evidence files → readers | Logs, reports and screenshots are committed to the repo; whatever they contain is disclosed |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigating Task |
|-----------|----------|----------|-----------|-------------|-----------------|
| T-1-05 | Tampering | High | engage gate end to end | mitigate | 01-05-02 — the staging log shows the same connection id refused and then allowed purely on the strength of the stored acceptance |
| T-1-06 | Tampering (integrity) | Medium | `UpdateProfile` column fallbacks | mitigate | 01-05-02 step 10 of the canned report and 01-05-03 screenshot `10-after-timers-save.png` — an unrelated sub-resource save is proven not to blank the AI fields |
| T-1-10 | Denial of Service (schema drift) | Medium | staging deploy path | mitigate | 01-05-02 — the startup log states the applied migration version and the fallback result, closing RESEARCH Open Question 1 without a query |
| T-1-11 | Information Disclosure | Medium | committed evidence files | mitigate | 01-05-02 — the log excerpts are grepped for conduct-rules, approach-guidance and policy prose before being filed; the canned report truncates policy `text`; screenshots are of the owner's own test profile |
| T-1-SC | Tampering (supply chain) | Low | `go.mod`, `frontend/package.json` | accept | 01-05-01 — the `### DEPENDENCY DRIFT` section of the test report must be empty for the whole phase |
</threat_model>

<verification>
1. `evidence/01-test-report.txt` exists with every command captured verbatim, zero new FAIL lines and an empty dependency-drift section.
2. `evidence/02-staging-startup.log` contains `Migrations completed successfully (version=10` and `AI Player columns ensured`.
3. `evidence/03-canned-report.txt` shows PASS for C1..C4 with zero FAIL lines and prints the response body under every step.
4. `evidence/04-staging-ai-player.log` contains the acceptance, repeat-acceptance, both gate decisions and the settings-save lines.
5. Eight screenshots exist under `evidence/` with the exact filenames listed above.
6. `01-05-SUMMARY.md`'s four-row table cites a real file (and line number, for text evidence) per ROADMAP criterion, with a verdict.
7. Both checkpoints are explicitly approved by the owner.
8. No evidence in this phase is a database query.
</verification>

<success_criteria>
- ROADMAP Phase 1 criterion 1 demonstrated by the `TestResolveAISettings` lines in `evidence/01-test-report.txt`, the `PASS C1` line in `evidence/03-canned-report.txt`, and `evidence/09-blank-fields.png`.
- Criterion 2 demonstrated by the `PASS C2` line in `evidence/03-canned-report.txt` and screenshots `07-values-after-reload.png`, `08-values-new-session.png` and `10-after-timers-save.png`.
- Criterion 3 demonstrated by screenshots `05-policy-first.png` and `06-accepted-line.png`, by the `C3` lines in `evidence/03-canned-report.txt`, and by the `allowed=false` / `allowed=true` lines in `evidence/04-staging-ai-player.log`.
- Criterion 4 demonstrated by the `PASS C4` line in `evidence/03-canned-report.txt`, the paired acceptance lines in `evidence/04-staging-ai-player.log`, and `evidence/11-recreated-profile-policy-again.png`.
- Migration 010 is proven live on staging by `evidence/02-staging-startup.log`, not by any query.
</success_criteria>

<output>
Create `.planning/phases/01-profile-foundation-and-policy-gate/01-05-SUMMARY.md` when done, containing the evidence file inventory and the four-row criterion table with citations and verdicts.
</output>
