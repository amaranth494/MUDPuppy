---
phase: 01-profile-foundation-and-policy-gate
plan: 05
type: execute
wave: 4
depends_on: [01-01, 01-02, 01-03, 01-04]
files_modified: []
autonomous: false
requirements: [REQ-profile-ai-fields, REQ-policy-gate]

must_haves:
  truths:
    - "The full Go suite and the frontend build are green on the phase branch, and no package was added to go.mod or frontend/package.json (T-1-SC)"
    - "Migration 010 has run on Railway staging and a profiles row there shows all five new columns, settling RESEARCH Open Question 1 about migrate.zip"
    - "On staging, a fresh profile's AI Player section presents the policy first and the engage-gate endpoint refuses that profile with the exact message (ROADMAP criterion 3)"
    - "After one Accept press the acceptance row carries version 1.0 and a timestamp, the editor is usable, and the engage-gate endpoint allows that profile (ROADMAP criteria 3 and 4)"
    - "Edited conduct rules, approach guidance and AI settings survive a hard reload and a new login session (ROADMAP criterion 2)"
    - "Saving an unrelated sub-resource (timers) does not blank the AI fields (T-1-06)"
    - "Deleting the profile and recreating it for the same game asks for the policy again (ROADMAP criterion 4, D-06)"
  artifacts:
    - path: ".planning/phases/01-profile-foundation-and-policy-gate/01-05-SUMMARY.md"
      provides: "The phase's evidence record: test output, staging SELECT output, and the walkthrough result per ROADMAP success criterion"
      contains: "Phase Validation"
  key_links:
    - from: "Railway staging database"
      to: "migration 010 columns"
      via: "SELECT against profiles after deploy"
      pattern: "policy_version_accepted"
    - from: "browser AI Player section on staging"
      to: "GET /api/v1/profiles/{connection_id}/engage-gate"
      via: "the same acceptance record driving both surfaces"
      pattern: "engage-gate"
---

<objective>
Demonstrate the phase. CLAUDE.md rule 2: Phase 1 is complete when the ROADMAP's Phase Validation line has actually been performed, not when its tasks are done. This plan performs it and records the evidence.

Purpose: the four ROADMAP success criteria are half diagnostic (go test, database inspection) and half player-observable (the browser walkthrough). Both halves happen here, against Railway staging, and the result is written down.
Output: `01-05-SUMMARY.md` carrying the test output, the staging SELECT output, and a pass/fail line per success criterion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/ROADMAP.md
@.planning/phases/01-profile-foundation-and-policy-gate/01-VALIDATION.md
@.planning/phases/01-profile-foundation-and-policy-gate/01-UI-SPEC.md
@.planning/phases/01-profile-foundation-and-policy-gate/01-01-SUMMARY.md
@.planning/phases/01-profile-foundation-and-policy-gate/01-02-SUMMARY.md
@.planning/phases/01-profile-foundation-and-policy-gate/01-03-SUMMARY.md
@.planning/phases/01-profile-foundation-and-policy-gate/01-04-SUMMARY.md

ROADMAP Phase Validation line for Phase 1 (what this plan must demonstrate):
Diagnostic — `go test` covers default resolution for blank AI settings and the engage-gate decision (not accepted, accepted); a database inspection of a migrated profile shows the new fields and the acceptance columns carrying policy version 1.0 and a timestamp.
Player-observable — open the AI Player section on a fresh profile and the policy appears first; accept once, then the settings editor is usable; edit the three fields, reload, values persist; call the engage gate on an un-accepted profile and receive the refusal message; on the accepted profile it passes; delete the profile, recreate it, and the policy is asked again.

Environment: Railway project `mudpuppy`, environment `staging`. Production is untouched.
</context>

<tasks>

<task type="auto">
  <name>Task 01-05-01: Full suite green and no dependency drift</name>
  <files>.planning/phases/01-profile-foundation-and-policy-gate/01-05-SUMMARY.md</files>
  <read_first>
    - .planning/phases/01-profile-foundation-and-policy-gate/01-VALIDATION.md (Sampling Rate section — the phase-gate commands)
    - .planning/phases/01-profile-foundation-and-policy-gate/01-01-SUMMARY.md, 01-02-SUMMARY.md, 01-03-SUMMARY.md, 01-04-SUMMARY.md (what each earlier plan reports as done, so gaps are caught before the walkthrough)
  </read_first>
  <action>
    Run and capture, from the repository root: `go build ./...`; `go test ./... -v`; `go test ./internal/store/... -run TestResolveAISettings -v`; `go test ./internal/store/... -run TestEngageGate -v`; `go test ./internal/policy/... -run TestParseVersion -v`; `go test ./internal/profiles/... -run TestAISettings -v`; `go test ./internal/profiles/... -run TestEngageGateHandler -v`; and `cd frontend && npm run build`.

    Also run `git diff --stat main -- go.mod frontend/package.json` (or the equivalent against the phase's base commit) and confirm it is empty — this phase installs no packages, so a non-empty result is a supply-chain finding (T-1-SC), not a nuisance.

    Record each command with its exit status in the plan's SUMMARY. `internal/icm` failures that predate this phase are noted as the pre-existing baseline, not as new failures; any failure in `internal/store`, `internal/policy` or `internal/profiles` blocks the phase and must be fixed before the checkpoints below are attempted.
  </action>
  <verify>
    <automated>go test ./... -v && cd frontend && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/store/... ./internal/policy/... ./internal/profiles/... -v` exits 0
    - `cd frontend && npm run build` exits 0
    - `git diff --stat` against the phase base for `go.mod` and `frontend/package.json` produces no output
    - The SUMMARY lists all eight commands with their exit statuses
  </acceptance_criteria>
  <done>Every automated gate in 01-VALIDATION.md is green and recorded, with no dependency drift.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 01-05-02: Migration 010 is live on staging and the columns are inspectable</name>
  <what-built>
    Migration `010_add_ai_fields` (plan 01-01) plus the defensive startup `ALTER TABLE` in `cmd/server/main.go`, which together should put `conduct_rules`, `approach_guidance`, `ai_settings`, `policy_version_accepted` and `policy_accepted_at` on the staging `profiles` table whether or not the deploy path packages migrations the way the repo does. RESEARCH Open Question 1 flagged this as unsettled; this checkpoint settles it empirically.
  </what-built>
  <read_first>
    - migrations/010_add_ai_fields.up.sql
    - cmd/server/main.go (the migrate bootstrap and both fallback blocks)
    - .planning/phases/01-profile-foundation-and-policy-gate/01-RESEARCH.md Pitfall 2 and Open Question 1
  </read_first>
  <how-to-verify>
    1. Deploy the branch to Railway project `mudpuppy`, environment `staging`, and confirm the startup log shows the migrate step and the "ensuring AI columns" fallback line without a warning.
    2. Against the staging database (Railway console, `railway connect`, or the project's Postgres plugin console), run: `SELECT column_name, data_type, is_nullable, column_default FROM information_schema.columns WHERE table_name = 'profiles' AND column_name IN ('conduct_rules','approach_guidance','ai_settings','policy_version_accepted','policy_accepted_at') ORDER BY column_name;`
       Expect five rows: `ai_settings` jsonb NOT NULL default `'{}'::jsonb`; `approach_guidance` text NOT NULL default `''`; `conduct_rules` text NOT NULL default `''`; `policy_accepted_at` timestamp with time zone, nullable, no default; `policy_version_accepted` text, nullable, no default.
    3. Run `SELECT conduct_rules, approach_guidance, ai_settings, policy_version_accepted, policy_accepted_at FROM profiles LIMIT 1;` and confirm an existing profile reads back blank text, `{}` settings and NULL acceptance.
    4. Paste both query outputs into the plan SUMMARY, and state in one line which mechanism actually applied the columns (migrate step, or the startup fallback) — that answers Open Question 1 for every later phase.
  </how-to-verify>
  <verify>
    <human-check>Five columns exist on staging with the types, nullability and defaults above, and a pre-existing profile row reads blank/NULL.</human-check>
  </verify>
  <acceptance_criteria>
    - The information_schema query returns exactly five rows with the stated types and nullability
    - `policy_accepted_at` and `policy_version_accepted` are reported as nullable with no default
    - `ai_settings` is reported as jsonb NOT NULL with default `'{}'::jsonb`
    - The SUMMARY records both query outputs and names the mechanism that applied the columns
  </acceptance_criteria>
  <resume-signal>Type "approved" with the query output pasted, or describe what is missing on staging.</resume-signal>
  <done>The staging database carries the phase's schema and the deploy mechanism question is answered with evidence.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 01-05-03: The gate holds and the settings persist, walked through on staging</name>
  <what-built>
    The AI Player section (plan 01-04) over the five endpoints (plan 01-03) over the acceptance record and gate function (plan 01-01) with the embedded policy (plan 01-02). This walkthrough is the ROADMAP Phase Validation's player-observable half plus the two integrity checks that no unit test can reach.
  </what-built>
  <read_first>
    - .planning/ROADMAP.md Phase 1 (Success Criteria and the Phase Validation line)
    - .planning/phases/01-profile-foundation-and-policy-gate/01-UI-SPEC.md (what each state must show and say)
    - .planning/phases/01-profile-foundation-and-policy-gate/01-CONTEXT.md (D-03, D-04, D-06, D-12)
  </read_first>
  <how-to-verify>
    On staging, logged in as the owner, with a connection profile that has never accepted the policy:
    1. Open Settings, select that connection, click AI Player. Expect: the policy text, a `Version 1.0` line, and a single `Accept Policy` button. Expect no conduct-rules, approach-guidance, model, cap or threshold field anywhere on screen, and no reconnect control (criterion 3, D-03, D-04, D-13).
    2. Before accepting, call the gate for that profile: `GET /api/v1/profiles/<connection_id>/engage-gate` from the browser devtools network console or `curl` with the session cookie. Expect `{"allowed":false,"message":"AI Player has not been configured for this connection. Accept the Safety and Abuse policy in AI Player settings before engaging autopilot."}` — the exact Phase 2 string (criterion 3, D-05).
    3. Press `Accept Policy` once. Expect the panel to switch, without a page reload, to `✓ Accepted v1.0 on <today's date>` followed by the five-field editor (criterion 3).
    4. Call the gate again. Expect `{"allowed":true}` with no message (criterion 3).
    5. In the staging database run `SELECT policy_version_accepted, policy_accepted_at FROM profiles WHERE connection_id = '<connection_id>';`. Expect `1.0` and a timestamp. Press Accept again if the UI still allows it, or re-POST `policy/accept`, then re-run the query and confirm the timestamp is unchanged (criterion 4, D-06).
    6. Enter conduct rules, approach guidance, a model name, a call cap and a disengage threshold; press `Save AI Settings`. Hard-reload the page, reopen AI Player, and confirm all five values are still there. Log out, log back in, and confirm again (criterion 2).
    7. Clear model name, call cap and disengage threshold; save; reload. Expect all three blank, with the hints still describing server default, no cap, and built-in error handling (criterion 1, D-09, D-10, D-11).
    8. Integrity check for T-1-06: open the Timers section for the same connection, save timers (any change or an unchanged save), then reopen AI Player and confirm the conduct rules and approach guidance are still present.
    9. Delete that connection profile, create a new one for the same game, open AI Player. Expect the policy to be presented again (criterion 4, D-06).
    Record each step's outcome in the SUMMARY against its ROADMAP criterion number.
  </how-to-verify>
  <verify>
    <human-check>All nine steps observed on staging with the expected results, each recorded against the ROADMAP success criterion it demonstrates.</human-check>
  </verify>
  <acceptance_criteria>
    - Step 1 shows policy-only content: zero settings inputs rendered before acceptance
    - Step 2's response body matches the refusal string character for character
    - Step 4 returns `allowed: true` for the same profile after one accept
    - Step 5 shows `1.0` plus a timestamp, unchanged after a second accept attempt
    - Step 6 shows all five values surviving both a hard reload and a fresh login
    - Step 7 shows the three fields blank after being cleared, not repopulated with numbers or a model name
    - Step 8 shows AI fields intact after an unrelated timers save
    - Step 9 presents the policy again on the recreated profile
    - The SUMMARY maps each ROADMAP Phase 1 success criterion (1-4) to the steps that demonstrated it, with a pass or fail verdict
  </acceptance_criteria>
  <resume-signal>Type "approved" to record the phase as demonstrated, or list the steps that failed.</resume-signal>
  <done>The ROADMAP Phase Validation has been performed on staging and the evidence is written down.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repository → Railway staging | The deployed binary and the deployed schema may diverge from the repo; this plan checks both |
| Owner browser session → profile data | The walkthrough exercises the real session-scoped path end to end |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigating Task |
|-----------|----------|----------|-----------|-------------|-----------------|
| T-1-05 | Tampering | High | engage gate end to end | mitigate | 01-05-03 steps 2 and 4 — the gate is observed refusing and then allowing the same profile purely on the strength of the stored acceptance |
| T-1-06 | Tampering (integrity) | Medium | `UpdateProfile` column fallbacks | mitigate | 01-05-03 step 8 — an unrelated sub-resource save is proven not to blank the AI fields, which no pure-function test can show |
| T-1-10 | Denial of Service (schema drift) | Medium | staging deploy path | mitigate | 01-05-02 — the columns are confirmed present on staging and the applying mechanism is identified, closing RESEARCH Open Question 1 |
| T-1-SC | Tampering (supply chain) | Low | `go.mod`, `frontend/package.json` | accept | 01-05-01 — both files confirmed unchanged for the whole phase |
</threat_model>

<verification>
1. All automated commands from 01-VALIDATION.md exit 0 and are recorded.
2. The staging information_schema query returns the five expected columns.
3. The nine walkthrough steps are recorded against ROADMAP success criteria 1-4 with verdicts.
4. Both checkpoints are explicitly approved by the owner.
</verification>

<success_criteria>
- ROADMAP Phase 1 criterion 1 demonstrated: blank settings store, return blank, and resolve in Go (go test) with the columns present on staging.
- Criterion 2 demonstrated: edited values survive a reload and a new session in the browser.
- Criterion 3 demonstrated: policy first on a fresh profile, editor only after acceptance, gate refusing with the exact message before acceptance and passing after.
- Criterion 4 demonstrated: acceptance recorded once with version 1.0 and a timestamp, unchanged on repeat, and gone when the profile is deleted.
</success_criteria>

<output>
Create `.planning/phases/01-profile-foundation-and-policy-gate/01-05-SUMMARY.md` when done, containing the recorded command output, the staging query output, and the criterion-by-criterion verdict table.
</output>
