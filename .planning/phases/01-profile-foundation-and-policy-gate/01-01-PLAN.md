---
phase: 01-profile-foundation-and-policy-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - migrations/010_add_ai_fields.up.sql
  - migrations/010_add_ai_fields.down.sql
  - cmd/server/main.go
  - internal/store/profile.go
  - internal/store/profile_test.go
autonomous: true
requirements: [REQ-profile-ai-fields, REQ-policy-gate]

must_haves:
  truths:
    - "A migrated profiles row carries conduct_rules, approach_guidance, ai_settings, policy_version_accepted and policy_accepted_at, and a newly created profile has blank AI fields with NULL acceptance columns (D-07)"
    - "Go resolves a blank model name to the server-default argument, a blank call cap to no cap at all, and a blank disengage threshold to 3 consecutive transient failures, provable by go test (D-09, D-10, D-11)"
    - "store.EngageGateAllowed returns false while the acceptance columns are unset and true once both are set, provable by go test (D-08)"
    - "Recording acceptance a second time leaves the first timestamp and version untouched, because AcceptPolicy only writes when policy_accepted_at IS NULL (D-06)"
    - "The acceptance columns are unreachable through ProfileUpdate; only store.AcceptPolicy writes them (T-1-01)"
    - "A write to any other sub-resource (timers, aliases, triggers, environment) leaves the AI fields intact because UpdateProfile re-marshals every column on every write (T-1-06)"
    - "AISettings carries exactly model_name, call_cap and disengage_threshold — there is no reconnect field (D-13)"
  artifacts:
    - path: "migrations/010_add_ai_fields.up.sql"
      provides: "Five new profiles columns"
      contains: "ADD COLUMN IF NOT EXISTS policy_accepted_at"
    - path: "migrations/010_add_ai_fields.down.sql"
      provides: "Reversal of migration 010"
      contains: "DROP COLUMN IF EXISTS policy_accepted_at"
    - path: "internal/store/profile.go"
      provides: "AISettings type, Profile/ProfileUpdate fields, AcceptPolicy, ResolveAISettings, EngageGateAllowed"
      exports: ["AISettings", "ResolvedAISettings", "ResolveAISettings", "EngageGateAllowed", "EngageGateRefusalMessage", "DefaultDisengageThreshold"]
    - path: "internal/store/profile_test.go"
      provides: "Table tests for blank-setting resolution and the engage-gate decision"
      contains: "func TestResolveAISettings"
  key_links:
    - from: "internal/store/profile.go GetProfileByConnection"
      to: "profiles.policy_accepted_at"
      via: "sql.NullString scan converted to *string"
      pattern: "sql\\.NullString"
    - from: "internal/store/profile.go UpdateProfile"
      to: "profiles.conduct_rules / approach_guidance / ai_settings"
      via: "SET clause + positional arg + nil-fallback branch"
      pattern: "conduct_rules = \\$"
    - from: "internal/store/profile.go AcceptPolicy"
      to: "profiles.policy_version_accepted / policy_accepted_at"
      via: "guarded UPDATE that only fires when policy_accepted_at IS NULL"
      pattern: "policy_accepted_at IS NULL"
---

<objective>
Make the profile row the AI's per-game home at the data layer: five new columns on `profiles`, matching Go struct fields that round-trip blank values unchanged, a dedicated one-time acceptance write, and two pure functions that resolve blank AI settings and answer the engage-gate question.

Purpose: every other plan in this phase reads or writes through these fields. The blank-value semantics (D-09, D-10, D-11) and the gate decision (D-08) are decided here, in Go, once.
Output: migration `010` (up and down), an extended `internal/store/profile.go`, and `internal/store/profile_test.go` — the phase's first store test file and one of the two Wave 0 test files named in 01-VALIDATION.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/ROADMAP.md
@.planning/phases/01-profile-foundation-and-policy-gate/01-CONTEXT.md
@.planning/phases/01-profile-foundation-and-policy-gate/01-PATTERNS.md
@.planning/phases/01-profile-foundation-and-policy-gate/01-VALIDATION.md

Column and type contract fixed for this phase (all other plans depend on it):

| Column | Type | Null | Default |
|--------|------|------|---------|
| conduct_rules | TEXT | NOT NULL | `''` |
| approach_guidance | TEXT | NOT NULL | `''` |
| ai_settings | JSONB | NOT NULL | `'{}'::jsonb` |
| policy_version_accepted | TEXT | NULL | none |
| policy_accepted_at | TIMESTAMPTZ | NULL | none |

Go contract produced by this plan and consumed by plan 01-03:

- `AISettings` struct: `ModelName string` (json tag `model_name`), `CallCap *int` (json tag `call_cap`), `DisengageThreshold *int` (json tag `disengage_threshold`)
- `Profile` gains `ConductRules string`, `ApproachGuidance string`, `AISettings AISettings`, `PolicyVersionAccepted *string`, `PolicyAcceptedAt *string` (json tags `conduct_rules`, `approach_guidance`, `ai_settings`, `policy_version_accepted`, `policy_accepted_at`)
- `ProfileUpdate` gains `ConductRules *string`, `ApproachGuidance *string`, `AISettings *AISettings` — and nothing else
- `func (s *ProfileStore) AcceptPolicy(userID, profileID uuid.UUID, version string) (*Profile, error)`
- `func ResolveAISettings(s AISettings, serverDefaultModel string) ResolvedAISettings`
- `func EngageGateAllowed(policyVersionAccepted *string, policyAcceptedAt *string) bool`
- `const EngageGateRefusalMessage = "AI Player has not been configured for this connection. Accept the Safety and Abuse policy in AI Player settings before engaging autopilot."`
- `const DefaultDisengageThreshold = 3`
</context>

<tasks>

<task type="auto">
  <name>Task 01-01-01: Migration 010 adds the five AI columns and the startup fallback ensures them</name>
  <files>migrations/010_add_ai_fields.up.sql, migrations/010_add_ai_fields.down.sql, cmd/server/main.go</files>
  <read_first>
    - migrations/009_add_timers.up.sql and migrations/009_add_timers.down.sql (single-column ALTER shape, `-- +migrate Up` / `-- +migrate Down` header comments)
    - migrations/008_add_automation.up.sql (comma-separated multi-column ADD COLUMN shape)
    - migrations/006_create_profiles.up.sql (base table definition)
    - cmd/server/main.go lines 60-140 (migrate bootstrap `m.Up()` and the existing "Ensuring timers column exists" fallback block)
    - .planning/phases/01-profile-foundation-and-policy-gate/01-PATTERNS.md section "migrations/010_add_ai_fields"
  </read_first>
  <action>
    Create `migrations/010_add_ai_fields.up.sql` with a `-- +migrate Up` header comment and one `ALTER TABLE profiles` statement adding all five columns with `ADD COLUMN IF NOT EXISTS`, comma-separated, exactly: `conduct_rules TEXT NOT NULL DEFAULT ''`, `approach_guidance TEXT NOT NULL DEFAULT ''`, `ai_settings JSONB NOT NULL DEFAULT '{}'::jsonb`, `policy_version_accepted TEXT`, `policy_accepted_at TIMESTAMPTZ`. The last two take no DEFAULT and no NOT NULL — nullable is deliberate and is what "not yet accepted" means (D-07).

    Create `migrations/010_add_ai_fields.down.sql` with a `-- +migrate Down` header comment and one `ALTER TABLE profiles` statement dropping all five with `DROP COLUMN IF EXISTS`.

    In `cmd/server/main.go`, immediately after the existing timers-column fallback block (the one commented "fallback for when migration 009 isn't in migrate.zip"), add the same defensive shape for migration 010: a `log.Println` announcing it, one `db.Exec` running an `ALTER TABLE profiles ADD COLUMN IF NOT EXISTS ...` covering the same five columns with the same types and defaults, and the same `if err != nil { log.Printf("Warning: ...") } else { log.Println(...) }` handling. Do not modify or remove the existing timers fallback, do not change the `m.Up()` call, and do not make a failure here fatal — a warning log matches the existing precedent.

    Reason this fallback exists: RESEARCH Pitfall 2 — the deploy path may run migrations from a packaged `migrate.zip` that lags the repo, and the 009 precedent already works around it. Plan 01-05 confirms the staging state empirically.
  </action>
  <verify>
    <automated>go build ./... && grep -v '^--' migrations/010_add_ai_fields.up.sql | grep -c "ADD COLUMN IF NOT EXISTS"</automated>
  </verify>
  <acceptance_criteria>
    - `migrations/010_add_ai_fields.up.sql` exists and, ignoring comment lines, contains exactly 5 occurrences of `ADD COLUMN IF NOT EXISTS`
    - `migrations/010_add_ai_fields.up.sql` contains the literals `conduct_rules TEXT NOT NULL DEFAULT ''`, `approach_guidance TEXT NOT NULL DEFAULT ''`, `ai_settings JSONB NOT NULL DEFAULT '{}'::jsonb`, `policy_version_accepted TEXT`, `policy_accepted_at TIMESTAMPTZ`
    - `migrations/010_add_ai_fields.up.sql` contains no `DEFAULT` on the `policy_accepted_at` or `policy_version_accepted` clauses and no `NOT NULL` on either
    - `migrations/010_add_ai_fields.down.sql` contains 5 occurrences of `DROP COLUMN IF EXISTS`, one per column
    - `cmd/server/main.go` contains a second `ADD COLUMN IF NOT EXISTS ai_settings` string inside a `db.Exec` call placed after the existing timers fallback, and the existing timers fallback block is unchanged
    - `go build ./...` exits 0
  </acceptance_criteria>
  <done>Migration 010 up/down exist with all five columns, and the server's startup fallback ensures the same columns without failing startup on error.</done>
</task>

<task type="auto">
  <name>Task 01-01-02: Store reads, writes and one-time-accepts the new columns</name>
  <files>internal/store/profile.go</files>
  <read_first>
    - internal/store/profile.go (whole file — `Profile` struct at line 11, `ProfileSettings`/`DefaultProfileSettings` at 82-99, `normalizeSettings` at 102, `ProfileUpdate` at 116, `CreateProfile` at 156, `GetProfile` at 192, `GetProfileByConnection` at 249, `UpdateProfile` at 306, `DeleteProfile` at 398)
    - migrations/010_add_ai_fields.up.sql (created in task 01-01-01 — column order and types)
    - .planning/phases/01-profile-foundation-and-policy-gate/01-PATTERNS.md section "internal/store/profile.go" (Pitfall 1 four-point checklist, Pitfall 3 nullable scan, and the anti-pattern warning about normalizeSettings)
  </read_first>
  <action>
    Add `type AISettings struct` with fields `ModelName string` (json `model_name`), `CallCap *int` (json `call_cap`), `DisengageThreshold *int` (json `disengage_threshold`). Pointers are load-bearing: nil is the user-visible "blank" state that must round-trip (D-10, D-11). There is no reconnect field and none may be added (D-13).

    Extend `Profile` with `ConductRules string` (json `conduct_rules`), `ApproachGuidance string` (json `approach_guidance`), `AISettings AISettings` (json `ai_settings`), `PolicyVersionAccepted *string` (json `policy_version_accepted`), `PolicyAcceptedAt *string` (json `policy_accepted_at`).

    Extend the `SELECT` column list and `Scan` argument list of BOTH `GetProfile` and `GetProfileByConnection` with `conduct_rules, approach_guidance, ai_settings, policy_version_accepted, policy_accepted_at`, inserted after `timers` and before `created_at, updated_at` so the scan order matches. `conduct_rules` and `approach_guidance` scan straight into the string fields. `ai_settings` scans into a `[]byte` and is `json.Unmarshal`ed into `profile.AISettings` exactly like `timersJSON`. `policy_version_accepted` and `policy_accepted_at` each scan into a `sql.NullString` local and are converted to `*string` on the struct: set the pointer when `Valid`, leave nil otherwise (RESEARCH Pitfall 3 — a plain string destination errors on NULL for every unaccepted profile). `database/sql` is already imported.

    Do NOT add a `DefaultAISettings()` and do NOT touch `normalizeSettings` or call anything like it for AI settings: blank values must survive a read unchanged (RESEARCH Anti-Patterns). Leave `CreateProfile`'s INSERT alone — the column defaults from migration 010 give a new profile blank AI fields and NULL acceptance, which is the correct starting state.

    Extend `ProfileUpdate` with `ConductRules *string` (json `conduct_rules,omitempty`), `ApproachGuidance *string`, `AISettings *AISettings`. Do NOT add `PolicyVersionAccepted` or `PolicyAcceptedAt` to `ProfileUpdate` under any circumstances — that is threat T-1-01; acceptance is writable only by `AcceptPolicy` below.

    Extend `UpdateProfile` at all four points named in 01-PATTERNS.md: (a) add `conduct_rules`, `approach_guidance`, `ai_settings` to the single `UPDATE profiles SET ...` clause, renumbering placeholders so `id` and `user_id` stay last; (b) add the three positional args to `QueryRow` in the same order; (c) add an `if updates.X != nil { use *updates.X } else { use existing.X }` branch for each of the three (the two text fields pass as plain string args, `ai_settings` is `json.Marshal`ed like `timersJSON`); (d) add the `if updates.X != nil { existing.X = *updates.X }` assignment in the return path. Missing any of these means an unrelated `PUT /timers` wipes the AI fields (threat T-1-06). The `SET` clause must not mention `policy_version_accepted` or `policy_accepted_at`.

    Add `func (s *ProfileStore) AcceptPolicy(userID, profileID uuid.UUID, version string) (*Profile, error)`. It runs `UPDATE profiles SET policy_version_accepted = $1, policy_accepted_at = NOW(), updated_at = NOW() WHERE id = $2 AND user_id = $3 AND policy_accepted_at IS NULL`, ignores the "0 rows affected" case as a successful no-op (acceptance is one-time and never re-written — D-06), then returns the result of `s.GetProfile(userID, profileID)` so the caller always sees the stored record. The version argument is supplied by the caller from the server's embedded policy and is never read from a request body (threat T-1-02).

    Leave `DeleteProfile` exactly as it is — a plain row `DELETE` already discards the acceptance with the profile (D-06); this is verified, not implemented.
  </action>
  <verify>
    <automated>go build ./... && go vet ./internal/store/...</automated>
  </verify>
  <acceptance_criteria>
    - `internal/store/profile.go` contains `type AISettings struct` with exactly three fields and no field whose name or json tag contains `reconnect`
    - Both `GetProfile` and `GetProfileByConnection` SELECT lists contain `conduct_rules, approach_guidance, ai_settings, policy_version_accepted, policy_accepted_at`
    - `internal/store/profile.go` contains at least two `sql.NullString` declarations used for the acceptance columns
    - `UpdateProfile`'s SET clause contains `conduct_rules = $`, `approach_guidance = $`, `ai_settings = $` and does NOT contain `policy_version_accepted =` or `policy_accepted_at =`
    - `UpdateProfile` contains an else-branch falling back to `existing.ConductRules`, `existing.ApproachGuidance` and `existing.AISettings`
    - `ProfileUpdate` has no `PolicyVersionAccepted` or `PolicyAcceptedAt` field (source assertion: `grep -c "PolicyVersionAccepted \*string" internal/store/profile.go` counts only the `Profile` struct occurrence)
    - `internal/store/profile.go` contains `func (s *ProfileStore) AcceptPolicy(` and the literal `policy_accepted_at IS NULL`
    - `DeleteProfile`'s body is byte-identical to its pre-task form
    - `go build ./...` and `go vet ./internal/store/...` both exit 0
  </acceptance_criteria>
  <done>The store reads all five columns (NULL-safe), writes the three editable ones without clobbering neighbours, and records acceptance exactly once through a dedicated method.</done>
</task>

<task type="auto">
  <name>Task 01-01-03: Blank AI settings resolve in Go and the engage gate answers from the row</name>
  <files>internal/store/profile.go, internal/store/profile_test.go</files>
  <read_first>
    - internal/store/profile.go (as modified by task 01-01-02)
    - internal/icm/icm_test.go lines 1-60 (the repo's only test file — plain `testing`, table of struct literals, `t.Run` subtests, no mocking library)
    - .planning/phases/01-profile-foundation-and-policy-gate/01-VALIDATION.md (the automated commands these tests must satisfy)
    - .planning/phases/01-profile-foundation-and-policy-gate/01-UI-SPEC.md Copywriting Contract (the verbatim engage-gate refusal string)
  </read_first>
  <action>
    Add to `internal/store/profile.go`, as package-level declarations with no `*sql.DB` dependency so they are testable with zero fixtures:

    `const DefaultDisengageThreshold = 3` — the number of consecutive transient AI failures tolerated before disengage when the profile leaves the field blank. Claude's Discretion per CONTEXT.md; 3 is the chosen value and the doc comment must say so and must state that non-transient failures (missing key, auth error) disengage on the first occurrence. Phase 4 consumes it; Phase 1 only resolves it (D-11).

    `const EngageGateRefusalMessage` set verbatim to `AI Player has not been configured for this connection. Accept the Safety and Abuse policy in AI Player settings before engaging autopilot.` — this exact string is the Phase 2 contract (D-05, 01-UI-SPEC.md Copywriting Contract).

    `type ResolvedAISettings struct { ModelName string; CallCapSet bool; CallCap int; DisengageThreshold int }` and `func ResolveAISettings(s AISettings, serverDefaultModel string) ResolvedAISettings`: a blank `ModelName` resolves to `serverDefaultModel` (Phase 3 passes the env-configured value; Phase 1 only resolves — D-09); a nil `CallCap` yields `CallCapSet: false` and `CallCap: 0`, meaning no cap at all rather than a cap of zero (D-10); a nil `DisengageThreshold` yields `DefaultDisengageThreshold` (D-11). Non-nil values pass through unchanged. This function must NOT be called from `GetProfile`/`GetProfileByConnection` — blank must round-trip to the browser as blank.

    `func EngageGateAllowed(policyVersionAccepted *string, policyAcceptedAt *string) bool` returns true only when `policyAcceptedAt` is non-nil and `policyVersionAccepted` is non-nil and non-empty. It compares nothing to the current policy version — a later policy change never re-gates (D-06). It takes plain values read from the server-fetched row, never a client-supplied flag (threat T-1-05). This is the whole of what Phase 2 needs from Phase 1 (D-08).

    Create `internal/store/profile_test.go` (`package store`, stdlib `testing` only, table-driven with `t.Run` subtests in the style of `internal/icm/icm_test.go`) with:
    - `TestResolveAISettings` — cases: all-blank input with serverDefaultModel `"gemini-server-default"` gives that model, `CallCapSet` false and threshold 3; explicit model `"gemini-2.5-pro"` passes through; explicit cap 25 gives `CallCapSet` true and `CallCap` 25; explicit threshold 1 gives 1.
    - `TestEngageGateAllowed` — cases: both nil → false; version set, timestamp nil → false; version nil, timestamp set → false; version pointing at the empty string with timestamp set → false; both set → true.
    - `TestEngageGateRefusalMessageIsPhase2Contract` — asserts `EngageGateRefusalMessage` equals the UI-SPEC string literal, so any later edit to the message is caught as a contract change.
  </action>
  <verify>
    <automated>go test ./internal/store/... -run "TestResolveAISettings|TestEngageGate" -v</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/store/... -run "TestResolveAISettings|TestEngageGate" -v` exits 0 and its output shows PASS for `TestResolveAISettings`, `TestEngageGateAllowed` and `TestEngageGateRefusalMessageIsPhase2Contract`
    - `go test ./internal/store/... -run TestResolveAISettings -v` (the exact command in 01-VALIDATION.md) exits 0
    - `internal/store/profile.go` contains `const DefaultDisengageThreshold = 3`, `func ResolveAISettings(`, `func EngageGateAllowed(` and `EngageGateRefusalMessage`
    - `EngageGateRefusalMessage`'s value matches the UI-SPEC string character for character, including the trailing period
    - `grep -n "ResolveAISettings" internal/store/profile.go` shows no call site inside `GetProfile` or `GetProfileByConnection`
    - `go test ./...` shows no new failures compared to the pre-task baseline
  </acceptance_criteria>
  <done>`go test` proves blank model name, blank call cap and blank disengage threshold each resolve to their documented behaviour, and that the gate refuses until both acceptance columns are set.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP request body → store | Client JSON reaches `ProfileUpdate`; anything present in that struct is writable by the client |
| Go process → Postgres | The single `UPDATE profiles SET ...` statement rewrites every mutable column on every write |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigating Task |
|-----------|----------|----------|-----------|-------------|-----------------|
| T-1-01 | Tampering | High | `store.ProfileUpdate` / `UpdateProfile` | mitigate | 01-01-02 — acceptance columns are absent from `ProfileUpdate` and from the `SET` clause; only `AcceptPolicy` writes them |
| T-1-05 | Tampering | High | `store.EngageGateAllowed` | mitigate | 01-01-03 — the gate takes values read from the server-fetched row, has no client-supplied input, and does no version comparison |
| T-1-06 | Tampering (integrity) | Medium | `store.UpdateProfile` | mitigate | 01-01-02 — nil-fallback branches for all three new editable columns; confirmed end to end by the 01-05 walkthrough (PUT timers, re-GET ai-settings) |
| T-1-02 | Spoofing | Medium | `store.AcceptPolicy` | mitigate | 01-01-02 — the version is a caller-supplied argument sourced from the embedded policy in plan 01-03, never from a request body; the timestamp is `NOW()` server-side |
| T-1-SC | Tampering (supply chain) | Low | `go.mod` | accept | No packages are installed by this phase; `go.mod` must be unchanged at plan end |
</threat_model>

<verification>
1. `go build ./...` exits 0.
2. `go test ./internal/store/... -v` passes, including `TestResolveAISettings`, `TestEngageGateAllowed`, `TestEngageGateRefusalMessageIsPhase2Contract`.
3. `go test ./...` shows no new failures relative to the pre-plan baseline.
4. `git diff --stat go.mod frontend/package.json` is empty (T-1-SC).
5. Migration 010 up and down files each list all five columns.
</verification>

<success_criteria>
- The `profiles` table gains conduct rules, approach guidance, AI settings and a two-column acceptance record, reversible by `010_add_ai_fields.down.sql`.
- Blank AI settings survive a read unchanged and resolve — in Go, on demand — to server default model, no cap, and threshold 3, demonstrated by `go test ./internal/store/...`.
- `store.EngageGateAllowed` answers the Phase 2 gate question from the two acceptance columns alone, demonstrated by `go test ./internal/store/...`.
- Policy acceptance cannot be written through any general profile update path.
</success_criteria>

<output>
Create `.planning/phases/01-profile-foundation-and-policy-gate/01-01-SUMMARY.md` when done.
</output>
