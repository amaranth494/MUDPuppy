---
phase: 02-autopilot-switch
plan: 07
type: execute
wave: 5
depends_on: [02-01, 02-02, 02-03, 02-04, 02-05, 02-06]
files_modified:
  - .planning/phases/02-autopilot-switch/evidence/01-test-report.txt
  - .planning/phases/02-autopilot-switch/evidence/02-harness-selftest.txt
  - .planning/phases/02-autopilot-switch/evidence/03-canned-report.txt
  - .planning/phases/02-autopilot-switch/evidence/04-staging-ai-player.log
  - .planning/phases/02-autopilot-switch/evidence/05-badge-after-refresh.png
  - .planning/phases/02-autopilot-switch/evidence/06-refused-before-acceptance.png
  - .planning/phases/02-autopilot-switch/evidence/07-wheel-grab.png
  - .planning/phases/02-autopilot-switch/evidence/08-trigger-keeps-on.png
  - .planning/phases/02-autopilot-switch/evidence/09-waiting-after-drop.png
  - .planning/phases/02-autopilot-switch/evidence/10-resume-after-reconnect.png
  - .planning/phases/02-autopilot-switch/evidence/11-off-while-waiting-stays-off.png
  - .planning/phases/02-autopilot-switch/evidence/12-hand-play-unchanged.png
  - .planning/phases/02-autopilot-switch/02-SECURITY-AGENDA.md
  - .planning/phases/02-autopilot-switch/02-07-SUMMARY.md
autonomous: false
requirements: [REQ-autopilot-directives, REQ-wheel-grab, REQ-no-auto-reconnect, REQ-doc-hand-play-and-gate]

must_haves:
  truths:
    - "The full Go suite and the frontend build are green on the phase branch and no package was added, proven by evidence/01-test-report.txt containing zero unexpected FAIL lines and an empty dependency-drift section (T-2-SC)"
    - "The canned-report harness is falsifiable before it is trusted, proven by evidence/02-harness-selftest.txt showing the self-test clean and the negative self-test producing FAIL C1 with a non-zero exit (T-2-07)"
    - "Phase 2's HTTP-reachable half holds on staging, proven by evidence/03-canned-report.txt showing PASS for C1 and C4 with the response body printed under every step and SKIP lines for C2 and C3 naming their screenshots"
    - "Every autopilot movement on staging is readable as one [AI-PLAYER] autopilot line carrying ids, old and new state and a cause, including cause=wheel-grab, cause=disconnect and cause=resume, and none of them carries game text, proven by evidence/04-staging-ai-player.log"
    - "The owner sees the badge match the server after a hard refresh, the gate refusal before acceptance, the wheel-grab notice with the game's reply, a trigger-fired command leaving the badge on, the drop parking it and the reconnect resuming it, #AUTO OFF while waiting staying off after reconnecting, and ordinary play unchanged — proven by evidence/05 through evidence/12"
    - "Every one of the four ROADMAP Phase 2 success criteria has a row in 02-07-SUMMARY.md citing a named evidence file, with line numbers for text evidence and a PASS or FAIL verdict, and no row cites a database query"
    - "The Phase 2 security review has a written agenda carrying the waiting-to-on auto-resume question against policy section 2 and the two carried-forward Phase 1 risks, with nothing decided in advance, proven by 02-SECURITY-AGENDA.md"
  artifacts:
    - path: ".planning/phases/02-autopilot-switch/evidence/01-test-report.txt"
      provides: "Canned report: go build, go vet, the full go test -v suite, the targeted Phase 2 runs, npm run build, and the dependency-drift check, captured verbatim"
      contains: "GO TEST EXIT"
    - path: ".planning/phases/02-autopilot-switch/evidence/02-harness-selftest.txt"
      provides: "The harness proving itself: a clean self-test and a negative self-test that fails on purpose"
      contains: "FAIL C1"
    - path: ".planning/phases/02-autopilot-switch/evidence/03-canned-report.txt"
      provides: "scripts/verify-phase2.sh output against staging: PASS/FAIL/SKIP per ROADMAP criterion with response bodies"
      contains: "PASS C1"
    - path: ".planning/phases/02-autopilot-switch/evidence/04-staging-ai-player.log"
      provides: "Staging log excerpt carrying every autopilot transition and refusal of the walkthrough"
      contains: "[AI-PLAYER] autopilot"
    - path: ".planning/phases/02-autopilot-switch/evidence/09-waiting-after-drop.png"
      provides: "Screenshot: [Disconnected] then [Autopilot waiting for reconnect] with the badge reading Waiting"
    - path: ".planning/phases/02-autopilot-switch/evidence/10-resume-after-reconnect.png"
      provides: "Screenshot: [Reconnected] then [Autopilot resuming] with the badge reading On, nothing typed"
    - path: ".planning/phases/02-autopilot-switch/02-SECURITY-AGENDA.md"
      provides: "The Phase 2 security review agenda: the waiting-to-on policy question plus carried-forward R-02 and R-04, each with the three dispositions and no decision pre-made"
      contains: "Remediate Now"
    - path: ".planning/phases/02-autopilot-switch/02-07-SUMMARY.md"
      provides: "One row per ROADMAP Phase 2 success criterion, each citing the evidence file and line numbers or screenshot that prove it, with a verdict"
      contains: "Phase Validation"
  key_links:
    - from: "scripts/verify-phase2.sh run against staging"
      to: "evidence/03-canned-report.txt"
      via: "the script's own tee of the report path"
      pattern: "PASS C1"
    - from: "internal/session [AI-PLAYER] autopilot log lines"
      to: "evidence/04-staging-ai-player.log"
      via: "railway logs filtered on the [AI-PLAYER] prefix during the walkthrough"
      pattern: "cause=(disconnect|resume|wheel-grab)"
    - from: "the browser play screen on staging"
      to: "evidence/05-badge-after-refresh.png through evidence/12-hand-play-unchanged.png"
      via: "one screenshot per observable state, end-user perspective only"
      pattern: "wheel-grab"
---

<objective>
Demonstrate the phase and file the proof. CLAUDE.md rule 2: Phase 2 is complete when the ROADMAP's Phase Validation line has actually been performed, not when its tasks are done. The owner's evidence rule narrows how: every criterion is proven by a canned report — a test run, a script run or a log excerpt captured verbatim to a file — or by an end-user screenshot of the browser as the owner sees it. A database query proves nothing here and is not to be run.

Purpose: turn four ROADMAP success criteria into four cited rows, each pointing at a file a reader can open, and hand the Phase 2 security review a written agenda so the one open policy question and the two risks carried forward from Phase 1 are decided rather than forgotten.
Output: an `evidence/` directory holding two local reports, one staging report, one staging log excerpt and eight screenshots; a security-review agenda; and `02-07-SUMMARY.md` mapping each criterion to the evidence that proves it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/ROADMAP.md
@.planning/phases/02-autopilot-switch/02-VALIDATION.md
@.planning/phases/02-autopilot-switch/02-UI-SPEC.md
@.planning/phases/02-autopilot-switch/02-CONTEXT.md
@.planning/phases/02-autopilot-switch/02-01-SUMMARY.md
@.planning/phases/02-autopilot-switch/02-02-SUMMARY.md
@.planning/phases/02-autopilot-switch/02-03-SUMMARY.md
@.planning/phases/02-autopilot-switch/02-04-SUMMARY.md
@.planning/phases/02-autopilot-switch/02-05-SUMMARY.md
@.planning/phases/02-autopilot-switch/02-06-SUMMARY.md

ROADMAP Phase Validation line for Phase 2 (what this plan must demonstrate):
Player-observable on staging against a live MUD — `#AUTO ON` is refused before acceptance and engages after it; the indicator matches the server state after a page refresh; typing a command while engaged shows the disengage notice and the command's game response; a trigger-fired command leaves autopilot engaged; dropping the connection shows the indicator as waiting and no command is sent while disconnected; reconnecting by hand shows it resuming to ON without typing `#AUTO ON`; `#AUTO OFF` while waiting lands on OFF and it stays OFF after reconnecting. Diagnostic — `go test` covers the engaged-state machine and the human-versus-automation source flag.

Evidence directory (create it in task 02-07-01): `.planning/phases/02-autopilot-switch/evidence/`. Every filename below is fixed — earlier plans' `must_haves` cite these exact names.

| File | Type | Produced by |
|------|------|-------------|
| `01-test-report.txt` | canned report | task 02-07-01 |
| `02-harness-selftest.txt` | canned report | task 02-07-01 |
| `03-canned-report.txt` | canned report from `scripts/verify-phase2.sh` against staging | task 02-07-02 |
| `04-staging-ai-player.log` | log excerpt | task 02-07-02 |
| `05-badge-after-refresh.png` through `12-hand-play-unchanged.png` | screenshots, end-user perspective only | task 02-07-03 |

Environment: Railway project `mudpuppy`, environment `staging`. Production is untouched. Log capture used the Railway CLI (`railway logs --environment staging`) in Phase 1 and that is the known-good route.

Standing instruction that constrains the walkthrough: do not sign out of the staging session at any point. Sign-in is one-time-code based and signing back in is the owner's step, not the executor's. Nothing in Phase 2's validation requires a new session.
</context>

<tasks>

<task type="auto">
  <name>Task 02-07-01: The test report and the harness self-test are captured verbatim as files</name>
  <files>.planning/phases/02-autopilot-switch/evidence/01-test-report.txt, .planning/phases/02-autopilot-switch/evidence/02-harness-selftest.txt</files>
  <read_first>
    - .planning/phases/02-autopilot-switch/02-VALIDATION.md (Sampling Rate — the phase-gate commands)
    - .planning/phases/02-autopilot-switch/02-01-SUMMARY.md through 02-06-SUMMARY.md (what each plan reports as done, so gaps are caught before the checkpoints)
    - scripts/verify-phase2.sh (its usage block: the flags, the environment variables and the report path argument)
  </read_first>
  <action>
    Create the directory `.planning/phases/02-autopilot-switch/evidence/`.

    From the repository root, run each command below and append its command line, its full output and its exit status to `evidence/01-test-report.txt`. Capture verbatim — no summarising, no truncation, no reformatting. Prefix each with a line reading `### <command>` and follow each with `EXIT: <n>` so the file is greppable.

    1. `go build ./...`
    2. `go vet ./internal/session/... ./cmd/...`
    3. `go test ./... -v`
    4. `go test ./internal/session/... -race -v`
    5. `go test ./internal/session/... -run TestAutopilotTransitions -v`
    6. `go test ./internal/session/... -run TestEngageAutopilot -v`
    7. `go test ./internal/session/... -run TestWaitingSurvivesDisconnectAndResumesOnConnect -race -v`
    8. `go test ./internal/session/... -run TestWheelGrabSourceRule -v`
    9. `go test ./internal/session/... -run "TestAutopilotHandler|TestStatusCarriesAutopilotState" -v`
    10. `go test ./internal/store/... -run TestEngageGate -v` — the Phase 1 regression the gate still depends on
    11. `cd frontend && npm run build`
    12. `git diff --stat <phase-base-commit> -- go.mod frontend/package.json` under a `### DEPENDENCY DRIFT` heading. This phase installs no package, so a non-empty section is a supply-chain finding (T-2-SC), not a nuisance.

    End the file with a line `GO TEST EXIT: <n>` carrying the exit status of command 3, matching the convention both verification harnesses use, so every canned report in this project reads the same way.

    `internal/icm` failures that predate this phase are annotated in the report as the pre-existing baseline with a one-line note naming them; any failure in `internal/session` or `internal/store` blocks the phase and must be fixed before the checkpoints below are attempted.

    Then produce `evidence/02-harness-selftest.txt` the same way, with two `### ` sections: `bash scripts/verify-phase2.sh --self-test /tmp/phase2-selftest.txt` followed by the contents of that file and its exit status, and `bash scripts/verify-phase2.sh --self-test-negative /tmp/phase2-negative.txt` followed by the contents of that file and its exit status. The first must be clean and exit 0; the second must contain `FAIL C1` and exit 1. This is what makes the staging report in the next task worth reading: the harness is demonstrably able to fail (T-2-07).

    Run no SQL. Nothing in this phase is proven by a query.
  </action>
  <verify>
    <automated>bash scripts/verify-phase2.sh --self-test /tmp/phase2-selftest.txt && grep -c "GO TEST EXIT" .planning/phases/02-autopilot-switch/evidence/01-test-report.txt && grep -c "FAIL C1" .planning/phases/02-autopilot-switch/evidence/02-harness-selftest.txt</automated>
  </verify>
  <acceptance_criteria>
    - `evidence/01-test-report.txt` exists and contains a `### ` heading for each of the twelve commands
    - The report contains `EXIT: 0` for `go build ./...`, for `go vet`, for each of the seven targeted `go test` runs and for `npm run build`
    - The report contains zero lines matching `^--- FAIL` outside the annotated pre-existing `internal/icm` baseline, and the annotation names each baseline entry
    - The report's `-race` section contains no `DATA RACE` line
    - The `### DEPENDENCY DRIFT` section is empty (T-2-SC)
    - The report ends with a `GO TEST EXIT:` line
    - `evidence/02-harness-selftest.txt` contains both self-test sections; the first shows zero lines beginning with `FAIL` and `EXIT: 0`, the second contains at least one `FAIL C1` line and a non-zero exit status
    - No SQL statement appears anywhere in either file
  </acceptance_criteria>
  <done>The diagnostic half of the Phase Validation line exists as two files a reader can open, with no new failures, no data race, no dependency drift, and a harness that has proven it can fail.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 02-07-02: Staging proves itself in its own log and in the canned report</name>
  <files>.planning/phases/02-autopilot-switch/evidence/03-canned-report.txt, .planning/phases/02-autopilot-switch/evidence/04-staging-ai-player.log</files>
  <what-built>
    The server-held switch with its waiting state and its two lifecycle hooks (plan 02-01), the autopilot endpoint with the Phase 1 engage gate behind it and the autopilot field on the status response (plan 02-02), the wheel-grab at the single command-ingress point (plan 02-03), and the canned-report harness (plan 02-05). Together they make staging provable from its own log plus one report file, with no database access at all.
  </what-built>
  <read_first>
    - scripts/verify-phase2.sh (its usage block: `BASE_URL`, `SESSION_COOKIE`, `CONNECTION_ID`, `NOT_OWNED_CONNECTION_ID`, report path)
    - .planning/phases/02-autopilot-switch/evidence/02-harness-selftest.txt (from task 02-07-01 — the harness has already proven it can fail)
    - internal/session/manager.go and internal/session/handler.go (the `[AI-PLAYER] autopilot` line format and its cause vocabulary)
    - .planning/phases/01-profile-foundation-and-policy-gate/01-05-PLAN.md (the Phase 1 capture procedure that worked, including the Railway CLI route)
  </read_first>
  <action>
    Deploy the branch to Railway staging, then produce two evidence files with no database access: `evidence/03-canned-report.txt`, the output of `scripts/verify-phase2.sh` run against staging with a fresh unaccepted profile, and `evidence/04-staging-ai-player.log`, the `[AI-PLAYER]` lines produced by that run and by the browser walkthrough in the next task. Follow the numbered steps below exactly and in order; the owner approves both files at this checkpoint.
  </action>
  <how-to-verify>
    1. Deploy the branch to Railway project `mudpuppy`, environment `staging`. Do not deploy to production.
    2. Create a connection profile on staging that has never accepted the policy, and note its `connection_id`. Use the browser session that is already signed in — do not sign out.
    3. Run the harness against staging and save its report:
       `BASE_URL=https://<staging-host> SESSION_COOKIE='<cookie>' CONNECTION_ID=<connection_id> scripts/verify-phase2.sh .planning/phases/02-autopilot-switch/evidence/03-canned-report.txt`
       Expect exit status 0, PASS for C1 and C4, SKIP lines for C2 and C3 naming their screenshots, and zero lines beginning with `FAIL`.
    4. Read the report and confirm four specific things with your own eyes, because they are the claims no unit test can make about the deployed system: the pre-acceptance `on` step answered `refused-gate` with the refusal sentence and left `autopilot_state` at `off`; the post-acceptance `on` step answered `refused-no-session` and still left it at `off`; the not-owned-connection step answered `refused-gate`; and the two endpoints agreed on the switch position in the closing steps.
    5. After the browser walkthrough in task 02-07-03 is complete, capture the `[AI-PLAYER]` lines that both this run and the walkthrough produced, with `railway logs --environment staging | grep '\[AI-PLAYER\]'`, or the Railway MCP log tool, or the dashboard log pane with copy-all. Name the tool used in the SUMMARY. Save the excerpt to `evidence/04-staging-ai-player.log`.
    6. Confirm the excerpt contains, for the test connection id, at least one line each with `cause=refused-gate`, `cause=refused-no-session`, `cause=engage`, `cause=wheel-grab`, `cause=disconnect` and `cause=resume`, every one of them in the format `[AI-PLAYER] autopilot user_id=... connection_id=... old=... new=... cause=...`.
    7. Read both files back and confirm neither contains MUD output, a typed command, conduct-rules text or approach-guidance text (threat T-2-04).
    Do not run a SQL query at any point in this task. If the switch were not behaving, the canned report and the log would both say so.
  </how-to-verify>
  <verify>
    <human-check>Both evidence files exist on disk with the strings named above, the canned report shows PASS for C1 and C4 with SKIP for C2 and C3 and zero FAIL lines, the log carries all six causes, and no SQL was run.</human-check>
  </verify>
  <acceptance_criteria>
    - `evidence/03-canned-report.txt` exists, its header records the staging `BASE_URL` and a git SHA, and it contains `PASS C1`, `PASS C4`, `SKIP C2`, `SKIP C3` and zero lines starting with `FAIL`
    - `evidence/03-canned-report.txt` prints the engage-gate refusal body containing the refusal sentence character for character, and shows the `refused-no-session` outcome on the post-acceptance engage attempt
    - The `SKIP C2` and `SKIP C3` lines each name at least one screenshot file that proves that criterion instead
    - `evidence/04-staging-ai-player.log` contains at least one `[AI-PLAYER] autopilot` line for each of the causes `refused-gate`, `refused-no-session`, `engage`, `wheel-grab`, `disconnect` and `resume`
    - `grep -ci 'conduct_rules\|approach_guidance\|you took the wheel' .planning/phases/02-autopilot-switch/evidence/04-staging-ai-player.log` returns 0 (T-2-04)
    - The SUMMARY names the tool used for log capture
    - No SQL query appears in this task's evidence or in the SUMMARY
  </acceptance_criteria>
  <resume-signal>Type "approved" once both evidence files are on disk, or name the file that could not be produced and what the report or log said instead.</resume-signal>
  <done>Staging's own log proves every transition and refusal, and one report file proves everything Phase 2 exposes over HTTP — with no database inspection anywhere.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 02-07-03: The owner sees the switch work, and each observable is a screenshot on disk</name>
  <files>.planning/phases/02-autopilot-switch/evidence/05-badge-after-refresh.png, .planning/phases/02-autopilot-switch/evidence/06-refused-before-acceptance.png, .planning/phases/02-autopilot-switch/evidence/07-wheel-grab.png, .planning/phases/02-autopilot-switch/evidence/08-trigger-keeps-on.png, .planning/phases/02-autopilot-switch/evidence/09-waiting-after-drop.png, .planning/phases/02-autopilot-switch/evidence/10-resume-after-reconnect.png, .planning/phases/02-autopilot-switch/evidence/11-off-while-waiting-stays-off.png, .planning/phases/02-autopilot-switch/evidence/12-hand-play-unchanged.png, .planning/phases/02-autopilot-switch/02-07-SUMMARY.md</files>
  <what-built>
    The badge and the notices (plan 02-06) over the directive grammar and the source flag (plan 02-04) over the endpoint and the gate (plan 02-02) over the state machine and its lifecycle hooks (plan 02-01), with the wheel-grab enforced server-side (plan 02-03). This is the Phase Validation line's player-observable half, which is the larger half of Phase 2: two of the four criteria have no other proof. A visual claim is proven by an image, not by prose and not by a query.
  </what-built>
  <read_first>
    - .planning/ROADMAP.md §Phase 2 (Success Criteria 1-4 and the Phase Validation line)
    - .planning/phases/02-autopilot-switch/02-UI-SPEC.md (what each state must show and say, verbatim)
    - .planning/phases/02-autopilot-switch/02-CONTEXT.md (D-03, D-04, D-05, D-07, D-08, D-11, D-12)
    - .planning/phases/02-autopilot-switch/02-VALIDATION.md Manual-Only Verifications table (the rows this task satisfies)
    - .planning/phases/02-autopilot-switch/evidence/03-canned-report.txt (from task 02-07-02 — the HTTP-level result the browser must agree with)
  </read_first>
  <action>
    Walk the play screen on staging against a live MUD in the order below and capture one PNG per observable into `evidence/`, using the eight exact filenames listed. Capture with a browser automation tool such as Chrome MCP, or let the owner take them by hand — either is acceptable, and the owner confirms the set at this checkpoint. Then write `02-07-SUMMARY.md` with the four-row criterion table specified below, citing evidence files by name and text evidence by line number.
  </action>
  <how-to-verify>
    On staging, signed in as the owner, with a live MUD connection available. Do not sign out at any point. Every shot must be the browser as the owner sees it — the play screen with the header badges visible, no devtools pane, no terminal window, no raw JSON in frame.

    1. `06-refused-before-acceptance.png` — on a connection profile that has never accepted the policy, connect to the MUD and type `#AUTO ON`. Must show the bracketed engage-gate refusal sentence in the terminal and the header badge still reading `Autopilot: Off` (criterion 1; D-02, D-11).
    2. Accept the policy for that profile in the AI Player section, return to the play screen and type `#AUTO ON`. Confirm `[Autopilot engaged]` appears and the badge reads `Autopilot: On`. Then hard-refresh the page and take no other action. `05-badge-after-refresh.png` must show the badge still reading `Autopilot: On` after the reload, with the `#AUTO` status line visible in the terminal if convenient (criterion 1's refresh clause; D-05, D-10).
    3. `07-wheel-grab.png` — while engaged, type a harmless game command such as `look`. Must show `[Autopilot disengaged: you took the wheel]`, the command, the game's reply beneath it, and the badge now reading `Autopilot: Off` — notice, response and badge in one frame (criterion 2). In the same session, engage again and press Enter on an empty line; confirm that also disengages (D-07). Either capture that in the same frame or record it in the SUMMARY's step notes for this file.
    4. `08-trigger-keeps-on.png` — create a browser trigger that sends a command on a line the MUD reliably prints, engage autopilot, and let it fire. Must show the trigger's command and the game's reply with the badge still reading `Autopilot: On` (criterion 2; D-09).
    5. `09-waiting-after-drop.png` — with autopilot on, drop the connection. The disconnect control is fine; the point is that the connection went away, not how. Must show `[Disconnected]` and, on the next line, `[Autopilot waiting for reconnect]`, with the badge reading `Autopilot: Waiting` and no command sent while disconnected (criterion 3; D-11).
    6. `10-resume-after-reconnect.png` — reconnect by hand. Must show `[Reconnected]` and, on the next line, `[Autopilot resuming]`, with the badge reading `Autopilot: On`, and `#AUTO ON` must not have been typed (criterion 3).
    7. `11-off-while-waiting-stays-off.png` — engage again, drop the connection, and while the badge reads Waiting type `#AUTO OFF`; confirm `[Autopilot disengaged]` and the badge reading Off. Then reconnect by hand. Must show the badge still reading `Autopilot: Off` after the connection is back (criterion 3's last clause; D-01).
    8. `12-hand-play-unchanged.png` — create a fresh profile, accept the policy on it, connect, and hand-play several commands with autopilot off. Must show ordinary play with no bracketed autopilot line of any kind, no banner and no new control, with the badge reading `Autopilot: Off` (criterion 4; D-12).

    Then write `02-07-SUMMARY.md`. Its centrepiece is a table with one row per ROADMAP Phase 2 success criterion 1 to 4, each row naming the evidence that proves it and a PASS or FAIL verdict:

    | Criterion | What it claims | Evidence | Verdict |
    |-----------|----------------|----------|---------|
    | 1 | `#AUTO ON` engages only past the gate, `#AUTO OFF` disengages, the indicator always matches the server including after a refresh | `evidence/01-test-report.txt` lines n-m (TestEngageAutopilot), `evidence/03-canned-report.txt` line n (PASS C1), `evidence/06-refused-before-acceptance.png`, `evidence/05-badge-after-refresh.png` | PASS |
    | 2 | Typing disengages before the command is sent with no lost keystrokes; aliases, triggers and timers do not trip it | `evidence/01-test-report.txt` lines n-m (TestWheelGrabSourceRule), `evidence/07-wheel-grab.png`, `evidence/08-trigger-keeps-on.png`, `evidence/04-staging-ai-player.log` line n (cause=wheel-grab) | PASS |
    | 3 | A drop parks it at waiting issuing nothing, the connection returning resumes it, `#AUTO OFF` while waiting stays off | `evidence/01-test-report.txt` lines n-m (TestWaitingSurvivesDisconnectAndResumesOnConnect), `evidence/09-waiting-after-drop.png`, `evidence/10-resume-after-reconnect.png`, `evidence/11-off-while-waiting-stays-off.png`, `evidence/04-staging-ai-player.log` lines n-m (cause=disconnect, cause=resume) | PASS |
    | 4 | A profile can be created, the policy accepted, and the character hand-played with nothing about ordinary play changed | `evidence/03-canned-report.txt` line n (PASS C4), `evidence/12-hand-play-unchanged.png` | PASS |

    Line numbers are required for report and log citations — "see the report" is not a citation. Screenshots are cited by filename. No row may cite a database query.
  </how-to-verify>
  <verify>
    <human-check>All eight screenshots exist under evidence/ with the exact filenames, each showing the stated content, and the SUMMARY's four-row table cites a real file and line number for every criterion.</human-check>
  </verify>
  <acceptance_criteria>
    - All eight PNGs exist with exactly these names: `05-badge-after-refresh.png`, `06-refused-before-acceptance.png`, `07-wheel-grab.png`, `08-trigger-keeps-on.png`, `09-waiting-after-drop.png`, `10-resume-after-reconnect.png`, `11-off-while-waiting-stays-off.png`, `12-hand-play-unchanged.png`
    - Every PNG is taken from the end-user perspective: the MUDPuppy browser page with the header badges visible, no devtools pane, no terminal, no raw JSON in frame
    - `06-refused-before-acceptance.png` shows the refusal sentence and a badge reading `Autopilot: Off`
    - `05-badge-after-refresh.png` shows `Autopilot: On` after a hard reload with no re-engage action taken
    - `07-wheel-grab.png` shows the wheel-grab notice, the typed command and the game's reply together
    - `08-trigger-keeps-on.png` shows a trigger-fired command with the badge still reading `Autopilot: On`
    - `09-waiting-after-drop.png` shows `[Disconnected]` and `[Autopilot waiting for reconnect]` as two separate lines with a badge reading `Autopilot: Waiting`
    - `10-resume-after-reconnect.png` shows `[Reconnected]` and `[Autopilot resuming]` as two separate lines with a badge reading `Autopilot: On`
    - `11-off-while-waiting-stays-off.png` shows the badge reading `Autopilot: Off` after the connection has returned
    - `12-hand-play-unchanged.png` shows no bracketed autopilot line anywhere in the visible terminal history
    - `02-07-SUMMARY.md` contains a four-row criterion table; every row cites at least one evidence file, report and log citations carry line numbers, and every row carries a PASS or FAIL
    - No row in the table cites a SQL query or a database inspection as its evidence
  </acceptance_criteria>
  <resume-signal>Type "approved" to record the phase as demonstrated, or list the screenshots that could not be produced and what was on screen instead.</resume-signal>
  <done>The ROADMAP Phase Validation has been performed on staging and every criterion points at a file — a report line, a log line, or an image.</done>
</task>

<task type="auto">
  <name>Task 02-07-04: The Phase 2 security review gets a written agenda with nothing decided in advance</name>
  <files>.planning/phases/02-autopilot-switch/02-SECURITY-AGENDA.md</files>
  <read_first>
    - .planning/phases/02-autopilot-switch/02-CONTEXT.md Deferred Ideas (the waiting-to-on security-review item in the owner's own framing)
    - .planning/phases/02-autopilot-switch/02-RESEARCH.md Open Questions and Security Domain (the policy-section-2 tension and the proposed bounded-waiting remediation)
    - .specify/specs/safety-and-abuse-policy-v1.md sections 1 and 2 (the text the auto-resume is weighed against — quote it, do not paraphrase from memory)
    - .planning/todos/pending/2026-09-15-deferred-security-risks.md (R-02 and R-04 with the remediation wording the owner already saw)
    - .planning/phases/01-profile-foundation-and-policy-gate/01-SECURITY.md (the Phase 1 register's format and its DR-1-01 and DR-1-02 rows)
    - the threat_model blocks of plans 02-01 through 02-06 (the T-2 register this phase produced)
  </read_first>
  <action>
    Write `.planning/phases/02-autopilot-switch/02-SECURITY-AGENDA.md`: the input to the Phase 2 security review, not its output. It decides nothing. Every item carries the same three dispositions — Accept, Defer, Remediate Now — with none marked as chosen and none recommended, because that choice is the owner's at the review.

    Open with one paragraph stating what the agenda is for and that the project cannot close while any item on it remains deferred.

    Item 1, new this phase: the waiting-to-on auto-resume. State the behaviour plainly — an autopilot parked by a dropped connection returns to on by itself when the connection comes back, with no owner action and no time bound (DEC-autopilot-waits-across-disconnect, D-01). Quote policy section 2's sentence about not re-engaging until the disconnection is understood, and state the tension in one sentence: the policy addresses the owner re-engaging, and the system now does something adjacent automatically. Record that Phase 2 deliberately built no time bound, and that `AutopilotRecord.WaitingSince` is already stored so a bounded waiting lifetime can be added later without redesign. Name the candidate remediation, should Remediate Now be chosen, as a bounded waiting lifetime after which the switch lands on off, with the bound itself left to the owner. Cross-reference threat T-2-08.

    Item 2, carried forward from Phase 1: R-02 — invalid numeric input in the AI Player panel silently saves as blank rather than raising a validation error, in `frontend/src/components/AIPlayerPanel.tsx` handleSave. Reproduce the proposed remediation from the pending todo verbatim. Note that Phase 2 did not touch that file and built no fix.

    Item 3, carried forward from Phase 1: R-04 — the staging deploy log prints the one-time sign-in code, so anyone with staging log access can sign in as any staging user. Reproduce the proposed remediation verbatim. Note that this phase's own evidence capture read the staging log, which is exactly the access path the risk describes.

    Then a short carry-forward table of the Phase 2 threats that were dispositioned as accept inside the plans rather than mitigated, so the review sees them in one place: T-2-SC (no packages installed; confirm `go.mod` and `frontend/package.json` are unchanged) and the residual on T-2-03 recorded in plan 02-04 (the autopilot websocket handler receives a state and a cause for a connection the browser already owns). State for each that it was accepted at plan level and is listed for confirmation, not re-litigation.

    Close with one line noting that Phase 3 must re-confirm T-2-01, the wheel-grab source flag, because from Phase 3 an engaged autopilot starts issuing real commands and the consequence of a forged flag changes.

    Build no remediation in this task and change no code. If the owner chooses Remediate Now on any item, that becomes its own plan or its own phase.
  </action>
  <verify>
    <automated>test -f .planning/phases/02-autopilot-switch/02-SECURITY-AGENDA.md && grep -c "Remediate Now" .planning/phases/02-autopilot-switch/02-SECURITY-AGENDA.md && grep -c "R-02" .planning/phases/02-autopilot-switch/02-SECURITY-AGENDA.md && grep -c "R-04" .planning/phases/02-autopilot-switch/02-SECURITY-AGENDA.md && grep -c "T-2-08" .planning/phases/02-autopilot-switch/02-SECURITY-AGENDA.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/02-autopilot-switch/02-SECURITY-AGENDA.md` exists and contains three numbered items: the waiting-to-on auto-resume, R-02 and R-04
    - Each of the three items lists all three dispositions `Accept`, `Defer` and `Remediate Now`, and none is marked as selected or recommended
    - The agenda quotes policy section 2 rather than paraphrasing it, and cites `T-2-08`
    - The R-02 and R-04 entries reproduce the remediation wording from `.planning/todos/pending/2026-09-15-deferred-security-risks.md` and state that Phase 2 built no fix for either
    - The agenda contains a carry-forward table naming `T-2-SC` and the accepted residual from plan 02-04
    - The agenda states that the project cannot close while any item remains deferred
    - Source assertion: no file outside `.planning/` is modified by this task — `git status --porcelain` shows changes only under `.planning/`
  </acceptance_criteria>
  <done>The security review has a written agenda: one new policy question and two carried-forward risks, each with three choices and none of them made in advance.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repository → Railway staging | The deployed binary may diverge from the repo; the canned report and the log are what reconcile them |
| Owner browser session → the live MUD | The walkthrough exercises the real session, websocket and wheel-grab path end to end |
| Evidence files → readers | Logs, reports and screenshots are committed to the repo; whatever they contain is disclosed |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigating Task |
|-----------|----------|----------|-----------|-------------|-----------------|
| T-2-07 | Repudiation (unfalsifiable evidence) | High | a canned report that always passes | mitigate | 02-07-01 — the negative self-test is captured to `evidence/02-harness-selftest.txt` before the staging report is trusted, so a clean report carries information |
| T-2-04 | Information Disclosure | Medium | the committed log excerpt and reports | mitigate | 02-07-02 — the excerpt is read back and grepped for conduct-rules text, approach-guidance text and command text before it is filed |
| T-2-01 | Spoofing / Tampering | High | the wheel-grab source flag, end to end | mitigate | 02-07-03 — the walkthrough proves both directions on a live MUD: a typed command disengages, a trigger-fired command does not, and the staging log shows the `cause=wheel-grab` line for the former only |
| T-2-06 | Spoofing (a badge that lies) | High | the badge after a page refresh | mitigate | 02-07-03 — the refresh screenshot is taken with no action after the reload, so the badge could only be correct by reading the server |
| T-2-08 | Policy risk (not a STRIDE category) | Medium | waiting-to-on auto-resume with no bound | defer to security review | 02-07-04 — written onto the agenda with three dispositions and no decision; no remediation is built in this phase |
| T-2-20 | Repudiation (carried-forward risks forgotten) | Medium | Phase 1's R-02 and R-04 | mitigate | 02-07-04 — both are on the agenda with their original remediation wording, and the agenda states the project cannot close while either remains deferred |
| T-2-17 | Tampering (wrong target) | Medium | a deploy or a harness run aimed at production | mitigate | 02-07-02 — staging only, and the report header records `BASE_URL` and the git SHA so every piece of evidence is attributable to an environment |
| T-2-SC | Tampering (supply chain) | Low | `go.mod`, `frontend/package.json` | accept | 02-07-01 — the `### DEPENDENCY DRIFT` section of the test report must be empty for the whole phase |
</threat_model>

<verification>
1. `evidence/01-test-report.txt` exists with every command captured verbatim, zero new FAIL lines, no data race and an empty dependency-drift section.
2. `evidence/02-harness-selftest.txt` shows a clean self-test and a negative self-test containing `FAIL C1` with a non-zero exit.
3. `evidence/03-canned-report.txt` shows PASS for C1 and C4, SKIP for C2 and C3 naming their screenshots, zero FAIL lines, and a response body under every step.
4. `evidence/04-staging-ai-player.log` contains `[AI-PLAYER] autopilot` lines for all six causes and no game or profile text.
5. Eight screenshots exist under `evidence/` with the exact filenames listed, all from the end-user perspective.
6. `02-07-SUMMARY.md`'s four-row table cites a real file, and a line number for text evidence, per ROADMAP criterion, with a verdict.
7. `02-SECURITY-AGENDA.md` lists the three review items with three dispositions each and none chosen.
8. Both checkpoints are explicitly approved by the owner.
9. No evidence in this phase is a database query.
</verification>

<success_criteria>
- ROADMAP Phase 2 criterion 1 demonstrated by the `TestEngageAutopilot` lines in `evidence/01-test-report.txt`, the `PASS C1` lines in `evidence/03-canned-report.txt`, and screenshots `06-refused-before-acceptance.png` and `05-badge-after-refresh.png`.
- Criterion 2 demonstrated by the `TestWheelGrabSourceRule` lines in `evidence/01-test-report.txt`, screenshots `07-wheel-grab.png` and `08-trigger-keeps-on.png`, and the `cause=wheel-grab` line in `evidence/04-staging-ai-player.log`.
- Criterion 3 demonstrated by the `TestWaitingSurvivesDisconnectAndResumesOnConnect` lines in `evidence/01-test-report.txt`, screenshots `09-waiting-after-drop.png`, `10-resume-after-reconnect.png` and `11-off-while-waiting-stays-off.png`, and the `cause=disconnect` and `cause=resume` lines in `evidence/04-staging-ai-player.log`.
- Criterion 4 demonstrated by the `PASS C4` line in `evidence/03-canned-report.txt` and screenshot `12-hand-play-unchanged.png`.
- The Phase 2 security review can begin, because its agenda exists in writing with the waiting-to-on question and the two carried-forward Phase 1 risks on it.
</success_criteria>

<output>
Create `.planning/phases/02-autopilot-switch/02-07-SUMMARY.md` when done, containing the evidence file inventory, the four-row criterion table with citations and verdicts, and a pointer to `02-SECURITY-AGENDA.md`.
</output>
