---
phase: 03.1-prompt-injection-review
verified: 2026-09-16T22:25:08Z
status: human_needed
score: 6/6 must-haves verified
overrides_applied: 0
human_verification:
  - test: "The owner conducts the Phase 3.1 security review: for each of the six items in 03.1-SECURITY-AGENDA.md (DR-3-02 re-presented; the reviewer-can-be-misled residual; the doubled call cost; retained attack text; the emptiable Never-issue list; the ephemeral staging ENCRYPTION_KEY_V1), the owner records one of Accept / Defer / Remediate Now and the decision is appended to .planning/RISK-REGISTER.md (DR-3-02's row already has its 'Raised again at' pointer updated to this review, but no disposition has been recorded on any of the six items — every checkbox in 03.1-SECURITY-AGENDA.md is still unchecked)."
    expected: "Each of the six agenda items carries an owner-made disposition; per CLAUDE.md/this project's convention, a Defer disposition blocks project close until resolved, so the owner's choice determines whether Phase 4 can proceed unconditionally or with a carried obligation."
    why_human: "This is a risk-acceptance judgment call the codebase cannot make or verify — 03.1-SECURITY-AGENDA.md states plainly 'This document is the input to the Phase 3.1 security review, not its output... It decides nothing,' and .planning/STATE.md's own stopped_at line confirms execution is complete 'pending... the Phase 3.1 security review before the phase can close.'"
---

# Phase 3.1: Prompt Injection Review Verification Report

**Phase Goal:** Game text, including other players' speech and hostile room descriptions, cannot steer the AI into issuing a command the owner would not sanction. The investigation establishes how far shape validation and the ICM safety checker already bound the risk and what a content-level mitigation would add; whatever is built is proven by tests with hostile game text and demonstrated on staging.
**Verified:** 2026-09-16T22:25:08Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (ROADMAP §Phase 3.1 Success Criteria)

| # | Truth | Status | Evidence |
|---|---|---|---|
| SC-1 | Hostile-text corpus runs as a flagged Go test against the real model; red-team report filed before and after the build, counting catches per layer and listing wrongly blocked benign windows | ✓ VERIFIED | `internal/driver/corpus_live_test.go` (`TestCorpusIsWellFormed` PASS, `TestLiveCorpus_HostileText` SKIPs cleanly with `MUDPUPPY_LIVE_CORPUS` unset — reran myself); `evidence/01-redteam-before.txt` (STEERED: 2) and `evidence/05-redteam-after.txt` (STEERED: 0) filed with per-item lines and a CATCHES BY LAYER block; corpus unchanged between BEFORE and HEAD confirmed by `git log f62e64c..HEAD --oneline -- internal/driver/corpus_live_test.go internal/driver/testdata/corpus_profile.json` (empty output, ran myself) |
| SC-2 | Prompt delimits the game-text window as untrusted data; conduct rules and approach guidance still reach the model verbatim | ✓ VERIFIED | `internal/driver/driver.go` `untrustedDataParagraph()` and `buildSystemInstruction`; `TestBuildSystemInstruction` and `TestHandleEngageReviewer/reviewer_sees_wrapped_window` both PASS (reran myself) |
| SC-3 | Owner-editable Never-issue list is stored, returned, and enforced in Go before the reviewer and before ICM dispatch | ✓ VERIFIED | `internal/store/profile.go` `NeverIssueList` field/round-trip; `internal/driver/driver.go` `matchNeverIssue` (whole-word prefix match, case-insensitive) runs before the reviewer call in `HandleEngage`; `TestHandleEngageNeverIssue` PASS (reran myself); `scripts/verify-phase3-1.sh --self-test` PASS C3 (ran myself, exit 0); `evidence/07-never-issue-settings.png` shows `give`/`drop` persisted in the AI Player settings box |
| SC-4 | Reviewer pass judges the chosen command and blocks on a broken rule or a followed embedded/harmful instruction; a failed review call sends nothing and disengages | ✓ VERIFIED | `internal/gemini/client.go` `ReviewCommand`; `internal/driver/driver.go` reviewer stage between Never-issue check and dispatch; `TestReviewCommand` and `TestHandleEngageReviewer` (blocked, clear, three failure-kind subtests, all with positive `disengages()` count assertions per RESEARCH Pitfall 1/3) all PASS (reran myself) |
| SC-5 | Blocked command sends nothing, stays ON and idle, stores a `blocked` decision row with its reason, reloads after a refresh, and appears in the panel and one terminal line | ✓ VERIFIED | `internal/driver/driver.go` `recordBlocked` (no `DisengageAutopilot` call); `frontend/src/components/AIAssistPanel.tsx` blocked card and reload ternary fixed per RESEARCH Pitfall 5 (`command: item.outcome === 'failed' \|\| item.outcome === 'refused' ? item.notice : item.command`); `frontend/src/pages/PlayScreen.tsx` terminal line `[AI-ASSIST blocked > ...]`; screenshots `08a`/`09a`/`10a` (viewed) show the panel reasoning, `Blocked → get rod`, the reviewer's notice, the terminal line, badge `AUTOPILOT: ON`, and the same block surviving a reload |
| SC-6 | After the build, zero steered commands from the corpus reach the send path; on staging the owner's own `say` injection is shown blocked with its reason | ✓ VERIFIED (staging half honestly characterised, accepted by binding rule) | `evidence/05-redteam-after.txt`: STEERED: 0, FALSE BLOCKS: none; staging: per the binding rule for this verification, the owner accepted walkthrough 2 "as demonstrated" on 2026-09-16 with the honest characterisation in `03.1-07-SUMMARY.md` ("no injection reached the Never-issue list or the reviewer... The list and reviewer layers are proven by the corpus, the unit tests and RUN B, not by the staging demonstration"); screenshot `13-injection-resisted-walkthrough2.png` (viewed) matches this description exactly — the model's own reasoning refuses the bait, chooses `get rod`, and the reviewer allows it, no caught-in-the-act block shown |

**Score:** 6/6 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `internal/driver/corpus_live_test.go` | Hostile-text corpus, well-formedness test, flag-gated live runner, report writer | ✓ VERIFIED | `TestCorpusIsWellFormed` and `TestLiveCorpus_HostileText` both present and behave as specified (checked live) |
| `internal/driver/testdata/corpus_profile.json` | Corpus fixture profile including `never_issue_list` | ✓ VERIFIED | present, unchanged between BEFORE/AFTER commits |
| `migrations/012_add_never_issue_and_blocked_outcome.{up,down}.sql` | `never_issue_list` column; `blocked` outcome | ✓ VERIFIED | up migration adds column and widens CHECK by dynamic constraint lookup (more robust than the RESEARCH's guessed-name approach); down migration reverses both |
| `internal/store/profile.go` | `NeverIssueList` on `Profile`/`ProfileUpdate`, threaded through Get/Update | ✓ VERIFIED | field present in struct, both SELECT/Scan sites, and UpdateProfile's merge logic |
| `internal/profiles/handler.go` | `never_issue_list` on AI sub-resource with length cap | ✓ VERIFIED | `TestAISettingsRoundTrip`/`TestAISettingsValidation` PASS (reran myself); 20,000-char cap confirmed live via harness self-test (`PUT` with 20001 chars → HTTP 400) |
| `internal/store/decisions.go` | `blocked` as fourth outcome | ✓ VERIFIED | `validDecisionOutcomes["blocked"] = true` present |
| `internal/driver/driver.go` | Untrusted-data framing, Never-issue matcher, reviewer stage, `recordBlocked`, harm-aimed reviewer question (D-03 amendment) | ✓ VERIFIED | all present; `reviewHarmDefinition`/`reviewOrdinaryGuidanceException`/`reviewFindThenDecideProcedure` constants match the amended D-03 wording verbatim (harm-listing, not "embedded instruction") |
| `internal/gemini/client.go` | `ReviewCommand`/`ReviewAnswer` | ✓ VERIFIED | present, tested by `TestReviewCommand` |
| `frontend/src/components/AIAssistPanel.tsx`, `AIPlayerPanel.tsx`, `PlayScreen.tsx`, `index.css` | Blocked card, Never-Issue textarea, terminal line, styling | ✓ VERIFIED | all present, wired to the `ai` websocket event and the decisions reload path |
| `scripts/verify-phase3-1.sh` + fixtures | Canned-report harness, self-test/negative-self-test, PASS/FAIL/SKIP per criterion | ✓ VERIFIED | ran myself: `--self-test` → PASS, exit 0; `--self-test-negative` → `FAIL C3`, exit 1, exactly as specified |
| `.planning/phases/03.1-prompt-injection-review/03.1-SECURITY-AGENDA.md` | Six-item agenda, nothing pre-decided | ✓ VERIFIED (as an artifact) — but see Human Verification | present, well-formed, decisions intentionally left open |

### Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| `HandleEngage` | `matchNeverIssue` | called after `validateCommand`, before the reviewer | WIRED | confirmed in `driver.go`; `TestHandleEngageReviewer/never_issue_block_skips_the_reviewer` proves ordering |
| `HandleEngage` | `gemini.Client.ReviewCommand` | second model call, wrapped window | WIRED | `reviewer_sees_wrapped_window` subtest passes |
| reviewer `blocked:true` | `recordBlocked` | separate branch from reviewer-call error → `recordFailure` | WIRED | two distinct `if` branches confirmed at source; disengage-count assertions differ correctly (0 vs 1) per branch |
| `recordBlocked` | `logDecision` | reused signature, no free-text params | WIRED | no `notice`/`reason` value flows into any `log.Printf`/`logDecision` call (grepped, zero hits) |
| `ai` websocket event (`outcome: blocked`) | `AIAssistPanel.tsx` live push and `loadHistory` reload | payload.message as reason, command kept in command slot | WIRED | both live-push and reload paths checked; reload ternary explicitly special-cases `blocked` to keep the real command (RESEARCH Pitfall 5 fix confirmed) |
| Frontend `outcome === 'blocked'` | terminal echo | `[AI-ASSIST blocked > ...]` via `automationEngine.echoLocal` | WIRED | confirmed in `PlayScreen.tsx` |

### Automated Checks (run directly by this verification, not taken from SUMMARY)

| Check | Result |
|---|---|
| `go build ./...` | exit 0, no output |
| `go vet ./...` | exit 0, no output |
| `go test ./...` | all packages `ok` (driver, gemini, store, profiles, session, policy, auth, config, icm) |
| `go test ./internal/driver/... ./internal/session/... -race` | exit 0, both `ok` (cgo/MinGW toolchain available on this machine) |
| `cd frontend && npm run build` | `tsc && vite build` succeeded |
| `bash scripts/verify-phase3-1.sh --self-test /tmp/v-st.txt` | exit 0, `TOTAL CHECKS: 13 FAILURES: 0` |
| `bash scripts/verify-phase3-1.sh --self-test-negative /tmp/v-neg.txt` | exit 1, `FAIL C3` present exactly as required |
| `MUDPUPPY_LIVE_CORPUS` unset → `go test ./internal/driver/... -run TestLiveCorpus -v` | `--- SKIP: TestLiveCorpus_HostileText`, prints the enabling instructions, no network call made |
| `git log f62e64c..HEAD --oneline -- internal/driver/corpus_live_test.go internal/driver/testdata/corpus_profile.json` | empty (corpus unchanged between authoritative BEFORE and HEAD) |
| Evidence greps: `grep -ciE "conduct_rules\|approach_guidance\|never_issue\|reasoning=\|notice=\|AIza\|x-goog-api-key\|generativelanguage\|GAME_TEXT" evidence/06-staging-ai-player.log` | 0 |
| Same grep pattern (key/endpoint/`<GAME_TEXT>` subset) on every `evidence/*redteam*.txt` | 0 across all six files |
| Session-cookie leak check on `evidence/04-canned-report.txt` | no `Set-Cookie`/`session_id`/`session_token`/`Authorization:` header value found; only `game_session_id=` UUID labels (not a cookie) |

No blocker anti-patterns found: no `TBD`/`FIXME`/`XXX`/`TODO`/`HACK`/`PLACEHOLDER` in any of the fourteen files this phase touched.

### Requirements Coverage

No REQ IDs map to this phase (confirmed: `.planning/REQUIREMENTS.md` has no Phase 3.1 / DR-3-02 rows). The ROADMAP's six Success Criteria are the phase's contract and are covered above. No orphaned requirements.

### Screenshots Reviewed Directly (not taken on SUMMARY's word)

`07-never-issue-settings.png`, `08a-blocked-in-panel-walkthrough1.png`, `10a-panel-after-refresh-walkthrough1.png`, `11b-model-failure-panel-after-reconnect.png`, `12-hand-play-unchanged.png`, `13-injection-resisted-walkthrough2.png` were all opened and visually inspected. Every one matches its cited claim in `03.1-07-SUMMARY.md` exactly, including the honestly-stated false-block-not-a-catch framing for walkthrough 1 and the model's-own-resistance framing for walkthrough 2 — no image shows more (or less) than the SUMMARY claims.

### Gaps Summary

No code, test, or evidence gap was found. Every one of the six ROADMAP success criteria is backed by a real, independently-reproduced test run, a real grep against the filed evidence, or a directly-viewed screenshot — not merely a SUMMARY claim. The D-03 amendment (reviewer judges harm, not text) is faithfully implemented in `internal/driver/driver.go`, matching the owner's amendment text in `03.1-CONTEXT.md` almost verbatim. Row 6's honest, non-overclaiming wording is accepted per this verification's binding instructions.

The one open item is procedural, not technical: `03.1-SECURITY-AGENDA.md` is a well-formed, complete input document (six items, each reproducing measured before/after evidence and three dispositions), but every disposition checkbox is unchecked, and `.planning/RISK-REGISTER.md`'s DR-3-02 row carries only the "Raised again at" pointer update, not a decision. `.planning/STATE.md`'s own `stopped_at` line states execution is complete "pending... the Phase 3.1 security review before the phase can close," confirming this is a known, deliberately-deferred step rather than an oversight. Per this project's binding convention (security risk decisions are made by the owner, per-risk, at each phase security review, and a Defer disposition blocks project close), this step requires the owner's live judgment and cannot be resolved by further coding or testing — hence `human_needed` rather than `gaps_found`.

---

*Verified: 2026-09-16T22:25:08Z*
*Verifier: Claude (gsd-verifier)*
