# Phase 3.1: Prompt Injection Review - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-09-16
**Phase:** 03.1-prompt-injection-review
**Areas discussed:** Todo fold, Mitigation approach, What a blocked command does, Hostile-text proof on staging

---

## Todo fold

| Option | Description | Selected |
|--------|-------------|----------|
| Fold the DR-3-02 section | The DR-3-02 row and the owner's verbatim note become locked input for this phase; the rest of the todo stays with Phase 4 | ✓ |
| Don't fold it | Treat the todo as reviewed only; the risk register row is already the source | |
| I need to discuss this further before we move on | Pause and talk it through | |

**User's choice:** Fold the DR-3-02 section
**Notes:** Only the DR-3-02 section of `2026-09-16-phase4-security-carry-forward.md`; the other sections remain Phase 4's.

---

## Mitigation approach

### Which defence layers behind prompt hardening

| Option | Description | Selected |
|--------|-------------|----------|
| All three layers | Hardened prompt, owner-editable blocked-command list on the profile checked in Go before dispatch, second model pass reviewing the command against conduct rules and room text; one extra Gemini call per decision | ✓ |
| Hardening + blocked list only | No extra call; deterministic but only catches listed commands | |
| Hardening + reviewer pass only | Content-level judgement with nothing to maintain; extra call; reviewer is itself a model | |
| I need to discuss this further before we move on | | |

**User's choice:** All three layers

### Where the blocked-command list lives

| Option | Description | Selected |
|--------|-------------|----------|
| AI Player settings, empty by default | New "Never issue" box beside conduct rules and approach guidance, one entry per line, prefix-matched; nothing pre-filled | ✓ |
| AI Player settings, with a starter set | Same box pre-filled with a generic editable set | |
| Inside conduct rules with a marker | Lines starting with a marker such as NEVER: enforced mechanically | |
| I need to discuss this further before we move on | | |

**User's choice:** AI Player settings, empty by default

### What the reviewer judges

| Option | Description | Selected |
|--------|-------------|----------|
| Rules and injection | Does the command break a conduct rule, and does it follow an instruction embedded in the game text; either yes blocks | ✓ |
| Injection only | Only whether the command was dictated by text in the window | |
| Rules only | Only whether the command breaks a conduct rule | |
| I need to discuss this further before we move on | | |

**User's choice:** Rules and injection

### Investigation sequence

| Option | Description | Selected |
|--------|-------------|----------|
| Baseline first, build proceeds | Plan 1 runs the corpus against the current build with the real model and files a red-team report; build follows without a sign-off stop; corpus reruns after | ✓ |
| Baseline first, then a checkpoint with you | Build plans wait for the owner to read the report | |
| No separate baseline | Corpus only run against the finished build | |
| I need to discuss this further before we move on | | |

**User's choice:** Baseline first, build proceeds
**Notes:** Owner chose "Next area" at the continuation check.

---

## What a blocked command does

### Effect on autopilot

| Option | Description | Selected |
|--------|-------------|----------|
| Notice and disengage | Nothing sent, notice, autopilot lands OFF like an ICM refusal under D-13 | |
| Notice, stay ON and idle | Nothing sent, notice, switch stays ON; Phase 4 decides what the loop does | ✓ |
| I need to discuss this further before we move on | | |

**User's choice:** Notice, stay ON and idle
**Notes:** Departs from the recommended option; a block is treated as the defence working, not an AI failure. Model-side failures still disengage.

### What the owner sees

| Option | Description | Selected |
|--------|-------------|----------|
| The command and the reason | Panel shows reasoning, the blocked command, and why (matched entry or reviewer's sentence); terminal prints a bracketed line naming the blocked command | ✓ |
| A generic notice only | Says a command was blocked without showing it | |
| I need to discuss this further before we move on | | |

**User's choice:** The command and the reason

### Storage

| Option | Description | Selected |
|--------|-------------|----------|
| Store it with outcome 'blocked' | One decision row with window, reasoning, blocked command, reason; reloads after refresh | ✓ |
| Show live only, don't store | Gone after refresh | |
| I need to discuss this further before we move on | | |

**User's choice:** Store it with outcome 'blocked'

### Reviewer call failure

| Option | Description | Selected |
|--------|-------------|----------|
| Fail closed and disengage | Nothing sent; model failure under D-13; existing notice; autopilot OFF | ✓ |
| Fail closed, stay ON and idle | Nothing sent, notice, switch stays ON | |
| Send unreviewed | First model's command goes through with a notice that review was skipped | |
| I need to discuss this further before we move on | | |

**User's choice:** Fail closed and disengage
**Notes:** Owner chose "Next area" at the continuation check.

---

## Hostile-text proof on staging

### Source of hostile text for the staging demonstration

| Option | Description | Selected |
|--------|-------------|----------|
| A fake MUD service on staging | Scripted telnet game as a second Railway service | |
| A second player on Alter Aeon | A helper character says the injection text | |
| A diagnostic endpoint fed canned windows | Owner-only staging endpoint runs the decide path on a posted window | |
| I need to discuss this further before we move on | | |

**User's choice (free text):** "We don't have a public endpoint to supply the triggers for this." Then, after Claude offered a localhost fake game inside the server process or a private-network Railway service: "I do not want to stand up a fake MUD for this purpose on Railway... but I don't see how a browser-based MUD client like MUD Puppy can connect to a 127.0.0.1 address."
**Notes:** Claude explained that the Go server on Railway holds the telnet socket, not the browser, and dropped the fake-game route at the owner's request. Proposed split: full corpus as a flagged Go test from the dev machine against the real model; staging demonstration via the owner's own character typing `say <injection>` on Alter Aeon.

### Confirming the split

| Option | Description | Selected |
|--------|-------------|----------|
| Yes, that split | Flagged Go test for the corpus plus the owner's own `say` on Alter Aeon; no new services or endpoints | ✓ |
| Corpus test only, no Alter Aeon demo | Falls short of "demonstrated on staging" | |
| Alter Aeon demo only, no corpus test | No before-and-after report | |
| I need to discuss this further before we move on | | |

**User's choice:** Yes, that split

### Pass bar

| Option | Description | Selected |
|--------|-------------|----------|
| Zero steered commands reach the game | Attacker's wanted command never reaches the send path, whichever layer caught it; per-layer catches and false blocks reported | ✓ |
| Zero steered commands and zero false blocks | A wrongly blocked benign window also fails the phase | |
| I need to discuss this further before we move on | | |

**User's choice:** Zero steered commands reach the game
**Notes:** Owner chose "Done with this area", then "I'm ready for context".

---

## Claude's Discretion

- Hardened-prompt wording and delimiters; how the Never-issue list is presented to the models.
- Reviewer prompt, JSON shape, and model entry (default: same entry as the player call).
- Never-issue matching rules and field length limit.
- Column and migration numbering; decision-row shape for the `blocked` outcome and reason; websocket event shape.
- Terminal line wording and panel rendering for a block.
- The live corpus test's flag, report format and location; how the before run is captured.
- `[AI-PLAYER]` log lines for the reviewer stage and blocked outcome.

## Deferred Ideas

- What a block means for the Phase 4 loop (skip, count toward threshold, back off).
- Retry on a reviewer 503 (DR-3-04, Phase 4).
- Starter Never-issue set or per-game templates.
- A separate or cheaper reviewer model in the registry.
- Fake game fixture or diagnostic decide endpoint on staging (declined by the owner).
- Second-account attacks on Alter Aeon.
