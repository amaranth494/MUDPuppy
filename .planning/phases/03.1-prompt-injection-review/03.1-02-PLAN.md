---
phase: 03.1-prompt-injection-review
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - migrations/012_add_never_issue_and_blocked_outcome.up.sql
  - migrations/012_add_never_issue_and_blocked_outcome.down.sql
  - internal/store/profile.go
  - internal/profiles/handler.go
  - internal/profiles/handler_test.go
  - frontend/src/types/index.ts
  - frontend/src/components/AIPlayerPanel.tsx
autonomous: true
requirements: [SC-3]

must_haves:
  truths:
    - "The owner can type forbidden commands into a Never-Issue List box in AI Player settings, one per line, save them with the other AI settings and find them still there after a page reload, proven by evidence/07-never-issue-settings.png and by the never-issue steps of evidence/04-canned-report.txt (D-02)"
    - "The Never-issue list travels with conduct rules and approach guidance through the same PUT and GET of the AI sub-resource, so nothing new has to be saved separately, proven by the TestAISettingsRoundTrip PASS line in evidence/02-test-report.txt (D-02)"
    - "A blank Never-issue list is the default on every profile and no starter set ships, proven by the empty-by-default assertion inside TestAISettingsRoundTrip in evidence/02-test-report.txt (D-02, CON-engine-has-no-game-knowledge)"
    - "An over-long Never-issue list is refused by the server with a plain message rather than stored, proven by the length-cap assertion inside TestAISettingsValidation in evidence/02-test-report.txt (T-3.1-03)"
    - "The database will accept a blocked decision once the driver starts writing one, proven by the Migrations completed successfully (version=12) line in evidence/06-staging-ai-player.log (D-08)"
  artifacts:
    - path: "migrations/012_add_never_issue_and_blocked_outcome.up.sql"
      provides: "profiles.never_issue_list TEXT NOT NULL DEFAULT '' and the ai_decisions.outcome CHECK constraint widened to include blocked"
      contains: "never_issue_list"
    - path: "migrations/012_add_never_issue_and_blocked_outcome.down.sql"
      provides: "The reverse: the column dropped and the CHECK constraint restored to the original three values"
      contains: "ai_decisions_outcome_check"
    - path: "internal/store/profile.go"
      provides: "NeverIssueList on Profile and ProfileUpdate, read by both profile getters and written by UpdateProfile"
      contains: "NeverIssueList"
    - path: "internal/profiles/handler.go"
      provides: "never_issue_list on the AI settings sub-resource, with a length cap in validateAISettings"
      contains: "NeverIssueList"
    - path: "frontend/src/components/AIPlayerPanel.tsx"
      provides: "The Never-Issue List textarea beside Conduct Rules and Approach Guidance, saved by the existing Save AI Settings button"
      contains: "Never-Issue List"
  key_links:
    - from: "frontend/src/components/AIPlayerPanel.tsx"
      to: "PUT /api/v1/profiles/{connection_id}/ai-settings"
      via: "the existing handleSave body, one new field"
      pattern: "never_issue_list"
    - from: "internal/profiles/handler.go PutAISettings"
      to: "internal/store/profile.go UpdateProfile"
      via: "store.ProfileUpdate.NeverIssueList"
      pattern: "NeverIssueList:\\s*&req.NeverIssueList"
    - from: "internal/store/profile.go GetProfileByConnection"
      to: "profiles.never_issue_list"
      via: "the SELECT column list and the Scan target the driver reads on every decision"
      pattern: "never_issue_list"
---

<objective>
Give the owner a place to say what the AI must never type. The AI Player settings section gains a Never-Issue List box beside Conduct Rules and Approach Guidance: one entry per line, empty on every profile by default, saved and reloaded with the other AI fields, and carried on the profile row where the driver will read it on every decision.

Purpose: D-02's owner-facing half. The list is the only part of this phase's defence the owner controls directly, and it has to exist and round-trip before the driver can enforce it (plan 03.1-03) or the model can be told about it. This plan also carries migration `012`, which does both schema changes this phase needs in one file (RESEARCH Pattern 2, no speed bumps): the new profile column, and widening `ai_decisions.outcome` so the `blocked` outcome plan 03.1-03 introduces has somewhere to land.
Output: migration `012` up and down, the field threaded through the store and the AI sub-resource with a length cap, and the textarea with its locked copy.

Names fixed here and used verbatim by every later plan: Postgres column `never_issue_list`, Go field `NeverIssueList`, JSON key `never_issue_list`, React state `neverIssueList`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/ROADMAP.md
@.planning/phases/03.1-prompt-injection-review/03.1-CONTEXT.md
@.planning/phases/03.1-prompt-injection-review/03.1-RESEARCH.md
@.planning/phases/03.1-prompt-injection-review/03.1-PATTERNS.md
@.planning/phases/03.1-prompt-injection-review/03.1-UI-SPEC.md

This repository has no database-backed Go test and no frontend test tooling. The store layer's round trip is therefore proven at the handler layer with the existing hand-written `fakeProfileStore` (`internal/profiles/handler_test.go`), over HTTP by `scripts/verify-phase3-1.sh` (plan 03.1-06), and on screen by a staging screenshot (plan 03.1-07). The frontend gate is `cd frontend && npm run build` (tsc + vite) — there is nothing else.

Locked copy for the new field, reproduced from `03.1-UI-SPEC.md`'s Copywriting Contract and to be used character for character:

- Label: `Never-Issue List`
- Placeholder: none
- Hint: `One entry per line. A command is blocked if it starts with an entry, matched whole-word and case-insensitive — so "give" blocks "give sword to bob" but not "giveaway". Blank means none: nothing is blocked by this list, though the reviewer and the model's own judgment still apply.`

The field's character cap is the existing 20,000 used by Conduct Rules and Approach Guidance — `03.1-UI-SPEC.md` locks reuse of that constant rather than a new number, overriding `03.1-RESEARCH.md`'s 5,000 suggestion (Assumption A2). The cap is not shown in the hint, matching its siblings.
</context>

<tasks>

<task type="auto">
  <name>Task 03.1-02-01: The schema can hold a forbidden-command list and a blocked decision</name>
  <files>migrations/012_add_never_issue_and_blocked_outcome.up.sql, migrations/012_add_never_issue_and_blocked_outcome.down.sql</files>
  <read_first>
    - migrations/010_add_ai_fields.up.sql and .down.sql (the `ALTER TABLE profiles ADD COLUMN IF NOT EXISTS` convention and its reverse)
    - migrations/011_add_ai_session_tables.up.sql lines 40-62 (the `ai_decisions` table, in particular the inline, unnamed `outcome TEXT NOT NULL CHECK (outcome IN ('sent','refused','failed'))` and the fact that no other CHECK exists on that table)
    - migrations/011_add_ai_session_tables.down.sql (the down-migration voice)
    - cmd/server/main.go lines 108-160 (how migrations are applied at startup with golang-migrate, and that a failed `m.Up()` is a `log.Fatalf` — a badly written migration takes staging down, which is why this one must be name-agnostic and idempotent)
    - .planning/phases/03.1-prompt-injection-review/03.1-RESEARCH.md Pattern 2 and Assumption A1 (the assumed default constraint name and why it must not be trusted blindly)
  </read_first>
  <action>
    Create `migrations/012_add_never_issue_and_blocked_outcome.up.sql` with a `-- +migrate Up` header and a comment block stating that the column is the owner-editable Never-issue list (D-02), that it ships empty on every profile with no starter set because the engine holds no game knowledge, and that the `blocked` outcome (D-06, D-08) joins `ai_decisions`' existing outcome values.

    Two statements:
    1. `ALTER TABLE profiles ADD COLUMN IF NOT EXISTS never_issue_list TEXT NOT NULL DEFAULT ''` — exactly the shape migration `010` used for `conduct_rules`.
    2. The `outcome` CHECK swap, written name-agnostically rather than assuming Postgres's default constraint name. Use a `DO $$ ... $$` block that selects from `pg_constraint` every row where `conrelid` is `'ai_decisions'::regclass`, `contype` is `'c'` and `pg_get_constraintdef(oid)` mentions `outcome`, and executes `ALTER TABLE ai_decisions DROP CONSTRAINT` with each found name quoted via `format('%I', ...)`; then, outside the block, `ALTER TABLE ai_decisions ADD CONSTRAINT ai_decisions_outcome_check CHECK (outcome IN ('sent','refused','failed','blocked'))`. Migration `011` created exactly one CHECK on that table and it is the outcome one, so the loop cannot catch anything else. Comment that this form is used because the original constraint was declared inline and unnamed, so its real name is not knowable from the source, and a `DROP CONSTRAINT IF EXISTS` on a guessed name would silently leave the old constraint in place and reject every `blocked` row at runtime.

    Create the matching `.down.sql` with a `-- +migrate Down` header: the same name-agnostic drop loop, then `ALTER TABLE ai_decisions ADD CONSTRAINT ai_decisions_outcome_check CHECK (outcome IN ('sent','refused','failed'))`, then `ALTER TABLE profiles DROP COLUMN IF EXISTS never_issue_list`. Both files must be safe to run twice.

    Do not add a column-ensure fallback to `cmd/server/main.go`: the fallbacks there exist for migrations `009` and `010` that predate the current packaging, and migration `011` applied cleanly on staging from `file://migrations` without one.

    Run no SQL by hand and do not connect to any database in this task. The migration is proven applied by the startup line plan 03.1-07 captures.
  </action>
  <verify>
    <automated>test -f migrations/012_add_never_issue_and_blocked_outcome.up.sql && test -f migrations/012_add_never_issue_and_blocked_outcome.down.sql && grep -c "blocked" migrations/012_add_never_issue_and_blocked_outcome.up.sql && grep -c "never_issue_list" migrations/012_add_never_issue_and_blocked_outcome.down.sql</automated>
  </verify>
  <acceptance_criteria>
    - Both migration files exist with the exact names above and carry `-- +migrate Up` / `-- +migrate Down` headers
    - The up file contains `ADD COLUMN IF NOT EXISTS never_issue_list TEXT NOT NULL DEFAULT ''`
    - The up file contains a `DO $$` block referencing `pg_constraint`, `'ai_decisions'::regclass` and `pg_get_constraintdef`, and then `ADD CONSTRAINT ai_decisions_outcome_check CHECK (outcome IN ('sent','refused','failed','blocked'))`
    - Source assertion: `grep -c "DROP CONSTRAINT IF EXISTS ai_decisions_outcome_check" migrations/012_add_never_issue_and_blocked_outcome.up.sql` returns 0 — the guessed-name form is deliberately not used
    - The down file restores `CHECK (outcome IN ('sent','refused','failed'))` and drops the column with `IF EXISTS`
    - Source assertion: `git diff --stat cmd/server/main.go` is empty for this task
    - `go build ./...` exits 0
  </acceptance_criteria>
  <done>The schema can store a Never-issue list on a profile and a blocked decision on a decision row, and the migration cannot take staging down on a wrong constraint name.</done>
</task>

<task type="auto">
  <name>Task 03.1-02-02: The Never-issue list is stored with the profile and answered by the AI settings endpoints</name>
  <files>internal/store/profile.go, internal/profiles/handler.go, internal/profiles/handler_test.go</files>
  <read_first>
    - .planning/phases/03.1-prompt-injection-review/03.1-PATTERNS.md sections `internal/store/profile.go` and `internal/profiles/handler.go` (the five store touch points and the four handler touch points, each quoted verbatim with line numbers)
    - internal/store/profile.go lines 15-35, 135-150, 205-340 and 380-495 (`Profile`, `ProfileUpdate`, `GetProfile`, `GetProfileByConnection`, `UpdateProfile` — the `ConductRules`/`ApproachGuidance` pattern to follow exactly)
    - internal/profiles/handler.go lines 125-145, 575-650 and 830-870 (`AISettingsResponse`, `GetAISettings`, `PutAISettings`, `validateAISettings`)
    - internal/profiles/handler_test.go lines 1-130 and `TestAISettingsRoundTrip` in full (the `fakeProfileStore` whose `UpdateProfile` must learn the new field, and the round-trip test to extend)
    - .planning/phases/03.1-prompt-injection-review/03.1-CONTEXT.md D-02
  </read_first>
  <action>
    In `internal/store/profile.go`, add `NeverIssueList string` with JSON tag `never_issue_list` to `Profile` as a third sibling immediately after `ApproachGuidance`, and `NeverIssueList *string` with tag `never_issue_list,omitempty` to `ProfileUpdate` in the same relative position. Add `never_issue_list` to the SELECT column list and `&profile.NeverIssueList` to the `Scan` argument list in **both** `GetProfile` and `GetProfileByConnection`, positioned immediately after `approach_guidance` / `&profile.ApproachGuidance` so the column order and the scan order stay aligned. In `UpdateProfile`, add the fourth sibling in each of its four places: the local `var neverIssueList string` declaration, the nil-coalesce block that falls back to `existing.NeverIssueList`, the `UPDATE profiles SET ... never_issue_list = $N` clause with the placeholder numbers renumbered so `profileID` and `userID` remain the last two arguments, and the write-back block that copies the new value onto `existing`.

    In `internal/profiles/handler.go`, add `NeverIssueList string` with JSON tag `never_issue_list` to `AISettingsResponse` after `ApproachGuidance`, add the field to both literal constructions in `GetAISettings` and to both in `PutAISettings` (the `store.ProfileUpdate` it builds and the `AISettingsResponse` it returns), and add a length check to `validateAISettings` as a sibling of the two existing ones: over 20,000 characters returns a `ValidationError` reading `Never-issue list must be 20000 characters or less`. Keep the cap as the same literal the two sibling checks use — no new constant, no new number (03.1-UI-SPEC.md's locked reuse decision). Extend `AISettingsResponse`'s doc comment to say the field is an owner-written list, one entry per line, enforced mechanically in Go before dispatch, and empty by default on every profile.

    In `internal/profiles/handler_test.go`, teach `fakeProfileStore.UpdateProfile` to apply `updates.NeverIssueList` exactly as it applies `ConductRules`. Extend `TestAISettingsRoundTrip` so the PUT body carries a two-line `never_issue_list` and the GET assertion requires it back character for character, and add a first assertion that a freshly built test profile answers GET with an empty `never_issue_list` (the empty-by-default guarantee, D-02). Add `func TestAISettingsValidation(t *testing.T)` if no equivalent exists — or extend the existing validation test — with a case PUTting a `never_issue_list` of 20,001 characters and asserting a 400 with the exact message above and that `fakeProfileStore.lastUpdate` was never set.

    Change nothing about the policy gate, the engage gate, `ResolveAISettings` or any other profile field.
  </action>
  <verify>
    <automated>go build ./... && go vet ./internal/store/... ./internal/profiles/... && go test ./internal/profiles/... -run "TestAISettings" -v</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/profiles/... -run "TestAISettings" -v` exits 0 with PASS lines for `TestAISettingsRoundTrip` and the validation test
    - `TestAISettingsRoundTrip` asserts an empty `never_issue_list` before the PUT and the exact two-line value after it
    - Source assertion: `grep -c "never_issue_list" internal/store/profile.go` returns at least 5 (two SELECT lists, the UPDATE clause, and the two struct tags)
    - Source assertion: `grep -c "NeverIssueList" internal/store/profile.go` returns at least 7 (two struct fields, two Scan targets, the declaration, the coalesce, the write-back)
    - Source assertion: `grep -c "Never-issue list must be 20000 characters or less" internal/profiles/handler.go` returns 1
    - Source assertion: `sed -n '/SET keybindings/,/RETURNING updated_at/p' internal/store/profile.go` shows `never_issue_list = $` and the WHERE clause's placeholders renumbered consistently
    - `go build ./...` and `go vet ./internal/store/... ./internal/profiles/...` exit 0; `git diff --stat go.mod` is empty (T-3.1-SC)
  </acceptance_criteria>
  <done>A Never-issue list saved through the AI settings endpoint comes back from it unchanged, an over-long one is refused, and a new profile's is empty.</done>
</task>

<task type="auto">
  <name>Task 03.1-02-03: The owner writes the list in the AI Player settings section</name>
  <files>frontend/src/types/index.ts, frontend/src/components/AIPlayerPanel.tsx</files>
  <read_first>
    - .planning/phases/03.1-prompt-injection-review/03.1-UI-SPEC.md §Layout & Component Reuse item 1 (the locked markup, the locked hint copy, the 120px height and the field's position) and §Copywriting Contract (the label and hint, character for character)
    - frontend/src/components/AIPlayerPanel.tsx lines 18-95 and 200-235 (the state block, `applyAISettings`, `handleSave`'s body, and the Conduct Rules / Approach Guidance `.form-group` blocks this field mirrors)
    - frontend/src/types/index.ts lines 294-313 (`AISettingsResponse`)
    - .planning/phases/03.1-prompt-injection-review/03.1-CONTEXT.md D-02
  </read_first>
  <action>
    In `frontend/src/types/index.ts`, add `never_issue_list: string` to the `AISettingsResponse` interface as a third sibling after `approach_guidance`. Change nothing else in this file — the `AIDecisionPayload` outcome union belongs to plan 03.1-05.

    In `frontend/src/components/AIPlayerPanel.tsx`: add `const [neverIssueList, setNeverIssueList] = useState('')` immediately after the `approachGuidance` state; set it from `data.never_issue_list` in `applyAISettings`; include `never_issue_list: neverIssueList` in `handleSave`'s request body. Insert one new `.form-group` between the Approach Guidance group and the Model Name group, using the existing `.form-group` / `.form-label` / `.form-input form-textarea` / `.form-hint` classes and `style={{ minHeight: '120px' }}` — the same literal its two siblings use — with the label `Never-Issue List` and the hint copy from this plan's context block, character for character, with the quotes and apostrophe escaped as JSX entities exactly as 03.1-UI-SPEC.md writes them.

    Add no new CSS class, no new colour, no separate save button, no separate loading state and no character counter: the field saves with the existing shared Save AI Settings button and shares its `Saving…` state and its success and error messages. The whole settings body still renders only after the policy is accepted — do not touch that condition (the Regression Invariant).
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npm run build` exits 0 with no TypeScript error
    - Source assertion: `grep -c "never_issue_list" frontend/src/types/index.ts` returns 1 and `grep -c "never_issue_list" frontend/src/components/AIPlayerPanel.tsx` returns at least 2 (the apply and the save body)
    - Source assertion: `grep -c "Never-Issue List" frontend/src/components/AIPlayerPanel.tsx` returns 1, and the hint text matches 03.1-UI-SPEC.md's locked sentence word for word
    - Source assertion: the new `.form-group` appears after the Approach Guidance group and before the Model Name group in file order
    - Source assertion: `git diff frontend/src/index.css` is empty for this task — no new CSS is introduced here
    - Deferred proof: `evidence/07-never-issue-settings.png` shows two saved entries still present after a page reload (plan 03.1-07)
  </acceptance_criteria>
  <done>The owner can write forbidden commands into the AI Player settings section and save them with everything else on the profile.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → `PUT /api/v1/profiles/{connection_id}/ai-settings` | Owner-supplied free text crosses into a stored profile field that is later fed to two models and to a matcher |
| Profile row → the driver's decision path | The stored list becomes a security control read on every decision (plan 03.1-03) |
| Migration → the live database | A failed migration is a `log.Fatalf` at startup and takes staging down |

## STRIDE Threat Register

> Threat ids are phase-wide: `T-3.1-NN` names one threat across Phase 3.1, as `T-3-NN` did across the Phase 3 plans. A row that also appears in another plan is the same threat carried forward to this plan's own mitigation or disposition, not a new threat and not a collision. The phase-level register is the union of these tables; the evidence plan's security agenda reads them by id.

| Threat ID | Category | Severity | Component | Disposition | Mitigation |
|-----------|----------|----------|-----------|-------------|------------|
| T-3.1-03 | Denial of Service | Medium | an unbounded Never-issue list inflating every prompt and every match loop | mitigate | 03.1-02-02's 20,000-character cap in `validateAISettings`, asserted by a 20,001-character test case; the matcher itself is non-regex by construction (plan 03.1-03) |
| T-3.1-08 | Tampering (data integrity) | Medium | the `ai_decisions.outcome` CHECK left in place under an unexpected name, rejecting every blocked row at runtime | mitigate | 03.1-02-01 drops every outcome CHECK found in `pg_constraint` by its real name rather than a guessed one, then re-adds an explicitly named constraint |
| T-3.1-09 | Information Disclosure (IDOR) | Low | the AI settings endpoints now carrying one more owner field | accept — unchanged | `GetAISettings`/`PutAISettings` still resolve ownership through `GetProfileByConnection(userID, connectionID)` behind the existing session middleware; this plan adds a field to an already-authorised response and changes no authorisation path. Plan 03.1-06's harness re-proves the not-owned case |
| T-3.1-20 | Tampering | Low | a starter Never-issue set shipping as a default and becoming engine-held game knowledge | mitigate | The column defaults to the empty string and no seed data is written; the round-trip test asserts a new profile answers with an empty list (D-02, CON-engine-has-no-game-knowledge) |
| T-3.1-SC | Tampering (supply chain) | Low | `go.mod`, `frontend/package.json` | accept | This plan installs no package; both must be unchanged at plan end |
</threat_model>

<verification>
1. `go build ./...` exits 0; `go vet ./internal/store/... ./internal/profiles/...` is clean.
2. `go test ./internal/profiles/... -run "TestAISettings" -v` exits 0 with the round-trip and validation PASS lines.
3. `go test ./...` shows no new failures relative to the pre-plan baseline.
4. `cd frontend && npm run build` exits 0.
5. Both migration `012` files exist, the up file adds the column and the four-value CHECK, the down file restores the three-value CHECK.
6. `git diff --stat go.mod frontend/package.json` is empty (T-3.1-SC).
</verification>

<success_criteria>
- SC-3's storage half holds: the profile carries an owner-editable Never-issue list, empty by default, stored and returned with the other AI fields, proven by the `TestAISettingsRoundTrip` PASS line in `evidence/02-test-report.txt` and by `evidence/07-never-issue-settings.png` showing entries surviving a reload (D-02).
- An over-long list is refused rather than stored, proven by the validation PASS line in `evidence/02-test-report.txt` (T-3.1-03).
- The schema is ready for the blocked outcome, proven by the `version=12` migration line in `evidence/06-staging-ai-player.log` (D-08).
</success_criteria>

<output>
Create `.planning/phases/03.1-prompt-injection-review/03.1-02-SUMMARY.md` when done, recording the migration file names, the chosen character cap and why it matches its siblings, the exact JSON key, and the fact that the store round trip has no Go unit test in this repository and is proven at the handler layer, over HTTP by plan 03.1-06's harness, and on screen by plan 03.1-07.
</output>
