---
phase: 05-coaching-channel
plan: 10
type: execute
wave: 5
depends_on: [05-01, 05-04, 05-06]
files_modified:
  - scripts/verify-phase5.sh
autonomous: true
requirements: [REQ-coaching-chat, REQ-pause-resume]

must_haves:
  truths:
    - "A single command run against staging produces a report file that says, criterion by criterion, whether the HTTP-reachable half of Phase 5 holds — the pause and resume actions and their waiting reasons, the coaching list and the conversation reading back, the new speed-limit setting round-tripping with blank meaning the server default, and another user's connection being refused — with the request path and response body printed under every step (CLAUDE.md evidence rule), proven by evidence/03-canned-report.txt"
    - "The report can fail: a negative self-test makes the harness produce a FAIL line and a non-zero exit, so a passing run means something (T-5-45), proven by evidence/02-harness-selftest.txt"
    - "The harness never proves anything with a database query and never prints a key, a cookie or a Railway variable value (CLAUDE.md evidence rule), proven by the greps in this plan's acceptance criteria"
    - "The harness leaves the profile as it found it: anything it changes to make an assertion, it puts back, including when it is interrupted"
  artifacts:
    - path: "scripts/verify-phase5.sh"
      provides: "The Phase 5 canned-report harness, with a clean self-test, a negative self-test and a live mode"
      contains: "PASS C1"
  key_links:
    - from: "scripts/verify-phase5.sh"
      to: "POST /api/v1/session/autopilot"
      via: "the pause and resume actions, asserting the state and the two waiting reasons in the response"
      pattern: "\"action\":\"pause\""
    - from: "scripts/verify-phase5.sh"
      to: "GET /api/v1/profiles/{connection_id}/ai-coaching and ai-conversation"
      via: "read-backs asserted against what the run itself put there"
      pattern: "ai-coaching"
---

<objective>
Make the reachable half of this phase provable by one command. The owner's evidence rule allows exactly three kinds of proof, and a canned report is one of them; every phase since Phase 3.1 has had a harness that produces it. Phase 5 adds four things a script can reach over HTTP without a model call: the pause and resume actions with their waiting reasons, the coaching list, the conversation, and the new speed-limit setting with its blank-means-default behaviour.

Purpose: criteria 1 and 2 cannot be fully shown without the live model and the browser, and this script does not pretend otherwise — the parts it cannot reach print a SKIP line naming the Go test and the screenshot that prove them instead, exactly as Phase 4's harness does. What it can reach, it proves with the raw request and response printed underneath.
Output: `scripts/verify-phase5.sh`, with a clean self-test, a negative self-test and a live mode, ready for plan 05-11 to run against staging twice.

This plan writes the harness. It does not deploy, does not run against staging and makes no model call; plan 05-11 does all three.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/ROADMAP.md
@.planning/phases/05-coaching-channel/05-CONTEXT.md
@.planning/phases/05-coaching-channel/05-VALIDATION.md
@.planning/phases/05-coaching-channel/05-01-SUMMARY.md
@.planning/phases/05-coaching-channel/05-04-SUMMARY.md
@.planning/phases/05-coaching-channel/05-06-SUMMARY.md

Criterion labels for this phase's harness, fixed here so plan 05-11's report can be read against them:
- **C1** Pause and resume over the switch endpoint: pausing an engaged switch reports WAITING with `paused_by_owner` true, resuming reports ON with both reasons false, and pausing an already-off switch is refused harmlessly. Reachable.
- **C2** The coaching list and the conversation read back what the session holds, and both are GET-only (a PUT, POST or DELETE is refused). Reachable.
- **C3** The AI command rate limit round-trips as a profile setting: a set value returns, a blank value returns blank, and an out-of-range value is rejected with the bounds message. Reachable.
- **C4** Another user's connection id is refused on every one of the new reads. Reachable.
- **C5** A coaching message reaching the next decision's reasoning, and the reviewer, prompt and limiter behaviour. NOT reachable — this script prints a SKIP naming the Go tests and the screenshots that prove them instead.

Machine rules for the executor (inherit these, they are not optional):
- Never run `find /`, `find ~`, `find /c`, `find /d`, `ls -R` or any search outside `D:\Projects\MUDPuppy`. Never launch a command in the background. Use Glob/Grep scoped to the repo and exclude `node_modules`.
- The GSD CLI form on this machine is `gsd-sdk <command> <sub>`; `gsd-sdk query ...` fails.
- Use the Write tool for the script; bash heredocs with backticks or quotes fail in this shell.
- Deploy to staging only with `railway up`. **No deploy and no live run happens in this plan.**
- Never sign the owner out of any session.
- No package may be added to `go.mod` or `frontend/package.json`. The harness uses `bash` and `curl` only — no `jq`, no Python, no Node.
- Make no live model call in this plan.
- `.specify/` specs other than the four already named are excluded.

Claude's Discretion settled by the planner for this plan:
- The harness is a copy of `scripts/verify-phase4.sh`'s own skeleton pointed at this phase's endpoints: same argument parsing, same fixture-or-curl request helper, same flat-JSON field extractor, same PASS/FAIL/SKIP-per-criterion report, same restore-what-it-changed discipline and the same two self-test modes.
- **This harness is not destructive.** Phase 4's ran a real delete; Phase 5's has no equivalent and must not acquire one. It changes exactly one thing — the AI settings' rate-limit field — and puts it back, including on an interrupt.
- The harness makes no model call and therefore cannot push coaching itself. C2's read-back asserts on whatever the session already holds, and its own write-side proof is the SKIP in C5 plus plan 05-11's screenshots.
</context>

<tasks>

<task type="auto">
  <name>Task 05-10-01: One command produces a report that says whether this phase's reachable half holds, and can be made to fail on purpose</name>
  <files>scripts/verify-phase5.sh</files>
  <read_first>
    - scripts/verify-phase4.sh in full — the argument parsing and usage block, the fixture-or-curl request helper, the flat-JSON field extractor, the per-step `### ` heading with request path and response body, the PASS/FAIL/SKIP-per-criterion summary, the restore-on-exit trap, and the two self-test modes with their exact invocations
    - scripts/verify-phase3-1.sh (the skeleton Phase 4's own harness copied, for anything Phase 4's version changed)
    - scripts/fixtures/ (the canned response files the self-test modes read, and their naming convention)
    - internal/session/handler.go as left by plan 05-01 (`AutopilotResponse`'s field names including `paused_by_owner` and `connection_lost`, and the outcome strings the four actions return)
    - internal/profiles/handler.go as left by plans 05-04 and 05-06 (`GetCoaching`, `GetConversation`, their response field names, `PutAISettings`'s body shape with `rate_limit_per_second`, and the exact bounds-rejection message)
    - .planning/phases/04-continuous-play/04-10-SUMMARY.md (the live invocation line and environment variables Phase 4's harness took, which this one mirrors)
    - CLAUDE.md's evidence rule (a database query is never evidence; this script runs no SQL)
  </read_first>
  <action>
    Create `scripts/verify-phase5.sh`, executable, copying `scripts/verify-phase4.sh`'s skeleton in full and pointing it at this phase's endpoints. Open with the same style of header comment naming the phase, listing the criterion labels C1 to C5 from this plan's context table verbatim, stating that this harness **is** the canned report the owner's evidence rule requires, that it runs no SQL anywhere, and — in contrast to Phase 4's — that a live run is **not** destructive: it changes only the rate-limit setting and puts it back.

    Take the same arguments and environment variables Phase 4's harness takes (base URL, session cookie, connection id, a not-owned connection id, report path), and the same two self-test modes.

    Steps, each printing its request path and full response body under a `### ` heading, and each contributing to exactly one criterion:

    C1 — pause and resume:
    1. Read the switch's current state and record it, so the run can restore it.
    2. If the switch is not On, say so and mark C1 as SKIPPED-WITH-REASON rather than engaging the AI from a script — the harness never turns autopilot on.
    3. `POST /api/v1/session/autopilot` with action `pause`; assert the response `state` is `waiting`, `outcome` is `paused` and `paused_by_owner` is true.
    4. `POST` the same endpoint with action `pause` again; assert it is refused harmlessly with the already-waiting outcome and changes nothing.
    5. `POST` with action `resume`; assert `state` is `on`, `outcome` is `resumed` and both `paused_by_owner` and `connection_lost` are false.

    C2 — the two reads:
    6. `GET /api/v1/profiles/{connection_id}/ai-coaching`; assert the body carries a `coaching` array (empty is a pass) and never `null`.
    7. `GET /api/v1/profiles/{connection_id}/ai-conversation`; assert the body carries a `lines` array and never `null`.
    8. `PUT` and `DELETE` both of those paths; assert each is refused with a method-not-allowed status — they are read-only by design.

    C3 — the speed-limit setting:
    9. `GET .../ai-settings` and record the current `ai_settings` object verbatim, and install an exit trap that PUTs it back on any exit path, including an interrupt.
    10. `PUT .../ai-settings` with `rate_limit_per_second` set to a value inside the bounds; `GET` it back and assert the same number returns.
    11. `PUT` with `rate_limit_per_second` null; `GET` it back and assert it returns null — blank round-trips as blank and is resolved to the server's default in Go, not in the browser.
    12. `PUT` with a value outside the bounds; assert it is rejected and the error body carries the bounds message.
    13. Restore the recorded settings object and assert the `GET` matches what was recorded at step 9.

    C4 — ownership:
    14. `GET` the two new reads against the not-owned connection id; assert both are refused.

    C5 — not reachable: print exactly a `SKIP C5:` line naming the Go tests that prove the unreachable half (`go test ./internal/driver/... -run "TestHandleChat_|TestCoaching" -count=1 -v` and `go test ./internal/session/... -run "TestManager_Pause|TestManager_Resume" -count=1 -v`) and the screenshots `evidence/06-coaching-in-next-decision.png` and `evidence/18-paused-waiting-reason.png`.

    End with a per-criterion summary block of `PASS C1` through `PASS C4` and `SKIP C5` lines, and exit non-zero if any criterion failed.

    Never print the session cookie, any header value, any key or any Railway variable value; redact the cookie in every echoed request. Run no SQL and open no database connection. Do not turn autopilot on, do not send a game command, do not send a chat message and do not make a model call.

    Provide the two self-test modes exactly as Phase 4's harness does: a clean self-test that runs every step against the fixtures and produces a report with zero `FAIL` lines and exit 0, and a negative self-test that feeds one deliberately wrong fixture and produces a report containing `FAIL C3` with a non-zero exit. Add the fixture files under `scripts/fixtures/` following the existing naming convention.
  </action>
  <verify>
    <automated>bash -n scripts/verify-phase5.sh && bash scripts/verify-phase5.sh --self-test && bash scripts/verify-phase5.sh --self-test-negative; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n scripts/verify-phase5.sh` exits 0 (the script parses) and the file is executable
    - The clean self-test exits 0 and its report contains zero lines beginning with `FAIL` and one each of `PASS C1`, `PASS C2`, `PASS C3`, `PASS C4` and `SKIP C5`
    - The negative self-test exits non-zero and its report contains `FAIL C3`
    - Source assertion: `grep -ciE "psql|SELECT |INSERT |pg_" scripts/verify-phase5.sh` returns 0 (no SQL anywhere)
    - Source assertion: `grep -ciE "jq |python|node " scripts/verify-phase5.sh` returns 0 (bash and curl only)
    - Source assertion: `grep -c "action\":\"on\"\|action=on" scripts/verify-phase5.sh` returns 0 (the harness never engages autopilot)
    - Source assertion: `grep -c "captured-text" scripts/verify-phase5.sh` returns 0 (this harness is not destructive and borrows no destructive step from Phase 4's)
    - Source assertion: `grep -c "trap " scripts/verify-phase5.sh` returns at least 1 (the settings restore survives an interrupt)
    - Source assertion: `grep -ciE "AIza|x-goog-api-key|Set-Cookie:|ENCRYPTION_KEY" scripts/verify-phase5.sh` returns 0
    - The usage block names the exact live invocation plan 05-11 will run, with placeholders for the base URL, cookie and both connection ids
  </acceptance_criteria>
  <done>There is one command that turns the reachable half of this phase into a file a reader can open, and it has already been shown to fail when it should.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Harness → the live staging profile | A script that changes a real profile's settings and switch state |
| Report file → readers | Whatever the report prints is committed to the repository |
| Harness → the autopilot switch | A script that could start the AI playing unattended |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation |
|-----------|----------|----------|-----------|-------------|------------|
| T-5-45 | Repudiation (unfalsifiable evidence) | High | a canned report that always passes, making the phase look proven | mitigate | The negative self-test must produce `FAIL C3` with a non-zero exit, and plan 05-11 files both self-tests before the live report is trusted |
| T-5-46 | Information Disclosure | High | a session cookie, a key or a Railway variable value reaching the committed report | mitigate | The cookie is redacted in every echoed request; the acceptance criteria grep the script, and plan 05-11 greps the filed report |
| T-5-47 | Tampering (unattended operation) | High | a script engaging autopilot and leaving the AI playing with nobody watching, against the policy's supervision rule | mitigate | The harness never sends the `on` action; if the switch is not already On it skips C1 with a stated reason |
| T-5-48 | Tampering (profile left changed) | Medium | the rate-limit setting left at the harness's value after an interrupted run | mitigate | The original settings object is recorded before the first write and restored by an exit trap on every path |
| T-5-49 | Denial of Service (cost) | Low | a harness step that spends a model call | mitigate | No step sends a chat message or engages the loop; the harness makes no model call at all |
| T-5-SC | Tampering (supply chain) | Low | `go.mod`, `frontend/package.json` | accept | The harness uses only bash and curl |
</threat_model>

<verification>
1. `bash -n scripts/verify-phase5.sh` exits 0 and the file is executable.
2. The clean self-test exits 0 with zero `FAIL` lines and the five criterion lines present.
3. The negative self-test exits non-zero and contains `FAIL C3`.
4. `grep -ciE "psql|SELECT |INSERT " scripts/verify-phase5.sh` returns 0.
5. The script never sends the `on` autopilot action and never calls a delete endpoint.
6. `git diff --stat go.mod frontend/package.json` is empty.
</verification>

<success_criteria>
- The HTTP-reachable half of Phase 5 — pause and resume with their reasons, the coaching and conversation reads, the new speed-limit setting and the ownership refusals — can be turned into a canned report by one command, with every request and response printed (CLAUDE.md evidence rule).
- The report is falsifiable before it is trusted: the negative self-test produces a FAIL line and a non-zero exit (T-5-45).
- Nothing in the harness proves anything with a database query, engages autopilot, spends a model call or leaves the profile changed.
</success_criteria>

<output>
Create `.planning/phases/05-coaching-channel/05-10-SUMMARY.md` when done, recording the exact live invocation line and every environment variable it takes, the criterion labels as shipped, the fixture files added, what the negative self-test breaks on purpose, and a plain statement that the harness is not destructive, runs no SQL, never engages autopilot and makes no model call.
</output>
