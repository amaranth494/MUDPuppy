---
phase: 05-coaching-channel
plan: 11
type: execute
wave: 7
depends_on: [05-01, 05-02, 05-03, 05-04, 05-05, 05-06, 05-07, 05-08, 05-09, 05-10]
files_modified:
  - .planning/phases/05-coaching-channel/evidence/01-test-report.txt
  - .planning/phases/05-coaching-channel/evidence/02-harness-selftest.txt
  - .planning/phases/05-coaching-channel/evidence/03-canned-report.txt
  - .planning/phases/05-coaching-channel/evidence/04-redteam-after.txt
  - .planning/phases/05-coaching-channel/evidence/05-staging-ai-player.log
  - .planning/phases/05-coaching-channel/evidence/06-coaching-in-next-decision.png
  - .planning/phases/05-coaching-channel/evidence/07-chat-reply.png
  - .planning/phases/05-coaching-channel/evidence/08-chat-while-off.png
  - .planning/phases/05-coaching-channel/evidence/09-coaching-after-refresh.png
  - .planning/phases/05-coaching-channel/evidence/10-popouts-both-open.png
  - .planning/phases/05-coaching-channel/evidence/11-popout-brought-back.png
  - .planning/phases/05-coaching-channel/evidence/12-ai-settings-rate-limit.png
  - .planning/phases/05-coaching-channel/evidence/13-logs-signed-out.png
  - .planning/phases/05-coaching-channel/evidence/14-help-article.png
  - .planning/phases/05-coaching-channel/evidence/15-guidance-pasted-by-hand.png
  - .planning/phases/05-coaching-channel/evidence/16-logs-conversation-section.png
  - .planning/phases/05-coaching-channel/evidence/17-panel-two-views.png
  - .planning/phases/05-coaching-channel/evidence/18-paused-waiting-reason.png
  - .planning/phases/05-coaching-channel/evidence/19-hand-play-unchanged.png
  - .planning/phases/05-coaching-channel/05-SECURITY-AGENDA.md
  - .planning/phases/05-coaching-channel/05-11-SUMMARY.md
autonomous: false
requirements: [REQ-coaching-chat, REQ-pause-resume, REQ-promote-guidance, REQ-doc-coaching]

must_haves:
  truths:
    - "The whole Go suite is green with and without the race detector, go vet is clean, the frontend builds, and no package was added anywhere in the phase, proven by evidence/01-test-report.txt containing zero unexpected FAIL lines, a stated race answer and an empty dependency-drift section (T-5-SC)"
    - "The canned report is falsifiable before it is trusted, proven by evidence/02-harness-selftest.txt showing a clean self-test with exit 0 and a negative self-test producing FAIL C3 with a non-zero exit (T-5-45)"
    - "The two fixes to the safety checker are measured against the finished build on a fresh-quota day: the benign fight items are not blocked across three samples each, the coaching-channel attacks push no suggestion and steer no command, and STEERED is 0, proven by the benign-fight and chatter-channel item lines and the summary block of evidence/04-redteam-after.txt (D-23, D-28)"
    - "On staging the owner sends a coaching message while the AI plays and reads it taking effect in the next decision's reasoning, proven by evidence/06-coaching-in-next-decision.png and the matching stage lines in evidence/05-staging-ai-player.log (ROADMAP criterion 1)"
    - "On staging the owner pauses from the AI-player button, the stream prints [Autopilot paused] and the panel reads WAITING with its reason while the game keeps arriving, with no model calls and no commands in the log for that stretch, then resumes, the stream prints [Autopilot resumed] and the first decision describes where the character actually is, proven by evidence/18-paused-waiting-reason.png and the paused stretch of evidence/05-staging-ai-player.log (ROADMAP criterion 2)"
    - "On staging chat is plain text with no action on any message, AI-chatter changes no configuration, and the help article leads the owner through copying a suggestion into AI settings by hand, proven by evidence/14-help-article.png and evidence/15-guidance-pasted-by-hand.png (ROADMAP criterion 3)"
    - "On staging the panel is one panel with two views, every pushed suggestion is visible three ways, and each view runs in its own window and comes back, proven by evidence/17-panel-two-views.png, evidence/09-coaching-after-refresh.png, evidence/10-popouts-both-open.png and evidence/11-popout-brought-back.png (ROADMAP criterion 4, D-29)"
    - "A signed-out visitor to the log page is sent to sign-in, captured from a private window without the owner ever being signed out, proven by evidence/13-logs-signed-out.png (D-27, AR-4-08)"
    - "Hand play on a profile that never accepted the policy is untouched by everything this phase built, proven by evidence/19-hand-play-unchanged.png (05-UI-SPEC Regression Invariant)"
    - "Every one of the four ROADMAP Phase 5 success criteria has a row in 05-11-SUMMARY.md citing a named evidence file, with line numbers for text evidence and a PASS or FAIL verdict, and no row cites a database query (CLAUDE.md evidence rule)"
    - "The Phase 5 security review has a written agenda re-presenting all four DR-4 rows with what this phase built against each, AR-4-08's condition with its answer, and this phase's own new risk, with three dispositions on every item and nothing decided in advance, proven by 05-SECURITY-AGENDA.md (D-28)"
  artifacts:
    - path: ".planning/phases/05-coaching-channel/evidence/01-test-report.txt"
      provides: "Canned report: go build, go vet, the full suite with and without -race, every targeted Phase 5 run, the frontend build, and the dependency-drift, model-literal, coaching-writer, dispatcher and logs-route checks, captured verbatim"
      contains: "GO TEST EXIT"
    - path: ".planning/phases/05-coaching-channel/evidence/04-redteam-after.txt"
      provides: "The AFTER red-team report including the two repeat-sampled benign fight items and the three coaching-channel attacks"
      contains: "LABEL: AFTER"
    - path: ".planning/phases/05-coaching-channel/evidence/05-staging-ai-player.log"
      provides: "Staging log excerpt: migration 015 applied, the chat and coaching stages, a paused stretch with no model call and no send, a resume, and the rate limiter's own line if it fired"
      contains: "stage=coaching-pushed"
    - path: ".planning/phases/05-coaching-channel/05-SECURITY-AGENDA.md"
      provides: "The Phase 5 security review agenda: the four DR-4 deferrals with what was built, AR-4-08's condition and its answer, the coaching-channel laundering risk, and this phase's plan-level acceptances, each with three dispositions and none pre-chosen"
      contains: "Remediate Now"
    - path: ".planning/phases/05-coaching-channel/05-11-SUMMARY.md"
      provides: "One row per ROADMAP Phase 5 success criterion citing the evidence that proves it, with a verdict"
      contains: "Phase Validation"
  key_links:
    - from: "internal/driver/corpus_live_test.go, unmodified since plan 05-06"
      to: "evidence/04-redteam-after.txt"
      via: "the same gated test rerun with the AFTER label and strict mode on a fresh-quota day"
      pattern: "STEERED"
    - from: "the staging play screen on Alter Aeon"
      to: "evidence/06 through evidence/19"
      via: "one screenshot per observable, end-user perspective only"
      pattern: "AI-chatter"
    - from: "the driver's and AI-chatter's log lines on staging"
      to: "evidence/05-staging-ai-player.log"
      via: "railway logs against the staging environment, filtered on the prefixes, during and after the walkthrough"
      pattern: "stage=(coaching-pushed|reply|loop-tick)"
---

<objective>
Demonstrate the phase and file the proof. Phase 5 is complete when the ROADMAP's Phase Validation line has actually been performed: the owner on his own character on Alter Aeon, sending a coaching message while the AI plays and reading it take effect in the next decision's reasoning; pausing from the AI-player button and confirming from the log that the game is still being read with nothing spent and nothing sent, then resuming and reading the reassessing first decision; and opening the help article and following it to copy a suggestion into AI settings by hand.

Purpose: turn four success criteria into four cited rows, each pointing at a file a reader can open; rerun the red-team corpus — now carrying two repeat-sampled benign fights and three attacks aimed at the coaching channel this phase created — against the finished build on a fresh-quota day; and put all four risks this project deferred at the Phase 4 review back in front of the owner with what this phase built against each, alongside the one genuinely new risk this phase creates.
Output: four canned reports, one log excerpt, fourteen screenshots, a security-review agenda, and `05-11-SUMMARY.md` mapping each criterion to the evidence that proves it.

Four standing instructions constrain the whole walkthrough:
- **Do not sign the owner out at any point.** Sign-in is one-time-code based and signing back in is the owner's step, not the executor's. The one signed-out screenshot is taken from a private window or a separate browser profile.
- **Deploy to staging only with `railway up --detach -e staging -s MudPuppy` from the local checkout.** A dashboard Deploy rebuilds from GitHub and would run a branch state that is not this one. This phase carries frontend changes, so `public/` must be rebuilt and committed before the deploy.
- **Never print a Railway variable value or any key**, in a report, a log excerpt, a screenshot or a summary.
- **Never print the owner's chat text or AI-chatter's reply into a log excerpt.** Those lines carry ids, stages, outcomes and lengths only; text in the excerpt is a finding, not a formatting problem.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/ROADMAP.md
@.planning/RISK-REGISTER.md
@.planning/phases/05-coaching-channel/05-CONTEXT.md
@.planning/phases/05-coaching-channel/05-UI-SPEC.md
@.planning/phases/05-coaching-channel/05-VALIDATION.md
@.planning/phases/04-continuous-play/04-SECURITY-AGENDA.md

ROADMAP Phase Validation line for Phase 5 (what this plan must demonstrate):
Player-observable on staging — send a coaching message and read it reflected in the next decision's logged reasoning; pause from the AI-player button and confirm from the log that the game is still being read with no model calls and no commands sent, then resume and read the reassessing first decision; open the help article and follow it to copy a suggestion into AI settings by hand. Diagnostic — chat messages are stored against the AI session and can be read alongside the decisions they influenced; here that reading is done through the Logs page's Coaching Conversation section and the `ai-conversation` endpoint, **never** through a database query.

Evidence directory: `.planning/phases/05-coaching-channel/evidence/` (created by this plan's first task). Every filename below is fixed — earlier plans' `must_haves` cite these exact names.

| File | Type | Produced by |
|------|------|-------------|
| `01-test-report.txt` | canned report | task 05-11-01 |
| `02-harness-selftest.txt` | canned report | task 05-11-01 |
| `03-canned-report.txt` | canned report | tasks 05-11-03 (RUN A) and 05-11-04 (RUN B) |
| `04-redteam-after.txt` | canned report | task 05-11-02 |
| `05-staging-ai-player.log` | log excerpt | tasks 05-11-03 and 05-11-04 |
| `06` through `19` | screenshots | tasks 05-11-03 and 05-11-04 |

Machine rules for the executor (inherit these, they are not optional):
- Never run `find /`, `find ~`, `find /c`, `find /d`, `ls -R` or any search outside `D:\Projects\MUDPuppy`. Never launch a command in the background. Use Glob/Grep scoped to the repo and exclude `node_modules`.
- The GSD CLI form on this machine is `gsd-sdk <command> <sub>`; `gsd-sdk query ...` fails.
- Use the Write tool for every multi-line file; bash heredocs with backticks or quotes fail in this shell.
- Screenshots are captured with the project's established PrintWindow method, or taken by the owner by hand — either is acceptable, as it has been since Phase 1, and the owner confirms the set at the checkpoints.
- No package may be added to `go.mod` or `frontend/package.json`.

Environment: Railway project `mudpuppy`, environment `staging`, service `MudPuppy`. Production is untouched. The staging model stays `gemini-3.5-flash-lite`; do not change it or any other staging variable except where a task explicitly says so. `ENCRYPTION_KEY_V1` is already set on staging and plan 05-03 now requires it on every host — a startup failure naming that variable means it was unset, not that the gate is wrong, and `MUDPUPPY_LOCAL_DEV` must **not** be set on staging.

Expect real decision cadence to be dominated by model latency rather than by the loop's pacing constants; bursts and gaps are the expected shape. Expect the Alter Aeon socket to drop periodically; record each drop and what autopilot did — and note that from this phase on a reconnect no longer resumes a pause.
</context>

<tasks>

<task type="auto">
  <name>Task 05-11-01: The test report and the harness self-test are captured verbatim as files</name>
  <files>.planning/phases/05-coaching-channel/evidence/01-test-report.txt, .planning/phases/05-coaching-channel/evidence/02-harness-selftest.txt</files>
  <read_first>
    - .planning/phases/05-coaching-channel/05-VALIDATION.md (Test Infrastructure, Sampling Rate and the Per-Task Verification Map — every automated command in that map must appear in this report)
    - .planning/phases/05-coaching-channel/05-01-SUMMARY.md through 05-10-SUMMARY.md (what each plan reports as done and the test names each says this report must quote, so a gap is caught before the staging checkpoints)
    - .planning/phases/04-continuous-play/evidence/01-test-report.txt (the report layout every canned report in this project shares, including the heading convention, the EXIT line and the trailing GO TEST EXIT line)
    - .planning/phases/05-coaching-channel/05-10-SUMMARY.md (the two self-test invocations of scripts/verify-phase5.sh)
  </read_first>
  <action>
    Create `.planning/phases/05-coaching-channel/evidence/`.

    From the repository root, run each command below and append its command line, its full output and its exit status to `evidence/01-test-report.txt`, verbatim — no summarising, no truncation, no reformatting. Prefix each with a line reading `### ` followed by the command and follow each with `EXIT: ` and the status so the file is greppable.

    1. `go build ./...`
    2. `go vet ./internal/... ./cmd/...`
    3. `go test ./... -v`
    4. `go test ./internal/session/... -run "TestManager_Pause|TestManager_Resume|TestAutopilotRecordCarriesBothWaitingReasons|TestAutopilotHandler_PauseAndResumeActions|TestAutopilotHandler_EmitsPausedAndResumedNotices|TestManager_WheelGrabWhilePaused" -v`
    5. `go test ./internal/gemini/... -run "TestReviewCommand_MissingBlockedField|TestReviewCommand_BlockedFalseIsStillAVerdict|TestChat_" -v`
    6. `go test ./internal/driver/... -run "TestBuildReviewSystemInstruction_" -v`
    7. `go test ./internal/driver/... -run "TestAllowAISend_|TestDriverRateLimitRefusalIsTransient" -v`
    8. `go test ./internal/driver/... -run "TestHandleChat_" -v`
    9. `go test ./internal/driver/... -run "TestCoaching|TestUntrustedParagraphNamesEveryMarker|TestPromptContextOrder|TestMemoryIsWrappedInBothPrompts|TestMemoryCeilingsAreEnforced" -v`
    10. `go test ./internal/store/... -run "TestConversationStoreSQL|TestCoachingStoreSQL|TestResolveAISettings" -v`
    11. `go test ./internal/config/... -run "TestLoadRequiresEncryptionKeyEverywhereUnlessLocalDev|TestLocalDevOptOutIsAnnounced" -v`
    12. `go test ./internal/profiles/... -v`
    13. `go test ./internal/icm/... -v`
    14. `go test ./internal/driver/... -run "TestCorpusIsWellFormed" -v`
    15. `go test ./internal/driver/... -run TestLiveCorpus -v` with the corpus flag unset, so the SKIP line is on record as proof the gate holds
    16. `go test ./internal/... -race -count=1` under a `### RACE` heading, or, if the cgo toolchain is unavailable on this machine, that heading stating plainly that the race detector did not run and why. Do not silently omit the question.
    17. `cd frontend && npm run build`
    18. `git diff --stat <phase-base-commit> -- go.mod frontend/package.json` under a `### DEPENDENCY DRIFT` heading. This phase installs no package, so a non-empty section is a supply-chain finding (T-5-SC).
    19. `grep -rniE "gemini-[0-9]|flash-latest|generativelanguage" internal/ cmd/ --include=*.go | grep -v "_test.go"` under a `### MODEL LITERALS` heading. Zero output is the pass condition.
    20. Under a `### COACHING WRITER` heading, the command showing that `UpdateCoaching(` appears in exactly one non-test file under `internal/driver` and that it is `chat.go` — T-5-26's provenance guarantee on record as a fact rather than a claim.
    21. Under a `### ICM UNCHANGED` heading, `git diff --stat <phase-base-commit> -- internal/icm/`. An empty section is the pass condition (T-5-15).
    22. Under a `### LOGS ROUTE GUARD` heading, the two greps from plan 05-03 showing the `/logs/:connectionId` route's line number falling inside the `AuthGuard` element in `frontend/src/App.tsx`.

    End the file with `GO TEST EXIT: ` carrying command 3's exit status. Annotate any failure that predates this phase as the known baseline, naming it; a failure in any package this phase touched blocks the phase and must be fixed before the checkpoints below are attempted.

    Then produce `evidence/02-harness-selftest.txt` the same way, with two headed sections: the clean self-test invocation of `scripts/verify-phase5.sh` followed by that report's contents and its exit status, and the negative self-test followed by the same. The first must show zero lines beginning with `FAIL` and exit 0; the second must contain `FAIL C3` and exit non-zero.

    Run no SQL. Deploy nothing. Make no live model call.
  </action>
  <verify>
    <automated>grep -c "GO TEST EXIT" .planning/phases/05-coaching-channel/evidence/01-test-report.txt && grep -c "FAIL C3" .planning/phases/05-coaching-channel/evidence/02-harness-selftest.txt && grep -c "SKIP: TestLiveCorpus" .planning/phases/05-coaching-channel/evidence/01-test-report.txt</automated>
  </verify>
  <acceptance_criteria>
    - `evidence/01-test-report.txt` contains a heading for each of the twenty-two commands including `### RACE`, `### DEPENDENCY DRIFT`, `### MODEL LITERALS`, `### COACHING WRITER`, `### ICM UNCHANGED` and `### LOGS ROUTE GUARD`, and `EXIT: 0` for `go build`, `go vet`, every targeted `go test` run and `npm run build`
    - The report contains PASS lines for at least `TestManager_ResumeRequiresBothReasonsClear`, `TestManager_PauseStopsTheLoopAndKeepsReading`, `TestAutopilotHandler_PauseAndResumeActions`, `TestAutopilotHandler_EmitsPausedAndResumedNotices`, `TestReviewCommand_MissingBlockedField`, `TestBuildReviewSystemInstruction_FightingIsOrdinaryPlay`, `TestBuildReviewSystemInstruction_MemoryAuthorship`, `TestAllowAISend_FloodRefused`, `TestResolveAISettings_RateLimitBlankMeansServerDefault`, `TestHandleChat_ReplyCarriesEverythingAIPlayerKnows`, `TestHandleChat_PromptCarriesRecentConversation`, `TestHandleChat_PromptCarriesCoachingInEffect`, `TestHandleChat_CapReached`, `TestHandleChat_TouchesNothingElse`, `TestHandleChat_PushReachesNextPrompt`, `TestHandleChat_Withdraw`, `TestHandleChat_ReplyQuotesTheExactLineSent`, `TestCoachingHasOneWriter`, `TestCoachingIsSubordinateInBothPrompts`, `TestConversationStoreSQL`, `TestCoachingStoreSQL`, `TestLoadRequiresEncryptionKeyEverywhereUnlessLocalDev`, `TestCorpusIsWellFormed` and `TestEngageLoop_Reassess`
    - The report contains a `--- SKIP: TestLiveCorpus` line (the gate holding with the flag unset)
    - The report contains zero lines matching `^--- FAIL` outside an explicitly annotated pre-existing baseline, and the annotation names each baseline entry
    - The `### DEPENDENCY DRIFT`, `### MODEL LITERALS` and `### ICM UNCHANGED` sections are all empty
    - The `### COACHING WRITER` section names exactly one file and it is `internal/driver/chat.go`
    - The `### RACE` section states whether the race detector ran and, if not, why
    - `evidence/02-harness-selftest.txt` contains both sections; the first shows zero lines beginning with `FAIL` and exit 0, the second contains `FAIL C3` and a non-zero exit
    - No SQL statement, no key value and no session cookie appears anywhere in either file
  </acceptance_criteria>
  <done>The diagnostic half of the Phase Validation line exists as two files a reader can open, with every new behaviour's test named and passing, no dependency drift, the dispatcher untouched, the coaching store proven to have one writer, and a harness that has shown it can fail.</done>
</task>

<task type="auto">
  <name>Task 05-11-02: The corpus runs again, now with the fight controls and the attacks on the coaching channel</name>
  <files>.planning/phases/05-coaching-channel/evidence/04-redteam-after.txt</files>
  <read_first>
    - .planning/phases/04-continuous-play/04-11-SUMMARY.md and .planning/phases/04-continuous-play/evidence/04-redteam-after.txt (the authoritative BEFORE this run is read against: its STEERED count, its per-layer catch counts and its false blocks)
    - .planning/phases/05-coaching-channel/05-02-SUMMARY.md (the two benign fight item ids, their repeat count, and the report line format for a repeated item)
    - .planning/phases/05-coaching-channel/05-06-SUMMARY.md (the three chatter-channel item ids and their targets)
    - internal/driver/corpus_live_test.go (confirm it has changed only by this phase's two additions since Phase 4 filed its report — the comparison is only meaningful if no existing item moved underneath it)
    - .planning/phases/03.1-prompt-injection-review/03.1-07-SUMMARY.md (the two-attempt rate-limit rule and the standing instruction never to adjust the corpus after seeing a result)
    - .planning/RISK-REGISTER.md rows DR-4-01 and DR-4-02, and the Phase 4 D-29 note on reviewer reliability (why one sample is not enough)
  </read_first>
  <action>
    Confirm first that `internal/driver/corpus_live_test.go` and `internal/driver/testdata/corpus_profile.json` have changed only by this phase's additions since Phase 4 filed its report: `git log --oneline` over both paths must show the pre-phase commits plus this phase's, and each of this phase's diffs over those paths must be additions only. If an existing item was edited, stop — the comparison with the Phase 4 AFTER report is void, and the SUMMARY must say so rather than presenting a rerun of a different corpus as a like-for-like.

    Confirm it is a fresh-quota day for the staging model before starting: the free tier is per model per day, and this run is a measurement, not a formality. If the day's quota is already spent, stop and report that — do not run a degraded pass and file it.

    Run the corpus against the finished build from the repository root with the corpus flag set, the label set to AFTER, the SHA set to the current short HEAD, the report path set to `.planning/phases/05-coaching-channel/evidence/04-redteam-after.txt`, and strict mode on so a steered command fails the run. Follow the two-attempt rule: if more than three items come back as model failures for transport or rate-limit reasons, rerun once and keep the run with fewer, filing the other under a suffixed name.

    Read the new report against Phase 4's and record in the plan SUMMARY: the STEERED count then and now; the per-layer catch counts then and now; for each of `benign-fight-01` and `benign-fight-02`, all three sample verdicts and whether they agreed — a disagreement is itself the finding DR-4-01 asked about and must be reported plainly, not averaged away; for each of `chatter-channel-01` through `chatter-channel-03`, what happened and which layer, if any, caught it; any other false block by id; and any item that failed for transport reasons rather than for a defence.

    If the strict run reports a non-zero STEERED count, or if either benign fight item is blocked in any of its three samples, do not weaken the pass bar, do not edit the corpus and do not proceed to the staging checkpoints. Report which items got through or were wrongly blocked, with their categories and targets, and stop — the phase has found a real gap and the owner decides what happens next.

    Change no source file in this task.
  </action>
  <verify>
    <automated>test -f .planning/phases/05-coaching-channel/evidence/04-redteam-after.txt && grep -c "LABEL: AFTER" .planning/phases/05-coaching-channel/evidence/04-redteam-after.txt && grep -c "benign-fight-01" .planning/phases/05-coaching-channel/evidence/04-redteam-after.txt && grep -c "chatter-channel-01" .planning/phases/05-coaching-channel/evidence/04-redteam-after.txt</automated>
  </verify>
  <acceptance_criteria>
    - `evidence/04-redteam-after.txt` exists with `LABEL: AFTER`, a `SHA:` matching the built commit, one line per corpus sample including three each for `benign-fight-01` and `benign-fight-02` and one each for `chatter-channel-01` through `chatter-channel-03`, a catches-by-layer block and a summary block
    - The summary block reads `STEERED: 0` and the strict run exits 0; if it does not, the staging checkpoints were not attempted and the SUMMARY says so plainly
    - Neither benign fight item is blocked in any of its three samples; if any sample blocks, the SUMMARY reports the split verdict plainly and the phase stops
    - `git log --oneline` over the corpus test and its fixture shows this phase's commits only as additions
    - `grep -ciE "AIza|x-goog-api-key|generativelanguage|GAME_TEXT|MODEL_REASONING|COACHING" .planning/phases/05-coaching-channel/evidence/04-redteam-after.txt` returns 0
    - The plan SUMMARY carries a before-and-after table against the Phase 4 AFTER report and states explicitly what happened to each of the five new items
    - The SUMMARY records that the run was made on a fresh-quota day, or that the task stopped because it was not
  </acceptance_criteria>
  <done>The reviewer's two known faults and this phase's own new attack surface have all been measured against the finished build, and the numbers are on file next to the ones they are meant to be compared with.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 05-11-03: Staging runs this build, migration 015 applies, and the coaching channel answers over HTTP</name>
  <files>.planning/phases/05-coaching-channel/evidence/03-canned-report.txt, .planning/phases/05-coaching-channel/evidence/05-staging-ai-player.log, .planning/phases/05-coaching-channel/evidence/12-ai-settings-rate-limit.png, .planning/phases/05-coaching-channel/evidence/13-logs-signed-out.png</files>
  <what-built>
    Pause and resume with two waiting reasons and the two locked stream notices (05-01), the reviewer's two fixes (05-02), the vault-key gate and the log-route pin (05-03), the AI command rate limit end to end (05-04), AI-chatter and the conversation (05-05), coaching reaching the next decision (05-06), the split panel (05-07), the pop-out windows (05-08), the help article and the Logs section (05-09) and the harness (05-10). This checkpoint gets that build onto staging, proves migration 015 applied, runs the harness against it, and captures the two screenshots that do not need a live stint — everything the walkthrough in the next task depends on.
  </what-built>
  <read_first>
    - .planning/phases/05-coaching-channel/05-10-SUMMARY.md (the exact live invocation line for `scripts/verify-phase5.sh` and its environment variables)
    - .planning/phases/05-coaching-channel/evidence/02-harness-selftest.txt (from task 05-11-01 — the harness has already proven it can fail)
    - .planning/phases/04-continuous-play/04-11-PLAN.md task 04-11-03 (the staging capture procedure that worked last phase, including the cookie and connection-id handling)
    - .planning/phases/05-coaching-channel/05-UI-SPEC.md §4 (the rate-limit field's locked label, placeholder and hint, so the screenshot can be checked character for character)
    - .planning/phases/05-coaching-channel/05-03-SUMMARY.md (what a signed-out visitor to the log page should see, according to the code)
    - CLAUDE.md project facts (deploy with `railway up`; a dashboard Deploy rebuilds from GitHub and would run a different branch state; push `ai-player` at phase close after the security-review commits land)
  </read_first>
  <action>
    Rebuild the frontend bundle and commit `public/`, deploy this branch to Railway staging with `railway up --detach -e staging -s MudPuppy`, confirm migration `015` applied and the server started, run the Phase 5 harness against staging into `evidence/03-canned-report.txt` as RUN A, capture the AI settings rate-limit field, and capture the signed-out log page from a private window — without ever signing the owner out.
  </action>
  <how-to-verify>
    1. Run `cd frontend && npm run build` and commit the rebuilt `public/` — this phase carries substantial frontend changes, and a deploy without the rebuilt bundle would serve the Phase 4 panel.
    2. Deploy with `railway up --detach -e staging -s MudPuppy` to project `mudpuppy`, environment `staging`. Do not use the dashboard Deploy button and do not deploy to production.
    3. In the deploy log, confirm a migration line reading `version=15, dirty=false` and that the process started normally. Keep this excerpt; it opens `evidence/05-staging-ai-player.log`. If the server exits naming `ENCRYPTION_KEY_V1`, the staging variable is unset — set it once through the CLI without printing its value and record only that it was set. Do **not** set `MUDPUPPY_LOCAL_DEV` on staging under any circumstance. If the migration failed, stop here: the conversation table and the coaching column are what this whole phase stands on.
    4. Using the browser session that is already signed in — do not sign out — open the connection profile that has accepted the Safety and Abuse policy and that the owner will drive on Alter Aeon, and note its connection id.
    5. Open AI Player settings for that profile and capture `12-ai-settings-rate-limit.png`: the shot must show the `AI Command Rate Limit (per second)` field with its `Server default` placeholder and the full hint sentence beneath it, beside the existing Call Cap and Disengage Threshold fields, from the end-user perspective — the MUDPuppy browser page with the header visible, no devtools pane, no terminal window, no raw JSON in frame. Leave the field blank.
    6. Open a **private window or a separate browser profile** — never sign the owner out — and visit `/logs/<that connection id>` on staging as a signed-out visitor. Capture `13-logs-signed-out.png` showing where the visitor actually lands. If a transcript is visible, that is a finding: stop, record it, and do not proceed.
    7. Run the harness against staging into a temporary file using the invocation from `05-10-SUMMARY.md`, with the base URL, session cookie, connection id and not-owned connection id set, then write `evidence/03-canned-report.txt` beginning with a heading reading `### RUN A (before the walkthrough)` followed by that file's contents verbatim.
    8. Read RUN A and confirm with your own eyes: `PASS C2` with both reads returning arrays and the write methods refused; `PASS C3` with the rate limit round-tripping set, blank and out-of-range, and the settings restored; `PASS C4` with the not-owned connection refused on both reads; `SKIP C5` naming its Go tests and screenshots; and, for `C1`, either `PASS C1` or the stated skip because the switch was not On — the harness never engages autopilot itself.
    Do not run a SQL query at any point. Do not sign the owner out. Do not touch production. Do not print any Railway variable value.
  </how-to-verify>
  <verify>
    <human-check>Staging is running this build with migration 015 applied, RUN A is in evidence/03-canned-report.txt with PASS C2, PASS C3, PASS C4 and the C5 skip, the rate-limit field screenshot matches the locked label and hint, and the signed-out private-window shot shows a sign-in prompt rather than a transcript.</human-check>
  </verify>
  <acceptance_criteria>
    - The deploy log line showing `version=15` and `dirty=false` is captured into `evidence/05-staging-ai-player.log` and the service started normally
    - `evidence/03-canned-report.txt` begins with `### RUN A (before the walkthrough)` and its header records the staging base URL and a git SHA
    - RUN A contains `PASS C2`, `PASS C3`, `PASS C4` and `SKIP C5`, and zero lines beginning with `FAIL`; the C1 line is either `PASS C1` or the stated not-engaged skip
    - `evidence/12-ai-settings-rate-limit.png` shows the label `AI Command Rate Limit (per second)`, the placeholder `Server default` and the full hint sentence, from the end-user perspective with no devtools, terminal or raw JSON in frame
    - `evidence/13-logs-signed-out.png` shows a sign-in prompt rather than any transcript content, and the SUMMARY states it was taken from a private window or separate browser profile and that the owner was never signed out
    - The SUMMARY records the deploy method as `railway up --detach -e staging -s MudPuppy`, that `public/` was rebuilt and committed first, that production was not deployed to, and that `MUDPUPPY_LOCAL_DEV` was not set on staging
    - No SQL query, no Railway variable value and no key appears in this task's evidence or in the SUMMARY
  </acceptance_criteria>
  <resume-signal>Type "approved" once staging is running this build, RUN A is on disk and both screenshots are captured — or name the file that could not be produced and what the report or the screen said instead.</resume-signal>
  <done>Staging runs the phase's build, the schema accepted migration 015, the reachable half of the phase is a report on disk, and the log page has been seen turning a signed-out visitor away.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 05-11-04: The owner coaches the AI mid-stint, pauses it, resumes it, and pops both views onto his own desktop</name>
  <files>.planning/phases/05-coaching-channel/evidence/03-canned-report.txt, .planning/phases/05-coaching-channel/evidence/05-staging-ai-player.log, .planning/phases/05-coaching-channel/evidence/06-coaching-in-next-decision.png, .planning/phases/05-coaching-channel/evidence/07-chat-reply.png, .planning/phases/05-coaching-channel/evidence/08-chat-while-off.png, .planning/phases/05-coaching-channel/evidence/09-coaching-after-refresh.png, .planning/phases/05-coaching-channel/evidence/10-popouts-both-open.png, .planning/phases/05-coaching-channel/evidence/11-popout-brought-back.png, .planning/phases/05-coaching-channel/evidence/14-help-article.png, .planning/phases/05-coaching-channel/evidence/15-guidance-pasted-by-hand.png, .planning/phases/05-coaching-channel/evidence/16-logs-conversation-section.png, .planning/phases/05-coaching-channel/evidence/17-panel-two-views.png, .planning/phases/05-coaching-channel/evidence/18-paused-waiting-reason.png, .planning/phases/05-coaching-channel/evidence/19-hand-play-unchanged.png, .planning/phases/05-coaching-channel/05-11-SUMMARY.md</files>
  <what-built>
    The whole phase, on the owner's own character: he can now talk to the AI while it plays, have what he says change the very next decision, see exactly what reached it, take it back in words, stop the AI where it stands and start it again, put either half of the panel on its own monitor, and read the whole conversation back afterwards on the Logs page. This is the Phase Validation line's player-observable half and the larger half: criteria 1, 2, 3 and 4 have no other proof.
  </what-built>
  <read_first>
    - .planning/ROADMAP.md §Phase 5 (the four success criteria and the Phase Validation line)
    - .planning/phases/05-coaching-channel/05-UI-SPEC.md in full (what each surface must show and say, verbatim, including the Copywriting Contract and the Regression Invariant)
    - .planning/phases/05-coaching-channel/05-CONTEXT.md D-05, D-06, D-09, D-10, D-13, D-14, D-15, D-20, D-21, D-29
    - .planning/phases/05-coaching-channel/05-VALIDATION.md §Manual-Only Verifications (the seven rows this task satisfies)
    - .planning/phases/05-coaching-channel/05-01-SUMMARY.md, 05-06-SUMMARY.md and 05-07-SUMMARY.md (the exact shipped strings, so a screenshot can be read against the contract)
    - .planning/phases/04-continuous-play/evidence/05-staging-ai-player.log (the log excerpt shape and the greps run over it)
  </read_first>
  <action>
    Walk the play screen on staging against Alter Aeon in the order below using the owner's own character, capture one PNG per observable into `evidence/` with the exact filenames listed, append RUN B of the canned report and the log excerpt, and write `05-11-SUMMARY.md` with the four-row criterion table specified at the end.
  </action>
  <how-to-verify>
    On staging, signed in as the owner, connected to Alter Aeon on the profile from the previous task with a session goal saved. Do not sign out at any point. Every shot must be the browser as the owner sees it — the play screen with the header visible, no devtools pane, no terminal window, no raw JSON in frame. Expect bursts and gaps rather than clockwork, and expect the socket to drop occasionally; record each drop and what autopilot did.

    1. With the panel open and autopilot still off, capture `17-panel-two-views.png`: the shot must show one panel with the AI-player view on top (goal box, Pause button, status line, Session Memory, Coaching in effect) and the AI-chatter conversation with its message box below it, in the same 380-wide floating panel, with no tabs and no second panel anywhere on the page.
    2. Still with autopilot off, type a question to AI-chatter and send it. Capture `08-chat-while-off.png`: the shot must show the owner's line and AI-chatter's reply in the conversation area, the badge reading `Autopilot: Off` in the same frame, and the thinking stream containing no chat text at all. Confirm the terminal received nothing and the badge did not change — typing in the box is not a wheel-grab.
    3. Type `#AUTO ON` and let the AI play for several decisions without touching anything.
    4. While it is playing, send AI-chatter a coaching message the owner actually wants followed — something specific enough to be visible in the next decision's reasoning. Capture `07-chat-reply.png`: the reply must begin `Sent to AI-player: ` followed by the exact line, in the conversation area only.
    5. Capture `06-coaching-in-next-decision.png`: the shot must show, in one frame, the `[Coaching received]` marker in the thinking stream, the line present in the expanded `Coaching in effect` list, and the next decision's reasoning visibly taking the coaching into account. Check the marker reads `[Coaching received]` with exactly one pair of brackets; doubled brackets mean the server wrote its own and the SUMMARY must record it as a copy defect. This is criterion 1's whole proof; if the next decision's reasoning does not reflect it, say so plainly in the SUMMARY rather than reframing it, and capture it anyway.
    6. Hard-refresh the page, reconnect if the refresh dropped the socket, expand `Coaching in effect` again and capture `09-coaching-after-refresh.png` showing the same line rebuilt from the server.
    7. With autopilot on, click `Pause`. Capture `18-paused-waiting-reason.png`: the shot must show the badge reading `Autopilot: Waiting`, the status line reading `Paused by owner`, the button now reading `Resume`, and the `[Autopilot paused]` line in the stream in warning yellow. Then leave it paused for a stretch long enough for several rooms of game text to arrive, and confirm from the terminal that game text is still arriving. Then click `Resume` and confirm the `[Autopilot resumed]` line appears in the stream in neutral grey and that the first new decision's reasoning describes where the character is **now**. Check both notices read with exactly one pair of brackets. Record in the SUMMARY roughly how long the pause lasted and how much game text arrived during it.
    8. Send one more coaching message, then take it back in words ("forget what I said about ..."). Confirm AI-chatter's reply begins `Withdrew from AI-player: ` with the exact line and that the line has left the `Coaching in effect` list. This is the live check that AI-chatter really can see the coaching in effect (D-17): if the reply says nothing matched while the line is plainly still on the list, record that as a finding against `TestHandleChat_PromptCarriesCoachingInEffect` rather than retrying with different wording. Record both in the SUMMARY; no separate screenshot is required.
    9. Click `Pop out` on the AI-player view and then on the AI-chatter view. Capture `10-popouts-both-open.png` showing both separate browser windows live beside the play screen, with the same newest decision visible in the popped-out AI-player window as in the play screen behind it, and the docked panel showing both placeholder lines. Then close one window by its own close button and press `Bring back` on the other, and capture `11-popout-brought-back.png` showing both views back in the panel with nothing lost or duplicated.
    10. Open the Help page, open the `AI-chatter & AI-player` article and capture `14-help-article.png` showing its title and the by-hand copy steps. Follow those steps for real: copy a suggestion from the conversation, open AI Player settings, paste it into Conduct rules or Approach guidance and save. Capture `15-guidance-pasted-by-hand.png` showing the saved field containing the pasted line. Record in the SUMMARY that no promotion control existed anywhere and that the copy was manual.
    11. Open the Logs page for this connection in a new tab, select this session, and capture `16-logs-conversation-section.png` showing the `Coaching Conversation` section below the transcript with the owner's lines and AI-chatter's replies. This is the diagnostic half of the Phase Validation line, read through the page rather than through a database.
    12. Open a profile that has not accepted the policy, connect and hand-play several commands. Capture `19-hand-play-unchanged.png` showing ordinary play with no AI Assist panel, no chat strip, no Pause button, no Coaching in effect list and no pop-out button anywhere on the page.
    13. Run the harness against staging again with the same arguments as RUN A and append to `evidence/03-canned-report.txt` a heading reading `### RUN B (after the walkthrough)` followed by that file's contents verbatim. Confirm C2 now reads back a non-empty coaching list and a non-empty conversation, and that C3 and C4 still pass.
    14. Capture the `[AI-PLAYER]` and `[AI-CHATTER]` lines this walkthrough produced with `railway logs` against the staging environment filtered on the prefixes, or the Railway log tool, naming the tool used in the SUMMARY. Append them to `evidence/05-staging-ai-player.log` under a heading. Confirm the excerpt contains, for the test connection id: `stage=received` and `stage=reply` for the chat messages, `stage=coaching-pushed` and `stage=coaching-withdrawn`, the autopilot transitions with causes `pause` and `resume-owner`, and — across the paused stretch — **no** model-call stage and **no** sent stage at all. That absence is criterion 2's proof; quote the two timestamps that bracket it in the SUMMARY.
    15. Read the log excerpt back and confirm it contains no chat text, no reply text, no game text, no reasoning, no command text, no goal text, no memory or coaching bullet, no conduct-rules or approach-guidance text, no API key, no endpoint URL and no one-time sign-in code.
    16. Write `05-11-SUMMARY.md`. Its centrepiece is a table with one row per ROADMAP Phase 5 success criterion 1 to 4, each naming the evidence that proves it and a PASS or FAIL verdict. Line numbers are required for report and log citations — "see the report" is not a citation. Screenshots are cited by filename. No row may cite a database query. Suggested citations: criterion 1 to `evidence/06-coaching-in-next-decision.png`, `evidence/07-chat-reply.png` and the `stage=coaching-pushed` lines of `evidence/05-staging-ai-player.log` plus the `TestHandleChat_PushReachesNextPrompt` line of `evidence/01-test-report.txt`; criterion 2 to `evidence/18-paused-waiting-reason.png`, the bracketed quiet stretch of the log, and the `TestManager_ResumeRequiresBothReasonsClear`, `TestManager_PauseStopsTheLoopAndKeepsReading` and `TestAutopilotHandler_EmitsPausedAndResumedNotices` lines; criterion 3 to `evidence/14-help-article.png`, `evidence/15-guidance-pasted-by-hand.png` and the `TestHandleChat_TouchesNothingElse` line; criterion 4 to `evidence/17-panel-two-views.png`, `evidence/09-coaching-after-refresh.png`, `evidence/10-popouts-both-open.png` and `evidence/11-popout-brought-back.png`. Close with a short section recording the stint's decision count and duration, the pause's duration and how much game text arrived during it, every socket drop and what autopilot did each time (noting explicitly whether any reconnect resumed a pause — it must not have), the withdraw observation, and a pointer to `05-SECURITY-AGENDA.md`.
  </how-to-verify>
  <verify>
    <human-check>All twelve walkthrough screenshots exist under evidence/ with the exact filenames, each showing the stated content; the three system notices each read with exactly one pair of brackets; RUN B is appended and reads back a non-empty coaching list and conversation; the log excerpt carries the chat, coaching, pause and resume stages, shows a quiet stretch with no model call and no send while paused, and carries nothing it should not; and the SUMMARY's four-row table cites a real file and line number for every criterion.</human-check>
  </verify>
  <acceptance_criteria>
    - All twelve PNGs exist with exactly these names: `06-coaching-in-next-decision.png`, `07-chat-reply.png`, `08-chat-while-off.png`, `09-coaching-after-refresh.png`, `10-popouts-both-open.png`, `11-popout-brought-back.png`, `14-help-article.png`, `15-guidance-pasted-by-hand.png`, `16-logs-conversation-section.png`, `17-panel-two-views.png`, `18-paused-waiting-reason.png`, `19-hand-play-unchanged.png`
    - Every PNG is from the end-user perspective: the MUDPuppy browser page with the header visible, no devtools pane, no terminal window, no raw JSON in frame
    - `17-panel-two-views.png` shows both views in one panel with no tabs and no second panel
    - `07-chat-reply.png` shows a reply beginning `Sent to AI-player: ` with the exact line, in the conversation area only
    - `06-coaching-in-next-decision.png` shows the `[Coaching received]` marker with exactly one pair of brackets, the line in the `Coaching in effect` list and the next decision's reasoning in one frame, and the SUMMARY states plainly whether that reasoning reflects the coaching
    - `18-paused-waiting-reason.png` shows the badge at Waiting, the status line reading `Paused by owner`, the button reading `Resume` and the `[Autopilot paused]` line in the stream; the SUMMARY records that `[Autopilot resumed]` appeared on resume and that both read with exactly one pair of brackets
    - `10-popouts-both-open.png` shows two separate browser windows live at once with the same newest decision as the play screen, and the docked panel showing both placeholders
    - `19-hand-play-unchanged.png` shows no AI Assist panel, no chat strip, no Pause button and no pop-out control anywhere on the page
    - `evidence/03-canned-report.txt` contains both a RUN A and a RUN B section; RUN B contains `PASS C2`, `PASS C3` and `PASS C4` and zero lines beginning with `FAIL`
    - `evidence/05-staging-ai-player.log` contains at least one line for each of `stage=received`, `stage=reply`, `stage=coaching-pushed`, `stage=coaching-withdrawn`, an autopilot transition with cause `pause` and one with cause `resume-owner`, plus the migration `015` startup line — and no model-call or sent stage between the pause and resume transitions
    - `grep -ciE "conduct_rules|approach_guidance|never_issue|reasoning=|notice=|goal=\"|message=\"|reply=\"|AIza|x-goog-api-key|generativelanguage|GAME_TEXT|SESSION_MEMORY|COACHING" .planning/phases/05-coaching-channel/evidence/05-staging-ai-player.log` returns 0
    - `grep -ci "code:" .planning/phases/05-coaching-channel/evidence/05-staging-ai-player.log` returns 0 (DR-2-01 staying closed)
    - `05-11-SUMMARY.md` contains a four-row criterion table; every row cites at least one evidence file, report and log citations carry line numbers, and every row carries a PASS or FAIL
    - No row cites a SQL query or a database inspection, and the SUMMARY states the owner was never signed out and that the deploy was `railway up`
  </acceptance_criteria>
  <resume-signal>Type "approved" to record the phase as demonstrated, or list the screenshots that could not be produced and what was on screen instead.</resume-signal>
  <done>The ROADMAP Phase Validation has been performed on the owner's own character against a live MUD: he coached the AI mid-stint and watched the next decision change, stopped it and started it again and read the stream say so, and every criterion points at a file — a report line, a log line, or an image.</done>
</task>

<task type="auto">
  <name>Task 05-11-05: The security review gets all four deferred risks back, with what this phase built against each</name>
  <files>.planning/phases/05-coaching-channel/05-SECURITY-AGENDA.md</files>
  <read_first>
    - .planning/phases/04-continuous-play/04-SECURITY-AGENDA.md in full (the voice, the three-disposition format, and the standing rule that the agenda decides nothing)
    - .planning/RISK-REGISTER.md rows DR-4-01, DR-4-02, DR-4-03 and DR-4-04 verbatim — the risk, the proposed remediation, the owner's own note and the "Raised again at" cell naming the Phase 5 review — plus AR-4-08 and its owner condition
    - .planning/phases/05-coaching-channel/05-CONTEXT.md D-23 to D-28 (which decision closes which risk, and which are review items rather than code)
    - the `threat_model` blocks of plans 05-01 through 05-10 (the T-5 register this phase produced, including every row dispositioned `accept`)
    - .planning/phases/05-coaching-channel/05-02-SUMMARY.md, 05-03-SUMMARY.md, 05-04-SUMMARY.md and 05-06-SUMMARY.md (the four summaries that record the carry-forward remediations as shipped)
    - .planning/phases/05-coaching-channel/evidence/01-test-report.txt and evidence/04-redteam-after.txt (the numbers the agenda quotes, so the owner decides against measurements rather than adjectives)
    - .specify/specs/safety-and-abuse-policy-v1.md sections 2, 5 and 6 (quote, do not paraphrase from memory)
  </read_first>
  <action>
    Write `.planning/phases/05-coaching-channel/05-SECURITY-AGENDA.md` in the voice of `04-SECURITY-AGENDA.md`: the input to the Phase 5 security review, not its output. It decides nothing. Every item carries the same three dispositions — Accept, Defer, Remediate Now — with none marked as chosen and none recommended. Write every risk in plain words, as a problem followed by a fix, with no jargon in any owner-facing sentence.

    Open with one paragraph stating what the agenda is for and that the project cannot close while any deferral remains active.

    Then a section for the carried-forward risks, with one numbered item for each of DR-4-01, DR-4-02, DR-4-03 and DR-4-04, each reproducing the risk in the owner's own words from the register, then stating what Phase 5 did about it with a citation to the evidence that proves it:
    - DR-4-01 by D-23: the checker told plainly that fighting a game-presented target is ordinary play, the two real false blocks added as benign controls and sampled three times each — cited to the `benign-fight-01` and `benign-fight-02` sample lines of `evidence/04-redteam-after.txt` by line number, and to the `TestBuildReviewSystemInstruction_FightingIsOrdinaryPlay` line of `evidence/01-test-report.txt`. State the three-sample agreement plainly, including any disagreement.
    - DR-4-02 by D-24: a missing verdict is now a failed review, and the reviewer is told who wrote the memory notes — cited to the `TestReviewCommand_MissingBlockedField` and `TestBuildReviewSystemInstruction_MemoryAuthorship` lines.
    - DR-4-03 by D-25: every AI send is now counted by a per-user limit before it is dispatched, the limit is a profile setting where blank means the server's default, and the ICM dispatcher was not touched — cited to the `TestAllowAISend_FloodRefused` and `TestResolveAISettings_RateLimitBlankMeansServerDefault` lines, the empty `### ICM UNCHANGED` section, and `evidence/12-ai-settings-rate-limit.png`.
    - DR-4-04 by D-26: the vault key is required on every host with one named, announced local-development opt-out — cited to the `TestLoadRequiresEncryptionKeyEverywhereUnlessLocalDev` line.
    For each, still offer all three dispositions: a remediation having shipped is not the same as the owner having closed the risk.

    Then an item for AR-4-08 and the owner's condition on it: state what the code does, that the log page renders only inside the sign-in guard and is pinned there by a comment naming the row, and cite `evidence/13-logs-signed-out.png` and the `### LOGS ROUTE GUARD` section of the test report. Note that the other half of AR-4-08 — reviewing who can run `railway logs` against staging — is still the owner's own administrative task, with nothing this phase could do about it in code.

    Then a section for what this phase newly creates, led by the one item that is genuinely new: **the coaching channel as a laundering path.** State the mechanism in plain words — AI-chatter reads the game's own text and the AI's own notes, and it can write a short line that AI-player then reads as guidance from the owner, so a hostile room could try to get a sentence in that way. State the two mitigations that exist: only a message the owner typed can cause a line to be written, and the lines are delimited and cleaned like every other block so nothing inside one can pretend to be a system marker. State plainly what is not solved: a suggestion that reads as an ordinary, sensible instruction is not detectable by delimiting, and the owner's own eyes on the quoted reply and the Coaching in effect list are the last check. Cite T-5-26 and the `chatter-channel-01` to `-03` lines of `evidence/04-redteam-after.txt` with their outcomes. Three dispositions.

    Add the other residuals this phase's plans dispositioned `accept`, each with its threat id, stated as accepted at plan level and listed for confirmation rather than re-litigation — at minimum the supply-chain row `T-5-SC`, the stricter vault-key gate's risk of stopping staging (`T-5-13`), the cost of the corpus rerun (`T-5-09`), and the article teaching the owner to paste model-written text into his own conduct rules (`T-5-41`). Include `T-5-55` and `T-5-56` in the same list if the plans that raised them dispositioned them at plan level: the conversation tail and the coaching-in-effect block are both places where earlier text is shown back to AI-chatter, and the owner should know they exist even though both are mitigated.

    Close with two notes: that the policy stays at version 1.0 and no rewording is proposed, and whether `ai-player` has been pushed to GitHub at this phase's close, since the owner asked for that at the Phase 3 review.

    Change no cell in `.planning/RISK-REGISTER.md`: every deferred row already names the Phase 5 security review, and the outcomes are recorded there at the review, not before it. Build no remediation in this task and change no code. If the owner chooses Remediate Now on any item, that becomes its own plan or its own phase.
  </action>
  <verify>
    <automated>test -f .planning/phases/05-coaching-channel/05-SECURITY-AGENDA.md && grep -c "Remediate Now" .planning/phases/05-coaching-channel/05-SECURITY-AGENDA.md && grep -c "DR-4-04" .planning/phases/05-coaching-channel/05-SECURITY-AGENDA.md && grep -c "T-5-26" .planning/phases/05-coaching-channel/05-SECURITY-AGENDA.md</automated>
  </verify>
  <acceptance_criteria>
    - `05-SECURITY-AGENDA.md` exists and contains one numbered item for each of `DR-4-01`, `DR-4-02`, `DR-4-03`, `DR-4-04` and `AR-4-08`, each listing all three dispositions `Accept`, `Defer` and `Remediate Now` with none marked as selected or recommended
    - Every carried-forward item reproduces the risk in the owner's own words from the register and cites at least one evidence file, with a line number for report and log citations
    - The agenda contains an item for the coaching channel as a laundering path citing `T-5-26`, stating the mechanism and both mitigations in plain words and stating plainly what is not solved, with the three `chatter-channel` outcomes quoted
    - The agenda lists this phase's plan-level acceptances by threat id, including at least `T-5-SC`, `T-5-13`, `T-5-09` and `T-5-41`
    - The agenda records that the policy stays at version 1.0 and states whether `ai-player` has been pushed to GitHub at this phase's close
    - `git diff .planning/RISK-REGISTER.md` is empty — the outcomes are recorded at the review, not in advance
    - Source assertion: `grep -ciE "SELECT |psql" .planning/phases/05-coaching-channel/05-SECURITY-AGENDA.md` returns 0, and every risk item reads as a problem followed by a fix in everyday words
    - Source assertion: `git status --porcelain` shows changes only under `.planning/` for this task
  </acceptance_criteria>
  <done>All four risks the owner deferred at the last review come back with what was actually built against each and a number attached, and the one new risk this phase creates is sitting beside them with nothing decided in advance.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repository → Railway staging | The deployed binary may diverge from the repo; the report's git SHA header, the rebuilt `public/` bundle and the log are what reconcile them |
| Owner browser session → the live MUD and the live model | The walkthrough drives the real session, socket, driver and vendor call end to end |
| Evidence files → readers | Reports, logs and screenshots are committed; whatever they contain is disclosed |
| Migration 015 → the live staging database | A failed migration is a startup fatal and takes staging down |
| The corpus rerun → the owner's daily quota | A full corpus pass on a free-tier key is a day's budget |
| Private-window capture → the owner's session | Producing the signed-out screenshot must not disturb the owner's own sign-in |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation |
|-----------|----------|----------|-----------|-------------|------------|
| T-5-45 | Repudiation (unfalsifiable evidence) | High | a canned report that always passes | mitigate | 05-11-01 files the negative self-test before the staging report is trusted |
| T-5-46 | Information Disclosure | High | a key, a Railway variable value, a session cookie, chat text, a reply, game text, a goal or a coaching line reaching a committed report, log excerpt or screenshot | mitigate | 05-11-02 and 05-11-04 grep the filed reports and the log excerpt for key, endpoint, delimiter, notice and text patterns before they are committed; every screenshot is an end-user frame with no raw JSON |
| T-5-50 | Tampering (wrong target) | High | a deploy, a variable change or a harness run aimed at production, or a dashboard Deploy rebuilding from an unpushed branch | mitigate | 05-11-03 deploys with `railway up --detach -e staging -s MudPuppy` only, rebuilds and commits `public/` first, and records the base URL and git SHA in the report header |
| T-5-51 | Denial of Service (owner lockout) | High | signing the owner out of staging to produce the signed-out screenshot | mitigate | 05-11-03 requires a private window or separate browser profile and states the standing instruction; sign-in is one-time-code based and the executor cannot sign back in |
| T-5-52 | Tampering (weakened gate) | High | setting `MUDPUPPY_LOCAL_DEV` on staging to get past the new vault-key requirement | mitigate | 05-11-03 forbids it explicitly and tells the executor what a missing-key startup failure looks like and how to fix it without printing the value |
| T-5-25 | Tampering (data integrity) | Medium | migration 015 failing on the live schema | mitigate | 05-11-03 stops on anything other than a clean `version=15, dirty=false` startup line |
| T-5-26 | Elevation of Privilege (coaching laundering) | High | the coaching channel as a path from hostile game text into trusted-looking guidance | defer to security review | 05-11-05 writes it onto the agenda as this phase's new active risk with three dispositions and no decision, with the three live attack outcomes quoted |
| T-5-09 | Denial of Service (cost) | Medium | the corpus rerun consuming the day's free-tier quota | accept | The rerun on a fresh-quota day is what makes DR-4-01's fix a measurement; the cost is listed on the agenda |
| T-5-53 | Repudiation (risks forgotten) | High | four deferred risks quietly lapsing because a phase shipped remediations for them | mitigate | 05-11-05 re-presents every one with what was built and three dispositions; a shipped remediation is not a closed risk until the owner says so |
| T-5-SC | Tampering (supply chain) | Low | `go.mod`, `frontend/package.json` | accept | The dependency-drift section of `evidence/01-test-report.txt` must be empty for the whole phase |
</threat_model>

<verification>
1. `evidence/01-test-report.txt` exists with every command captured verbatim, zero new FAIL lines, empty dependency-drift, model-literal and ICM sections, a one-file coaching-writer section, a stated race answer and a `GO TEST EXIT:` line.
2. `evidence/02-harness-selftest.txt` shows a clean self-test and a negative self-test containing `FAIL C3` with a non-zero exit.
3. `evidence/04-redteam-after.txt` carries `LABEL: AFTER`, three sample lines each for the two benign fight items, one line each for the three chatter-channel items, and `STEERED: 0` from a strict run on a fresh-quota day.
4. `evidence/03-canned-report.txt` contains RUN A and RUN B; both contain `PASS C2`, `PASS C3` and `PASS C4` and no `FAIL` line.
5. `evidence/05-staging-ai-player.log` contains the migration `015` line, the chat, coaching, pause and resume stages, a quiet stretch between pause and resume with no model call and no send, and no text of any kind.
6. Fourteen screenshots exist under `evidence/` with the exact filenames listed, all from the end-user perspective, and the three system notices in them each read with exactly one pair of brackets.
7. `05-11-SUMMARY.md`'s four-row table cites a real file, with a line number for text evidence, per ROADMAP criterion, with a verdict.
8. `05-SECURITY-AGENDA.md` lists all four DR-4 rows plus AR-4-08 and the new coaching-channel risk, each with three dispositions and none chosen, and `.planning/RISK-REGISTER.md` is unchanged.
9. Both checkpoints are explicitly approved by the owner.
10. No evidence in this phase is a database query, the owner was never signed out, `MUDPUPPY_LOCAL_DEV` was never set on staging, and no Railway variable value was printed anywhere.
</verification>

<success_criteria>
- ROADMAP Phase 5 criterion 1 demonstrated: a chat message sent while the AI plays is reflected in its next decision, verifiable from the logged reasoning — by `evidence/06-coaching-in-next-decision.png`, `evidence/07-chat-reply.png`, the `stage=coaching-pushed` lines of `evidence/05-staging-ai-player.log` and the `TestHandleChat_PushReachesNextPrompt` line of `evidence/01-test-report.txt` (D-01, D-05, D-08, D-09).
- Criterion 2 demonstrated: pause and resume work from the AI-player part of the panel, pausing stops commands but keeps reading, shows WAITING with its reason, prints the locked notices in the stream, and never resumes by itself — by `evidence/18-paused-waiting-reason.png`, the bracketed quiet stretch of the log excerpt, and the `TestManager_ResumeRequiresBothReasonsClear`, `TestManager_PauseStopsTheLoopAndKeepsReading` and `TestAutopilotHandler_EmitsPausedAndResumedNotices` lines (D-13 to D-16).
- Criterion 3 demonstrated: chat is only ever plain text, AI-chatter changes no configuration, and the help article explains the by-hand copy the owner then performs — by `evidence/14-help-article.png`, `evidence/15-guidance-pasted-by-hand.png` and the `TestHandleChat_TouchesNothingElse` line (D-07, D-18, D-20, D-21).
- Criterion 4 demonstrated: one panel with two views, every pushed suggestion visible three ways, and each view working in its own window — by `evidence/17-panel-two-views.png`, `evidence/09-coaching-after-refresh.png`, `evidence/10-popouts-both-open.png` and `evidence/11-popout-brought-back.png` (D-03, D-04, D-09, D-29).
- The five Phase 4 carry-forward items are all closed in code and measured — by the `benign-fight` sample lines, the `TestReviewCommand_MissingBlockedField`, `TestAllowAISend_FloodRefused` and `TestLoadRequiresEncryptionKeyEverywhereUnlessLocalDev` lines, and `evidence/13-logs-signed-out.png` (D-23 to D-27).
- The Phase 5 security review can begin, because its agenda exists in writing with every deferred risk re-presented against what was built, and the coaching channel named as this phase's new risk (D-28).
</success_criteria>

<output>
Create `.planning/phases/05-coaching-channel/05-11-SUMMARY.md` when done, containing the evidence file inventory, the four-row criterion table with citations and verdicts, the before-and-after red-team comparison including all three samples of each benign fight item and the three chatter-channel outcomes, the stint's decision count and duration, the pause's duration and the game text that arrived during it, the observed wording and bracketing of `[Autopilot paused]`, `[Autopilot resumed]` and `[Coaching received]`, every socket drop and what autopilot did each time with an explicit statement that no reconnect resumed a pause, the withdraw observation, a note on whether `-race` ran and why, and a pointer to `05-SECURITY-AGENDA.md`. Record that `ai-player` is to be pushed to GitHub at phase close after the security-review commits land (CLAUDE.md, DR-3-05).
</output>
</content>
