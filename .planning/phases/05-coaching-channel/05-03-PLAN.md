---
phase: 05-coaching-channel
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/config/config.go
  - internal/config/config_test.go
  - frontend/src/App.tsx
autonomous: true
requirements: [REQ-safety-limits-hold]

must_haves:
  truths:
    - "No host can start this server on a made-up password-vault key: the key is required everywhere and the only way out is an explicit local-development setting the operator has to type on purpose (D-26, DR-4-04), proven by the TestLoadRequiresEncryptionKeyEverywhereUnlessLocalDev PASS line in evidence/01-test-report.txt"
    - "Turning the local-development escape hatch on is a deliberate act and is announced: the server logs that it is running without a vault key because the local-development setting is on, and never prints the key or any other variable value (D-26), proven by the TestLocalDevOptOutIsAnnounced PASS line"
    - "A signed-out visitor who opens a log page link is sent to sign-in rather than shown a transcript, and the route is pinned so a later tidy-up cannot move it outside the sign-in guard (D-27, AR-4-08), proven by the source assertion in this plan and by the private-window screenshot evidence/13-logs-signed-out.png filed by plan 05-11"
  artifacts:
    - path: "internal/config/config.go"
      provides: "A vault-key requirement that holds on every host, with one named local-development opt-out"
      contains: "MUDPUPPY_LOCAL_DEV"
    - path: "internal/config/config_test.go"
      provides: "The three cases: no key and no flag fails; no key with the flag starts; a key present is always validated"
      contains: "func TestLoadRequiresEncryptionKeyEverywhereUnlessLocalDev"
    - path: "frontend/src/App.tsx"
      provides: "The /logs/:connectionId route, pinned inside AuthGuard by a comment that says why it must never move out"
      contains: "AuthGuard"
  key_links:
    - from: "internal/config/config.go Load"
      to: "the startup failure path"
      via: "a missing ENCRYPTION_KEY_V1 returns an error unless MUDPUPPY_LOCAL_DEV is truthy"
      pattern: "ENCRYPTION_KEY_V1"
    - from: "frontend/src/App.tsx AuthGuard"
      to: "the /logs/:connectionId route"
      via: "the route element is rendered inside the guard, so a signed-out visitor is redirected before LogsPage mounts"
      pattern: "logs/:connectionId"
---

<objective>
Shut two doors that are currently ajar. The check that refuses to start the server without a password-vault key only runs when `RAILWAY_ENVIRONMENT` is set: on any other host the server quietly invents a new random key at every restart, and every saved MUD password stops working — the exact failure that already bit this project once on staging. And the owner's condition on the Phase 4 review was that the linked log display must sit behind the signed-in user's login; nobody has yet looked at what a signed-out visitor actually gets.

Purpose: D-26 places DR-4-04's fix in this phase, and D-27 places the log-page sign-in confirmation here with an end-user screenshot from a private window. Neither is large; both are register rows that cannot be closed by assertion alone.
Output: a vault-key requirement that holds on every host with one deliberately-named escape hatch, the tests that prove all three cases, and the log route pinned inside the sign-in guard with the source assertion that keeps it there.

The screenshot half of D-27 is not taken here — it is an end-user capture from a private window or a separate browser profile, filed by plan 05-11. **The owner is never to be signed out to produce it.**
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/ROADMAP.md
@.planning/RISK-REGISTER.md
@.planning/phases/05-coaching-channel/05-CONTEXT.md
@.planning/phases/05-coaching-channel/05-RESEARCH.md
@.planning/phases/05-coaching-channel/05-PATTERNS.md
@.planning/phases/05-coaching-channel/05-VALIDATION.md

Evidence rule (owner-directed, binding): proof is a canned report, an `[AI-PLAYER]` log excerpt, or an end-user screenshot. A database query is never evidence.

Machine rules for the executor (inherit these, they are not optional):
- Never run `find /`, `find ~`, `find /c`, `find /d`, `ls -R` or any search outside `D:\Projects\MUDPuppy`. Never launch a command in the background. Use Glob/Grep scoped to the repo and exclude `node_modules`.
- The GSD CLI form on this machine is `gsd-sdk <command> <sub>`; `gsd-sdk query ...` fails.
- Use the Write/Edit tools for every multi-line file; bash heredocs with backticks or quotes fail in this shell.
- Deploy to staging only with `railway up`. No deploy happens in this plan, and no Railway variable is read, set or printed here.
- **Never sign the owner out of any session**, on staging or anywhere else. Sign-in is one-time-code based and signing back in is the owner's own step.
- No package may be added to `go.mod` or `frontend/package.json` anywhere in this phase. There is no frontend test framework and none is to be added; frontend work is verified by `npm run build` in `frontend/` plus source assertions.
- `.specify/` specs other than `ai-game-player-design-v3.md`, `safety-and-abuse-policy-v1.md`, `ai-memory-model-v1.md` and `phase-based-development-approach.md` are excluded.

Claude's Discretion settled by the planner for this plan:
- The escape hatch is the environment variable `MUDPUPPY_LOCAL_DEV`, treated as on when its value is `1` or `true` (case-insensitive) and off for every other value including unset. `RAILWAY_ENVIRONMENT` stops being part of the decision entirely.
- The startup error message names the variable and the escape hatch and never prints a value.
- No new frontend guard is written: `AuthGuard` already wraps the log route. The work here is to confirm that by reading the code, pin it with a comment and a source assertion, and hand the screenshot to plan 05-11.
</context>

<tasks>

<task type="auto">
  <name>Task 05-03-01: The password vault refuses a made-up key on every host, not just on Railway</name>
  <files>internal/config/config.go, internal/config/config_test.go</files>
  <read_first>
    - internal/config/config.go in full, and lines 200-240 in particular: the `if os.Getenv("RAILWAY_ENVIRONMENT") != ""` block, the `ENCRYPTION_KEY_V1 environment variable is required outside local development` error, the per-key `crypto.ParseKey` validity loop over V1/V2/V3, and the `Config` struct fields `EncryptionKeyV1`, `EncryptionKeyV2`, `EncryptionKeyV3`
    - internal/crypto/crypto.go `DefaultKeyStore` and `ParseKey` — the silent random-key generation this gate exists to prevent, and the strictness that must not be weakened
    - internal/config/config_test.go in full, especially `TestLoadRequiresEncryptionKeyOutsideDevelopment` — the existing test that pins today's Railway-only behaviour and must be replaced by the everywhere rule, and the `t.Setenv` idiom the package already uses
    - .planning/RISK-REGISTER.md rows DR-4-04 and DR-3.1-04 (the owner's own words and what Phase 4 already shipped)
    - .planning/phases/05-coaching-channel/05-PATTERNS.md § `internal/config/config.go`
  </read_first>
  <action>
    In `internal/config/config.go`, invert the vault-key gate. Read `MUDPUPPY_LOCAL_DEV` once, treating `1` and `true` (any letter case) as on and everything else — including unset — as off. Delete `RAILWAY_ENVIRONMENT` from this decision; it is no longer consulted anywhere in the key gate. When the local-development flag is off, a blank `EncryptionKeyV1` returns an error reading that `ENCRYPTION_KEY_V1` is required and that a local development machine can opt out by setting `MUDPUPPY_LOCAL_DEV=true`; keep the existing fail-fast `return nil, errors.New(...)` shape and the existing rule that no value is ever logged. When the flag is on and the key is blank, log one line stating plainly that the server is starting without a password vault key because `MUDPUPPY_LOCAL_DEV` is set, and that saved MUD passwords will not survive a restart.

    Leave the per-key `crypto.ParseKey` validity loop exactly as it is, and run it in every case where a key value is present, flag on or off — a present-but-malformed key is still a startup failure. Do not weaken `crypto.ParseKey` or `DefaultKeyStore` in any way.

    In `internal/config/config_test.go`, replace `TestLoadRequiresEncryptionKeyOutsideDevelopment` with `TestLoadRequiresEncryptionKeyEverywhereUnlessLocalDev`, a table test over at least these rows, each using `t.Setenv` and each asserting on the returned error rather than on any logged text:
    - no `ENCRYPTION_KEY_V1`, no `MUDPUPPY_LOCAL_DEV`, no `RAILWAY_ENVIRONMENT` → `Load` fails and the error names `ENCRYPTION_KEY_V1`
    - no key, `RAILWAY_ENVIRONMENT` set → still fails (the old escape route is gone)
    - no key, `MUDPUPPY_LOCAL_DEV=true` → succeeds
    - no key, `MUDPUPPY_LOCAL_DEV=TRUE` and `MUDPUPPY_LOCAL_DEV=1` → succeed (the accepted spellings)
    - no key, `MUDPUPPY_LOCAL_DEV=no` / `0` / empty → fails (anything else is off)
    - a valid key present with the flag off → succeeds
    - a malformed key present with the flag **on** → still fails, naming the key that would not parse (the opt-out skips the requirement, never the validation)

    Add `TestLocalDevOptOutIsAnnounced` — with the flag on and no key, capture the package's log output the way the package's existing tests capture logs (or add a minimal `log.SetOutput` capture beside them) and assert the announcement line is present and contains `MUDPUPPY_LOCAL_DEV`, and that no test in this file ever asserts on, prints or constructs a real key value.
  </action>
  <verify>
    <automated>go vet ./internal/config/... && go test ./internal/config/... -count=1 -v</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/config/... -count=1 -v` exits 0 with PASS lines for `TestLoadRequiresEncryptionKeyEverywhereUnlessLocalDev` and `TestLocalDevOptOutIsAnnounced`, and every pre-existing test in the package still passing
    - Source assertion: `grep -c "MUDPUPPY_LOCAL_DEV" internal/config/config.go` returns at least 2 (the read and the message)
    - Source assertion: `grep -c "RAILWAY_ENVIRONMENT" internal/config/config.go` returns 0
    - Source assertion: `grep -c "TestLoadRequiresEncryptionKeyOutsideDevelopment" internal/config/config_test.go` returns 0 (the old, weaker pin is gone)
    - Source assertion: `git diff -- internal/crypto/crypto.go` is empty (the key parsing was not weakened)
    - `grep -ciE "AIza|x-goog-api-key|[0-9a-f]{32}" internal/config/config_test.go` returns 0 (no real or realistic key value in the test file)
    - `go build ./...` exits 0 and `git diff --stat go.mod frontend/package.json` is empty
  </acceptance_criteria>
  <done>Wherever this server runs, it either has a real password-vault key or the operator said in so many words that this is a development machine — and it says which out loud at startup.</done>
</task>

<task type="auto">
  <name>Task 05-03-02: The log page link is pinned behind the sign-in guard, so a signed-out visitor never reads a transcript</name>
  <files>frontend/src/App.tsx</files>
  <read_first>
    - frontend/src/App.tsx lines 165-200 in full: `AppRoutes`, the `AuthGuard` wrapper, the existing block comment explaining why `/logs/:connectionId` renders standalone rather than nested inside `AppContent`, and the catch-all `*` route
    - frontend/src/components/AuthGuard.tsx (or wherever `AuthGuard` is defined — find it with a repo-scoped Grep, never a disk-wide search): what it renders while the session is unknown, and where it sends a visitor with no session
    - frontend/src/pages/LogsPage.tsx's data fetch (the API call it makes on mount) and `frontend/src/services/api.ts`'s `handleAuthError` — what the server already does to an unauthenticated read
    - .planning/RISK-REGISTER.md row AR-4-08 (the owner's condition, verbatim) and `.planning/todos/pending/2026-09-17-confirm-log-page-login-gate.md`
    - .planning/phases/05-coaching-channel/05-CONTEXT.md D-27
  </read_first>
  <action>
    Read `AppRoutes` and `AuthGuard` and establish, from the code rather than from assumption, exactly what a visitor with no session gets when they open `/logs/<some-connection-id>`: which component renders, whether `LogsPage` mounts at all, and where the visitor ends up. Record the finding in the plan SUMMARY in plain words.

    If the route is already inside `AuthGuard` (it is, as of this branch's HEAD), change no behaviour. Extend the existing block comment above the `/logs/:connectionId` route with two sentences stating that this route must stay inside `AuthGuard` because a log transcript is the owner's own game text, that this placement is the owner's recorded condition on AR-4-08, and that moving the route outside the guard is a security regression, not a refactor. Add nothing else to this file.

    If — and only if — reading the code shows a signed-out visitor can reach `LogsPage`, move the route inside `AuthGuard` (or add the guard around it), keeping the standalone-render behaviour the existing comment protects, and say plainly in the SUMMARY that the guard was missing and was added.

    Add no test framework, no test file and no dependency. There is no frontend test framework in this repository and none is being introduced; this route's proof is the source assertion below plus the private-window screenshot plan 05-11 files.

    Do not sign the owner out to check anything. Do not open a private window in this task — that capture belongs to plan 05-11's staging walkthrough.
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` in `frontend/` exits 0
    - Source assertion: running `grep -n "AuthGuard" frontend/src/App.tsx` and `grep -n "logs/:connectionId" frontend/src/App.tsx` shows the route's line number falling between the opening `<AuthGuard>` and closing `</AuthGuard>` line numbers
    - Source assertion: `grep -c "AR-4-08" frontend/src/App.tsx` returns 1 (the pin names the register row it protects)
    - Source assertion: `git diff -- frontend/src/App.tsx` contains no change other than comment lines, unless the SUMMARY states that the guard was missing and was added
    - The SUMMARY states, from reading the code, what a signed-out visitor to `/logs/<id>` actually sees and where they are sent
    - The SUMMARY records that the owner was not signed out and that no private-window capture was attempted in this plan
  </acceptance_criteria>
  <done>The log page's place behind the sign-in guard is written down in the code that enforces it, with the register row it answers named beside it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Process environment → the credential vault | Whatever the environment says decides whether saved MUD passwords are really encrypted |
| Anonymous visitor → the log page | A log page URL is a plain link that can be pasted anywhere |
| Startup logs → readers | The startup path handles key material and must never print any of it |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation |
|-----------|----------|----------|-----------|-------------|------------|
| T-5-10 | Tampering (silent credential loss) | Medium | `DefaultKeyStore` inventing a random key on any non-Railway host, breaking every saved password at each restart | mitigate | 05-03-01 requires the key on every host; the only opt-out is an explicitly named variable and it is announced in the log |
| T-5-11 | Information Disclosure | High | a signed-out visitor reading another person's game transcript through a pasted log link | mitigate | 05-03-02 confirms and pins the route inside `AuthGuard`; plan 05-11 files the private-window screenshot as AR-4-08's evidence |
| T-5-12 | Information Disclosure | High | a key value reaching a log line, a test fixture or a committed report | mitigate | The announcement line names only the variable; the acceptance criteria grep the test file for key-shaped strings |
| T-5-13 | Denial of Service (self-inflicted) | Low | the stricter gate stopping staging because `ENCRYPTION_KEY_V1` was never set there | accept | The variable is already set on staging (Phase 4); plan 05-11's deploy task states what a missing-key startup failure looks like and how to fix it without printing the value |
| T-5-SC | Tampering (supply chain) | Low | `go.mod`, `frontend/package.json` | accept | This plan installs nothing |
</threat_model>

<verification>
1. `go build ./...` exits 0 and `go vet ./internal/config/...` is clean.
2. `go test ./internal/config/... -count=1 -v` exits 0 with both new PASS lines.
3. `grep -c "RAILWAY_ENVIRONMENT" internal/config/config.go` returns 0.
4. `npm run build` in `frontend/` exits 0.
5. The `/logs/:connectionId` route's line number falls inside the `AuthGuard` element in `frontend/src/App.tsx`.
6. `git diff --stat go.mod frontend/package.json` is empty.
</verification>

<success_criteria>
- DR-4-04 is closed in code: the vault key is required on every host and the only way past it is a named local-development setting that announces itself — proven by the `TestLoadRequiresEncryptionKeyEverywhereUnlessLocalDev` and `TestLocalDevOptOutIsAnnounced` PASS lines (D-26).
- AR-4-08's owner condition has an answer in the code: the log page renders only inside the sign-in guard, pinned by a comment naming the register row — proven by the source assertion here, with the signed-out screenshot `evidence/13-logs-signed-out.png` filed by plan 05-11 from a private window (D-27).
</success_criteria>

<output>
Create `.planning/phases/05-coaching-channel/05-03-SUMMARY.md` when done, recording the exact startup error text and announcement line (no values), the accepted spellings of the opt-out, what a signed-out visitor to `/logs/<id>` actually gets according to the code, whether the guard had to be added or was already present, and a plain statement that the owner was not signed out.
</output>
