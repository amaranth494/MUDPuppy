---
phase: 05-coaching-channel
plan: 07
type: execute
wave: 5
depends_on: [05-04, 05-06]
files_modified:
  - frontend/src/types/index.ts
  - frontend/src/services/api.ts
  - frontend/src/context/SessionContext.tsx
  - frontend/src/index.css
  - frontend/src/components/AIAssistPanel.tsx
autonomous: true
requirements: [REQ-coaching-chat, REQ-pause-resume]

must_haves:
  truths:
    - "The AI panel is one panel with two views at once: AI-player's thinking stream on top, the owner's conversation with AI-chatter below it, no tabs and no second panel, in the same 380 by 760 geometry Phase 4 left (D-03), proven by evidence/17-panel-two-views.png"
    - "The owner's messages and AI-chatter's replies appear only in the conversation area and never in the thinking stream, and the thinking stream's own decisions are unchanged (D-04), proven by evidence/17-panel-two-views.png and the unchanged Phase 3/4 decision rendering beside it"
    - "The message box works whenever the panel shows — autopilot On, Off or Waiting — and typing in it never takes the wheel (D-06, D-16), proven by evidence/08-chat-while-off.png"
    - "The owner can see everything that reached AI-player: the reply quotes the line sent, the read-only Coaching in effect list beside Session Memory shows the current set and survives a refresh, and the thinking stream shows [Coaching received] at the moment it happened (D-05, D-09), proven by evidence/06-coaching-in-next-decision.png and evidence/09-coaching-after-refresh.png"
    - "Pause and Resume are one button in the AI-player part of the panel, greyed out rather than hidden when autopilot is fully off, the status line says why the switch is waiting, and the stream prints [Autopilot paused] in warning yellow and [Autopilot resumed] in neutral grey (D-13, D-15, 05-UI-SPEC Copywriting Contract and Color), proven by evidence/18-paused-waiting-reason.png"
    - "A chat message that cannot be sent says so instead of vanishing: when the socket is not open or the send throws, the conversation shows [Message failed to send — try again] in red and the owner's draft is not silently lost (05-UI-SPEC Copywriting Contract), proven by the source assertions here and by the reviewed send path recorded in the SUMMARY"
    - "Session Memory is still read-only and chat messages still carry no buttons or actions of any kind (D-07, D-19), proven by the absence of any edit or per-message control in evidence/17-panel-two-views.png"
  artifacts:
    - path: "frontend/src/services/api.ts"
      provides: "Chat send and receive over the existing websocket with a truthful success answer, the pause and resume actions, and the coaching and conversation reloads"
      contains: "sendChat"
    - path: "frontend/src/index.css"
      provides: "Every new class this phase needs, copied verbatim from the UI contract, plus the two new .ai-system-line state colours for the pause and resume notices"
      contains: ".ai-assist-chat"
    - path: "frontend/src/components/AIAssistPanel.tsx"
      provides: "The split panel: pause row, coaching list, conversation area and message box, with the send-failure notice"
      contains: "Message AI-chatter"
  key_links:
    - from: "frontend/src/components/AIAssistPanel.tsx"
      to: "the chat websocket message"
      via: "sendChat on submit and an onChat subscription appending to the conversation"
      pattern: "onChat\\("
    - from: "frontend/src/components/AIAssistPanel.tsx"
      to: "POST /api/v1/session/autopilot"
      via: "the pause and resume actions behind one button"
      pattern: "'pause'|'resume'"
    - from: "frontend/src/components/AIAssistPanel.tsx"
      to: "GET /api/v1/profiles/{connection_id}/ai-coaching and ai-conversation"
      via: "a reload on attach so a refresh restores both lists"
      pattern: "ai-coaching|getCoaching"
    - from: "the server's paused and resumed system events"
      to: "frontend/src/index.css .ai-system-line.state-paused and .state-resumed"
      via: "the existing handleAI system-line branch, which needs no new code — only the two colour rules"
      pattern: "state-paused|state-resumed"
---

<objective>
Put the two levels on screen. Everything this phase built on the server — the conversation, the standing coaching, the pause with its reason and its two notices — is invisible until the panel shows it. D-03 puts it all in the one AI Assist panel the owner already has, as a split with both views visible at once, in the exact geometry Phase 4 left.

Purpose: this is ROADMAP criterion 4's docked half and the only surface criteria 1 and 2 can be observed from. The UI contract `05-UI-SPEC.md` is approved and locks the markup, the class names, the colours and every string; this plan copies it rather than re-deriving it.
Output: the chat and pause plumbing in the API client and the session context, every new CSS class this phase needs, and the panel itself — pause row, Coaching in effect list, conversation area and message box.

There is no frontend test framework in this repository and none is being added. These tasks verify with `npm run build` and source assertions; their behavioural proof is plan 05-11's staging screenshots.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/ROADMAP.md
@.planning/phases/05-coaching-channel/05-CONTEXT.md
@.planning/phases/05-coaching-channel/05-UI-SPEC.md
@.planning/phases/05-coaching-channel/05-PATTERNS.md
@.planning/phases/05-coaching-channel/05-VALIDATION.md
@.planning/phases/05-coaching-channel/05-01-SUMMARY.md
@.planning/phases/05-coaching-channel/05-05-SUMMARY.md
@.planning/phases/05-coaching-channel/05-06-SUMMARY.md

`05-UI-SPEC.md` is an approved contract. Where it gives markup, CSS or a string, copy it verbatim. Where this plan and the contract disagree, the contract wins and the SUMMARY records the discrepancy.

Evidence rule (owner-directed, binding): proof is a canned report, an `[AI-PLAYER]` log excerpt, or an end-user screenshot. A database query is never evidence.

Machine rules for the executor (inherit these, they are not optional):
- Never run `find /`, `find ~`, `find /c`, `find /d`, `ls -R` or any search outside `D:\Projects\MUDPuppy`. Never launch a command in the background. Use Glob/Grep scoped to the repo and exclude `node_modules`.
- The GSD CLI form on this machine is `gsd-sdk <command> <sub>`; `gsd-sdk query ...` fails.
- Use the Write/Edit tools for every multi-line file; bash heredocs with backticks or quotes fail in this shell.
- Deploy to staging only with `railway up`. No deploy happens in this plan.
- Never sign the owner out of any session.
- No package may be added to `frontend/package.json` or `go.mod` anywhere in this phase. No test framework is to be introduced, including for the pop-out mechanism in plan 05-08.
- `.specify/` specs other than `ai-game-player-design-v3.md`, `safety-and-abuse-policy-v1.md`, `ai-memory-model-v1.md` and `phase-based-development-approach.md` are excluded.

Claude's Discretion settled by the planner for this plan:
- This plan adds **every** new CSS class Phase 5 needs, including the ones plans 05-08 and 05-09 will use (`.ai-assist-chat-header`, `.ai-assist-popout-placeholder`, `.logs-conversation*`), so that no later plan has to reopen `index.css` and the phase's whole stylesheet change lands in one reviewable diff.
- Chat travels over the websocket (`sendChat`/`onChat`/`offChat` on `WebSocketManager`); the coaching list and the conversation reload over REST on attach; pause and resume are two more `action` values on the existing autopilot POST helper.
- Coaching-in-effect state lives in `AIAssistPanel.tsx` as flat `useState`, like `sessionMemory` does today. It is refreshed live from the `session_memory`-style push carried on the chat or decision message, and reloaded from REST on attach and on connection change.
- **The bracket convention (verified in code; the same one plans 05-01, 05-05 and 05-06 record).** The panel already supplies the square brackets around every system line: `handleAI` stores a system event as ``message: `[${payload.message ?? ''}]` ``, and `05-UI-SPEC.md` §1's chat JSX renders a system chat line as `` `[${entry.text}]` ``. So every locked notice this plan renders is held **unbracketed** in state and reads bracketed on screen. Do not add a second pair anywhere, and do not strip the existing one.
- **The pause and resume notices need no new rendering code.** Plan 05-01 emits them as ordinary `kind: 'system'` AI events with `outcome: 'paused'` and `outcome: 'resumed'` and the unbracketed messages `Autopilot paused` and `Autopilot resumed`. `handleAI`'s existing system branch already turns any such event into `<div className={\`ai-system-line state-${entry.outcome}\`}>`, so all this plan owes them is two colour rules (task 05-07-02) and a check that they arrive (task 05-07-03). Confirm the two `outcome` strings against `05-01-SUMMARY.md` before writing the CSS; if they differ, the SUMMARY's strings win and this plan's SUMMARY records the discrepancy.
</context>

<tasks>

<task type="auto">
  <name>Task 05-07-01: The browser can send a chat message and know whether it went, pause the player, and reload what is in effect</name>
  <files>frontend/src/types/index.ts, frontend/src/services/api.ts, frontend/src/context/SessionContext.tsx</files>
  <read_first>
    - frontend/src/services/api.ts lines 95-200 (`WebSocketManager`'s handler arrays, its `handleMessage` switch including `case 'autopilot'` and `case 'ai'`, and how a handler list is invoked), the socket field and how `sendCommand` writes to it (including whatever readiness check it already makes, if any), lines 245-275 (`onAI`/`offAI` and `onAutopilot`/`offAutopilot` registration pairs), lines 640-690 (`getAISettings`/`putAISettings`'s fetch shape with `credentials: 'include'`, `handleAuthError` and the throw-on-not-ok convention) and lines 825-850 (the autopilot POST helper and its `action` body)
    - frontend/src/types/index.ts (the existing payload and response types, including the AI decision payload type and the AI settings type plan 05-04 extended)
    - frontend/src/context/SessionContext.tsx lines 25-100 (the context's exported shape and its `autopilotState` state) and lines 230-300 (the `refreshStatus` poll that sets `autopilotState`, and the `onAI`/`offAI` registration effect with its cleanup)
    - internal/session/websocket.go as left by plans 05-01 and 05-05 (the `chat` message type, `ChatPayload`'s exact JSON field names, and the `paused_by_owner`/`connection_lost` fields on the autopilot message) — the browser types must match these names exactly
    - internal/profiles/handler.go as left by plan 05-06 (`GetCoaching` and `GetConversation`'s response shapes and route paths)
    - .planning/phases/05-coaching-channel/05-UI-SPEC.md §1 and §3 (the conversation entry shape the panel renders and the pause button's states) and the Copywriting Contract row "Chat — send failed"
  </read_first>
  <action>
    In `frontend/src/types/index.ts`, add types matching the server's JSON exactly: a chat line type with an id, a speaker union of `owner`, `chatter` and `system`, text, an optional state union of `cap` and `failed`, and a timestamp; a coaching response type with a `coaching` string array; and a conversation response type with a `lines` array of chat lines. Add `paused_by_owner` and `connection_lost` booleans to whatever type models the autopilot websocket message and the autopilot REST response.

    In `frontend/src/services/api.ts`:
    - Extend `WebSocketManager` with a `chatHandlers` array and the `onChat`/`offChat` registration pair, copying `onAI`/`offAI` line for line, and add `case 'chat':` to `handleMessage`'s switch, dispatching the message's chat payload to the registered handlers.
    - Add `sendChat(text: string): boolean` to `WebSocketManager`. It writes `{ type: 'chat', data: text }` over the socket in the same way `sendCommand` writes a data message, and it **answers truthfully whether the message left the browser**: return `false` without throwing when the socket is missing or its `readyState` is not `OPEN`, and return `false` from a `catch` around the `send` call; return `true` only after the write actually happened. Add a comment saying that this deliberately does not go through the command path, because chat is not a game command and is not a wheel-grab (D-16), and a second comment saying that the boolean exists so the panel can show the locked send-failure notice rather than letting the owner's message disappear.
    - Extend the autopilot POST helper so it accepts `'pause'` and `'resume'` alongside the existing actions, returning the full response including the two new booleans. Do not add a second helper.
    - Add `getCoaching(connectionId)` and `getConversation(connectionId)` as GET-only functions hitting `/profiles/${connectionId}/ai-coaching` and `/profiles/${connectionId}/ai-conversation`, copying `getAISettings`'s fetch shape, `credentials: 'include'`, `handleAuthError` call and throw-on-not-ok exactly.
    - Extend the autopilot websocket handler signature so the two waiting-reason booleans reach subscribers, or carry them on the existing message object — follow whichever the current handler shape makes least invasive and say which in the SUMMARY.

    In `frontend/src/context/SessionContext.tsx`, carry the two waiting reasons beside `autopilotState`: set them from `refreshStatus`'s poll and from the autopilot websocket push, expose them on the context value, and reset them when the state goes to `off`. Change nothing about how `autopilotState` itself is derived, and add no chat state here — the conversation lives in the panel, next to the thinking stream it belongs beside.
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` in `frontend/` exits 0 with no type error
    - Source assertion: `grep -c "onChat" frontend/src/services/api.ts` returns at least 2 and `grep -c "offChat" frontend/src/services/api.ts` returns at least 2
    - Source assertion: `grep -c "case 'chat':" frontend/src/services/api.ts` returns 1
    - Source assertion: `grep -c "sendChat" frontend/src/services/api.ts` returns at least 1, the declaration's return type is `boolean`, and the `sendChat` body contains no call to `sendCommand`
    - Source assertion: the `sendChat` body contains a readiness check and a `catch` — `grep -c "readyState" frontend/src/services/api.ts` returns at least 1 and `awk '/sendChat/,/^  }/' frontend/src/services/api.ts | grep -cE "readyState|catch"` returns at least 2
    - Source assertion: `grep -c "ai-coaching" frontend/src/services/api.ts` returns 1 and `grep -c "ai-conversation" frontend/src/services/api.ts` returns 1
    - Source assertion: `grep -c "paused_by_owner" frontend/src/types/index.ts` returns at least 1 and `grep -c "pausedByOwner\|paused_by_owner" frontend/src/context/SessionContext.tsx` returns at least 1
    - Source assertion: `git diff -- frontend/src/context/SessionContext.tsx | grep -c "chatEntries\|chatDraft"` returns 0 (conversation state does not live in the context)
    - `git diff --stat frontend/package.json` is empty
  </acceptance_criteria>
  <done>Everything the panel needs is reachable from the browser: a message can be sent and the browser knows whether it went, the switch can be paused and resumed, and both lists can be fetched back after a refresh.</done>
</task>

<task type="auto">
  <name>Task 05-07-02: The stylesheet gains this phase's classes, and not one existing rule changes</name>
  <files>frontend/src/index.css</files>
  <read_first>
    - frontend/src/index.css `.ai-assist-panel`, `.ai-assist-panel-header`, `.ai-assist-panel-top`, `.ai-assist-panel-body`, `.ai-assist-memory*`, `.ai-assist-empty`, `.ai-system-line*` and its `state-*` colour classes (note `state-waiting` is `var(--color-warning)` and `state-goal`/`state-disengaged` are `#888`), `.form-group`/`.form-label`/`.form-input`/`.form-hint`, `.btn`/`.btn-sm`/`.btn-secondary`/`.btn-primary`/`.btn:disabled`, `.logs-transcript*` and the `--color-*` and `--spacing-*` custom properties — everything this phase reuses and must not alter
    - .planning/phases/05-coaching-channel/05-01-SUMMARY.md — the two `Outcome` strings the server sends on the pause and resume system events (expected `paused` and `resumed`); the two new state classes must match them character for character
    - .planning/phases/05-coaching-channel/05-UI-SPEC.md §1, §2, §3, §6 and §7 in full — every CSS block in them is copied verbatim into this file
    - .planning/phases/05-coaching-channel/05-UI-SPEC.md § Spacing Scale, § Typography, § Color and § Regression Invariant — the rules these additions must not break, in particular Color's rule 5 (yellow = waiting, in progress → the pause notice) and rule 4 (neutral `#888` = the owner's own action → the resume notice)
  </read_first>
  <action>
    Append this phase's new CSS rules to `frontend/src/index.css`, copied verbatim from `05-UI-SPEC.md`, grouped under one comment block naming Phase 5 and the decisions they serve:
    - From §1: `.ai-assist-chat` (fixed `height: 240px`, `flex-shrink: 0`, column flex, top border, background), `.ai-assist-chat-log`, `.ai-assist-chat-empty`, `.ai-assist-chat-line` with its `.speaker-owner`, `.speaker-chatter` and `.speaker-system.state-cap`/`.state-failed` variants, and `.ai-assist-chat-input-row` with its `.form-input { flex: 1 }` rule. The `.ai-assist-panel-body` rule is **not** edited — the spec's note that it now shares height with the chat strip is satisfied by its existing `flex: 1`.
    - From §3: `.ai-assist-pause-row`.
    - From §7: `.ai-assist-chat-header` and `.ai-assist-popout-placeholder`. These belong to plan 05-08's pop-out work; they land here so `index.css` is opened once this phase.
    - From §6: `.logs-conversation`, `.logs-conversation h3`, `.logs-conversation-line` with its `.human` and `.chatter` variants, and `.logs-conversation-empty`. These belong to plan 05-09; same reason.
    - **Two new `.ai-system-line` state variants for the pause and resume notices (D-15, § Color rules 4 and 5).** Add them beside the existing `state-*` colour rules, in the same one-line form those use, and nowhere else:
      `.ai-system-line.state-paused { color: var(--color-warning); }` — pausing is a WAITING state, the same bucket as the existing `state-waiting` rule.
      `.ai-system-line.state-resumed { color: #888; }` — resuming is the owner's own action, the same bucket and the same literal as the existing `state-goal` and `state-disengaged` rules.
      These are the whole of this phase's rendering work for `[Autopilot paused]` and `[Autopilot resumed]`: the markup, the bracketing and the text all come from the existing `handleAI` system-line path. Name both class suffixes in the SUMMARY and confirm they match `05-01-SUMMARY.md`'s `Outcome` strings.
    - No `.ai-assist-coaching*` block is needed: the Coaching in effect list reuses `.ai-assist-memory*` exactly as the spec permits. Say so in the comment block.

    Introduce no new CSS custom property. Every colour is one of the existing tokens named in the spec's Color table, used in the bucket that table assigns it: cyan for the owner's own lines, dim green for AI-chatter's replies, red for the cap and send-failure and pop-up-blocked notices, neutral `#888` for the coaching-received marker, the resume notice and the pop-out placeholders, yellow for the pause notice and the waiting text. Add no animation, no transition, no filled background and no accent-coloured border anywhere in the chat region.

    Change no existing rule in the file. If a new rule appears to need an existing one changed, stop and record the conflict in the SUMMARY rather than editing the existing rule.
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` in `frontend/` exits 0
    - Source assertion: `grep -c "^\.ai-assist-chat {" frontend/src/index.css` returns 1 and `grep -c "height: 240px" frontend/src/index.css` returns 1
    - Source assertion: each of `.ai-assist-chat-log`, `.ai-assist-chat-empty`, `.ai-assist-chat-line`, `.ai-assist-chat-input-row`, `.ai-assist-pause-row`, `.ai-assist-chat-header`, `.ai-assist-popout-placeholder`, `.logs-conversation`, `.logs-conversation-line` and `.logs-conversation-empty` appears at least once
    - Source assertion: `grep -c "\.ai-system-line\.state-paused" frontend/src/index.css` returns 1 and `grep -c "\.ai-system-line\.state-resumed" frontend/src/index.css` returns 1, the first resolving to `var(--color-warning)` and the second to the neutral `#888`
    - Source assertion: `git diff -- frontend/src/index.css | grep -c "^-"` returns 0 apart from the diff header line (nothing existing was removed or rewritten)
    - Source assertion: `git diff -- frontend/src/index.css | grep -cE "^\+.*(--color-[a-z-]+:|@keyframes|animation:|transition:)"` returns 0 (no new custom property, no animation)
    - Source assertion: `git diff -- frontend/src/index.css | grep -cE "^\+.*#[0-9a-fA-F]{3,6}"` returns 0 except for the `#888`-family neutral the spec's Color table already reserves, and the SUMMARY names every literal used
    - `git diff --stat frontend/package.json` is empty
  </acceptance_criteria>
  <done>Every class this phase's markup will ask for exists, in the tokens and buckets the approved contract assigns, the pause and resume notices have their two colours, and nothing that was already there moved.</done>
</task>

<task type="auto">
  <name>Task 05-07-03: One panel, two views — the stream on top, the conversation below, Pause and Coaching in effect beside the goal</name>
  <files>frontend/src/components/AIAssistPanel.tsx</files>
  <read_first>
    - frontend/src/components/AIAssistPanel.tsx in full (401 lines): the `useState` block at lines 49-87 (`collapsed`, `entries`, `callCount`, `callCap`, `failureCount`, `blockCount`, `memoryOpen`, `sessionMemory`), the `onAI`/`offAI` effect and its `handleAI` switch — including the system-line branch around line 277 that stores ``message: `[${payload.message ?? ''}]` `` and the render at line 393 that emits `<div className={\`ai-system-line state-${entry.outcome}\`}>` — the `.ai-assist-panel-header` with its Minimize control, the `.ai-assist-panel-top` region at lines 318-364 (goal box, status line, Session Memory section), and the `.ai-assist-panel-body` decision stream below it
    - frontend/src/services/api.ts and frontend/src/types/index.ts as left by task 05-07-01 (`sendChat` and its boolean, `onChat`/`offChat`, `getCoaching`, `getConversation`, the autopilot helper's `pause`/`resume` actions, the chat line type)
    - frontend/src/context/SessionContext.tsx as left by task 05-07-01 (`autopilotState` and the two waiting-reason booleans on the context value)
    - .planning/phases/05-coaching-channel/05-01-SUMMARY.md (the two system-event `Outcome` strings, so the arrival check names the right ones)
    - .planning/phases/05-coaching-channel/05-UI-SPEC.md §1, §2 and §3 in full — the JSX blocks, the class names and every string, all locked
    - .planning/phases/05-coaching-channel/05-UI-SPEC.md § Copywriting Contract in full — the exact strings this task renders, including "Chat — send failed"
    - .planning/phases/05-coaching-channel/05-UI-SPEC.md § Regression Invariant — what must not move, resize or restyle
  </read_first>
  <action>
    Extend `AIAssistPanel.tsx` with three additions inside the structure it already has. Copy §1, §2 and §3's JSX from the UI contract; do not re-derive markup.

    **1. The pause row (§3), inside `.ai-assist-panel-top`, directly under the goal box and above the status line.** One `.btn.btn-sm.btn-secondary` button whose visible text is `Pause` when the owner has not paused and `Resume` when he has, with `aria-label` and `title` reading `Pause AI-player` and `Resume AI-player` to match, `disabled` when `autopilotState === 'off'` — rendered disabled, never hidden. Clicking it calls the autopilot helper with `'pause'` or `'resume'` and applies the returned state and waiting reasons immediately; it takes no confirmation step. Beside it, when a reason is present, render the reason text in an `.ai-assist-status-line` span: exactly `Paused by owner` when only that reason stands, exactly `Connection lost` when only that one does, and exactly `Paused by owner · Connection lost` when both do.

    **2. The Coaching in effect list (§2), inside `.ai-assist-panel-top`, directly below the Session Memory section.** A byte-for-byte sibling of the Session Memory block reusing the same `.ai-assist-memory*` classes: a `.ai-assist-memory-header` button with the disclosure glyph `▾` when open and `▸` when closed, the label `Coaching in effect ({count})`, the empty state `No coaching in effect.`, and `.ai-assist-memory-item` list items. Default collapsed, read-only — no edit control, no delete control, no clear button, no per-item action of any kind. Load it with `getCoaching` on attach and whenever the connection changes, and update it live from whatever the server pushes alongside a chat or decision message. Leave the Session Memory section exactly as it is: still read-only, no editor is added anywhere (D-19).

    **3. The conversation (§1), as a new `.ai-assist-chat` region below `.ai-assist-panel-body`.** A scrolling `.ai-assist-chat-log` rendering the conversation as flat lines — `You: {text}` for an owner line, `AI-chatter: {text}` for a chatter line, `[{text}]` for a system line with its state class — with the empty state `No messages yet. Say something to AI-chatter.`, followed by a `.ai-assist-chat-input-row` holding a `.form-input` with the placeholder `Message AI-chatter…` and a `.btn.btn-sm.btn-primary` labelled `Send` with `aria-label="Send message to AI-chatter"`. Enter sends. The button is disabled only when the draft is empty or a send is in flight — **never** by `autopilotState` (D-06). Subscribe with `onChat` on mount and unsubscribe with `offChat` on unmount, mirroring the existing `onAI`/`offAI` effect exactly, and load the history with `getConversation` on attach. Scroll the log to the newest line when a line arrives. Render no button, icon or menu on any chat message (D-07).

    **The send that does not go (05-UI-SPEC Copywriting Contract, row "Chat — send failed").** Sending calls `sendChat` and uses its boolean answer. On `true`, clear the draft as normal. On `false` — the socket is not open, or the write threw — do **not** clear the draft, and append one local system entry to the chat log with `speaker: 'system'`, `state: 'failed'` and the text `Message failed to send — try again` (unbracketed: the `[{text}]` render above supplies the brackets, so the owner reads the locked `[Message failed to send — try again]`). This entry is local to the browser and is never sent to the server; it carries no id from the database, so give it a locally generated key in the same way any other client-only entry would be keyed. Append at most one such entry per failed attempt. It never calls the command path.

    Owner messages and AI-chatter replies go **only** into `.ai-assist-chat`. Do not add them to `entries`, and do not change `handleAI`'s existing branches.

    **Confirm, do not build, the three system notices.** `[Coaching received]` (plan 05-06) and `[Autopilot paused]` / `[Autopilot resumed]` (plan 05-01) all arrive as ordinary `kind: 'system'` AI events with outcomes `coaching-received`, `paused` and `resumed` and unbracketed messages. The existing `handleAI` system branch already brackets them and the existing render already emits `.ai-system-line.state-${outcome}`, whose three colour rules task 05-07-02 has now supplied. So add **no** new branch and **no** new string for any of them. Verify by reading `handleAI` that a system event with an unrecognised outcome falls through that branch rather than being dropped by a `switch` on `outcome`; if it would be dropped, that — and only that — is a change this task must make, and the SUMMARY must record it.

    Change nothing else: not the panel's geometry, not the header, not the Minimize control, not the goal box, not the status line's existing segments, not the Session Memory section, and not one decision card in the stream.
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` in `frontend/` exits 0 with no type error
    - Source assertion: each of these strings appears exactly once in `AIAssistPanel.tsx` — `Message AI-chatter…`, `No messages yet. Say something to AI-chatter.`, `No coaching in effect.`, `Send message to AI-chatter`, `Paused by owner`, `Connection lost`, `Paused by owner · Connection lost`, `Message failed to send — try again`
    - Source assertion: `grep -cE '\[Message failed to send' frontend/src/components/AIAssistPanel.tsx` returns 0 (the render supplies the brackets)
    - Source assertion: `grep -c "Coaching in effect (" frontend/src/components/AIAssistPanel.tsx` returns 1
    - Source assertion: `grep -c "disabled={autopilotState === 'off'}" frontend/src/components/AIAssistPanel.tsx` returns 1 and the chat Send button's `disabled` expression contains no reference to `autopilotState`
    - Source assertion: `grep -c "sendChat(" frontend/src/components/AIAssistPanel.tsx` returns at least 1 and `grep -c "sendCommand(" frontend/src/components/AIAssistPanel.tsx` returns 0
    - Source assertion: the `sendChat` call site branches on its return value — `awk '/sendChat\(/,/}/' frontend/src/components/AIAssistPanel.tsx | grep -cE "if|\?" ` returns at least 1, and the SUMMARY quotes the branch
    - Source assertion: `grep -c "onChat(" frontend/src/components/AIAssistPanel.tsx` returns 1 and `grep -c "offChat(" frontend/src/components/AIAssistPanel.tsx` returns 1
    - Source assertion: `grep -cE "'(paused|resumed|coaching-received)'" frontend/src/components/AIAssistPanel.tsx` returns 0 — the three server notices are rendered by the existing generic system-line path, with no per-outcome branch added
    - Source assertion: `git diff -- frontend/src/components/AIAssistPanel.tsx | grep -cE "^-.*(ai-assist-panel-body|ai-assist-memory-header|Session Memory|Session Goal)"` returns 0 (nothing existing was removed or rewritten)
    - Source assertion: `grep -ciE "contentEditable|<textarea|onEdit|Delete|Clear|Promote|Copy" frontend/src/components/AIAssistPanel.tsx` returns 0 for the coaching and chat regions — no per-message action, no coaching clear, no memory editor (D-07, D-19, deferred ideas)
    - `git diff --stat frontend/package.json` is empty and `git diff --stat frontend/src/index.css` is empty (the stylesheet was finished in task 05-07-02)
  </acceptance_criteria>
  <done>The owner opens the play screen and sees one panel with the AI playing on top and a conversation underneath, a Pause button by the goal, a list of exactly what he has told it so far, and a plain red line the one time a message does not get out.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Owner's keystrokes → chat versus the game | Two input boxes on one screen; only one of them may ever reach the MUD |
| Server push → the panel's rendering | Model-authored and game-derived text is rendered in the owner's browser |
| Panel → the new REST reads | Coaching and conversation are the owner's own text and must stay behind his session |
| The browser's own view of the socket → the owner's belief | What the panel shows must match what actually left the machine |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation |
|-----------|----------|----------|-----------|-------------|------------|
| T-5-32 | Tampering (wrong input path) | High | a chat message reaching the MUD, or a typed game command being swallowed by the chat box | mitigate | `sendChat` writes the chat message type and never calls `sendCommand`; the acceptance criteria assert `sendCommand` does not appear in the panel; the terminal's own input is untouched |
| T-5-33 | Elevation of Privilege (rendered markup) | Medium | model or game text rendered as markup in the conversation or the coaching list | mitigate | Every new line is rendered as a React text child, never through `dangerouslySetInnerHTML`; the acceptance criteria grep for edit and action controls, and the SUMMARY records that no raw-HTML render was introduced |
| T-5-34 | Information Disclosure | Medium | the coaching and conversation reads served to a visitor who is not the owner | mitigate | Both new API functions send `credentials: 'include'` and run `handleAuthError`; the server handlers reuse the existing ownership check unchanged (plan 05-06) |
| T-5-35 | Denial of Service (usability) | Low | a disabled message box making the owner think chat is broken while autopilot is off | mitigate | The Send button is disabled only by an empty draft or an in-flight send, never by `autopilotState` (D-06), asserted in the acceptance criteria |
| T-5-36 | Repudiation (invisible coaching) | Medium | a suggestion in effect with no visible trace after a refresh | mitigate | The Coaching in effect list reloads from REST on attach and on connection change; `evidence/09-coaching-after-refresh.png` is its staging proof |
| T-5-57 | Repudiation (silent loss) | Medium | a chat message the owner believes he sent that never left the browser, because the socket was closed and the draft was cleared anyway | mitigate | `sendChat` returns a truthful boolean; a false answer keeps the draft and appends the locked `[Message failed to send — try again]` line in red; asserted by the source criteria in task 05-07-03 |
| T-5-SC | Tampering (supply chain) | Low | `frontend/package.json` | accept | This plan installs nothing and adds no test framework |
</threat_model>

<verification>
1. `npm run build` in `frontend/` exits 0 after each task.
2. Every locked string from `05-UI-SPEC.md`'s Copywriting Contract that this plan renders appears verbatim in the source, asserted by the greps above, and none of them carries its own outer brackets.
3. `git diff -- frontend/src/index.css | grep -c "^-"` shows no removed rule; no new CSS custom property and no animation was added; `.ai-system-line.state-paused` and `.state-resumed` both exist.
4. `sendCommand` does not appear in `AIAssistPanel.tsx`, `onChat`/`offChat` are registered and unregistered as a pair, and the `sendChat` call site branches on its boolean.
5. No per-outcome branch was added for `paused`, `resumed` or `coaching-received` — they ride the existing generic system-line path.
6. No edit, delete, clear, promote or copy control exists on any chat message, on the coaching list or on Session Memory.
7. `git diff --stat frontend/package.json go.mod` is empty.
</verification>

<success_criteria>
- ROADMAP Phase 5 criterion 4's docked half is on screen: one panel, two views, the conversation below the stream, with every pushed suggestion visible three ways — proven by `evidence/17-panel-two-views.png` and `evidence/06-coaching-in-next-decision.png` (D-03, D-04, D-05, D-09).
- Criterion 2's control exists where D-13 put it, says why the switch is waiting, and prints the phase's locked pause and resume notices in the stream — proven by `evidence/18-paused-waiting-reason.png` (D-13, D-15).
- The message box works with autopilot off, typing in it never takes the wheel, and a message that cannot be sent says so — proven by `evidence/08-chat-while-off.png` and the send-failure source assertions (D-06, D-16).
- Chat is plain text with no functionality and Session Memory is still read-only — proven by the absence of any control in the screenshots and by the source assertions here (D-07, D-19).
</success_criteria>

<output>
Create `.planning/phases/05-coaching-channel/05-07-SUMMARY.md` when done, recording every locked string rendered and where, the confirmation that none of them was written with its own brackets, the two new `.ai-system-line` state classes and the `Outcome` strings they match, the CSS classes added and the one comment block they sit under, how the waiting reasons reach the panel (poll, push or both), where the coaching list's live updates come from, the exact `sendChat` failure branch (quoted), whether `handleAI` needed any change at all for the three system notices and what it was, any place the UI contract and the code disagreed and which won, and a plain statement that no test framework and no dependency was added.
</output>
</content>
