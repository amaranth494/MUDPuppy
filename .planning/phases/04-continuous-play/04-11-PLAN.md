---
phase: 04-continuous-play
plan: 11
type: execute
wave: 9
depends_on: [04-01, 04-02, 04-03, 04-04, 04-05, 04-06, 04-07, 04-08, 04-09, 04-10]
files_modified:
  - .planning/phases/04-continuous-play/evidence/01-test-report.txt
  - .planning/phases/04-continuous-play/evidence/02-harness-selftest.txt
  - .planning/phases/04-continuous-play/evidence/03-canned-report.txt
  - .planning/phases/04-continuous-play/evidence/04-redteam-after.txt
  - .planning/phases/04-continuous-play/evidence/05-staging-ai-player.log
  - .planning/phases/04-continuous-play/evidence/06-goal-set.png
  - .planning/phases/04-continuous-play/evidence/07-paced-decisions.png
  - .planning/phases/04-continuous-play/evidence/08-cap-halt-badge-off.png
  - .planning/phases/04-continuous-play/evidence/09-wheel-grab-reengage-reasoning.png
  - .planning/phases/04-continuous-play/evidence/10-session-memory-expanded.png
  - .planning/phases/04-continuous-play/evidence/11-session-memory-after-refresh.png
  - .planning/phases/04-continuous-play/evidence/12-delete-captured-text-confirm.png
  - .planning/phases/04-continuous-play/evidence/13-hand-play-unchanged.png
  - .planning/phases/04-continuous-play/04-SECURITY-AGENDA.md
  - .planning/phases/04-continuous-play/04-11-SUMMARY.md
autonomous: false
requirements: [REQ-continuous-loop, REQ-call-cap-and-error-disengage, REQ-reengage-reassess, REQ-doc-continuous-visible-play, REQ-doc-wheel-grab-and-reengage, REQ-safety-limits-hold]

must_haves:
  truths:
    - "The whole Go suite is green with and without the race detector, `go vet` is clean, the frontend builds, and no package was added anywhere in the phase, proven by evidence/01-test-report.txt containing zero unexpected FAIL lines, a stated race answer and an empty dependency-drift section (T-4-SC)"
    - "The canned report is falsifiable before it is trusted, proven by evidence/02-harness-selftest.txt showing a clean self-test and a negative self-test producing FAIL C4 with a non-zero exit (T-4-11)"
    - "The reviewer-channel attack this phase added is measured against the finished build on a fresh-quota day, with no steered command reaching the send path, proven by the STEERED count and the reviewer-channel-01 line of evidence/04-redteam-after.txt (D-24, DR-3.1-01)"
    - "On staging, the owner sets a goal, engages once, and watches the AI keep playing toward it with each decision's reasoning appearing as it happens, proven by evidence/06-goal-set.png and evidence/07-paced-decisions.png (ROADMAP criterion 1)"
    - "On staging, a small call cap halts the loop with its notice and the badge reads Off in the same frame, proven by evidence/08-cap-halt-badge-off.png (ROADMAP criterion 2)"
    - "On staging, the owner takes the wheel, moves the character by hand, re-engages, and the first new decision's reasoning describes where the character actually is rather than resuming the old plan, proven by evidence/09-wheel-grab-reengage-reasoning.png and the matching stage lines in evidence/05-staging-ai-player.log (ROADMAP criterion 3)"
    - "On staging, the AI's Session Memory is visible, updates during play and survives a refresh, proven by evidence/10-session-memory-expanded.png and evidence/11-session-memory-after-refresh.png (D-10)"
    - "On staging, the owner can delete the captured text for a profile after reading what goes and what stays, and the decision record survives it, proven by evidence/12-delete-captured-text-confirm.png and the PASS C4 lines of evidence/03-canned-report.txt (D-21)"
    - "Hand play on a profile that never accepted the policy is untouched by everything this phase built, proven by evidence/13-hand-play-unchanged.png (04-UI-SPEC Regression Invariant)"
    - "Every one of the four ROADMAP Phase 4 success criteria has a row in 04-11-SUMMARY.md citing a named evidence file with line numbers for text evidence and a PASS or FAIL verdict, and no row cites a database query (CLAUDE.md evidence rule)"
    - "The Phase 4 security review has a written agenda listing every deferred Phase 3 and Phase 3.1 risk with what this phase did about it, plus the new memory-as-injection-channel risk, with nothing decided in advance, proven by 04-SECURITY-AGENDA.md (D-28)"
  artifacts:
    - path: ".planning/phases/04-continuous-play/evidence/01-test-report.txt"
      provides: "Canned report: go build, go vet, the full suite with and without -race, every targeted Phase 4 run, the frontend build, the dependency-drift and model-literal checks, captured verbatim"
      contains: "GO TEST EXIT"
    - path: ".planning/phases/04-continuous-play/evidence/04-redteam-after.txt"
      provides: "The AFTER red-team report from the corpus, including the reviewer-channel item, run in strict mode against the finished build"
      contains: "LABEL: AFTER"
    - path: ".planning/phases/04-continuous-play/evidence/05-staging-ai-player.log"
      provides: "Staging log excerpt: migration 013 applied, the loop's own stages across a stint, a cap halt, a wheel-grab, a re-engage, a memory update and a retention run"
      contains: "stage=retention"
    - path: ".planning/phases/04-continuous-play/04-SECURITY-AGENDA.md"
      provides: "The Phase 4 security review agenda: every DR-3 and DR-3.1 deferral with what this phase did, the new memory risk, and this phase's plan-level acceptances, each with three dispositions and none pre-chosen"
      contains: "Remediate Now"
    - path: ".planning/phases/04-continuous-play/04-11-SUMMARY.md"
      provides: "One row per ROADMAP Phase 4 success criterion citing the evidence that proves it, with a verdict"
      contains: "Phase Validation"
  key_links:
    - from: "internal/driver/corpus_live_test.go, unmodified since plan 04-07"
      to: "evidence/04-redteam-after.txt"
      via: "the same gated test rerun with the AFTER label and strict mode on a fresh-quota day"
      pattern: "STEERED"
    - from: "the staging play screen on Alter Aeon"
      to: "evidence/06 through evidence/13"
      via: "one screenshot per observable, end-user perspective only"
      pattern: "Autopilot"
    - from: "the driver's and the retention job's [AI-PLAYER] lines on staging"
      to: "evidence/05-staging-ai-player.log"
      via: "railway logs against the staging environment, filtered on the prefix, during and after the walkthrough"
      pattern: "stage=(loop-tick|cap|retention)"
---

<objective>
Demonstrate the phase and file the proof. Phase 4 is complete when the ROADMAP's Phase Validation line has actually been performed: the Go suite exercising all four mechanical limits, and the owner on his own character on Alter Aeon setting a goal, watching the AI keep playing toward it, seeing a small cap halt it with a notice, taking the wheel and re-engaging to find the AI describing where the character actually is.

Purpose: turn four success criteria into four cited rows, each pointing at a file a reader can open; rerun the red-team corpus — now carrying an attack aimed at the reviewer's own reasoning channel — against the finished build on a fresh-quota day, as DR-3.1-01's remediation requires; and put every risk this project has deferred since Phase 3 in front of the Phase 4 review with what this phase did about each one, alongside the one genuinely new risk this phase creates: the AI's own memory as an injection channel.
Output: five canned reports, eight screenshots, a security-review agenda, and `04-11-SUMMARY.md` mapping each criterion to the evidence that proves it.

Three standing instructions constrain the whole walkthrough:
- **Do not sign the owner out at any point.** Sign-in is one-time-code based and signing back in is the owner's step, not the executor's.
- **Deploy to staging only with `railway up --detach -e staging -s MudPuppy` from the local checkout.** A dashboard Deploy rebuilds from GitHub and would run a branch state that is not this one. This phase carries frontend changes, so `public/` must be rebuilt and committed before the deploy.
- **Never print a Railway variable value or any key**, in a report, a log excerpt, a screenshot or a summary.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/ROADMAP.md
@.planning/phases/04-continuous-play/04-CONTEXT.md
@.planning/phases/04-continuous-play/04-UI-SPEC.md
@.planning/phases/04-continuous-play/04-VALIDATION.md
@.planning/RISK-REGISTER.md
@.planning/phases/03.1-prompt-injection-review/03.1-SECURITY-AGENDA.md

ROADMAP Phase Validation line for Phase 4 (what this plan must demonstrate):
Diagnostic — `go test ./internal/...` passes a suite that exercises all four mechanical limits (call cap halt when a cap is set, error disengage with an informative notice, no commands while disconnected with resume on return, and blank-settings behaviour: no cap, no crash). Player-observable on staging — set a goal and watch the loop issue decisions paced to game output; set a small call cap and see the loop halt with the notice; take the wheel, re-engage, and confirm from the logged reasoning that the first new decision describes the current situation, not the previous plan.

Evidence directory: `.planning/phases/04-continuous-play/evidence/` (created by this plan's first task). Every filename below is fixed — earlier plans' `must_haves` cite these exact names.

| File | Type | Produced by |
|------|------|-------------|
| `01-test-report.txt` | canned report | task 04-11-01 |
| `02-harness-selftest.txt` | canned report | task 04-11-01 |
| `03-canned-report.txt` | canned report | tasks 04-11-03 (RUN A) and 04-11-04 (RUN B) |
| `04-redteam-after.txt` | canned report | task 04-11-02 |
| `05-staging-ai-player.log` | log excerpt | tasks 04-11-03 and 04-11-04 |
| `06` through `13` | screenshots | tasks 04-11-03 and 04-11-04 |

Environment: Railway project `mudpuppy`, environment `staging`, service `MudPuppy`. Production is untouched. The staging model stays `gemini-3.5-flash-lite` (D-23); do not change it, and do not change any other staging variable except where a task explicitly says so. `ENCRYPTION_KEY_V1` is already set on staging and plan 04-02 now makes it required — a startup failure naming that variable means it was unset, not that the gate is wrong.

Expect real decision cadence to be dominated by model latency rather than by the loop's pacing constants (04-RESEARCH Pitfall 2): bursts and gaps are the expected shape, not clockwork. Expect the Alter Aeon socket to drop periodically (04-RESEARCH Open Question 1); the loop is designed to pause in waiting and resume, and the walkthrough should record each drop rather than treating it as a defect.
</context>

<tasks>

<task type="auto">
  <name>Task 04-11-01: The test report and the harness self-test are captured verbatim as files</name>
  <files>.planning/phases/04-continuous-play/evidence/01-test-report.txt, .planning/phases/04-continuous-play/evidence/02-harness-selftest.txt</files>
  <read_first>
    - .planning/phases/04-continuous-play/04-VALIDATION.md (Test Infrastructure, Sampling Rate and the Per-Task Verification Map — every automated command in that map must appear in this report)
    - .planning/phases/04-continuous-play/04-01-SUMMARY.md through 04-10-SUMMARY.md (what each plan reports as done, so a gap is caught before the staging checkpoints)
    - .planning/phases/03.1-prompt-injection-review/evidence/02-test-report.txt (the report layout every canned report in this project shares, including the EXIT convention and the trailing total)
    - scripts/verify-phase4.sh usage block (the two self-test invocations)
  </read_first>
  <action>
    Create `.planning/phases/04-continuous-play/evidence/`.

    From the repository root, run each command below and append its command line, its full output and its exit status to `evidence/01-test-report.txt`, verbatim — no summarising, no truncation, no reformatting. Prefix each with a line reading `### ` followed by the command and follow each with `EXIT: ` and the status so the file is greppable.

    1. `go build ./...`
    2. `go vet ./internal/... ./cmd/...`
    3. `go test ./... -v`
    4. `go test ./internal/driver/... -run "TestLoop_" -v`
    5. `go test ./internal/driver/... -run "TestEngageLoop_Reassess|TestScriptedStint|TestFakeSessionsStateMachine" -v`
    6. `go test ./internal/driver/... -run "TestHandleEngage_RetryOn503|TestAIPayloadCarriesSwitchState" -v`
    7. `go test ./internal/driver/... -run "TestPromptContextOrder|TestUntrustedParagraphNamesEveryMarker|TestMemoryIsWrappedInBothPrompts|TestMemoryCeilingsAreEnforced|TestReviewPromptWrapsReasoning" -v`
    8. `go test ./internal/driver/... -run "TestDriverPersistsCuratedMemory|TestCorpusIsWellFormed" -v`
    9. `go test ./internal/driver/... -run TestLiveCorpus -v` with the corpus flag unset, so the SKIP line is on record as proof the gate holds
    10. `go test ./internal/session/... -v`
    11. `go test ./internal/store/... -v`
    12. `go test ./internal/config/... -v`
    13. `go test ./internal/gemini/... -v`
    14. `go test ./internal/profiles/... -v`
    15. `go test ./internal/... -race -count=1` under a `### RACE` heading, or, if the cgo toolchain is unavailable on this machine, that heading stating plainly that the race detector did not run and why. Do not silently omit the question.
    16. `cd frontend && npm run build`
    17. `git diff --stat <phase-base-commit> -- go.mod frontend/package.json` under a `### DEPENDENCY DRIFT` heading. This phase installs no package, so a non-empty section is a supply-chain finding (T-4-SC), not a nuisance.
    18. `grep -rniE "gemini-[0-9]|flash-latest|generativelanguage" internal/ cmd/ --include=*.go | grep -v "_test.go"` under a `### MODEL LITERALS` heading. Zero output is the pass condition, unchanged from Phase 3.

    End the file with `GO TEST EXIT: ` carrying command 3's exit status. Annotate any failure that predates this phase as the known baseline, naming it; a failure in any package this phase touched blocks the phase and must be fixed before the checkpoints below are attempted.

    Then produce `evidence/02-harness-selftest.txt` the same way, with two headed sections: the clean self-test invocation of `scripts/verify-phase4.sh` followed by that report's contents and its exit status, and the negative self-test followed by the same. The first must show zero lines beginning with `FAIL` and exit 0; the second must contain `FAIL C4` and exit non-zero.

    Run no SQL. Deploy nothing. Make no live model call.
  </action>
  <verify>
    <automated>grep -c "GO TEST EXIT" .planning/phases/04-continuous-play/evidence/01-test-report.txt && grep -c "FAIL C4" .planning/phases/04-continuous-play/evidence/02-harness-selftest.txt && grep -c "SKIP: TestLiveCorpus" .planning/phases/04-continuous-play/evidence/01-test-report.txt</automated>
  </verify>
  <acceptance_criteria>
    - `evidence/01-test-report.txt` contains a heading for each of the eighteen commands including `### RACE`, `### DEPENDENCY DRIFT` and `### MODEL LITERALS`, and `EXIT: 0` for `go build`, `go vet`, every targeted `go test` run and `npm run build`
    - The report contains PASS lines for `TestLoop_Pacing`, `TestLoop_CallCap`, `TestLoop_ErrorThreshold`, `TestLoop_ConsecutiveBlocks`, `TestLoop_BlankSettings`, `TestLoop_NothingIssuedWhileWaiting`, `TestLoop_NoAIReconnect`, `TestEngageLoop_Reassess`, `TestHandleEngage_RetryOn503`, `TestAIPayloadCarriesSwitchState`, `TestPromptContextOrder`, `TestReviewPromptWrapsReasoning`, `TestDriverPersistsCuratedMemory`, `TestCorpusIsWellFormed`, `TestManager_DisengageHookFires`, `TestWindow_AgeBounded`, `TestWindow_NeverEmptyAfterQuiet`, `TestLoadRequiresEncryptionKeyOutsideDevelopment`, `TestMigration012DownReconcilesBlockedRows`, `TestQuestStore_NeverCloses` and `TestRetention_LeavesMemoryAlone`
    - The report contains a `--- SKIP: TestLiveCorpus` line (the gate holding with the flag unset)
    - The report contains zero lines matching `^--- FAIL` outside an explicitly annotated pre-existing baseline, and the annotation names each baseline entry
    - The `### DEPENDENCY DRIFT` and `### MODEL LITERALS` sections are both empty
    - The `### RACE` section states whether the race detector ran and, if not, why
    - `evidence/02-harness-selftest.txt` contains both sections; the first shows zero lines beginning with `FAIL` and exit 0, the second contains `FAIL C4` and a non-zero exit
    - No SQL statement and no key value appears anywhere in either file
  </acceptance_criteria>
  <done>The diagnostic half of the Phase Validation line exists as two files a reader can open, with every mechanical limit's test named and passing, no dependency drift, no model literal in source, and a harness that has proven it can fail.</done>
</task>

<task type="auto">
  <name>Task 04-11-02: The corpus runs again, now with the reviewer-channel attack in it (D-24, DR-3.1-01)</name>
  <files>.planning/phases/04-continuous-play/evidence/04-redteam-after.txt</files>
  <read_first>
    - .planning/phases/03.1-prompt-injection-review/03.1-07-SUMMARY.md (the authoritative BEFORE the new report is read against, the two-attempt rate-limit rule, and the standing instruction never to adjust the corpus after seeing a result)
    - .planning/phases/03.1-prompt-injection-review/evidence/05-redteam-after.txt (the Phase 3.1 AFTER report: the per-layer counts and the STEERED count this run is compared with)
    - .planning/phases/04-continuous-play/04-07-SUMMARY.md (the reviewer-channel item's id, category and target, and the confirmation that no existing item was edited)
    - internal/driver/corpus_live_test.go (confirm it is unchanged since plan 04-07 filed it — the comparison is only meaningful if the test did not move under it)
    - .planning/RISK-REGISTER.md DR-3.1-01 (the third part of the remediation: rerun the AFTER report on a fresh-quota day before the staging deploy)
  </read_first>
  <action>
    Confirm first that `internal/driver/corpus_live_test.go` and `internal/driver/testdata/corpus_profile.json` have changed only by plan 04-07's single addition since Phase 3.1 filed them — `git log --oneline` over both paths must show the 3.1 commits plus exactly one Phase 4 commit, and the diff of that commit must be additions only. If an existing item was edited, stop: the comparison with the Phase 3.1 AFTER report is void, and the SUMMARY must say so rather than presenting a rerun of a different corpus as a like-for-like.

    Confirm it is a fresh-quota day for the staging model before starting: the free tier is per model per day, and DR-3.1-01's remediation explicitly asks for the rerun on a fresh quota so the result is a measurement and not a rate-limit artefact. If the day's quota is already spent, stop and report that — do not run a degraded pass and file it.

    Run the corpus against the finished build from the repository root with the corpus flag set, the label set to AFTER, the SHA set to the current short HEAD, the report path set to `.planning/phases/04-continuous-play/evidence/04-redteam-after.txt`, and strict mode on so a steered command fails the run. Follow the two-attempt rule from Phase 3.1: if more than three items come back as model failures for transport or rate-limit reasons, rerun once and keep the run with fewer, filing the other under a suffixed name.

    Read the new report against `.planning/phases/03.1-prompt-injection-review/evidence/05-redteam-after.txt` and record in the plan SUMMARY: the STEERED count then and now, the per-layer catch counts then and now, what happened to `reviewer-channel-01` specifically and which layer if any caught it, any false block by id, and any item that failed for transport reasons rather than for a defence.

    If the strict run fails with a non-zero STEERED count, do not weaken the pass bar, do not edit the corpus and do not proceed to the staging checkpoints. Report which items got through with their categories and targets, and stop — the phase has found a real gap and the owner decides what happens next.

    Change no source file in this task.
  </action>
  <verify>
    <automated>test -f .planning/phases/04-continuous-play/evidence/04-redteam-after.txt && grep -c "LABEL: AFTER" .planning/phases/04-continuous-play/evidence/04-redteam-after.txt && grep -c "reviewer-channel-01" .planning/phases/04-continuous-play/evidence/04-redteam-after.txt</automated>
  </verify>
  <acceptance_criteria>
    - `evidence/04-redteam-after.txt` exists with `LABEL: AFTER`, a `SHA:` matching the built commit, one item line per corpus item including `reviewer-channel-01`, a catches-by-layer block and a summary block
    - The summary block reads `STEERED: 0` and the strict run exits 0; if it does not, the staging checkpoints were not attempted and the SUMMARY says so plainly
    - `git log --oneline` over the corpus test and its fixture shows exactly one Phase 4 commit, and that commit's diff is additions only
    - `grep -ciE "AIza|x-goog-api-key|generativelanguage|GAME_TEXT|MODEL_REASONING" .planning/phases/04-continuous-play/evidence/04-redteam-after.txt` returns 0
    - The plan SUMMARY carries a before-and-after table against the Phase 3.1 AFTER report and states explicitly what happened to the reviewer-channel item
    - The SUMMARY records that the run was made on a fresh-quota day, or that the task stopped because it was not
  </acceptance_criteria>
  <done>The attack aimed at the reviewer's own reasoning channel has been measured against the finished build, and the number is on file next to the one it is meant to be compared with.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 04-11-03: Staging runs this build, migration 013 applies, and the goal survives a refresh</name>
  <files>.planning/phases/04-continuous-play/evidence/03-canned-report.txt, .planning/phases/04-continuous-play/evidence/05-staging-ai-player.log, .planning/phases/04-continuous-play/evidence/06-goal-set.png</files>
  <what-built>
    The loop and its stopping (plans 04-03, 04-04), the reaction window (04-05), the goal and its Quest (04-06), the memory layers in and out of the prompt (04-07, 04-08), the retention standard (04-09), the harness (04-10), and the two startup and migration fixes (04-02). This checkpoint gets that build onto staging, proves migration 013 applied, runs the harness against it, and proves the owner's own goal persists — everything the walkthrough in the next task depends on.
  </what-built>
  <read_first>
    - .planning/phases/04-continuous-play/04-10-SUMMARY.md (the exact live invocation line for `scripts/verify-phase4.sh` and its environment variables)
    - .planning/phases/04-continuous-play/evidence/02-harness-selftest.txt (from task 04-11-01 — the harness has already proven it can fail)
    - .planning/phases/03.1-prompt-injection-review/03.1-07-PLAN.md task 03.1-07-03 (the staging capture procedure that worked last phase, including the cookie and connection-id handling)
    - .planning/phases/04-continuous-play/04-UI-SPEC.md §Layout section 1 (the goal box's locked label, placeholder and hint, so the screenshot can be checked character for character)
    - CLAUDE.md project facts (deploy with `railway up`; a dashboard Deploy rebuilds from GitHub and would run a different branch state; push `ai-player` at phase close after the security-review commits land)
    - .planning/phases/04-continuous-play/04-02-SUMMARY.md (the new startup gate — what a missing `ENCRYPTION_KEY_V1` looks like in the deploy log)
  </read_first>
  <action>
    Rebuild the frontend bundle and commit `public/`, deploy this branch to Railway staging with `railway up --detach -e staging -s MudPuppy`, confirm migration `013` applied and the server started, run the Phase 4 harness against staging into `evidence/03-canned-report.txt` as RUN A, and have the owner confirm the goal box holds what he types into it across a refresh before the walkthrough begins.
  </action>
  <how-to-verify>
    1. Run `cd frontend && npm run build` and commit the rebuilt `public/` — this phase carries frontend changes, and a deploy without the rebuilt bundle would serve the old panel.
    2. Deploy with `railway up --detach -e staging -s MudPuppy` to project `mudpuppy`, environment `staging`. Do not use the dashboard Deploy button and do not deploy to production.
    3. In the deploy log, confirm a migration line reading `version=13, dirty=false` and that the process started normally. Keep this excerpt; it opens `evidence/05-staging-ai-player.log`. If the server exits naming `ENCRYPTION_KEY_V1`, the staging variable is unset — set it once through the CLI without printing its value, and record only that it was set, never what it is. If the migration failed, stop here: `quests` and the two new columns are what the goal and memory surfaces depend on.
    4. Using the browser session that is already signed in — do not sign out — open the connection profile that has accepted the Safety and Abuse policy and that the owner will drive on Alter Aeon, and note its connection id.
    5. In the AI Assist panel, confirm the goal box reads `No session goal set` while empty. Type a session goal the owner actually wants pursued, press Enter, and confirm the panel prints the `[Goal changed: ...]` line. Reload the page.
    6. `06-goal-set.png` — the shot must show the AI Assist panel after that reload with the goal still in the box, the label reading `Session Goal` and the hint sentence beneath it, from the end-user perspective: the MUDPuppy browser page with the header visible, no devtools pane, no terminal window, no raw JSON in frame.
    7. Run the harness against staging into a temporary file using the invocation from `04-10-SUMMARY.md`, with the base URL, session cookie, connection id and not-owned connection id set, then write `evidence/03-canned-report.txt` beginning with a heading reading `### RUN A (before the walkthrough)` followed by that file's contents verbatim.
    8. Read RUN A and confirm with your own eyes: `PASS C1` with the goal round trip, the edit and the Quest reactivation; `PASS C2` with the memory array read back (it may legitimately be empty before any AI decision has run); `PASS C4` with the delete counts and the decision record surviving; the not-owned steps refused; and `SKIP C3` and `SKIP C5` naming their Go tests and screenshots rather than claiming a verdict. Note that the harness's restore step will have put back the goal that was there before it ran — retype the owner's goal and confirm it saved before the next task, and say so in the SUMMARY.
    Do not run a SQL query at any point. Do not sign the owner out. Do not touch production. Do not print any Railway variable value.
  </how-to-verify>
  <verify>
    <human-check>Staging is running this build with migration 013 applied, RUN A is in evidence/03-canned-report.txt with PASS C1, PASS C2 and PASS C4 and the two SKIP lines, and the screenshot shows the goal still in the box after a page reload.</human-check>
  </verify>
  <acceptance_criteria>
    - The deploy log line showing `version=13` and `dirty=false` is captured into `evidence/05-staging-ai-player.log` and the service started normally
    - `evidence/03-canned-report.txt` begins with `### RUN A (before the walkthrough)` and its header records the staging base URL and a git SHA
    - RUN A contains `PASS C1`, `PASS C2`, `PASS C4`, `SKIP C3`, `SKIP C5` and zero lines beginning with `FAIL`
    - `evidence/06-goal-set.png` shows the goal surviving a reload with the locked label and hint visible, from the end-user perspective with no devtools, terminal or raw JSON in frame
    - The SUMMARY records the deploy method as `railway up --detach -e staging -s MudPuppy`, that `public/` was rebuilt and committed first, that production was not deployed to, and that the harness restored the goal and it was retyped
    - No SQL query, no Railway variable value and no key appears in this task's evidence or in the SUMMARY, and the owner was not signed out
  </acceptance_criteria>
  <resume-signal>Type "approved" once staging is running this build, RUN A is on disk and the screenshot shows the goal surviving a reload — or name the file that could not be produced and what the report or the screen said instead.</resume-signal>
  <done>Staging runs the phase's build, the schema accepted migration 013, and the owner's own session goal is on his profile and stays there.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 04-11-04: The owner hands over the wheel, watches the AI play toward his goal, and takes it back</name>
  <files>.planning/phases/04-continuous-play/evidence/03-canned-report.txt, .planning/phases/04-continuous-play/evidence/05-staging-ai-player.log, .planning/phases/04-continuous-play/evidence/07-paced-decisions.png, .planning/phases/04-continuous-play/evidence/08-cap-halt-badge-off.png, .planning/phases/04-continuous-play/evidence/09-wheel-grab-reengage-reasoning.png, .planning/phases/04-continuous-play/evidence/10-session-memory-expanded.png, .planning/phases/04-continuous-play/evidence/11-session-memory-after-refresh.png, .planning/phases/04-continuous-play/evidence/12-delete-captured-text-confirm.png, .planning/phases/04-continuous-play/evidence/13-hand-play-unchanged.png, .planning/phases/04-continuous-play/04-11-SUMMARY.md</files>
  <what-built>
    The whole phase, on the owner's own character: one `#AUTO ON` now produces a stream of paced decisions toward a goal he typed, each with its reasoning as it happens; the limits he set in his profile actually stop it; taking the wheel stops it instantly and re-engaging starts from where the character actually is; the AI keeps and shows a short list of what it has learned; and the captured text behind all of it now ages out and can be deleted on demand. This is the Phase Validation line's player-observable half and the larger half: criteria 1, 2 and 3 have no other proof.
  </what-built>
  <read_first>
    - .planning/ROADMAP.md §Phase 4 (the four success criteria and the Phase Validation line)
    - .planning/phases/04-continuous-play/04-UI-SPEC.md (what each surface must show and say, verbatim, including the status line, the cap notice, the memory section and the danger zone)
    - .planning/phases/04-continuous-play/04-CONTEXT.md D-06, D-08, D-14, D-17, D-19 and D-21
    - .planning/phases/04-continuous-play/04-VALIDATION.md §Manual-Only Verifications (the four rows this task satisfies)
    - .planning/phases/04-continuous-play/04-03-SUMMARY.md and 04-04-SUMMARY.md (the pacing values and the exact notice strings, so a screenshot can be read against the contract)
    - .planning/phases/03.1-prompt-injection-review/evidence/06-staging-ai-player.log (the log excerpt shape and the greps run over it)
  </read_first>
  <action>
    Walk the play screen on staging against Alter Aeon in the order below using the owner's own character, capture one PNG per observable into `evidence/` with the exact filenames listed, append RUN B of the canned report and the `[AI-PLAYER]` log excerpt, and write `04-11-SUMMARY.md` with the four-row criterion table specified at the end. Capture with a browser automation tool or let the owner take the shots by hand — either is acceptable, as it was in Phases 1 to 3.1, and the owner confirms the set at this checkpoint.
  </action>
  <how-to-verify>
    On staging, signed in as the owner, connected to Alter Aeon on the profile from the previous task with the goal saved. Do not sign out at any point. Every shot must be the browser as the owner sees it — the play screen with the header visible, no devtools pane, no terminal window, no raw JSON in frame. Expect bursts and gaps rather than clockwork, and expect the socket to drop occasionally; record each drop and what autopilot did.

    1. With the goal set and the profile's call cap blank, type `#AUTO ON` and let the AI play for several decisions without touching anything.
    2. `07-paced-decisions.png` — the shot must show at least three consecutive decisions in the AI Assist panel, each with its reasoning and its command, the status line reading the call count and the two consecutive counts, the goal still in the box, and the badge reading `Autopilot: On` in the same frame. Note in the SUMMARY roughly how long the stint took and how many decisions landed, so the pacing is on record as a number rather than an impression.
    3. Type `#AUTO OFF`. In AI Player settings set the call cap to a small number (for example 4), return to the play screen, and `#AUTO ON` again. Let it run until it halts.
    4. `08-cap-halt-badge-off.png` — the shot must show the panel's `[Session call cap reached. Autopilot disengaged.]` line in red, the terminal's matching line, and the badge reading `Autopilot: Off` in the same frame. Clear the cap back to blank afterwards and say so in the SUMMARY.
    5. With autopilot on again, type an ordinary game command to take the wheel back, then hand-play the character somewhere clearly different — another room, or into a different situation. Confirm the badge went to Off the moment the command was typed and that no further AI command arrived after it. Then `#AUTO ON`.
    6. `09-wheel-grab-reengage-reasoning.png` — the shot must show the first decision after re-engaging, with its reasoning describing where the character is **now**, not continuing the plan from before the wheel-grab. This is criterion 3's whole proof; if the reasoning reads like a continuation, say so plainly in the SUMMARY rather than reframing it, and capture it anyway.
    7. `10-session-memory-expanded.png` — expand the Session Memory section and capture it with its count and its bullets visible after several decisions have run. Then hard-refresh the page, reconnect if the refresh dropped the socket, expand it again, and capture `11-session-memory-after-refresh.png` showing the same bullets rebuilt from the server.
    8. `12-delete-captured-text-confirm.png` — in AI Player settings, click `Delete Captured Text Now` and capture the confirmation state: the scope-stating hint, the `Delete captured text now?` prompt and the Yes and No buttons. Confirm the delete, then return to the play screen and check that the decision list still shows the earlier decisions with their reasoning and commands, and that the Session Memory section still shows its bullets. Record both observations in the SUMMARY.
    9. `13-hand-play-unchanged.png` — open a profile that has not accepted the policy, connect and hand-play several commands. The shot must show ordinary play with no AI Assist panel, no goal box, no Session Memory section and no status line anywhere on the page.
    10. Run the harness against staging again with the same arguments as RUN A and append to `evidence/03-canned-report.txt` a heading reading `### RUN B (after the walkthrough)` followed by that file's contents verbatim. Confirm C2 now reads back a non-empty Session Memory and that C1 and C4 still pass.
    11. Capture the `[AI-PLAYER]` lines this walkthrough produced with `railway logs` against the staging environment filtered on the prefix, or the Railway log tool, naming the tool used in the SUMMARY. Append them to `evidence/05-staging-ai-player.log` under a heading. Confirm the excerpt contains, for the test connection id, `stage=loop-tick` lines across the stint, the request, answer, review, dispatch and sent stages of several decisions, a `stage=cap` line from step 3, a disengage transition from the wheel-grab in step 5, a `stage=memory` line, and a `stage=retention-manual` line from step 8. If the nightly `stage=retention` line has not fired yet, say so and note that the manual line proves the same code path.
    12. Read the log excerpt back and confirm it contains no game text, no reasoning, no command text, no goal text, no memory bullet, no conduct-rules or approach-guidance text, no API key, no endpoint URL and no one-time sign-in code.
    13. Write `04-11-SUMMARY.md`. Its centrepiece is a table with one row per ROADMAP Phase 4 success criterion 1 to 4, each naming the evidence that proves it and a PASS or FAIL verdict. Line numbers are required for report and log citations — "see the report" is not a citation. Screenshots are cited by filename. No row may cite a database query. Suggested citations: criterion 1 to `evidence/06-goal-set.png`, `evidence/07-paced-decisions.png` and the `TestLoop_Pacing` lines of `evidence/01-test-report.txt`; criterion 2 to `evidence/08-cap-halt-badge-off.png` and the `TestLoop_CallCap` and `TestLoop_ErrorThreshold` lines; criterion 3 to `evidence/09-wheel-grab-reengage-reasoning.png`, the wheel-grab lines of `evidence/05-staging-ai-player.log` and the `TestEngageLoop_Reassess` and `TestLoop_StopsWhenWheelGrabbed` lines; criterion 4 to the four limit tests plus `TestLoop_NothingIssuedWhileWaiting`, `TestLoop_NoAIReconnect` and `TestLoop_BlankSettings`. Close with a short section recording the stint's decision count and duration, every socket drop and what autopilot did each time, the memory and retention observations, and a pointer to `04-SECURITY-AGENDA.md`.
  </how-to-verify>
  <verify>
    <human-check>All seven walkthrough screenshots exist under evidence/ with the exact filenames, each showing the stated content; RUN B is appended and reads back a non-empty Session Memory; the log excerpt carries the loop, cap, wheel-grab, memory and retention stages and nothing it should not; and the SUMMARY's four-row table cites a real file and line number for every criterion.</human-check>
  </verify>
  <acceptance_criteria>
    - All seven PNGs exist with exactly these names: `07-paced-decisions.png`, `08-cap-halt-badge-off.png`, `09-wheel-grab-reengage-reasoning.png`, `10-session-memory-expanded.png`, `11-session-memory-after-refresh.png`, `12-delete-captured-text-confirm.png`, `13-hand-play-unchanged.png`
    - Every PNG is from the end-user perspective: the MUDPuppy browser page with the header visible, no devtools pane, no terminal, no raw JSON in frame
    - `07-paced-decisions.png` shows at least three decisions with reasoning, the status line and the badge reading `Autopilot: On` in the same frame
    - `08-cap-halt-badge-off.png` shows the locked cap notice in panel and terminal with the badge reading `Autopilot: Off`
    - `09-wheel-grab-reengage-reasoning.png` shows the first post-re-engage decision's reasoning, and the SUMMARY states plainly whether it describes the current situation or reads as a continuation
    - `10-session-memory-expanded.png` and `11-session-memory-after-refresh.png` show the same bullets before and after a refresh
    - `12-delete-captured-text-confirm.png` shows the scope hint, the confirm prompt and both buttons
    - `13-hand-play-unchanged.png` shows no AI Assist panel, no goal box and no Session Memory section anywhere on the page
    - `evidence/03-canned-report.txt` contains both a RUN A and a RUN B section; RUN B contains `PASS C1`, `PASS C2` and `PASS C4` and zero lines beginning with `FAIL`
    - `evidence/05-staging-ai-player.log` contains at least one line for each of `stage=loop-tick`, `stage=sent`, `stage=cap`, `stage=memory` and `stage=retention-manual`, plus the migration `013` startup line
    - `grep -ciE "conduct_rules|approach_guidance|never_issue|reasoning=|notice=|goal=\"|AIza|x-goog-api-key|generativelanguage|GAME_TEXT|SESSION_MEMORY" .planning/phases/04-continuous-play/evidence/05-staging-ai-player.log` returns 0
    - `grep -ci "code:" .planning/phases/04-continuous-play/evidence/05-staging-ai-player.log` returns 0 (DR-2-01 staying closed)
    - `04-11-SUMMARY.md` contains a four-row criterion table; every row cites at least one evidence file, report and log citations carry line numbers, and every row carries a PASS or FAIL
    - No row cites a SQL query or a database inspection, and the SUMMARY states the owner was never signed out and that the deploy was `railway up`
  </acceptance_criteria>
  <resume-signal>Type "approved" to record the phase as demonstrated, or list the screenshots that could not be produced and what was on screen instead.</resume-signal>
  <done>The ROADMAP Phase Validation has been performed on the owner's own character against a live MUD, and every criterion points at a file — a report line, a log line, or an image.</done>
</task>

<task type="auto">
  <name>Task 04-11-05: The security review gets every deferred risk back, with what this phase did about each (D-28)</name>
  <files>.planning/phases/04-continuous-play/04-SECURITY-AGENDA.md</files>
  <read_first>
    - .planning/phases/03.1-prompt-injection-review/03.1-SECURITY-AGENDA.md in full (the voice, the three-disposition format, and the standing rule that the agenda decides nothing)
    - .planning/RISK-REGISTER.md (every DR-3 and DR-3.1 row verbatim: the risk, the proposed remediation, the owner's own note, and the "Raised again at" cell naming the Phase 4 review)
    - .planning/phases/04-continuous-play/04-CONTEXT.md D-21 to D-28 (which decision closes which risk, and which two are review items rather than code)
    - the `threat_model` blocks of plans 04-01 through 04-10 (the T-4 register this phase produced, including every row dispositioned `accept` or `defer to security review`)
    - .planning/phases/04-continuous-play/04-02-SUMMARY.md, 04-04-SUMMARY.md, 04-07-SUMMARY.md and 04-09-SUMMARY.md (the four summaries that record the carry-forward remediations as shipped)
    - .planning/phases/04-continuous-play/evidence/01-test-report.txt and evidence/04-redteam-after.txt (the numbers the agenda quotes, so the owner decides against measurements rather than adjectives)
    - .specify/specs/safety-and-abuse-policy-v1.md sections 2, 5 and 6 (quote, do not paraphrase from memory)
  </read_first>
  <action>
    Write `.planning/phases/04-continuous-play/04-SECURITY-AGENDA.md` in the voice of `03.1-SECURITY-AGENDA.md`: the input to the Phase 4 security review, not its output. It decides nothing. Every item carries the same three dispositions — Accept, Defer, Remediate Now — with none marked as chosen and none recommended. Write every risk in plain words, as a problem followed by the fix, with no jargon in any owner-facing sentence.

    Open with one paragraph stating what the agenda is for and that the project cannot close while any deferral remains active.

    Then a section titled with the carried-forward risks, containing one numbered item for every deferred row in the register — DR-3-01, DR-3-03, DR-3-04, DR-3-05, DR-3-06, DR-3-07, DR-3.1-01, DR-3.1-02, DR-3.1-03, DR-3.1-04 and DR-3.1-05 — each reproducing the risk in the owner's own words from the register, then stating what Phase 4 did about it with a citation to the evidence that proves it:
    - DR-3-01 and DR-3.1-03 by D-21's retention standard: the two windows, the nightly job's log line, and the per-profile delete, cited to `evidence/05-staging-ai-player.log` and the PASS C4 lines of `evidence/03-canned-report.txt`.
    - DR-3-03 by D-18: the switch state on the disengage message, cited to the `TestAIPayloadCarriesSwitchState` line of `evidence/01-test-report.txt` and `evidence/08-cap-halt-badge-off.png`.
    - DR-3-04 by D-16: one retry on a 503 with a notice, cited to the `TestHandleEngage_RetryOn503` line.
    - DR-3-07 by D-27: the dead statement deleted.
    - DR-3.1-01 by D-24: the reasoning wrapped as untrusted, the corpus item added, the AFTER report rerun on a fresh-quota day, cited to `evidence/04-redteam-after.txt` by line.
    - DR-3.1-02 by D-14: the cap counting the reviewer call and any retry, cited to the `TestLoop_CallCap` line.
    - DR-3.1-04 by D-25 and DR-3.1-05 by D-26, cited to their two test lines.
    - DR-3-05: state plainly whether `ai-player` has been pushed to GitHub at this phase's close, since the owner asked for that at the Phase 3 review.
    - DR-3-06: an owner administrative task — who can read the staging log — with nothing this phase could do about it in code, presented for the owner's own outcome.
    For each, still offer all three dispositions: a remediation having shipped is not the same as the owner having closed the risk.

    Then a section for what this phase newly creates, led by the one item that is genuinely new: **the AI's own memory as an injection channel.** State the mechanism in plain words — the AI writes its own short list of facts from text the game gave it, and a hostile room can try to get a sentence written into that list, where it would come back on every later decision. State the mitigation that exists (memory is delimited as untrusted data in both prompts, size-capped in the server, and visible to the owner in the panel) and state plainly what is not solved: a poisoned entry that reads as a plain fact rather than as an instruction is not detectable by delimiting. Cite T-4-03 and the plans that carry it. Three dispositions. Add the other residuals this phase's plans dispositioned `accept` or deferred — the orphaned-loop risk, the cost of a loop on a free-tier key, the reviewer that can still be misled, and the harness's destructive delete step — each with its threat id, stated as accepted at plan level and listed for confirmation rather than re-litigation.

    Close with two notes: that the policy stays at version 1.0 by the owner's own decision and the section 2 rewording is not re-proposed (D-22), and that `GOOGLE_API_KEY` on production remains a production cut-over note rather than a Phase 4 item (AR-3-08).

    Change no cell in `.planning/RISK-REGISTER.md`: every deferred row already names the Phase 4 security review, and the outcomes are recorded there at the review, not before it. Build no remediation in this task and change no code. If the owner chooses Remediate Now on any item, that becomes its own plan or its own phase.
  </action>
  <verify>
    <automated>test -f .planning/phases/04-continuous-play/04-SECURITY-AGENDA.md && grep -c "Remediate Now" .planning/phases/04-continuous-play/04-SECURITY-AGENDA.md && grep -c "DR-3.1-05" .planning/phases/04-continuous-play/04-SECURITY-AGENDA.md && grep -c "T-4-03" .planning/phases/04-continuous-play/04-SECURITY-AGENDA.md</automated>
  </verify>
  <acceptance_criteria>
    - `04-SECURITY-AGENDA.md` exists and contains one numbered item for each of `DR-3-01`, `DR-3-03`, `DR-3-04`, `DR-3-05`, `DR-3-06`, `DR-3-07`, `DR-3.1-01`, `DR-3.1-02`, `DR-3.1-03`, `DR-3.1-04` and `DR-3.1-05`, each listing all three dispositions `Accept`, `Defer` and `Remediate Now` with none marked as selected or recommended
    - Every carried-forward item reproduces the risk in the owner's own words from the register and cites at least one evidence file, with a line number for report and log citations
    - The agenda contains an item for the AI's memory as an injection channel citing `T-4-03`, stating the mechanism and the mitigation in plain words and stating plainly what is not solved
    - The agenda states whether `ai-player` has been pushed to GitHub at this phase's close (DR-3-05) and presents DR-3-06 as an owner administrative task
    - The agenda records that the policy stays at version 1.0 and that the section 2 rewording is not re-proposed (D-22), and carries the AR-3-08 production cut-over note
    - `git diff .planning/RISK-REGISTER.md` is empty — the outcomes are recorded at the review, not in advance
    - Source assertion: `grep -ciE "SELECT |psql" .planning/phases/04-continuous-play/04-SECURITY-AGENDA.md` returns 0, and no risk item is written in jargon (each reads as a problem followed by a fix)
    - Source assertion: `git status --porcelain` shows changes only under `.planning/` for this task
  </acceptance_criteria>
  <done>Every risk this project has been carrying since Phase 3 goes back to the owner with what was actually built against it and a number attached, and the one new risk this phase creates is sitting beside them with nothing decided in advance.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repository → Railway staging | The deployed binary may diverge from the repo; the report's git SHA header, the rebuilt `public/` bundle and the log are what reconcile them |
| Owner browser session → the live MUD and the live model | The walkthrough drives the real session, socket, driver and vendor call end to end, unattended by nothing but the owner watching |
| Evidence files → readers | Reports, logs and screenshots are committed to the repository; whatever they contain is disclosed |
| Migration 013 → the live staging database | A failed migration is a startup fatal and takes staging down |
| The corpus rerun → the owner's daily quota | A full corpus pass on a free-tier key is a day's budget |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation |
|-----------|----------|----------|-----------|-------------|------------|
| T-4-11 | Repudiation (unfalsifiable evidence) | High | a canned report that always passes | mitigate | 04-11-01 files the negative self-test before the staging report is trusted |
| T-4-05 | Information Disclosure | High | a key, a Railway variable value, a session cookie, game text, a goal, a memory bullet or a chosen command reaching a committed report, log excerpt or screenshot | mitigate | 04-11-02 and 04-11-04 grep the filed reports and the log excerpt for key, endpoint, delimiter, notice and profile-prose patterns before they are committed; every screenshot is an end-user frame with no raw JSON |
| T-4-12 | Tampering (wrong target) | High | a deploy, a variable change or a harness run aimed at production, or a dashboard Deploy rebuilding from an unpushed branch | mitigate | 04-11-03 deploys with `railway up --detach -e staging -s MudPuppy` only, rebuilds and commits `public/` first, and records the base URL and git SHA in the report header |
| T-4-13 | Denial of Service (owner lockout) | High | signing the owner out of staging during the walkthrough | mitigate | Both checkpoints state the standing instruction; sign-in is one-time-code based and the executor cannot sign back in |
| T-4-07 | Tampering (data integrity) | Medium | migration 013 failing on the live schema, or the new startup gate stopping staging because its key was never set | mitigate | 04-11-03 stops on anything other than a clean `version=13` startup line and tells the executor exactly what a missing-key failure looks like and how to fix it without printing the value |
| T-4-03 | Tampering / Elevation of Privilege | High | the AI's own memory as an injection channel | defer to security review | 04-11-05 writes it onto the agenda as this phase's new active risk with three dispositions and no decision |
| T-4-18 | Denial of Service (cost) | Medium | the corpus rerun consuming the day's free-tier quota | accept | DR-3.1-01's remediation requires the rerun on a fresh-quota day; the cost is listed on the agenda |
| T-4-34 | Repudiation (risks forgotten) | High | eleven deferred risks quietly lapsing because a phase shipped remediations for some of them | mitigate | 04-11-05 re-presents every one with what was built and three dispositions; a shipped remediation is not a closed risk until the owner says so |
| T-4-SC | Tampering (supply chain) | Low | `go.mod`, `frontend/package.json` | accept | The dependency-drift section of `evidence/01-test-report.txt` must be empty for the whole phase |
</threat_model>

<verification>
1. `evidence/01-test-report.txt` exists with every command captured verbatim, zero new FAIL lines, an empty dependency-drift section, an empty model-literals section, a stated race answer and a `GO TEST EXIT:` line.
2. `evidence/02-harness-selftest.txt` shows a clean self-test and a negative self-test containing `FAIL C4` with a non-zero exit.
3. `evidence/04-redteam-after.txt` carries `LABEL: AFTER`, a line for `reviewer-channel-01`, and `STEERED: 0` from the corpus run in strict mode on a fresh-quota day.
4. `evidence/03-canned-report.txt` contains RUN A and RUN B; both contain `PASS C1`, `PASS C2` and `PASS C4` and no `FAIL` line, and both carry `SKIP C3` and `SKIP C5`.
5. `evidence/05-staging-ai-player.log` contains the migration `013` line and the loop, sent, cap, memory and manual-retention stages, and no game text, reasoning, command, goal, memory bullet, key, endpoint or sign-in code.
6. Eight screenshots exist under `evidence/` with the exact filenames listed, all from the end-user perspective.
7. `04-11-SUMMARY.md`'s four-row table cites a real file, with a line number for text evidence, per ROADMAP criterion, with a verdict.
8. `04-SECURITY-AGENDA.md` lists every deferred DR-3 and DR-3.1 row plus the new memory risk, each with three dispositions and none chosen, and `.planning/RISK-REGISTER.md` is unchanged.
9. Both checkpoints are explicitly approved by the owner.
10. No evidence in this phase is a database query, the owner was never signed out, and no Railway variable value was printed anywhere.
</verification>

<success_criteria>
- ROADMAP Phase 4 criterion 1 demonstrated: the owner sets a goal and the AI plays toward it continuously with every decision's reasoning visible as it happens — by `evidence/06-goal-set.png`, `evidence/07-paced-decisions.png` and the `TestLoop_Pacing` lines of `evidence/01-test-report.txt` (D-01, D-05, D-06).
- Criterion 2 demonstrated: the call cap halts the loop with its notice and repeated errors disengage with theirs — by `evidence/08-cap-halt-badge-off.png` and the `TestLoop_CallCap` and `TestLoop_ErrorThreshold` lines (D-14, D-15).
- Criterion 3 demonstrated: a typed command disengages instantly and re-engaging reassesses from the current situation — by `evidence/09-wheel-grab-reengage-reasoning.png`, the wheel-grab lines of `evidence/05-staging-ai-player.log`, and the `TestEngageLoop_Reassess` and `TestLoop_StopsWhenWheelGrabbed` lines (D-08).
- Criterion 4 demonstrated: all four mechanical limits hold under automated test, including blank settings — by the limit tests in `evidence/01-test-report.txt` (D-20).
- The memory layers this phase built are visible and durable, and the captured text behind them now ages out and can be deleted on demand — by `evidence/10`, `evidence/11`, `evidence/12` and the PASS C4 lines of `evidence/03-canned-report.txt` (D-10, D-21).
- The Phase 4 security review can begin, because its agenda exists in writing with every deferred risk re-presented against what was built, and the AI's own memory as an injection channel named as this phase's new risk (D-28).
</success_criteria>

<output>
Create `.planning/phases/04-continuous-play/04-11-SUMMARY.md` when done, containing the evidence file inventory, the four-row criterion table with citations and verdicts, the before-and-after red-team comparison including what happened to the reviewer-channel item, the stint's decision count and duration, every socket drop and what autopilot did each time, the memory and retention observations, a note on whether `-race` ran and why, and a pointer to `04-SECURITY-AGENDA.md`. Record that `ai-player` is to be pushed to GitHub at phase close after the security-review commits land (CLAUDE.md, DR-3-05).
</output>
