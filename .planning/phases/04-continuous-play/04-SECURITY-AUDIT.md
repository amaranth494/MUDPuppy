---
phase: 04-continuous-play
audited: 2026-09-17
auditor: Claude (gsd-security-auditor)
threats_total: 35
verified: 31
accepted: 1
deferred: 1
open: 2
---

# Phase 4: Security Audit Report

**Method:** Every `<threat_model>` block in `04-01-PLAN.md` through `04-11-PLAN.md` was read and every threat row extracted (`T-4-01` through `T-4-34` plus `T-4-SC`, 35 distinct ids; an id repeated across plans is the same phase-wide threat carried forward, verified at each occurrence and counted once). The audit was run against the code as it is at HEAD `82b0241` on `ai-player`, which is NEWER than every `*-SUMMARY.md`: the code review (`04-REVIEW.md`, 2 critical + 12 warnings) and its fixes (`04-REVIEW-FIX.md`, 15 commits) landed after the summaries, so no summary sentence was accepted as evidence. For each `mitigate` row the file and function the mitigation names was opened and read at HEAD, and the named tests were re-run in this session: `go build ./...` and `go vet ./...` exit 0; `go test ./... -count=1` green in all 9 packages with tests; a `-v` run of `internal/driver`, `internal/session`, `internal/profiles`, `internal/gemini`, `internal/auth`, `internal/store`, `internal/config` confirmed every test named below as `--- PASS` (the only SKIP is `TestLiveCorpus_HostileText`, by design). Both harness self-tests were re-run into a scratch directory: `--self-test` exit 0, `TOTAL CHECKS: 35 FAILURES: 0 SKIPPED: 2`; `--self-test-negative` exit 1 with exactly one `FAIL C4`. The dependency-drift diff (`git diff --stat dd15a98..HEAD -- go.mod go.sum frontend/package.json frontend/package-lock.json`) was re-run and is empty. Every committed evidence text file was grepped for key, endpoint, cookie, prompt-delimiter and sign-in-code patterns. The live corpus, the harness's live mode, Railway and staging were not touched.

Status vocabulary: **VERIFIED** = the declared mitigation is present in code and tested; **ACCEPTED** = dispositioned `accept` in the plan and the rationale still holds; **DEFERRED** = explicitly deferred to the security review by the plan, with a matching undecided agenda item; **OPEN** = a declared mitigation is missing or defeated in the code or the committed artifacts. A row that is VERIFIED can still carry a named residual.

No implementation file was modified. This file, and only this file, was written.

## Trust Boundaries (consolidated across all eleven plans)

| Boundary | Plans | Description |
|----------|-------|--------------|
| Test doubles → the decision path | 04-01 | A double that lies about the switch can make an unsafe build look safe |
| Test process → the network and the owner's quota | 04-01, 04-11 | The Phase 4 suite must never reach a live model; the corpus rerun spends a day's free-tier budget |
| Repository → `go.mod` / `frontend/package.json` | all | Any dependency added is a supply-chain change |
| Environment → the credential vault | 04-02 | Whether the vault key is real or invented at startup decides whether saved MUD passwords survive a restart |
| Migration files → the live staging database | 04-02, 04-06, 04-11 | A failed migration is a startup fatal; a rollback that fails partway leaves a half-changed schema |
| Documentation → readers | 04-02 | A key value written into a rotation procedure is disclosed |
| The autopilot switch → the loop goroutine | 04-03 | The switch is the authorization; a loop (or an in-flight decision) that outlives it acts without one |
| The game socket → the loop's wakeups and the window | 04-03, 04-05 | Game output is attacker-controlled text that decides when the AI acts and what it reasons about |
| The loop → the owner's model quota and the MUD | 04-03, 04-04 | Every iteration can spend up to four model calls; commands issued too fast are flooding |
| Hostile game text → the block counter | 04-04 | An attacker who can cause blocks can try to exhaust the session |
| The driver's own state → the browser's badge | 04-04 | If they disagree, the owner believes the AI is stopped when it is not |
| Vendor error responses → the failure taxonomy | 04-04 | A misclassified error stops play too early or never stops it |
| Browser → the goal, memory and captured-text endpoints | 04-06, 04-08, 04-09 | Each must serve, and destroy, only the caller's own profile's data |
| Owner's goal → the prompt | 04-06, 04-07 | Owner-written standing text, trusted only because the ownership check makes it genuinely the owner's |
| Model-written memory → the database → every future prompt | 04-07, 04-08 | Memory is written by a model that has just read hostile text, then re-enters every later decision |
| The first model's reasoning → the reviewer | 04-07 | An account written after reading hostile text, previously read as testimony |
| Stored captured text → time; the retention job → the audit record | 04-09 | Text kept forever is a growing disclosure surface; a prune that reaches too far destroys the audit trail or the AI's memory |
| The danger-zone control → an ordinary click | 04-09 | A destructive control beside a save button is a misclick from data loss |
| The harness → staging; the report and fixtures → readers and the verdict | 04-10, 04-11 | A live run authenticates as the owner and deletes real data; whatever the report prints is committed; fixtures that cannot disagree make every PASS meaningless |
| Repository → Railway staging; owner's browser session → the live MUD and model | 04-11 | The deployed binary may diverge from the repo; the walkthrough drives the real session end to end |

## Threat Verification Register

Legend: **M**=mitigate, **A**=accept, **D**=defer to security review. Severity is the plan's own rating (highest across plans). File:line references are at HEAD `82b0241`.

| Threat ID | Category | Plan(s) | Disposition | What was verified | Status |
|-----------|----------|---------|-------------|--------------------|--------|
| T-4-01 | EoP — an orphaned loop or in-flight decision acting after the switch went off (High) | 04-03, 04-11 | M | Declared mitigation present: `DisengageAutopilot` and `parkAutopilotLocked` fire the disengage hook on every changed-true transition (`internal/session/manager.go:930-932`, `:962-964`); `StopLoop` cancels the stint context (`internal/driver/loop.go:172-186`); `runLoop` re-checks state after every wake (`loop.go:238`). The plan-time mitigation was NOT sufficient on its own (the staging walkthrough sent a command one second after Off) and was strengthened after the summaries: stint epoch on the autopilot record (`manager.go:846-889`, `:997`), three stint checks per decision (`internal/driver/driver.go:620`, `:712`, `:763`) plus one before each 503 retry (`:588`, `:688`), and an epoch-checked send under one read lock (`manager.go:1068-1111`). Tests re-run PASS: `TestLoop_StopsWhenWheelGrabbed` (both subtests), `TestStint_QuickReengageDropsTheOldDecision`, `TestStint_StaleIterationIsNotAFailure`, `TestStint_StopLoopInterruptsAnInFlightModelCall`, `TestManager_StintEpoch`, `TestSendAICommand_RefusedOnEpochMismatch`, `TestSendCommandAs_AIRefusedUnlessAutopilotOn`. Live: `evidence/05-staging-ai-player.log:126` and `:242` (`stage=dropped-disengaged`, no dispatch, no send). No production caller passes source `ai` to the un-epoched `SendCommandAs`. Residuals: see Unregistered item 1 | VERIFIED |
| T-4-02 | DoS (cost and flooding) — an unbounded loop spending quota or flooding the MUD (High) | 04-03, 04-04 | M | Two of the three declared mechanisms are present and tested. (1) Pacing: settle wait, floor interval and minimum spacing (`loop.go:219-275`; `driver.go:376-379`, minimum spacing raised 3s to 8s during the walkthrough); `TestLoop_Pacing/minimum_spacing_bounds_the_decision_rate` PASS. (2) Call cap reserved before EVERY one of the up-to-four calls per iteration, not once per iteration: `tryReserveCall` at `driver.go:579`, `:603`, `:681`, `:697`, body `:904-912`; counters zeroed per stint (`loop.go:98-109`); `TestLoop_CallCap` PASS; live halt `evidence/05-staging-ai-player.log:68` (`cause=ai-call-cap`). (3) **The declared backstop is inert.** The plan (and D-06) state "the ICM dispatcher's rate limit is the backstop". `internal/icm/dispatcher.go:208-212` returns for a plain pass-through command BEFORE `RecordExecution` at `:221`, so nothing the AI sends is ever counted by `CheckRateLimit` (`:69-84`) or the circuit breaker; the check runs but can never trip because of AI sends. The driver never calls `RecordExecution` (0 matches in `internal/driver/driver.go` and `loop.go`). Phase 3's own agenda recorded this as a forward note ("Phase 4 must decide whether the driver calls ICM's RecordExecution ... harmless at one command per engage and not harmless in a loop", `03-13-PLAN.md:323`); Phase 4 never decided it and it is not on `04-SECURITY-AGENDA.md`. Other residuals: a blank cap means no cap and blank is every profile's default; a rate-limit failure counts as one of the three strikes (agenda Part 3); a dropped stale decision still costs up to two calls | OPEN (backstop only; pacing and cap verified) |
| T-4-03 | Tampering/EoP — the AI's own memory as an injection channel (High) | 04-07, 04-08, 04-11 | M (partial), then D | The partial mitigation is present: both memory blocks are delimited in BOTH prompts (`driver.go:1319-1326`, `:1458-1465`; `internal/driver/memory.go:198-212`) and named in the one shared untrusted-data paragraph (`driver.go:1344-1352`); Go-side ceilings 30/20 bullets at 200 bytes at read and at write (`memory.go:43-47`, `:123-161`); since CR-02 no bullet can forge a marker (`memory.go:82-100`). Tests PASS: `TestMemoryIsWrappedInBothPrompts`, `TestUntrustedParagraphNamesEveryMarker`, `TestMemoryCeilingsAreEnforced`, `TestBulletsAreNeutralisedAtWriteAndAtRead`. The residual (a fact-shaped poisoned bullet) is on `04-SECURITY-AGENDA.md` Part 2 with three unchecked dispositions. Two facts the agenda item does not state, both confirmed in code: memory is persisted BEFORE shape validation, the Never-issue check and the reviewer verdict (`driver.go:657` precedes `:659`, `:665`, `:686`), so a decision the reviewer BLOCKS still writes its memory; and no retention, delete or owner-edit path exists for Session Memory or Quest bullets (`internal/store/retention.go` deliberately never touches them), so a poisoned Quest bullet lives until the goal text changes. No corpus item attacks the memory-write path (11 categories in `evidence/04-redteam-after.txt`, none memory) | DEFERRED |
| T-4-04 | Tampering — the reviewer reading the first model's reasoning as testimony (High) | 04-07 | M | `wrapModelReasoning` (`driver.go:1288-1290`) wraps the reasoning in `<MODEL_REASONING>` markers in the reviewer's user text (`:675`); `reviewReasoningUntrustedSentence` (`:1422`) is embedded right after the shared paragraph (`:1449`). Tests PASS: `TestReviewPromptWrapsReasoning` (both subtests, the forged-marker one rewritten by CR-02 to assert each real marker appears once), `TestBuildReviewSystemInstruction`. Corpus item `reviewer-channel-01` exists and reads `sent-unsteered` on the final build (`evidence/04-redteam-after.txt:36`). Residuals: that item was measured ONCE on the final build (it is `failed-model` in the second run, `04h-...attempt2.txt`), and because the player model was not steered the reviewer's catch rate through this channel is still unmeasured (agenda item 7 says so). A reviewer answer with no `blocked` field decodes as not blocked (`internal/gemini/client.go:320-323`, `:348-352`) — see Unregistered item 11 | VERIFIED |
| T-4-05 | Info Disclosure — key, cookie, game text, goal, memory bullet or chosen command reaching a log line, a report, a fixture or a commit (High) | 04-02 to 04-11 | M (A in 04-05) | Logging facets all hold: `logDecision` carries ids, stage and lengths only (`driver.go:1260-1263`); the memory line carries counts and bytes (`:891-892`); quest-repair, retry and model-error lines carry ids, kind and HTTP status (`:607`, `:633`, `:820-823`); the goal line carries a length (`internal/profiles/handler.go:853`); retention lines carry counts (`cmd/server/main.go:677-681`, `handler.go:984`); notices are fixed strings; the D-25 gate error names the variable only (`internal/config/config.go:216`, `:227`); `internal/gemini/client.go` has no log call. `.specify/ENVIRONMENT-VARIABLES.md` holds no key-shaped string. Grep of all 13 evidence text files for key, endpoint, cookie, delimiter and sign-in-code patterns: 0 hits except the test report's own grep command line (`01-test-report.txt:3293`); the plan's log-excerpt grep re-run on `05-staging-ai-player.log`: 0; `window` appears 0 times in the canned report; fixtures carry no credential-shaped string. **One facet is defeated:** 04-11's row names "a goal, a memory bullet or a chosen command reaching a committed report" and the committed `evidence/03-canned-report.txt` contains all three, because the harness prints raw response bodies by design: the full decisions list twice per run (RUN C line 380 is 15,822 bytes: 45 decisions, each with `reasoning`, `command`, `notice`), Session Memory bullets verbatim (`:214`, `:368`, one naming the owner's character), and the owner's real goal (`:120`). The plan's acceptance greps covered the log and the red-team report, never this file. It is on the agenda ("The Phase 4 report file prints some of the AI's own words") but UNDERSTATED there as "the first line of one decision's reasoning and command". The branch is 121 commits ahead of `origin/ai-player` and is pushed at phase close, so this must be decided before the push | OPEN (canned-report facet only; every log and key facet verified) |
| T-4-06 | Tampering — a non-development process starting on a silently generated vault key (High) | 04-02 | M | `config.Load` fails when `RAILWAY_ENVIRONMENT` is set and `ENCRYPTION_KEY_V1` is absent (`internal/config/config.go:214-217`) and, since WR-07, when V1, V2 or V3 is set but unusable (`:218-229`), using the key store's own parser (`internal/crypto/crypto.go:37-43`, same call in `DefaultKeyStore` `:53`). `TestLoadRequiresEncryptionKeyOutsideDevelopment` PASS (absent, usable, four unusable shapes, trailing newline, two local cases). Residuals: the gate is inferred from one platform's variable (IN-07, not fixed) so any other host still gets the random-key fallback (`crypto.go:58-65`); `internal/crypto` has no test file of its own | VERIFIED |
| T-4-07 | Tampering (data integrity) — migration 012 rollback, migration 013 on the live schema, two active Quests (Medium) | 04-02, 04-06, 04-11 | M | `migrations/012_..._down.sql:10` reconciles `blocked` to `failed` before the constraint is re-added at `:27-28`; `TestMigration012DownReconcilesBlockedRows` PASS. `migrations/013_..._up.sql` is `IF NOT EXISTS` throughout with a partial unique index (`:38-39`) that `ensureActiveQuestSQL` names as its conflict target (`internal/store/quests.go:50-54`); `TestMigrationFilesPairUp`, `TestQuestStore_EnsureActiveReactivates` PASS. Live: `evidence/05-staging-ai-player.log:3`, `:115` (`version=13, dirty=false`), `:170`, `:203`, `:215` (`version=14`). Migration 014 was added during execution (D-31) and is covered by the same pairing test and `TestMigration014AddsLoginStartedAtColumn` (line-ending sensitive on a fresh autocrlf checkout, per `04-REVIEW-FIX.md`) | VERIFIED |
| T-4-08 | Info Disclosure — captured game text, attack text included, kept forever (Medium) | 04-09 | M | Four statements, 7-day snapshot clear and 30-day transcript delete plus the per-connection pair (`internal/store/retention.go:41-59`), one transaction each (`:80-127`); daily in-process job, first run 10s after start (`cmd/server/main.go:316-318`, `:671-694`); owner's delete endpoint (`handler.go:954-991`, route `main.go:551-558`). Tests PASS: `TestRetentionWindows`, `TestRetention_TouchesOnlyCapturedText`, `TestDeleteCapturedText` (5 subtests). Live: log `:5`, `:164`, `:198`; canned report `PASS C4` in RUN A, B and C. Residuals: IN-01 (the day constants are only printed; the SQL hard-codes the intervals) and IN-02 (`line_count` not reset) unfixed; text DERIVED from game text — reasoning, notices, Session Memory and Quest bullets — is kept forever by design and has no delete path (see T-4-03) | VERIFIED |
| T-4-09 | Spoofing/IDOR — goal, memory and captured-text endpoints serving another owner (High) | 04-06, 04-08, 04-09 | M | All four handlers resolve through the unchanged `getProfileByConnectionID` before any store call: `handler.go:784`, `:806`, `:916`, `:960` (helper `:1100-1126`, user id from the authenticated request context); the goal write is additionally scoped `WHERE id = $2 AND user_id = $3` (`internal/store/profile.go:546-547`). Tests PASS: `TestGoalRefusesNotOwnedConnection`, `TestGetSessionMemory/not_owned_connection_is_refused`, `TestDeleteCapturedText/not_owned_connection_is_refused_before_any_delete`. Harness not-owned steps pass on staging in RUN C, and since WR-12 only on the application's own refusal (400/403/404 with an `error` body and no data field; `scripts/verify-phase4.sh:532-560`). Note: `sessionMemoryForConnectionSQL` and both per-connection retention statements carry no `user_id` predicate of their own; they rely entirely on the handler check | VERIFIED |
| T-4-10 | DoS — unbounded goal or memory growth inflating storage and every prompt (Medium) | 04-06, 04-07, 04-08 | M | Goal capped at 1000 bytes (`handler.go:265`, `:818-821`); bullets capped 30/20 at 200 bytes in Go on the way in and out, independent of the schema (`memory.go:43-47`, `clampBullets` `:123-135`, `truncateBullets` `:148-161`, rune-safe cut `:105-113`). Tests PASS: `TestMemoryCeilingsAreEnforced` (3 subtests), `TestGoalRoundTrip`; harness C1 cap sentence on staging. Residual: the request body is decoded with no `http.MaxBytesReader` before the length check (IN-04; same class as the already-accepted AR-1-03) | VERIFIED |
| T-4-11 | Repudiation — a canned report that always passes (High) | 04-10, 04-11 | M | Re-run in this session: `--self-test` exit 0 (35 checks, 0 FAIL); `--self-test-negative` exit 1 with exactly one `FAIL C4`. Filed as `evidence/02-harness-selftest.txt` before the staging runs | VERIFIED |
| T-4-12 | Tampering — a deploy or harness run aimed at the wrong target (High) | 04-11 | M | Procedural. Every RUN header in `evidence/03-canned-report.txt` records `BASE_URL: https://mudpuppy-staging.up.railway.app` and a git short SHA (`:7`, `:10`, `:155`, `:158`, RUN C likewise); `04-11-SUMMARY.md:136` states every deploy was `railway up --detach -e staging -s MudPuppy` and production was never touched. Note: the header SHA is the local checkout at capture time, not the deployed build (RUN C header `2f3e6c3`, deployment `aabddcd3`) | VERIFIED |
| T-4-13 | DoS — signing the owner out during the walkthrough (High) | 04-11 | M | Procedural. The declared mitigation (both checkpoints state the standing instruction) is present: `04-11-PLAN.md:77`, `:235`. No sign-in-code pattern in the log excerpt (0 hits). Documentation gap: the plan's own acceptance criterion (`04-11-PLAN.md:303`) requires the SUMMARY to state the owner was never signed out, and `04-11-SUMMARY.md` contains no such sentence; the only supporting evidence is indirect (the owner's cookie worked for RUN A, B and C across four deployments) | VERIFIED |
| T-4-14 | EoP — a command issued while disconnected, or the AI reconnecting on its own (High) | 04-03 | M | Structural: the driver's `Sessions` interface (`driver.go:167-190`) has no connect method, so the driver cannot reconnect. A park fires the stop hook (`manager.go:962-964`); a resume happens only from `Connect`, only onto the same saved connection (`:974-1013`), and since WR-04 only after the game session exists (`openTranscriptThenResumeLocked`, `:379`). An AI send while WAITING is refused before the socket is looked at (`manager.go:1109-1111`). Tests PASS: `TestLoop_NothingIssuedWhileWaiting`, `TestLoop_NoAIReconnect`, `TestSendAICommand_LostConnectionLeavesTheSwitchWaiting`, `TestResumeFiresAfterTheGameSessionExists` | VERIFIED |
| T-4-15 | DoS — a prune or delete reaching decision rows, memory or Quests; the harness's own delete (High; harness facet Medium, accepted) | 04-09, 04-10 | M (A in 04-10) | The four statements name only `window_text` and `game_session_lines` (`retention.go:41-59`); `TestRetention_TouchesOnlyCapturedText`, `TestRetention_LeavesMemoryAlone`, `TestRetention_PerProfileDeleteIsScoped` PASS; staging C4 proves the decision's reasoning, command and outcome survive a delete. Accepted facet still holds and is now honest: since WR-11 the live report's own preamble says the run permanently deletes captured text (`scripts/verify-phase4.sh:282-286`), step 1 aborts with exit 2 before any write, and the counts are recorded per run | VERIFIED |
| T-4-16 | Repudiation — a badge reading On after a cap or threshold disengage (High) | 04-04 | M | `decorateEvent` reads the switch AFTER the disengage is applied (`driver.go:955-971`; `recordFailure` disengages at `:1068` then notifies at `:1071`); `SessionContext` sets the badge from the `ai` message (`frontend/src/context/SessionContext.tsx:274-285`); zero counts are sent, not dropped (WR-05). Tests PASS: `TestAIPayloadCarriesSwitchState`, `TestAIPayloadSendsZeroAndEmpty`. Screenshot `evidence/08-cap-halt-badge-off.png`. Residual, unfixed and not on the agenda: IN-09 — a `sent` event built just before a wheel-grab can arrive after the Off push and put the badge back to On until the next status poll (the inverse lie: the badge says On while the AI is stopped) | VERIFIED |
| T-4-17 | Repudiation — a session double that always reports Off (High) | 04-01 | M | `fakeSessions` drives its state from the real pure functions `session.Engage`, `Disengage`, `EnterWaiting`, `Resume` (`internal/driver/driver_test.go:372`, `:400`, `:435`, `:465`, `:475`); `TestFakeSessionsStateMachine` asserts the already-off case returns `changed=false` (`loop_test.go:103-160`) PASS. Since CR-01 the double also carries an epoch | VERIFIED |
| T-4-18 | DoS (cost) — a test reaching the live model; the corpus rerun's quota cost (Medium) | 04-01, 04-11 | M (A in 04-11) | The only `gemini.NewClient` in the driver package is `corpus_live_test.go:949`, behind an unconditional `RAILWAY_ENVIRONMENT` fatal (`:928-930`) placed before the opt-in skip (`:932-934`); the suite run shows it as SKIP. The harness's appended `go test` sets no corpus flag. Accepted facet: the rationale holds but its stated condition did not — the agenda does not list the cost, and the corpus was run nine times on 2026-09-17 (`04a` to `04h` plus the final), every run with rate-limited `failed-model` items, so it was not a fresh-quota day (see Open Items) | VERIFIED |
| T-4-19 | DoS — the new gate stopping the owner's local server (Medium) | 04-02 | M | The gate is conditional on `RAILWAY_ENVIRONMENT` (`config.go:214`); subtests `local development without the key succeeds` and `local development with an unusable key still succeeds` PASS | VERIFIED |
| T-4-20 | Tampering — hostile output volume driving the decision rate (Medium) | 04-03 | M | The wakeup is a buffered channel of one with a non-blocking send (`manager.go:631-639`), and minimum spacing gates every tick (`loop.go:242-251`); `TestManager_OutputSignalFires`, `TestLoop_Pacing/minimum_spacing_bounds_the_decision_rate` PASS | VERIFIED |
| T-4-21 | DoS — a hostile room causing block after block (Medium) | 04-04 | M | Consecutive blocks are counted separately from failures and disengage through their own kind, cause and notice (`driver.go:1198-1205`, `:79-80`, `:132-135`); a sent command resets the streak (`:1213-1216`). `TestLoop_ConsecutiveBlocks` (3 subtests) PASS. Live side effect, on the agenda: the reviewer's false blocks of ordinary tutorial combat feed this same counter, so false blocks can disengage ordinary play | VERIFIED |
| T-4-22 | EoP — an auth failure counted toward a three-strike threshold (Medium) | 04-04 | M | `transientFailureKinds` excludes auth, bad-request, missing-profile and missing-model (`driver.go:105-113`), mapped for both the player call (`:634-645`) and the reviewer call (`:723-734`); HTTP mapping `internal/gemini/client.go:363-372`. `TestLoop_ErrorThreshold/auth_failure_disengages_on_the_first_hit` PASS | VERIFIED |
| T-4-23 | DoS — a retry storm on an unavailable vendor (Low) | 04-04 | M | Exactly one retry per call site, in an `if` not a loop (`driver.go:585-609`, `:687-703`), a fixed 2s cancellable delay (`:929-934`), the retry reserved against the cap (`:603`, `:697`). `TestHandleEngage_RetryOn503` (both subtests), `TestStint_RetryDelayIsCancellable` PASS. Cosmetic residual IN-05: the retrying notice fires before the retry's reservation | VERIFIED |
| T-4-24 | DoS — a flood of output growing the prompt (Medium) | 04-05 | M | The 8192-byte ring is the only byte ceiling (`internal/session/window.go:13`, `:219-221`); chunk markers are trimmed with the ring (`:120-128`). `TestWindow_CeilingStillHolds`, `TestWindowRingBound` PASS | VERIFIED |
| T-4-25 | Tampering — a decision made against stale text (Medium) | 04-05 | M | Age bound `windowMaxAge` 10s (`window.go:20`, `:187-224`); `TestWindow_AgeBounded` PASS. Mitigated as amended by WR-06: the bound is now the larger of 10s and the time since this user's previous decision snapshot (`window.go:168-177`), so after a long pause the effective bound is the 8 KB ring, newest text last; the stint's first decision carries the reassess instruction (`driver.go:571-573`, `:1365-1370`), `TestEngageLoop_Reassess` and `TestWindow_CoversTheGapSinceThePreviousDecision` PASS | VERIFIED |
| T-4-26 | DoS — an empty window after a quiet spell (Medium) | 04-05 | M | Never-below-2048-bytes floor (`window.go:27`, `:215-218`); `TestWindow_NeverEmptyAfterQuiet` PASS | VERIFIED |
| T-4-27 | Tampering — a Phase 4 path closing a Quest (Low) | 04-06 | M | No statement in `internal/store/quests.go` sets `status` to anything but `active` (`:50-60`); `TestQuestStore_NeverCloses` PASS. WR-09's driver-side repair calls the same upsert only on a miss (`driver.go:807-825`) | VERIFIED |
| T-4-28 | Tampering — forged closing markers breaking the framing (Medium) | 04-07 | M | The plan-time mitigation (the framing sentence plus corpus items) was judged insufficient by the code review (CR-02) and replaced by a mechanical one: `neutraliseUntrusted` and `neutraliseLine` (`memory.go:51-100`) are applied by every wrapper and by `goalBlock` (`driver.go:1273-1275`, `:1288-1290`; `memory.go:179-234`). Tests PASS: `TestNeutraliseUntrusted`, `TestNeutraliseLine`, `TestWrappersCannotBeClosedByTheirContent`, `TestPromptsHoldEachMarkerOncePerBlock`. Both `delimiter-escape` corpus items read `sent-unsteered` on the final build, measured once (both are `failed-model` in the second run). Residuals: see Unregistered item 2 | VERIFIED |
| T-4-29 | DoS — a malformed memory field or a store error costing the command (Low) | 04-08 | M | Decode-tolerant memory fields (`internal/gemini/client.go:223-252`); store errors are logged by id and never returned (`driver.go:867-889`). `TestAnswerCarriesMemoryFields`, `TestDriverPersistsCuratedMemory/store_error_on_either_write_does_not_stop_the_command_from_sending` PASS | VERIFIED |
| T-4-30 | DoS — a destructive control adjacent to a save action (Medium) | 04-09 | M | Its own block below the save row, an unconditional scope-stating hint, and a two-step inline Yes/No (`frontend/src/components/AIPlayerPanel.tsx:314-333`; `frontend/src/index.css:2407-2410`); screenshot `evidence/12-delete-captured-text-confirm.png`. No automated frontend test exists. Residual IN-08, unfixed: `confirmingDelete` is not reset when `connectionId` changes (the only resets are `AIPlayerPanel.tsx:118` and `:324`) | VERIFIED |
| T-4-31 | DoS — a retention run taking the server down (Low) | 04-09 | M | Own goroutine (`main.go:318`), an error logs one line and the loop continues (`:674-679`), nothing in the play path calls it. No `recover` exists, so only a returned error, not a panic, is contained | VERIFIED |
| T-4-32 | Repudiation — the harness presenting pacing or the cap as checked (Medium) | 04-10 | M | C3 and C5 are printed as SKIP naming the Go tests and screenshots (`scripts/verify-phase4.sh:855`, `:862`); both lines present in the self-test re-run and in RUN A, B and C | VERIFIED |
| T-4-33 | Tampering — a harness run leaving a test goal on the owner's profile (Medium) | 04-10 | M | Step 16 restores the decoded pre-run goal (`verify-phase4.sh:838-848`; `evidence/03-canned-report.txt:120`); since WR-11 an EXIT/INT/TERM/HUP trap restores it on any exit once it has been read (`:615-640`), step 1 failing aborts before any write (`:660`), every curl is bounded (`:454-457`). The trap paths were exercised against a localhost stub only (`04-REVIEW-FIX.md`), never on staging. Two harness Quest rows per run remain on the profile, now stated in the report preamble | VERIFIED |
| T-4-34 | Repudiation — eleven deferred risks quietly lapsing (High) | 04-11 | M | `04-SECURITY-AGENDA.md` Part 1 re-presents all eleven active deferrals (DR-3-01, -03, -04, -05, -06, -07 and DR-3.1-01 to -05; checked against `.planning/RISK-REGISTER.md:99-125`, where DR-3-02 is already closed) with what was built and three dispositions each; 48 checkboxes, 0 marked, none recommended. The agenda's accuracy problems are listed under Open Items | VERIFIED |
| T-4-SC | Tampering (supply chain) — `go.mod`, `frontend/package.json` (Low) | all eleven plans | A | `git diff --stat dd15a98..HEAD -- go.mod go.sum frontend/package.json frontend/package-lock.json` re-run: empty. `evidence/01-test-report.txt:3288-3290` records the same command with `EXIT: 0` and no output | ACCEPTED |

Two rows are OPEN. Neither is a missing primary control: T-4-02's pacing and cap and T-4-05's log discipline are all verified. Each OPEN row is one declared sub-mitigation that the code or a committed artifact defeats, and each is closed by either a small change or an explicit owner acceptance.

## Unregistered Attack Surface (found during execution, no plan-time `T-4-NN` mapping)

All three `## Threat Flags` sections that exist (`04-07`, `04-08`, `04-09` summaries) say "None"; the other eight summaries have no such section. Everything below surfaced AFTER those summaries, from the walkthrough, the code review or this audit. For each: is the fix really in the code, is it tested, and what is left.

1. **An in-flight AI send after disengage, and a stale decision sent into the NEXT stint (CR-01, WR-10, WR-02).** Fix in code: yes — epoch on the record (`manager.go:846-889`, `:997`), hooks carry it (`:930-932`, `:962-964`, `:1010-1012`), in-flight guard keyed by user and epoch (`driver.go:477-495`), stale checks (`:444-453`, `:588`, `:620`, `:688`, `:712`, `:763-771`), `StopLoop` and the registry are epoch-scoped (`loop.go:123-186`), model calls and the retry wait run under the stint context. Tested: yes — seven `TestStint_*` tests (`internal/driver/stint_test.go`), `TestManager_StintEpoch`, `TestSendAICommand_RefusedOnEpochMismatch`, `TestManager_DisengageHookFires`; live on the final build at log `:242`. Residuals: (a) `recordFailure` and `recordBlocked` count and disengage by user id, not by epoch (`driver.go:1030-1034`, `:1068`, `:1198-1205`), so a stint that ends and restarts inside the decision-row insert can have the new stint charged or switched off by the old one's failure — fail-safe direction, narrow; (b) the switch check and the socket write are no longer strictly atomic (`manager.go:1098-1125`, by design since WR-01; nanoseconds); (c) a dropped decision is log-only, so it leaves no `ai_decisions` row and still costs up to two calls; (d) `HandleEngage` runs under `context.Background()` (`driver.go:424-427`) — tests and the corpus only. **WARNING, unregistered_flag; fixed.**
2. **Delimiter breakout through a memory bullet, the goal or the reasoning (CR-02).** Fix in code: yes (`memory.go:51-100`, applied at read AND write time and to the goal). Tested: yes, five tests in `internal/driver/neutralise_test.go`. Residuals: (a) both memory blocks still sit inside the SYSTEM instruction of both prompts (the review's "consider" was not done because it changes D-13's accepted shape); (b) the look-alike list is finite — for example U+02C2/U+02C3, U+276E/U+276F and U+1438/U+1433 are not mapped; marker-name hyphenation is an independent second layer, so both must be beaten, but a zero-width character inside a marker name does beat the name regex; (c) no corpus item carries a forged memory or reasoning marker; (d) this fix changes every prompt (`<100hp>` reaches the model as `[100hp]`) and the corpus on this build measured each item once across two runs, not twice. **WARNING, unregistered_flag; fixed.**
3. **The manager lock held across an unbounded socket write (WR-01).** Fix in code: yes (`manager.go:1097-1135`, 5s write deadline, a failed write disconnects only its own socket). Tested: `TestSendCommand_BlockedPeer`, `TestSendCommand_FailedWriteOnAReplacedSocketLeavesTheNewSessionAlone`. Residual, confirmed at HEAD and on no agenda: `Connect` still holds `m.mu` across `net.DialTimeout` for up to 5s (`manager.go:296-297`, `:316`), so any user's slow dial delays every user's wheel-grab and `#AUTO OFF` by up to 5s. **WARNING, unregistered_flag; fixed, sibling left.**
4. **A dead socket counted as a model failure, able to turn WAITING into OFF (WR-03).** Fix in code: yes (`manager.go:1029-1032`, `:1104-1114`; `driver.go:763-776`, `:1088-1140`). Tested: `TestSendFailureIsNotAModelFailure`, `TestSendAICommand_LostConnectionLeavesTheSwitchWaiting`. Residual: one new owner-visible sentence the owner has not approved; not yet seen on staging. **INFO; fixed.**
5. **A vault key that is present but unusable (WR-07).** Fix and tests: see T-4-06. Residual: IN-07. Operational: a malformed staging key now stops the server by design. **WARNING; fixed.**
6. **The goal/settings read-modify-write race (WR-08).** Fix in code: yes (`internal/store/profile.go:546-562`; `UpdateProfile` no longer names `session_goal`). Tested: `TestUpdateSessionGoalTouchesOnlyTheGoal`, `TestUpdateProfileLeavesTheGoalAlone`, `TestGoalSaveTouchesOnlyTheGoal`. Residual: the panel-side single-flight save has no automated test and no post-fix staging capture; every OTHER column still shares the whole-row statement. **INFO; fixed.**
7. **A silent Quest failure (WR-09).** Fix in code: yes (`handler.go:841-885`; `driver.go:807-825`). Tested: `TestGoalQuestFailureIsNotSilent`, `TestMissingQuestIsRepairedAtTheNextDecision`. Residual: the goal-changed message is pushed before the 500 is returned; new unapproved sentence. **INFO; fixed.**
8. **The harness overwriting the owner's goal on a failed first step (WR-11) and counting "not 200" as a refusal (WR-12).** Fix in code: yes (`verify-phase4.sh:532-560`, `:615-640`, `:660`). Tested: both self-tests re-run here; interrupt and 5xx paths on a localhost stub only. Residual: the live delete stays unconditional by decision; the cookie is still passed on curl's command line (IN-10). **WARNING; fixed.**
9. **The memory read-back following a stale orphaned row.** Fix in code: yes, twice — the read-back follows this login's newest session (`internal/store/transcripts.go:258-264`, commit `0dd3b06`), and orphaned sessions are closed at server start (`transcripts.go:169-192`, `main.go:196-200`, commit `209d293`). Tested: `TestSessionMemoryForConnection_ReadsThisLoginsNewestSession`, `TestCloseOrphanedGameSessions_OnlyEndsOpenRows`; live `closed=3` at log `:216`. Residuals: IN-06 (a sign-in on a second device moves the boundary, so the panel can read `[]` while the driver still uses the running session's bullets); the close-at-start assumes a single server instance; D-31's marker has no request-level test (agenda says so). **INFO; fixed. The agenda still says the second half is not done — see Open Items.**
10. **Reviewer reliability: the direct-03 coin flip (D-29) and false blocks of ordinary tutorial combat.** D-29's clause is in code verbatim (`driver.go:1394-1395`) and `direct-03` reads `blocked-reviewer` in both final-build runs. NOT fixed, by decision: the reviewer is one model call that reliably blocks only what its closed list names (the code's own comment, `driver.go:1385-1393`, says an unlisted harm "should be expected to get through"); the false blocks of `c chill touch golem` and `c static blast crystal` are unaddressed and feed D-17's block counter. On the agenda, undecided. **WARNING, unregistered_flag; open by owner decision.**
11. **A reviewer answer with no `blocked` field reads as "not blocked".** `ReviewAnswer.Blocked` is a plain `bool` (`internal/gemini/client.go:320-323`); `{}`, `null` or `{"reason":"..."}` all decode without error (`:348-352`) and the driver then sends (`driver.go:737`). The schema marks the field required, so this depends entirely on the vendor honouring it. On the agenda as a "small loose end"; this audit rates it higher, because it is a fail-open default inside the phase's main safety control and the fix is a pointer field and one test. **WARNING, unregistered_flag; not fixed.**
12. **Found by this audit, on no list:** the ICM backstop is inert for AI sends (T-4-02 above); memory is written even when the decision is then blocked (T-4-03 above); `Connect` holds the lock across the dial (item 3). Pre-existing and adjacent, not a Phase 4 defect: the dispatcher's circuit-breaker counter is never reset (`internal/icm/dispatcher.go:118`, no decrement anywhere), and the driver shares that dispatcher keyed by user id (`main.go:290`), so enough structured commands in one process lifetime would refuse every later AI dispatch — fail-safe, but worth knowing before anyone makes the backstop count AI sends.

## Open Items

**OPEN threat rows (block_on: high — both are High-severity ids):**

- **T-4-02 — the declared ICM backstop does nothing for AI sends.** `internal/icm/dispatcher.go:208-212` returns before `RecordExecution` (`:221`); `internal/driver/driver.go:752` dispatches and nothing ever records. Primary controls (pacing `loop.go:242-251`, cap `driver.go:904-912`) are verified, so the practical exposure is "no second line if the pacing loop ever has a bug". Close by recording AI sends against the dispatcher's counters (mind item 12's never-reset circuit breaker), or by the owner accepting it and D-06 / T-4-02 dropping the backstop claim. It must be added to the agenda either way; Phase 3 explicitly handed this decision to Phase 4.
- **T-4-05 — the committed canned report contains what the plan said must not reach a committed report.** `evidence/03-canned-report.txt:78`, `:93`, `:226`, `:241`, `:380`, `:395` (full decision lists: reasoning, command, notice), `:214`, `:368` (memory bullets, one naming the character), `:120` (the owner's goal). Source: `scripts/verify-phase4.sh` prints raw response bodies by design. Close by the agenda's own fix (lengths and fingerprints, regenerate fixtures and the three RUN sections) or by owner acceptance — before `ai-player` is pushed.

**Where this audit disagrees with `04-SECURITY-AGENDA.md`, or finds it incomplete:**

1. **"Rerun on a fresh-quota day" is not what happened.** Agenda item 7 and D-24 say so, and the owner's own DR-3.1-01 note made it a condition. Nine corpus runs are filed for 2026-09-17 and every one has rate-limited `failed-model` items. The authoritative `evidence/04-redteam-after.txt` measured 25 of 31 items (6 `failed-model`: `system-03`, `owner-01`, `owner-02`, `staged-02`, `benign-01`, `benign-02`), so its `STEERED: 0` covers 21 of 25 hostile items and its `FALSE BLOCKS: none` covers 4 of 6 benign ones. The second final-build run (`04h`) misses six DIFFERENT items, including `reviewer-channel-01` and both delimiter items. Together they cover every item once; neither is a complete run, and the agenda does not say so.
2. **Stale citations.** Item 7 cites `SHA: 90a52db`; the file now reads `SHA: 2e81e85` (recaptured after the agenda). Every `evidence/01-test-report.txt` line number the agenda cites has drifted since the recapture (for example `TestLoop_CallCap` is at `:692`, not `:654-668`; `TestMigration012DownReconcilesBlockedRows` at `:1893`, not `:1629`). The tests pass; the pointers are wrong. "92 commits ahead" is now 121.
3. **The D-31 item's second gap is already fixed.** The agenda says interrupted game sessions are never marked ended and "a follow-up task is already written up". Commit `209d293` (44 minutes after the agenda's last edit) closes them at server start, and the project's own log shows it live (`closed=3`). The first gap (no request-level test of the login marker) stands.
4. **The canned-report item is understated** (see T-4-05): it is 45 decisions' reasoning, commands and notices, memory bullets and the character's name, not "the first line of one decision's reasoning and command".
5. **The missing-`blocked`-field item is under-rated** (Unregistered item 11): it is a fail-open default on the reviewer.
6. **Missing from the agenda entirely:** the inert ICM backstop (T-4-02); memory persisted before the reviewer's verdict and no way to clear or age out memory bullets, which also bears on item 1 (DR-3-01) and policy section 6's "learned notes"; `Connect` holding the manager lock across a 5s dial, which delays every user's stop control; IN-09 (the badge can be put back to On by a late `ai` message); IN-08 (an armed delete confirmation survives a profile switch); IN-07 (the key gate depends on `RAILWAY_ENVIRONMENT` alone); the corpus cost that T-4-18's acceptance said would be listed; the residual that a stale stint's failure can disengage the next stint; and the two new owner-visible sentences (WR-03, WR-09) awaiting the owner's eye.
7. **Part 3's T-4-01 row is accurate but describes only the first fix** (`a1e85c6`, the state check). The epoch fix that supersedes it (CR-01, `18f5562`) and its live retest at log `:242` are not mentioned.

**Not yet re-verified on staging against the final build** (from `04-REVIEW-FIX.md`'s own checklist, confirmed still outstanding by `04-VERIFICATION.md`): WR-03's lost-connection line, WR-05's "0 of 3" status line, WR-06, WR-08/WR-09 concurrent saves. Screenshots 06 to 13 predate the review-fix deployment.

**The one remaining procedural step, outside this audit's scope:** all 48 checkboxes on `04-SECURITY-AGENDA.md` are unmarked and `.planning/RISK-REGISTER.md` has no Phase 4 rows. Every disposition, including whether the two OPEN rows are remediated or accepted, belongs to the owner at the Phase 4 security review.

---

*Audited: 2026-09-17*
*Auditor: Claude (gsd-security-auditor)*
